# Slide Working Agreement

Slide is an operations platform. Changes must preserve authorization,
auditable state transitions, and deterministic recovery behavior.

Use the repository's shared UI components and design tokens. Treat generated
analysis and external automation output as untrusted data at every boundary.
