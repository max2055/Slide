---
phase: 139-release-qualification
plan: 02
type: execute
wave: 2
depends_on: [139-01]
files_modified:
  - tests/qualification/docker-compose.yml
  - tests/qualification/bootstrap-upgrade.spec.ts
  - tests/qualification/failover-recovery.spec.ts
  - tests/qualification/backup-restore.spec.ts
  - tests/qualification/stability.spec.ts
  - scripts/qualification/run-environment.sh
  - .planning/phases/139-release-qualification/139-RECOVERY-EVIDENCE.md
autonomous: true
requirements: [HI-01, HI-02, HI-12, TG-03, TG-05]
must_haves:
  truths:
    - 空库、代表性历史升级、重复启动、worker 接管和备份恢复在隔离环境自动验证
    - 恢复后不仅 API 200，还校验 schema version、业务行、血缘、密文可用性和任务恢复
    - 稳定性场景不会出现重复外部副作用、无界队列或虚假健康
---

# Phase 139 Plan 02：安装升级、故障转移、备份恢复与稳定性

<objective>
在接近生产的隔离环境验证 Slide 能安装、升级、失败、接管和恢复，而不是只证明单进程 happy path。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-release-qualification/139-CONTEXT.md
@.planning/phases/134-deterministic-schema-bootstrap/134-CONTEXT.md
@.planning/phases/137-durable-incident-closure/137-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 构建一次性 qualification 环境</name>
  <files>tests/qualification/docker-compose.yml, scripts/qualification/run-environment.sh, tests/qualification/bootstrap-upgrade.spec.ts</files>
  <action>
    compose 至少包含 MySQL、两 API candidate（按场景启停）和前端；每次运行使用随机 namespace/volume，注入非默认 secrets，记录 image/runtime/schema/build version。环境脚本支持 up/wait/test/down 并在失败时收集日志/DB manifest。测试空库迁移、二次 no-op、Phase 124/129 fixture 升级、checksum mismatch 和 migration 中断，断言未完成时 readiness 非 200/ready。
  </action>
  <verify><automated>bash scripts/qualification/run-environment.sh bootstrap-upgrade</automated></verify>
  <done>全新与两代历史环境均到达相同 schema invariant，数据数量/关系守恒；失败迁移不接流量。</done>
</task>

<task type="auto">
  <name>Task 2: 验证重复启动、worker failover 和副作用幂等</name>
  <files>tests/qualification/failover-recovery.spec.ts, scripts/qualification/run-environment.sh, .planning/phases/139-release-qualification/139-RECOVERY-EVIDENCE.md</files>
  <action>
    场景包含端口冲突进程、双 API/单 worker lease、kill -9 running job、DB 30 秒不可用、lease expiry 和旧 owner 恢复。用 report/notification/approval/Agent tool spy 计数业务副作用，断言每个 idempotency key 最多一次；无法确定的外部执行必须为 unknown。核对 outbox backlog、heartbeat、dead letter 和四维健康随故障变化。
  </action>
  <verify><automated>bash scripts/qualification/run-environment.sh failover</automated></verify>
  <done>新 worker 接管且不丢 job；旧 fencing owner 无法提交；端口失败进程无业务写。</done>
</task>

<task type="auto">
  <name>Task 3: 验证备份恢复和有界稳定性</name>
  <files>tests/qualification/backup-restore.spec.ts, tests/qualification/stability.spec.ts, scripts/qualification/run-environment.sh, .planning/phases/139-release-qualification/139-RECOVERY-EVIDENCE.md</files>
  <action>
    生成包含业务数据和 migration ledger 的备份，恢复到新 MySQL，轮换可配置 secret，启动并核对用户/资源/事件/Operation/report/analysis 引用与 secret 可解密性。稳定性场景以受控并发运行 Chat、只读 SQL、指标、告警、报表/通知和 WS 重连，记录 p95/error/queue age/memory/event-loop lag；阈值在测试前写入证据文档，不能运行后调整。禁止对生产目标执行压测。
  </action>
  <verify><automated>bash scripts/qualification/run-environment.sh backup-restore &amp;&amp; bash scripts/qualification/run-environment.sh stability</automated></verify>
  <done>恢复系统可通过 invariant 和关键用户查询；稳定运行无无界增长、数据丢失或重复副作用。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| qualification 误连生产 | 随机隔离网络/DB 名 + destructive guard |
| 备份含明文 secret | encrypted backup/retention/redacted artifact |
| 故障注入留下进程/volume | trap cleanup + run namespace |
| 压测阈值事后调整 | predeclared SLO in evidence |
</threat_model>

<verification>
- 四个 `run-environment.sh` 场景全部退出 0。
- 证据包含 commit、schema、镜像、测试计数、故障时间线和清理结果。
</verification>

<success_criteria>
1. HI-01/HI-12 的生产恢复风险有环境级证据。
2. 备份恢复和 worker 接管不依赖原进程内存。
3. 输出 `.planning/phases/139-release-qualification/139-02-SUMMARY.md`。
</success_criteria>

