# Phase 139 Evidence Matrix

This matrix records current runtime evidence only. A source-file mapping is not a release closure by itself.

| Finding | Status | Current evidence |
|---|---|---|
| CR-02 | Verified | `QUALIFICATION_ADMIN_PASSWORD=Tpam1234 PLAYWRIGHT_MANAGED_ENV=1 pnpm --filter slide-frontend exec playwright test e2e/release-security.spec.ts` passed on 2026-07-19 (4 tests). A viewer was denied REST history, update, and delete of an administrator-owned session; an administrator read confirmed no delete side effect. A viewer WebSocket was denied `chat.watch` and `chat.history` for the same session. |
| CR-01 | Verified | `pnpm --filter slide-api test -- src/adapter/__tests__/direct-adapter.test.ts` passed on 2026-07-19. It loads the real production platform catalog and runs an authenticated DirectAdapter WebSocket transport: a viewer sends `chat.send`, a tool-calling AgentRunner requests the owner-only, approval-required `get_instance_connection` tool, the WS emits the tool lifecycle events, and the actor-bound registry returns `OWNER_REQUIRED`. A spy proves `instanceDatabaseService.getInstanceWithDecryptedPassword` is never called, establishing zero handler side effect. `src/tools/policy.test.ts` independently covers missing/forged approval IDs. |
| CR-03 | Verified | `QUALIFICATION_ADMIN_PASSWORD=Tpam1234 PLAYWRIGHT_MANAGED_ENV=1 pnpm --filter slide-frontend exec playwright test e2e/release-security.spec.ts --workers=1` passed on 2026-07-19 (7 tests). Against a real managed MySQL instance, direct UPDATE, CREATE TABLE, and SELECT-plus-DELETE input each returned `409` with `NEEDS_APPROVAL`; a subsequent read-only MySQL check confirmed that `qualification_must_not_exist` was not created. |
| HI-02 | Verified | The same managed-environment command passed on 2026-07-19 after adding the concurrent-approval scenario (8 tests). Two concurrent approval-review requests targeted one real UPDATE; exactly one response succeeded and a read-only query verified that the controlled counter changed from 0 to 1, not 2. |
| HI-03 | Verified | The same managed-environment command passed on 2026-07-19 after adding the revocation scenario. A freshly created user was authenticated on REST, refresh, and an already-open DirectAdapter WS connection; after the administrator set the user to `inactive`, the old REST token and refresh token both returned 401, and the retained WS connection closed with 4001 on its next command. The adapter now revalidates the actor before every authenticated command, rather than waiting for its heartbeat interval. |
| HI-04 | Verified | Managed-environment Playwright coverage on 2026-07-19 loaded `ai-analysis-result` in a real browser with malicious structured values and sanitised Markdown. No `img` node was created from either payload and no event handler executed. |
| HI-05 | Verified | Managed critical-path Playwright coverage on 2026-07-19 creates an offline, collection-enabled server and reads `/api/health/readiness`. The server appears in `managedAvailability.failedRefs`, that dimension is not healthy, and `overall` is not healthy. |
| HI-06 / HI-07 / HI-08 | Verified | `QUALIFICATION_DEEPSEEK_API_KEY=… bash scripts/qualification/run-existing-mysql.sh alert-rca` passed against an isolated database: real server and instance observations crossed scoped rules, generated alerts, and completed structured RCA records. Managed Playwright passed the fresh native MySQL metric → scheduler persistence → scoped alert → DeepSeek RCA workflow 1/1 on 2026-07-20. Service-boundary tests prove same-name instance/server rules remain target-isolated, multi-level server thresholds produce the correct level, and duration windows reject intermittent breaches. Producer-parity coverage now requires every Linux server metric persisted by the collector to exist in the server-target registry; current-database readback found all 10 metric names across 6,517 historical samples registered, with no missing identities. |
| HI-09 | Verified | Current managed Playwright 22/22 includes a deterministic scheduled server report that is scoped, persisted, listed, downloaded, and linked to a durable notification outcome. Service/integration coverage verifies typed target filtering, idempotent occurrences, verified 2xx success, unavailable/policy failure, SMTP/OAuth encrypted credentials, Feishu HMAC body signing, activation boundaries, and write-only DTOs. Real public delivery remains outside the operator-approved no-public-egress environment and is not claimed; every original scheduling, scope, persistence, UI, and notification-control defect is directly covered. |
| HI-10 | Verified | `bash scripts/qualification/run-existing-mysql.sh agent-run-failure` passed on 2026-07-19. A deterministic provider failure traversed a real DirectAdapter WS transport and then MySQL readback of `agent_runs` confirmed the actor-bound idempotent run was terminal `failed`, had `finished_at`, and retained its failure stop-reason payload. |
| ME-01 | Verified | `bash scripts/qualification/run-existing-mysql.sh agent-run-cancel` independently proves DirectAdapter transport cancellation. On 2026-07-20, `QUALIFICATION_ADMIN_PASSWORD=… PLAYWRIGHT_MANAGED_ENV=1 QUALIFICATION_CANCELLATION_E2E=1 pnpm --filter slide-frontend exec playwright test e2e/agent-cancellation.spec.ts --workers=1` passed 1/1. A managed local streaming provider held the run open; the browser displayed and clicked Stop, received `cancelled`, reconciled the UI to interrupted, and direct MySQL readback confirmed the canonical run was terminal `cancelled` with `finished_at`. |
| ME-02 | Verified | The real DirectAdapter WS qualification reuses the canonical session and idempotency key after provider failure and receives the existing terminal `run.snapshot` without re-execution. DirectGateway unit coverage proves protocol v2 preserves idempotency and attachment payloads. Managed Playwright uploads an image, observes the explicit `PROTOCOL_V2_INVALID` terminal error from the server's versioned unsupported-attachment contract, and MySQL readback confirms the run count does not change, proving zero model/tool side effect. |
| ME-03 | Verified | `bash scripts/qualification/run-existing-mysql.sh collection-schedule` passed against isolated MySQL. `collection_schedule_state` readback proves independent per-metric scheduling: first collection is due, successful fast/slow metrics defer by their own intervals, and a failed fast metric becomes retryable while slow remains deferred. `collector-due-only.test.ts` closes the downstream boundary by proving a fast/slow/rare registry with only slow due causes exactly one slow provider call and persists no non-due metric. |
| ME-04 | Verified | `bash scripts/qualification/run-existing-mysql.sh server-collector-failures` passed against isolated MySQL: three returned credential failures marked the server `unreachable`. Registry + UnifiedCollector integration coverage proves three consecutive exceptions disable only the failing instance scope and prevent later calls, while a successful collection resets the counter and prevents two later failures from disabling the provider. |
| ME-05 | Verified | `QUALIFICATION_ADMIN_PASSWORD=Tpam1234 PLAYWRIGHT_MANAGED_ENV=1 pnpm --filter slide-frontend exec playwright test e2e/agent-capabilities.spec.ts --workers=1` passed on 2026-07-19. A real DirectAdapter reports unsupported files, model selection, tools, skills, and cron features; the authenticated Agent workspace exposes only Overview and omits every corresponding no-op or unavailable management control identified by ME-05. Broader publication and rollback coverage belongs to the release test gates. |
| ME-06 | Verified | Managed Playwright `adapter capability contract rejects unsupported database types before persistence` passed on 2026-07-19. The authenticated caller reads the real capability matrix (MySQL creatable; MongoDB, Redis, and Elasticsearch unsupported), and a MongoDB create request is rejected with `DATABASE_TYPE_UNSUPPORTED:mongodb` before any resource is created. `adapter-uat` passed against real local PostgreSQL 18 and Dameng 8, and `oracle-adapter-uat` passed against real Oracle 19c; each connected through the production database service and returned native realtime metrics. This closes the false compatibility claim and reject-before-persistence finding; broader cross-adapter workflows remain tracked by TG-05. |
| ME-07 | Verified | Managed Playwright `concurrent approval reviews execute a write exactly once` passed 1/1 on 2026-07-20 after extension. It proves one target update under concurrent review, reads the succeeded Operation and immutable CREATED→approval→execution event sequence, then directly joins the executed SQL history to its approval ID and reads `rollback_info = { available: false, reason: ROLLBACK_SQL_NOT_PROVIDED, executed_at }`. The existing cancellation/retry scenario proves the approval follows the current retry attempt. Migration 050 enforces SQL-history→approval, approval→Operation, and Operation→approval associations with `ON DELETE SET NULL`; `bootstrap-upgrade` passed with 51 migrations, all three primary-data orphan preflights returned zero, and the primary migration applied successfully. |
| LO-01 | Verified | Managed Playwright `removed legacy routes resolve to the current chat workspace` passed on 2026-07-19. The browser starts at each removed `/system` and `/appearance` route, authenticates, and lands at `/chat`. This covers the remediation for a discovered regression where legacy paths were incorrectly inferred as deployment base paths and became `/system/dashboard` or `/appearance/dashboard`. |
| DR-01 | Verified | `workflow-catalog` starts the complete isolated service and reads back completed jobs for all typed handlers: `capacity.collect`, `baseline.cleanup`, `alert.evaluate`, `report.schedule`, `notification.dispatch`, `notification.deliver`, and `report.notify`, including the audited unavailable-target skip. Current primary-data readback confirms all free-text Agent Cron tasks are disabled and enabled entries are deterministic scripts, so critical workflows no longer depend on model prompts. |
| DR-04 | Verified | `bash scripts/qualification/run-existing-mysql.sh health-truth` passed on 2026-07-19 against an isolated database in the existing `mysql` container. The real aggregation returned healthy control plane, degraded managed availability, critical missing-metric freshness, and degraded recent workflow failures; the combined overall state was correctly critical. |
| HI-11 | Verified | Current managed 22/22 browser/API evidence rejects private webhook targets, returns only redacted/write-only channel DTOs, and replays a dead letter through the protected UI with a required reason. The active durable scheduler/worker persists retry and audit state. Outbound-policy and notification-service coverage enforces HTTPS/allowlist, rejects DNS failure/private/mixed answers, pins the verified address, and rejects 3xx as `OUTBOUND_REDIRECT_DENIED` without following it. Public delivery is not claimed, but all original worker, disclosure, DNS, and redirect defects are closed. |
| HI-01 | Verified | `bash scripts/qualification/run-existing-mysql.sh bootstrap-upgrade` passed on 2026-07-20 against an isolated temporary database in the existing MySQL 9.6 container. It applied the current 51 migrations to an empty database, repeated initialization, and verified every ledger entry plus schema invariants, including `health_check_history.dimensions`, `alerts.level` value `p0`, and the approval/audit foreign keys. The preceding 49-migration contract also passed against user-provided `mysql3307` on port 3307. The temporary databases were removed by the script. |
| HI-12 / DR-02 | Verified | `bash scripts/qualification/run-existing-mysql.sh failover` passed on 2026-07-19 against an isolated temporary database in the existing MySQL 9.6 container: lease takeover, old fencing-token rejection, and new-owner completion. |
| HI-12 / DR-03 / HI-13 | Verified | `bash scripts/qualification/run-existing-mysql.sh startup-negative` passed on 2026-07-19 against isolated temporary databases in the existing `mysql` container. A weak production JWT secret and a deliberately interrupted ledger entry both caused a non-zero process exit before schema-current, listener, DirectAdapter, or worker initialization; the interrupted entry remained `running` for explicit repair. The earlier managed DTO-redaction evidence remains valid. |
| TG-03 | Verified | Bootstrap qualification runs the authoritative migration ledger and `assertSchemaInvariants`; the prior source-only schema validator is not used as release evidence. |
| TG-01 / TG-02 / TG-04 / TG-06 | Verified | Final current batch: API 93 files/982 tests, frontend 19 files/182 tests, Agent Core 7 files/69 tests, all typechecks, frontend build, lint 0 errors, coverage 37/37, CI gate parity, managed security/critical/capability Playwright 22/22, and cancellation/attachment 2/2 with the required flag and no skip. These behavior tests cover multi-actor authorization, concurrency, XSS, SSRF, revocation, persistence, refresh, feedback, and recovery rather than source-only assertions. |
| TG-05 | Verified | Current MySQL browser evidence covers CRUD/reload, health, query, metrics, scheduler/alert behavior and failure state. PostgreSQL 18, Dameng 8, and Oracle 19c pass connection, health, read query, native metrics, disconnect, and recovery. Oracle UAT exposed and drove the shared Oracle/Dameng AST fallback fix; the final licensed Dameng `adapter-uat` rerun passed the complete matrix on 2026-07-20. Unsupported database types remain rejected before persistence. |
| OPT-01 | Historical deferral, now closed | Deferred at the 2026-07-20 GO checkpoint; superseded by the 2026-07-23 OPT-01 follow-up row below. |
| OPT-02 | Historical deferral, now closed | Deferred at the 2026-07-20 GO checkpoint; superseded by the 2026-07-23 OPT-02 follow-up row below. |
| OPT-03 | Historical deferral, now closed | Deferred at the 2026-07-20 GO checkpoint; superseded by the 2026-07-23 OPT-03 follow-up row below. |
| Phase 137 durable closure | Verified | The managed security/critical/capability suite passed 22/22 on 2026-07-20 after resetting only `db_ops_ai_qualification` and applying the current 51-migration ledger. It replays a dead letter with a required reason, enforces recovery verification before incident close, creates a redacted Feishu channel, drives MySQL connection-test/reload and native metrics, observes scheduler alert persistence, completes server RCA, and persists/downloads a scheduled report with its notification outcome. |
| Remaining Phase 131 findings | Disposition complete | All 37 findings are mapped and have current behavioral evidence or an explicit deferred-optimization disposition. All Critical, High, Medium, Low, design-risk, and test-gap findings are Verified. The authoritative release decision is GO. |
| Post-GO capacity consistency (2026-07-21) | Focused verification passed | Reproduced Dashboard `0.9 GB` versus managed-instance `2.71 GB` and the false-positive health message `0.9GB 与 0.4GB 一致`. Unified the managed-instance scope, tightened the consistency tolerance to `0.01 GB`, made missing collection coverage actionable, and changed MySQL/Dameng aggregation to sum raw numeric bytes before rounding. Backend 20/20 focused tests, both typechecks, focused frontend test/build, forced capacity collection, and live API/database readback passed: Dashboard and instance total `2.71 GB`, `capacity_sum_match=pass`. Full Phase 139 release suites were not rerun after this delta. |
| Post-GO route closure (2026-07-23) | Verified with one operator input outstanding | Current local gate: API 98 files/1001 tests, frontend 23/188, Agent Core 7/69, all typechecks, build, lint 0 errors/249 warnings, 37/37 matrix, CI parity, contract drift, and lazy-route/locale browser 2/2. Hosted CI artifact/recovery run `29939147753` passed. Capacity monitor is durable and current live totals are `2.59 GB` on both instance and Dashboard surfaces with `capacity_sum_match=pass`. Five database credentials, DeepSeek, and Feishu are live-verified; server 3 SSH credential re-entry remains operator-only. |
| OPT-01 follow-up | Verified | Route/vendor splitting reduced the main entry from 2759.79 kB to 847.91 kB (69.3%). Five heavy route chunks are verified by Playwright with no page errors or 5xx; desktop/mobile checks have no horizontal overflow. |
| OPT-02 follow-up | Verified within agreed Chinese-product scope | Locale leaf-key and placeholder parity plus static-key existence are enforced. Server-synced locale now triggers app-shell re-render; browser evidence verifies a Chinese preference renders Chinese shell and Dashboard. Full English translation of legacy Chinese operational bodies is not claimed. |
| OPT-03 follow-up | Verified | TypeBox-backed Fastify response schemas deterministically generate OpenAPI and frontend DTOs. A real frontend consumer uses the generated database-instance type; unit tests compile the schemas in Fastify, and hosted CI runs `contracts:check` for drift. |
