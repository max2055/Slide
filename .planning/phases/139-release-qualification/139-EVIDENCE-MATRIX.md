# Phase 139 Evidence Matrix

This matrix records current runtime evidence only. A source-file mapping is not a release closure by itself.

| Finding | Status | Current evidence |
|---|---|---|
| CR-02 | Verified | `QUALIFICATION_ADMIN_PASSWORD=Tpam1234 PLAYWRIGHT_MANAGED_ENV=1 pnpm --filter slide-frontend exec playwright test e2e/release-security.spec.ts` passed on 2026-07-19 (4 tests). A viewer was denied REST history, update, and delete of an administrator-owned session; an administrator read confirmed no delete side effect. A viewer WebSocket was denied `chat.watch` and `chat.history` for the same session. |
| CR-01 | Verified | `pnpm --filter slide-api test -- src/adapter/__tests__/direct-adapter.test.ts` passed on 2026-07-19. It loads the real production platform catalog and runs an authenticated DirectAdapter WebSocket transport: a viewer sends `chat.send`, a tool-calling AgentRunner requests `get_instance_connection`, the WS emits the tool lifecycle events, and the actor-bound registry returns `OWNER_REQUIRED` before the owner-only, approval-required handler runs. `src/tools/policy.test.ts` independently covers missing/forged approval IDs. |
| CR-03 | Verified | `QUALIFICATION_ADMIN_PASSWORD=Tpam1234 PLAYWRIGHT_MANAGED_ENV=1 pnpm --filter slide-frontend exec playwright test e2e/release-security.spec.ts --workers=1` passed on 2026-07-19 (7 tests). Against a real managed MySQL instance, direct UPDATE, CREATE TABLE, and SELECT-plus-DELETE input each returned `409` with `NEEDS_APPROVAL`; a subsequent read-only MySQL check confirmed that `qualification_must_not_exist` was not created. |
| HI-02 | Verified | The same managed-environment command passed on 2026-07-19 after adding the concurrent-approval scenario (8 tests). Two concurrent approval-review requests targeted one real UPDATE; exactly one response succeeded and a read-only query verified that the controlled counter changed from 0 to 1, not 2. |
| HI-03 | Verified | The same managed-environment command passed on 2026-07-19 after adding the revocation scenario. A freshly created user was authenticated on REST, refresh, and an already-open DirectAdapter WS connection; after the administrator set the user to `inactive`, the old REST token and refresh token both returned 401, and the retained WS connection closed with 4001 on its next command. The adapter now revalidates the actor before every authenticated command, rather than waiting for its heartbeat interval. |
| HI-04 | Verified | Managed-environment Playwright coverage on 2026-07-19 loaded `ai-analysis-result` in a real browser with malicious structured values and sanitised Markdown. No `img` node was created from either payload and no event handler executed. |
| HI-05 | Verified | Managed critical-path Playwright coverage on 2026-07-19 creates an offline, collection-enabled server and reads `/api/health/readiness`. The server appears in `managedAvailability.failedRefs`, that dimension is not healthy, and `overall` is not healthy. |
| HI-06 / HI-07 / HI-08 | Partially verified | `QUALIFICATION_DEEPSEEK_API_KEY=… bash scripts/qualification/run-existing-mysql.sh alert-rca` passed on 2026-07-19 against an isolated database in user-provided `mysql3307`. Real server `cpu_usage` and instance `metrics_data.qualification_load` observations each crossed their scoped threshold rule and generated critical alerts. The database-configured DeepSeek provider then called `slide_complete_analysis`; MySQL readback confirmed both server-subject and instance-subject `alert_rca` records completed with structured envelopes. Managed Playwright `server alert RCA can be started from the browser without an instance id` passed on 2026-07-19: the Alerts workspace started a server-subject RCA and read its completed state. Full browser metric collection remains pending. |
| HI-09 | Partially verified | Managed-environment Playwright coverage on 2026-07-19 created a temporary server, `server_health` cron configuration, and configured notification channel; it observed the durable `report.schedule` worker create a server-scoped report, downloaded its HTML, and read the persisted `report.notify` failure outcome for a deliberately policy-rejected endpoint. `src/notification-service.test.ts` verifies a 2xx result from a verified, pinned target is treated as success. SMTP email delivery is implemented with TLS, encrypted password/OAuth refresh-token storage, Microsoft OAuth2 refresh exchange, XOAUTH2 authentication, non-revealing DTOs, and an isolated `email-delivery` qualification. The authorized Hotmail run created the temporary channel and attempted delivery, but the execution environment resolved `smtp-mail.outlook.com` to reserved address `198.18.3.84`; that peer provided no SMTP greeting or STARTTLS response, so no connection reached Microsoft and neither SMTP authentication nor inbox receipt occurred. The worker persists one idempotent scan per minute and MySQL sessions use UTC so cron timestamps are not shifted by eight hours. A real successful external delivery remains unverified. |
| HI-10 | Verified | `bash scripts/qualification/run-existing-mysql.sh agent-run-failure` passed on 2026-07-19. A deterministic provider failure traversed a real DirectAdapter WS transport and then MySQL readback of `agent_runs` confirmed the actor-bound idempotent run was terminal `failed`, had `finished_at`, and retained its failure stop-reason payload. |
| ME-01 | Partially verified | `bash scripts/qualification/run-existing-mysql.sh agent-run-cancel` passed on 2026-07-19. A cancellable provider ran through a real DirectAdapter WS transport; `chat.cancel` emitted `cancelled` without `complete`, and MySQL readback confirmed the actor-bound run was terminal `cancelled` with `finished_at`. Browser cancellation UI remains pending. |
| ME-02 | Partially verified | The same real DirectAdapter WS qualification reuses the `session.created` key with the same idempotency key after its provider failure. It receives the existing terminal `run.snapshot` for the same persisted run instead of invoking the provider again. Attachment behavior remains pending. |
| ME-03 | Partially verified | `bash scripts/qualification/run-existing-mysql.sh collection-schedule` passed on 2026-07-19 against an isolated database in the existing `mysql` container. `collection_schedule_state` readback proves independent per-metric scheduling: first collection is due, successful fast/slow metrics defer by their own intervals, and a failed fast metric becomes retryable while the slow metric remains deferred. |
| ME-04 | Partially verified | `bash scripts/qualification/run-existing-mysql.sh server-collector-failures` passed on 2026-07-19 against an isolated database in the existing `mysql` container. A collection-enabled server with unusable encrypted credentials returned a failed result on each collector tick; after the third tick, MySQL readback confirmed its status was `unreachable`. This covers the regression fix that makes non-throwing collection failures enter the collector failure counter. |
| ME-05 | Partially verified | `QUALIFICATION_ADMIN_PASSWORD=Tpam1234 PLAYWRIGHT_MANAGED_ENV=1 pnpm --filter slide-frontend exec playwright test e2e/agent-capabilities.spec.ts --workers=1` passed on 2026-07-19. A real DirectAdapter reports unsupported files, model selection, tools, skills, and cron features; the authenticated Agent workspace exposes only Overview and omits the corresponding management controls. Capability publication and rollback remain pending. |
| ME-06 | Partially verified | Managed Playwright `adapter capability contract rejects unsupported database types before persistence` passed on 2026-07-19. The authenticated caller reads the real capability matrix (MySQL creatable; MongoDB, Redis, and Elasticsearch unsupported), and a MongoDB create request is rejected with `DATABASE_TYPE_UNSUPPORTED:mongodb` before any resource is created. `QUALIFICATION_POSTGRES_PASSWORD=… QUALIFICATION_DAMENG_PASSWORD=… bash scripts/qualification/run-existing-mysql.sh adapter-uat` passed against real local PostgreSQL 18 and Dameng 8: both adapters connected through `databaseService` and returned native realtime metrics. After the listener recovery, `QUALIFICATION_ORACLE_USER=… QUALIFICATION_ORACLE_PASSWORD=… QUALIFICATION_MYSQL_CONTAINER=mysql3307 QUALIFICATION_MYSQL_PORT=3307 bash scripts/qualification/run-existing-mysql.sh oracle-adapter-uat` also passed on 2026-07-19 against real Oracle 19c: the project adapter connected and returned native realtime metrics. Full report, alert, and query workflows across every supported adapter remain pending. |
| ME-07 | Partially verified | Managed Playwright `concurrent approval reviews execute a write exactly once` passed on 2026-07-19. It submits a real approved SQL operation, concurrently reviews it twice, confirms one target update, then reads `/api/operations/:id` and `/events`: the operation is `succeeded` and the persisted audit sequence is CREATED, NEEDS_APPROVAL, APPROVAL_CLAIMED, APPROVAL_EXECUTION_STARTED, and APPROVAL_EXECUTION_SUCCEEDED. Cancellation/retry recovery remains pending. |
| LO-01 | Verified | Managed Playwright `removed legacy routes resolve to the current chat workspace` passed on 2026-07-19. The browser starts at each removed `/system` and `/appearance` route, authenticates, and lands at `/chat`. This covers the remediation for a discovered regression where legacy paths were incorrectly inferred as deployment base paths and became `/system/dashboard` or `/appearance/dashboard`. |
| DR-01 | Partially verified | `bash scripts/qualification/run-existing-mysql.sh workflow-catalog` passed on 2026-07-19. It starts the complete service against an isolated database on ports 3004/28891 (never 3000/5173), then readbacks the real worker completing a queued `alert.evaluate` workflow job. The full registered handler catalog and LLM-driven cron tasks remain pending. |
| DR-04 | Verified | `bash scripts/qualification/run-existing-mysql.sh health-truth` passed on 2026-07-19 against an isolated database in the existing `mysql` container. The real aggregation returned healthy control plane, degraded managed availability, critical missing-metric freshness, and degraded recent workflow failures; the combined overall state was correctly critical. |
| HI-11 | Partially verified | Managed-environment Playwright coverage passed on 2026-07-19. A private `127.0.0.1` webhook was rejected at channel creation; a saved channel response exposed only the origin endpoint and did not contain its webhook path or secret. A fixed dead-letter delivery was listed by the protected API, replayed through the Alerts "通知恢复" UI with a required operator reason, and removed from the dead-letter list. `src/security/outbound-policy.test.ts` covers HTTPS-only, allowlist, private/mixed DNS, and DNS-failure rejection; `src/notification-service.test.ts` confirms a 3xx response is rejected as `OUTBOUND_REDIRECT_DENIED` rather than followed. Real redirect-chain delivery remains pending. |
| HI-01 | Verified | `bash scripts/qualification/run-existing-mysql.sh bootstrap-upgrade` passed on 2026-07-19 against isolated temporary databases in both the existing MySQL 9.6 container and user-provided `mysql3307` on port 3307. It applied the current 47 migrations to an empty database, repeated initialization, and verified every ledger entry plus schema invariants. The temporary databases were removed by the script. |
| HI-12 / DR-02 | Verified | `bash scripts/qualification/run-existing-mysql.sh failover` passed on 2026-07-19 against an isolated temporary database in the existing MySQL 9.6 container: lease takeover, old fencing-token rejection, and new-owner completion. |
| HI-12 / DR-03 / HI-13 | Verified | `bash scripts/qualification/run-existing-mysql.sh startup-negative` passed on 2026-07-19 against isolated temporary databases in the existing `mysql` container. A weak production JWT secret and a deliberately interrupted ledger entry both caused a non-zero process exit before schema-current, listener, DirectAdapter, or worker initialization; the interrupted entry remained `running` for explicit repair. The earlier managed DTO-redaction evidence remains valid. |
| TG-03 | Verified | Bootstrap qualification runs the authoritative migration ledger and `assertSchemaInvariants`; the prior source-only schema validator is not used as release evidence. |
| Phase 137 durable closure | Verified | `QUALIFICATION_ADMIN_PASSWORD=… PLAYWRIGHT_MANAGED_ENV=1 pnpm --filter slide-frontend exec playwright test e2e/release-security.spec.ts e2e/release-critical-paths.spec.ts e2e/agent-capabilities.spec.ts --workers=1` passed 18/18 on 2026-07-19. The launcher first resets only `db_ops_ai_qualification` and applies the current 47-migration ledger, preventing a stale dedicated-test ledger from invalidating current browser evidence. The critical path listed a fixed dead-letter notification, replayed it through the protected UI with a required reason, and removed it; a control-plane incident was resolved, rejected at close with HTTP 409 before recovery verification, then verified, closed, and read back with its persisted verification reason. |
| Remaining Phase 131 findings | Pending evidence | The coverage index maps all 37 findings to relevant code/tests, but current behavior evidence has not yet been gathered for every finding. The release decision remains NO-GO. |
