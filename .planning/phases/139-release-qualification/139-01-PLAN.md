---
phase: 139-release-qualification
plan: 01
type: execute
wave: 1
depends_on: [132-02, 133-02, 134-02, 135-02, 136-03, 137-03, 138-02]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - apps/db-ops-api/package.json
  - apps/db-ops-api/tsconfig.json
  - frontend/package.json
  - packages/agent-core/package.json
  - .github/workflows/ci.yml
  - tests/qualification/fixtures.ts
  - tests/qualification/coverage-matrix.ts
  - .planning/phases/139-release-qualification/139-TEST-BASELINE.md
autonomous: true
requirements: [TG-01, TG-04, TG-06]
must_haves:
  truths:
    - 当前 commit 在干净安装中 lint、三组 typecheck/test、前端 build 和 schema check 全部零失败
    - 必需 job 不允许 skip、continue-on-error 或只做源码正则/console 断言
    - 固定 fixture 覆盖多主体、多资源、告警/RCA、审批、报表、通知和失败 provider
---

# Phase 139 Plan 01：工具链、测试基线与 CI Release Gate

<objective>
先建立可信、可重复、不会静默绿灯的验证底座；本计划只修工具链和陈旧测试契约，不借验证 Phase 修改业务行为。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-release-qualification/139-CONTEXT.md
@.planning/phases/131-system-level-code-review/131-UAT.md
@.github/workflows/ci.yml
@package.json
@apps/db-ops-api/package.json
@frontend/package.json
@packages/agent-core/package.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: 固定 workspace 工具链和零失败基线</name>
  <files>package.json, pnpm-lock.yaml, apps/db-ops-api/package.json, apps/db-ops-api/tsconfig.json, frontend/package.json, packages/agent-core/package.json, .planning/phases/139-release-qualification/139-TEST-BASELINE.md</files>
  <action>
    统一 Node/pnpm/TypeScript/Vitest 版本和 packageManager/engines，使用根 workspace 安装，不在子目录 npm ci。修复 `ignoreDeprecations` 等工具链配置。逐项分类 Phase 131 的 18 backend、41 frontend、1 agent failure：若是 Phase 132-138 行为回归，退回对应 Phase；只有确认陈旧 expectation/mock/import 才更新测试。记录 before/after 计数和每个失败归属。禁止删除失败测试换绿灯。
  </action>
  <verify><automated>pnpm install --frozen-lockfile &amp;&amp; pnpm --filter slide-api typecheck &amp;&amp; pnpm --filter slide-frontend typecheck &amp;&amp; pnpm --filter agent-core typecheck &amp;&amp; pnpm -r test &amp;&amp; pnpm --filter slide-frontend build</automated></verify>
  <done>所有命令退出 0，baseline 文档列出测试文件/用例/skip 数和 Phase 131 失败处置。</done>
</task>

<task type="auto">
  <name>Task 2: 创建确定性 qualification fixture 与覆盖矩阵</name>
  <files>tests/qualification/fixtures.ts, tests/qualification/coverage-matrix.ts, .planning/phases/139-release-qualification/139-TEST-BASELINE.md</files>
  <action>
    fixture 固定 admin/DBA/viewer/disabled user、两实例/一服务器、fresh/stale/critical Observation、告警/事件/RCA、pending/running/unknown Operation、report config、notification success/failure、LLM/tool failure。每个数据有 stable id 和 cleanup namespace。coverage matrix 将 Phase 131 CR/HI/ME/LO/TG/DR 逐项映射到测试 ID、层级、命令和证据文件；缺映射直接失败。
  </action>
  <verify><automated>pnpm exec tsx tests/qualification/coverage-matrix.ts --validate</automated></verify>
  <done>全部审计 finding 至少一个行为验证，安全/并发 finding 至少一个对抗或故障注入验证。</done>
</task>

<task type="auto">
  <name>Task 3: 重建分层 CI 并禁止静默跳过</name>
  <files>.github/workflows/ci.yml, package.json, .planning/phases/139-release-qualification/139-TEST-BASELINE.md</files>
  <action>
    CI job 固定 install/lint/typecheck/unit-integration/schema/API smoke/Playwright/security/recovery，上传 JUnit、Playwright、migration 和 environment manifest。MySQL 使用健康检查 service；需要商业 DB 的 qualification job 明确 required/manual evidence 策略，不能假绿。增加 skip/only/continue-on-error 扫描和 generated contract diff。PR quick jobs 与 release required jobs 分开，但 GO 必须基于完整 release workflow。
  </action>
  <verify><automated>pnpm lint &amp;&amp; pnpm exec tsx tests/qualification/coverage-matrix.ts --check-ci .github/workflows/ci.yml</automated></verify>
  <done>CI 中每个必需 gate 有命令、环境、artifact 和失败传播，未定义的 script 不再被调用。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 陈旧 mock 产生假绿 | 行为 fixture + 真实 DB/API 边界 |
| required test 被 skip | skip/only/continue-on-error gate |
| 依赖漂移导致不可复现 | frozen lockfile + pinned runtime manifest |
| 通过删除测试隐藏回归 | before/after case traceability |
</threat_model>

<verification>
- `pnpm install --frozen-lockfile`
- `pnpm lint && pnpm -r typecheck && pnpm -r test && pnpm build`
- `pnpm exec tsx tests/qualification/coverage-matrix.ts --validate --check-ci .github/workflows/ci.yml`
</verification>

<success_criteria>
1. TG-01/TG-06 基线归零且每个原失败有处置记录。
2. 必需测试零静默 skip。
3. 输出 `.planning/phases/139-release-qualification/139-01-SUMMARY.md`。
</success_criteria>

