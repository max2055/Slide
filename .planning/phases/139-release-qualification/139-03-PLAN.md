---
phase: 139-release-qualification
plan: 03
type: execute
wave: 3
depends_on: [139-02]
files_modified:
  - frontend/e2e/release-critical-paths.spec.ts
  - frontend/e2e/release-security.spec.ts
  - tests/qualification/security-regression.spec.ts
  - tests/qualification/documentation-parity.spec.ts
  - .planning/phases/139-release-qualification/139-UAT.md
  - .planning/phases/139-release-qualification/139-EVIDENCE-MATRIX.md
  - .planning/phases/139-release-qualification/139-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CR-01, CR-02, CR-03, HI-03, HI-04, HI-05, HI-06, HI-07, HI-08, HI-09, HI-10, HI-11, HI-13, TG-02, TG-04]
must_haves:
  truths:
    - 关键用户故事通过浏览器验证 UI、API、持久化、刷新、状态反馈和失败恢复
    - 多主体授权、SQL 并发、XSS、SSRF、secret、WS 撤销和工具策略对抗测试通过
    - 每个 Phase 131 finding 有当前 commit 的证据，最终结论只能是 GO 或 NO-GO
---

# Phase 139 Plan 03：安全回归、浏览器闭环 UAT 与 GO/NO-GO

<objective>
执行最终生产资格验证，汇总可复现证据并明确决定 Slide 是否具备生产使用条件。本计划发现业务缺陷时退回责任 Phase，不在验证中顺手修复。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-system-level-code-review/131-REVIEW.md
@.planning/phases/131-system-level-code-review/131-UAT.md
@.planning/phases/139-release-qualification/139-CONTEXT.md
@.planning/phases/139-release-qualification/139-TEST-BASELINE.md
@.planning/phases/139-release-qualification/139-RECOVERY-EVIDENCE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 执行不可绕过的安全回归矩阵</name>
  <files>tests/qualification/security-regression.spec.ts, frontend/e2e/release-security.spec.ts, .planning/phases/139-release-qualification/139-EVIDENCE-MATRIX.md</files>
  <action>
    覆盖 admin/DBA/viewer/disabled user 的 REST/refresh/WS/Chat/tool matrix；跨用户 session read/write/watch/delete；危险工具 owner/scope/approval；直接 DML/DDL/多语句和 20 并发批准；用户停用后的 token/WS；结构化与 Markdown XSS；SSRF 私网/DNS/redirect/rebinding；实例/服务器/通知/LLM DTO 和日志 secret 扫描。每个测试保存 request/response reason code、DB side-effect count 和浏览器 DOM/网络证据，artifact 脱敏。
  </action>
  <verify><automated>pnpm exec vitest run tests/qualification/security-regression.spec.ts &amp;&amp; pnpm --filter slide-frontend exec playwright test e2e/release-security.spec.ts</automated></verify>
  <done>所有 Critical/安全 High 对抗测试通过，deny 场景目标副作用为 0。</done>
</task>

<task type="auto">
  <name>Task 2: 执行关键用户故事与文档一致性 UAT</name>
  <files>frontend/e2e/release-critical-paths.spec.ts, tests/qualification/documentation-parity.spec.ts, .planning/phases/139-release-qualification/139-UAT.md</files>
  <action>
    浏览器实际完成：登录/撤销；实例/服务器资源与健康；只读 SQL/提交审批/批准/Operation/验证；指标 -> 告警 -> 事件 -> RCA -> 操作 -> 恢复验证 -> 关闭；报表配置 -> 到期 -> 下载 -> 通知失败重放；Chat 发送/附件/停止/重连；Agent capability/config publish/rollback。每条路径断言 UI、API、DB、刷新与失败恢复，不用 page loaded 或 console-only 通过。文档测试核对支持矩阵、端口、环境变量、安装/升级/恢复与真实 API。
  </action>
  <verify><automated>pnpm --filter slide-frontend exec playwright test e2e/release-critical-paths.spec.ts &amp;&amp; pnpm exec vitest run tests/qualification/documentation-parity.spec.ts</automated></verify>
  <done>用户故事闭环无 skip；文档与实现差异为 0，所有失败有截图/trace/DB evidence。</done>
</task>

<task type="auto">
  <name>Task 3: 汇总 Finding 证据并作 GO/NO-GO 决策</name>
  <files>.planning/phases/139-release-qualification/139-EVIDENCE-MATRIX.md, .planning/phases/139-release-qualification/139-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    对 Phase 131 的 3 Critical、13 High、7 Medium、1 Low、4 design risks、6 test gaps、3 optimizations 逐项标 fixed/accepted-not-defect/deferred-optimization，并链接当前 commit 的命令/测试/artifact。Critical/High、必需 CI、空库/恢复、安全或关键用户故事任一失败即 NO-GO，不可豁免。Medium 只有在不影响安全/一致性/闭环且有 owner+deadline 时可形成风险接受。Verification 写 commit/schema/runtime/test counts 和明确 GO/NO-GO；只有 GO 才把 v0.9/STATE 标 shipped/complete，否则保持 blocked/in progress 并创建返修 Phase 建议。
  </action>
  <verify><automated>pnpm exec tsx tests/qualification/coverage-matrix.ts --verify-evidence .planning/phases/139-release-qualification/139-EVIDENCE-MATRIX.md</automated></verify>
  <done>没有 finding 缺证据；ROADMAP/STATE 与结论一致；139-VERIFICATION.md 含唯一明确结论。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 用历史 passed 标签替代当前证据 | evidence 必须绑定 commit/schema/environment |
| 测试 artifact 泄露 secret | 自动 redaction + artifact scan |
| 验证 Phase 顺手改业务掩盖失败 | finding 退回 owner Phase，qualification commit 只含测试/文档 |
| 有条件通过弱化门禁 | Critical/High 与 required job 不可豁免 |
</threat_model>

<verification>
- 完整 release workflow 在隔离环境从零执行一次。
- `139-EVIDENCE-MATRIX.md` 对 Phase 131 每项唯一映射。
- `139-VERIFICATION.md` 明确 GO 或 NO-GO，并与 ROADMAP/STATE 一致。
</verification>

<success_criteria>
1. TG-02/TG-04 和所有系统级闭环有运行证据。
2. 生产资格判断不依赖旧 Phase 声明或单一 `/api/health`。
3. 输出 `.planning/phases/139-release-qualification/139-03-SUMMARY.md`；最终验证文档为 `139-VERIFICATION.md`。
</success_criteria>

