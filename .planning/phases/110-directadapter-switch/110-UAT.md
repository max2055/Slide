---
status: testing
phase: 110-directadapter-switch
source:
  - .planning/phases/110-directadapter-switch/110-05-SUMMARY.md
started: 2026-05-26T16:30:00Z
updated: 2026-05-26T16:30:00Z
---

## Current Test

number: 1
name: 冷启动无报错
expected: |
  1. Kill all running servers
  2. Start backend and frontend fresh
  3. Open http://localhost:5173
  4. Login page renders without console errors. No Gateway-related WebSocket errors in browser console.
awaiting: user response
diagnosis: |
  Backend API works via curl (200). Frontend login form returns 401.
  Pre-auth API calls (agents, sessions, chat-history) show 401 which is expected.
  Root cause of login 401: frontend directLogin() credential binding issue - state.password or state.settings.username may not be correctly populated at click time.

## Tests

### 1. 冷启动无报错 [D-16-01]
expected: Kill servers, restart both, open http://localhost:5173. Login page renders without console errors. No Gateway-related WS errors.
result: issue
reported: "Backend needed restart to pick up Phase 110 code (routes 404 before restart). Login page shows 401 on pre-auth API calls (expected for agents/sessions/chat-history before login). Login button click returns 401 - frontend directLogin() credential binding issue (API works via curl: 200)."
severity: major

### 2. 登录 + WS auth 成功 [D-16-02]
expected: Login with admin/Tpam1234. Redirect to dashboard. Console shows WS auth_ok response. No 4001 close codes.
result: [pending]

### 3. Chat 流式对话 [D-16-03]
expected: Navigate to Chat, send message. Text streams progressively (text_delta). Tool start/result visible. Complete event renders final message.
result: [pending]

### 4. 页面刷新后 Chat history 恢复 [D-16-04]
expected: Send 2+ messages, refresh page. Chat view loads previous messages via GET /api/chat/history. Messages visible.
result: [pending]

### 5. WS 断线重连 → 历史恢复 [D-16-05]
expected: Stop backend. Chat indicator turns red/magenta with "重新连接中...". Restart backend. Indicator turns green with "已连接". Messages reload. Can send new message.
result: [pending]

### 6. AI Analysis invoke 正常 [D-16-06]
expected: Navigate to alert/instance detail page. Click AI Analysis. Analysis runs via DirectAdapter invoke. Result appears.
result: [pending]

### 7. 多 Tab 互不干扰 [D-16-07]
expected: Open Chat in Tab A and Tab B. Both tabs work independently. Messages don't cross-contaminate.
result: [pending]

### 8. Chat History REST API 直接调用 [D-16-08]
expected: GET /api/chat/history with JWT token returns JSON with messages array. Each message has role, content, timestamp. Status 200.
result: pass

## Summary

total: 8
passed: 1
issues: 1
pending: 6
skipped: 0
blocked: 0

## Gaps

### Gap 1: Login form credential binding
- truth: "Login with admin/Tpam1234 redirects to main dashboard"
- status: failed
- reason: "Frontend login button click returns 401. Backend API works correctly via curl (200 + token returned). The frontend directLogin() does not send correct credentials - likely state.password or state.settings.username not populated at click time."
- severity: major
- test: 1
- root_cause: "login-gate.ts:76 captures state.password?.trim() and state.settings.username?.trim() but these Lit reactive properties may not be synchronized when the click event fires. The password input uses @input→state.setPassword(v) and username uses @input→state.applySettings()."
- fix: "Investigate Lit @state() binding timing. Add console.log of u/p before calling directLogin(). Consider using this.shadowRoot.querySelector('input') to read values directly from DOM instead of relying on reactive state."

### Gap 2: /api/sessions broken import path (FIXED)
- truth: "GET /api/sessions returns list of sessions"
- status: resolved
- reason: "Import path '../../../src/chat-database-service.js' resolved to 'apps/src/' instead of 'apps/db-ops-api/src/'. Fixed to './src/chat-database-service.js'."
- severity: blocker
- test: 8c
- fix_commit: "fix(110): correct sessions endpoint import path (../src → ./src)"
