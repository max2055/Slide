---
plan: 119-01
phase: 119-p0-bug-9600
status: completed
---

## Plan 119-01: 代码审查问题修复（P0 + P1 + P2）

### Objective
根据 2026-06-10 代码审查报告，修复关键 Bug、移除死代码、统一设计模式。

### Verification
- ✅ TypeScript 编译通过（无新增类型错误）
- ✅ Git diff: -9,247 行死代码，+482 行修复

---

## Wave 1: P0 关键 Bug 修复

### 1.1 backfillMissingToolResults 无限循环
- **文件**: `packages/agent-core/src/runner.ts:793-795`
- **修复**: while 循环体添加 `insertAt++`

### 1.2 database_instances 表缺失 db_version 列
- **文件**: `apps/db-ops-api/sql/schema.sql`
- **文件**: 创建 migration 016
- **修复**: 为 `database_instances` 添加 `db_version VARCHAR(50)` 列
- **注意**: `data_size_gb` 列存在于 `metrics_metadata` 表但代码中作为 `database_instances` 列使用，需确认是否需在 `database_instances` 表也添加

### 1.3 API_PORT vs BACKEND_PORT 不一致
- **文件**: `apps/db-ops-api/server.ts:4156`
- **修复**: 改为 `process.env.BACKEND_PORT || process.env.API_PORT || 3000` 以兼容两种变量名

---

## Wave 2: P1 前后端路由修复 + 安全问题

### 2.1 文档路由不匹配
- **文件**: `frontend/src/app/ui/views/docs-viewer.ts:96`
- **修复**: `/api/docs/content/` → `/api/docs/files/`

### 2.2 ENCRYPTION_KEY 未文档化且不安全 fallback
- **文件**: `.env.example`（两个）
- **文件**: `apps/db-ops-api/src/db-connection.ts`
- **文件**: `apps/db-ops-api/src/index-service.ts`
- **文件**: `apps/db-ops-api/src/schema-service.ts`
- **修复**: 添加 `ENCRYPTION_KEY` 到 .env.example；启动时无有效 key 则报错退出

### 2.3 生成技能 this 绑定错误
- **文件**: `apps/db-ops-api/src/skills/generated/check_health/tools.ts`
- **修复**: 箭头函数改为普通函数，或使用 bind

### 2.4 streamIdleTimeoutS 未实现
- **文件**: `packages/agent-core/src/openai-provider.ts`
- **修复**: 在 chatStream() 中实现空闲超时逻辑

### 2.5 apps/db-ops-skills 路径解析错误
- **文件**: `apps/db-ops-api/src/skills/loader.ts:405`
- **修复**: 使用基于源码文件位置的相对路径而非 CWD 相对路径
