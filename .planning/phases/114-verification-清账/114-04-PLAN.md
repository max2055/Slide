---
phase: 114-verification-清账
plan: 04
type: execute
wave: 2
depends_on: [114-01]
files_modified:
  - frontend/src/app/ui/navigation.ts
  - frontend/src/app/ui/app-render.ts
  - frontend/src/app/ui/views/settings-shell.ts
  - .planning/phases/100-安全紧急修复/100-HUMAN-UAT.md
  - .planning/phases/102-UI统一/102-VERIFICATION.md
  - .planning/phases/112-frontend-cleanup-cron/112-HUMAN-UAT.md
autonomous: false
requirements: [VER-01]
user_setup: ["Backend (:3000) + Frontend (:5173) running"]
must_haves:
  truths:
    - "Sidebar has a single 'Settings' entry pointing to /settings"
    - "/settings page has sub-navigation tabs: Appearance / LLM / AI / Scoring / Cron Jobs / Users / RBAC"
    - "Each sub-tab renders its corresponding view inline"
    - "All 12 human verification items are tested and have results recorded"
    - "100-HUMAN-UAT.md, 102-VERIFICATION.md, 112-HUMAN-UAT.md are updated"
  artifacts:
    - path: frontend/src/app/ui/views/settings-shell.ts
      provides: Settings shell component with sub-page routing
      contains: "settings-shell"
    - path: frontend/src/app/ui/navigation.ts
      provides: Updated tab groups with single settings entry
      contains: "TAB_GROUPS"
    - path: .planning/phases/100-安全紧急修复/100-HUMAN-UAT.md
      provides: Updated with verification results
      contains: "result: passed"
    - path: .planning/phases/102-UI统一/102-VERIFICATION.md
      provides: Status changed to resolved
      contains: "status: resolved"
    - path: .planning/phases/112-frontend-cleanup-cron/112-HUMAN-UAT.md
      provides: Updated with verification results
      contains: "result: passed"
  key_links:
    - from: navigation.ts TAB_GROUPS.settings
      to: settings-shell.ts
      via: app-render.ts routing
      pattern: "settings-shell"
---

<objective>
**Frontend Final Polish: Settings consolidation + 12 human verifications**

Restructure the flat settings tabs into a unified Settings page with sub-navigation. Complete all 12 pending human verification items from Phases 100/102/112.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-verification-清账/114-CONTEXT.md
@frontend/src/app/ui/navigation.ts
@frontend/src/app/ui/app-render.ts
@frontend/src/app/ui/views/ai-settings.ts
@frontend/src/app/ui/views/llm-config.ts
@frontend/src/app/ui/views/scoring-settings.ts
@frontend/src/app/ui/views/cron-jobs-settings.ts
@frontend/src/app/ui/views/users-management.ts
@frontend/src/app/ui/views/rbac-page.ts
@.planning/phases/100-安全紧急修复/100-HUMAN-UAT.md
@.planning/phases/102-UI统一/102-VERIFICATION.md
@.planning/phases/112-frontend-cleanup-cron/112-HUMAN-UAT.md
</context>

<tasks>

<task type="execute" gate="pass">
  <name>Task 1: Create settings-shell.ts unified settings page</name>
  <files>
    frontend/src/app/ui/views/settings-shell.ts (new)
    frontend/src/app/ui/navigation.ts
    frontend/src/app/ui/app-render.ts
  </files>
  <what-built>
    New settings-shell Lit component with sub-tab navigation. The sidebar now has a single "Settings" entry. Clicking it loads the shell which renders sub-pages inline.
  </what-built>
  <how-to-verify>
    A. Create frontend/src/app/ui/views/settings-shell.ts:
       - LitElement component with @customElement('settings-shell')
       - Internal state: selectedSubTab (default: 'ai-settings')
       - Sub-tabs bar at top: AI Settings / LLM Config / Scoring / Cron Jobs / Appearance / Users / RBAC
       - When a sub-tab is selected, render the corresponding component:
         - ai-settings → `<ai-settings></ai-settings>`
         - llm-config → `<llm-config-page></llm-config-page>` (check actual tag name from llm-config.ts)
         - scoring-settings → `<scoring-settings></scoring-settings>`
         - cron-jobs → `<cron-jobs-settings></cron-jobs-settings>`
         - appearance → `<appearance-settings></appearance-settings>` (simple component with theme toggle)
         - users → `<users-management></users-management>`
         - rbac → `<rbac-page></rbac-page>`
       - Use the same patterns from navigateToTab() in app-render.ts for component loading
       - Keep it simple: sub-tabs are just buttons/tabs that set this.selectedSubTab

    B. In frontend/src/app/ui/navigation.ts:
       - Change TAB_GROUPS.settings to: `settings: ['settings']` (single entry)
       - The 'settings' tab maps to path '/settings'

    C. In frontend/src/app/ui/app-render.ts:
       - Add a route for '/settings' that renders `<settings-shell></settings-shell>`
       - The existing individual settings routes can stay for backward compatibility (but sidebar only shows the unified entry)
       - Remove the `appearance` placeholder from lines ~741-742 — it should now render the actual settings-shell
       - Remove or comment out the OpenClaw `sessions`/`agents` tab group if still present

    Commit: "feat(114): create unified settings-shell with sub-page navigation"
  </how-to-verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Phase 100 (2 items) + Phase 102 (5 items)</name>
  <files>
    .planning/phases/100-安全紧急修复/100-HUMAN-UAT.md
    .planning/phases/102-UI统一/102-VERIFICATION.md
  </files>
  <what-built>
    7 human verification items tested in browser. Results recorded in HUMAN-UAT/VERIFICATION files.
  </what-built>
  <how-to-verify>
    PART A: Phase 100 (2 items)

    1. Start backend + frontend: `cd apps/db-ops-api && npx tsx server.ts &` and `cd frontend && npm run dev &`
    2. **Login page eyeOff icon**: Open http://localhost:5173, click password visibility toggle, confirm eye/eyeOff icons render correctly, no JS errors in console
    3. **401 responses**: Run these 4 curl commands (no auth header):
       `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/database/instances`
       `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/alerts`
       `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/metrics/1`
       `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/chat/history`
       All 4 must return 401.
    4. After human reports results → update 100-HUMAN-UAT.md → commit

    PART B: Phase 102 (5 items)

    5. **Login page renders without errors**: Login page loads, no console errors
    6. **Dashboard stat cards**: 6 stat cards visible, correct labels/values/hints, correct variant colors
    7. **Migrated stat-card views**: Check Alerts/Reports/Schema Management/Overview pages — stat cards have correct variant border colors
    8. **Emoji replacement**: Query Analysis tab (search + bar-chart SVG), Instance Detail capacity (package SVG), Event Management empty (triangle-alert SVG) — no emoji characters
    9. **No console errors**: Visit all pages, check DevTools Console — zero JS errors
    10. After human reports results → update 102-VERIFICATION.md frontmatter: status → resolved, add results → commit

    Wait for human feedback before proceeding. Update files and commit after each phase group is complete.
  </how-to-verify>
  <resume-signal>
    Report results for Phase 100 items (eyeOff + 401) and Phase 102 items (login + dashboard + stat-cards + emoji + console). Format: "Item X: passed/failed"
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Phase 112 (5 items) + settings-shell check</name>
  <files>
    .planning/phases/112-frontend-cleanup-cron/112-HUMAN-UAT.md
  </files>
  <what-built>
    5 human verification items tested in browser. Results recorded in 112-HUMAN-UAT.md. Settings shell verified working.
  </what-built>
  <how-to-verify>
    PART A: Settings shell verification

    1. Navigate to the unified Settings page
    2. Confirm all sub-tabs render: AI Settings / LLM Config / Scoring / Cron Jobs / Appearance / Users / RBAC
    3. Click each sub-tab, confirm the corresponding view loads
    4. Confirm no console errors during tab switching

    PART B: Phase 112 (5 items)

    5. **Settings tab navigation**: "Cron Jobs" sub-tab visible in Settings, clickable, loads cron page
    6. **Toggle switch**: Toggle a cron job on/off, confirm immediate UI change, refresh confirms persistence
    7. **Inline cron editor**: Double-click cron expression, select preset from dropdown, verify preview shows next 5 execution dates
    8. **Backend server startup**: Check terminal logs contain "CronManager: N jobs scheduled" (N >= 1)
    9. **E2E full flow**: Create new job → edit cron → toggle → delete → no errors throughout
    10. After human reports results → update 112-HUMAN-UAT.md → commit

    Wait for human feedback before proceeding.
  </how-to-verify>
  <resume-signal>
    Report results for Phase 112 items (settings tab + toggle + cron editor + server startup + E2E flow). Format: "Item X: passed/failed"
  </resume-signal>
</task>

</tasks>

<success_criteria>
1. Sidebar has single "Settings" entry → /settings page with sub-navigation
2. All 7 sub-pages render correctly
3. 12/12 human verification items tested and recorded
4. All HUMAN-UAT/VERIFICATION files updated and committed
</success_criteria>

<output>
Create .planning/phases/114-verification-清账/114-04-SUMMARY.md when done
</output>
