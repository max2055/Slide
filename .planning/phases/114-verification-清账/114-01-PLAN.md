---
phase: 114-verification-清账
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/server.ts
  - frontend/src/app/ui/views/users-management.ts
  - frontend/src/app/ui/views/rbac-page.ts
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "All 18 unprotected routes return 401 when called without JWT token"
    - "All 18 routes return normal data when called with valid JWT token"
    - "users-management.ts and rbac-page.ts use ApiClient.fetchWithAuth instead of raw fetch"
  artifacts:
    - path: apps/db-ops-api/server.ts
      provides: verifyToken preHandler added to 18 unprotected routes
      contains: "preHandler: [verifyToken]"
    - path: frontend/src/app/ui/views/users-management.ts
      provides: Standardized API calls via ApiClient
      contains: "apiClient.fetchWithAuth"
    - path: frontend/src/app/ui/views/rbac-page.ts
      provides: Standardized API calls via ApiClient
      contains: "apiClient.fetchWithAuth"
  key_links:
    - from: server.ts verifyToken middleware
      to: 18 route registrations
      via: preHandler array
      pattern: "preHandler: [verifyToken]"
---

<objective>
**Auth Audit & Fix: Close all authentication gaps**

Add `verifyToken` JWT middleware to 18 unprotected API routes. Replace raw `fetch()` calls with `ApiClient.fetchWithAuth()` in two frontend settings pages that bypass the token refresh mechanism.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-verification-清账/114-CONTEXT.md
@apps/db-ops-api/server.ts
@frontend/src/app/ui/views/users-management.ts
@frontend/src/app/ui/views/rbac-page.ts
@frontend/src/api/index.ts
</context>

<tasks>

<task type="execute" gate="pass">
  <name>Task 1: Add verifyToken to 18 unprotected routes in server.ts</name>
  <files>
    apps/db-ops-api/server.ts
  </files>
  <what-built>
    All 18 routes listed below get `preHandler: [verifyToken]` added. Health check and OPTIONS routes remain unprotected.
  </what-built>
  <how-to-verify>
    Routes to protect (all GET unless noted):

    1. /api/version
    2. /api/docs/list
    3. /api/docs/files/:file
    4. /api/llm/configs
    5. /api/llm/configs/:id
    6. POST /api/llm/test
    7. /api/dashboard/capacity-trend
    8. /api/dashboard/ai-stats
    9. /api/database/instances/:id/tables
    10. /api/database/instances/:id/tables/:tableName/describe
    11. /api/database/instances/:id/tables/:tableName/indexes
    12. /api/alerts/escalation/rules
    13. /api/maintenance-windows
    14. /api/maintenance-windows/check/:instanceId
    15. /api/silence
    16. /api/baseline/:instanceId/:metricName
    17. /api/chat/greeting

    For each: find the `server.{method}(url, async (...)` line, add `preHandler: [verifyToken]` to the route options if not already present.

    IMPORTANT: Some routes may already have a `preHandler` with `requireRole()` — use array spread: `preHandler: [verifyToken, requireRole('admin')]`.
    Some routes are registered with a third options argument — add `preHandler` there.
    Some routes use `server.register()` pattern — check the route handler setup.

    Verify: `grep "preHandler.*verifyToken" server.ts | wc -l` should increase by 17.
  </how-to-verify>
</task>

<task type="execute" gate="pass">
  <name>Task 2: Standardize frontend API calls in users-management.ts and rbac-page.ts</name>
  <files>
    frontend/src/app/ui/views/users-management.ts
    frontend/src/app/ui/views/rbac-page.ts
  </files>
  <what-built>
    Both files use `ApiClient.fetchWithAuth()` from `../../api/index.js` instead of raw `fetch()` with manual `localStorage.getItem('token')`.
  </what-built>
  <how-to-verify>
    1. Read frontend/src/api/index.ts to understand ApiClient export pattern
    2. In users-management.ts:
       - Import ApiClient or use the existing import
       - Replace `fetch(url, { headers: { 'Authorization': 'Bearer ' + token } })` patterns with `apiClient.fetchWithAuth(url, options)`
    3. In rbac-page.ts: same approach
    4. Check that localStorage.getItem('token') manual auth header patterns are replaced
    5. Verify no `localStorage.getItem('token')` remains in these files
  </how-to-verify>
</task>

</tasks>

<success_criteria>
1. 17 routes now require JWT auth (verifyToken)
2. users-management.ts uses ApiClient for all API calls
3. rbac-page.ts uses ApiClient for all API calls
4. TypeScript compilation passes on both backend and frontend
</success_criteria>

<output>
Create .planning/phases/114-verification-清账/114-01-SUMMARY.md when done
</output>
