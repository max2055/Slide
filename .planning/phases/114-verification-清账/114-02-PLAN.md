---
phase: 114-verification-清账
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/src/adapter/get-agent-engine.ts
  - apps/db-ops-api/server.ts
  - frontend/src/app/ui/navigation.ts
  - apps/db-ops-api/src/skills/generated/topsql-analysis/SKILL.md
  - apps/db-ops-api/src/skills/generated/fault-diagnosis/SKILL.md
  - apps/db-ops-api/src/skills/generated/alert-rca/SKILL.md
  - apps/db-ops-api/src/skills/generated/check_health/SKILL.md
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "The openclaw/ adapter directory no longer exists"
    - "getAdapterType() only returns 'direct' (no 'openclaw' branch)"
    - "server.ts has no OpenClaw-specific adapter code paths"
    - "navigation.ts has no 'openclaw' tab group"
    - "openclaw-reference/ directory is deleted"
    - "Stale test files referencing deleted OpenClaw UI paths are removed"
  artifacts:
    - path: apps/db-ops-api/src/adapter/get-agent-engine.ts
      provides: Simplified adapter selection (DirectAdapter only)
      contains: "getAdapterType"
    - path: apps/db-ops-api/server.ts
      provides: Cleaned startup without OpenClaw conditional branches
      contains: "getAgentEngine"
    - path: frontend/src/app/ui/navigation.ts
      provides: Tab groups without openclaw section
      contains: "TAB_GROUPS"
  key_links: []
---

<objective>
**OpenClaw Source Code Cleanup: Remove all dead OpenClaw code and invalid files**

Delete the OpenClaw adapter directory (11 files, 2485 lines), remove reference copies, clean up stale tests and skill files, simplify adapter selection to DirectAdapter-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-verification-清账/114-CONTEXT.md
@apps/db-ops-api/src/adapter/get-agent-engine.ts
@apps/db-ops-api/server.ts
@frontend/src/app/ui/navigation.ts
</context>

<tasks>

<task type="execute" gate="pass">
  <name>Task 1: Delete invalid directories and files</name>
  <files>
    (deletions only)
  </files>
  <what-built>
    Remove physical directories and files that are dead/legacy OpenClaw artifacts.
  </what-built>
  <how-to-verify>
    Delete these:
    A. Directories:
       - openclaw-reference/ (full repo copy, already gitignored)
       - .agents/skills/openclaw-* (14 skill dirs: openclaw-debugging, openclaw-docs, openclaw-ghsa-maintainer, openclaw-test-heap-leaks, openclaw-test-performance, openclaw-testing, openclaw-parallels-smoke, openclaw-pr-maintainer, openclaw-pre-release-plugin-testing, openclaw-qa-testing, openclaw-refactor-docs, openclaw-release-maintainer, openclaw-secret-scanning-maintainer, openclaw-small-bugfix-sweep)
       - apps/db-ops-api/.slide/
       - .openclaw/ (root)
       - tmp/sql-audit/.openclaw/
       - tmp/alert-ops/.openclaw/
       - tmp/db-admin/.openclaw/
    B. Files:
       - openclaw菜单_已填充.xlsx

    Use `rm -rf` for each. Commit with message: "chore(114): remove OpenClaw reference files and invalid directories"
  </how-to-verify>
</task>

<task type="execute" gate="pass">
  <name>Task 2: Remove OpenClaw adapter source code and simplify get-agent-engine.ts</name>
  <files>
    apps/db-ops-api/src/adapter/get-agent-engine.ts
    apps/db-ops-api/server.ts
  </files>
  <what-built>
    OpenClaw adapter directory deleted. get-agent-engine.ts simplified to DirectAdapter-only. server.ts OpenClaw conditional branches removed.
  </what-built>
  <how-to-verify>
    A. Delete these files/directories:
       - apps/db-ops-api/src/adapter/openclaw/ (entire directory, 11 files)
       - apps/db-ops-api/src/adapter/__tests__/openclaw-adapter.test.ts
       - apps/db-ops-api/src/adapter/__tests__/dual-run.test.ts
       - apps/db-ops-api/scripts/start-openclaw-gateway.mjs

    B. In apps/db-ops-api/src/adapter/get-agent-engine.ts:
       - Remove OpenClawAdapter import
       - Simplify getAdapterType() to always return 'direct'
       - Remove ENABLE_AGENT_ADAPTER_CHAT/ANALYSIS env var reads (not needed)
       - Remove openclawAdapterInstance variable
       - Remove OpenClawAdapter creation branch in getAgentEngine()
       - Clean up unused imports

    C. In apps/db-ops-api/server.ts:
       - Remove the conditional block at line ~3584: `if (getAdapterType('chat') === 'openclaw') { ... }`
       - Remove `syncLLMConfigToOpenClaw` dynamic imports and calls (lines ~559, ~571)
       - Remove OPENCLAW_STATE_DIR env var setup if it was only for openclaw adapter

    D. Run TypeScript check: `cd apps/db-ops-api && npx tsc --noEmit` (may have pre-existing errors unrelated to our changes)

    Commit with message: "refactor(114): remove OpenClaw adapter, simplify to DirectAdapter-only"
  </how-to-verify>
</task>

<task type="execute" gate="pass">
  <name>Task 3: Clean up SKILL.md frontmatter, navigation, and stale tests</name>
  <files>
    apps/db-ops-api/src/skills/generated/*/SKILL.md
    frontend/src/app/ui/navigation.ts
    apps/db-ops-api/tests/phase-94-*.test.ts
  </files>
  <what-built>
    SKILL.md files cleaned of metadata.openclaw. Navigation removed openclaw tab group. Stale test files removed.
  </what-built>
  <how-to-verify>
    A. In these 4 SKILL.md files, remove the `metadata.openclaw` field from YAML frontmatter:
       - apps/db-ops-api/src/skills/generated/topsql-analysis/SKILL.md
       - apps/db-ops-api/src/skills/generated/fault-diagnosis/SKILL.md
       - apps/db-ops-api/src/skills/generated/alert-rca/SKILL.md
       - apps/db-ops-api/src/skills/generated/check_health/SKILL.md

    B. In frontend/src/app/ui/navigation.ts:
       - Remove `openclaw: ['sessions', 'agents']` from TAB_GROUPS
       - Remove any references to 'openclaw' tab group in rendering logic

    C. Delete these stale test files (they reference deleted openclaw UI paths):
       - apps/db-ops-api/tests/phase-94-docs-viewer.test.ts
       - apps/db-ops-api/tests/phase-94-app-render-docs.test.ts
       - apps/db-ops-api/tests/phase-94-navigation-docs.test.ts

    Commit with message: "chore(114): clean OpenClaw from SKILL frontmatter, navigation, and stale tests"
  </how-to-verify>
</task>

</tasks>

<success_criteria>
1. No `apps/db-ops-api/src/adapter/openclaw/` directory
2. TypeScript compilation passes
3. `grep -rn "openclaw" apps/db-ops-api/src/ --include="*.ts" | grep -v node_modules` only shows comments
4. Navigation has no openclaw tab group
</success_criteria>

<output>
Create .planning/phases/114-verification-清账/114-02-SUMMARY.md when done
</output>
