---
phase: 108-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/src/adapter/types.ts (NEW)
  - apps/db-ops-api/src/adapter/llm-provider.ts (NEW)
  - apps/db-ops-api/src/adapter/direct-adapter.ts (NEW)
  - apps/db-ops-api/src/adapter/get-agent-engine.ts (NEW)
  - apps/db-ops-api/src/adapter/__tests__/ia-agent-engine.test.ts (NEW)
  - apps/db-ops-api/src/adapter/__tests__/adapter-parity.ts (NEW)
  - apps/db-ops-api/src/adapter/__tests__/direct-adapter.test.ts (NEW)
  - apps/db-ops-api/src/adapter/shared/protocol-types.ts (NEW)
  - apps/db-ops-api/src/adapter/shared/error-codes.ts (NEW)
  - apps/db-ops-api/src/agent-service.ts (MODIFY — trim dead code)
  - apps/db-ops-api/src/agent-service-v2.ts (DELETE)
  - apps/db-ops-api/src/gateway/openclaw-bridge.ts (MODIFY — remove sendMessageToAgent)
  - apps/db-ops-api/src/gateway/openclaw-integration.ts (DELETE)
autonomous: true
requirements:
  - MIG-01
user_setup: []
must_haves:
  truths:
    - "IAgentEngine 接口定义完整 -- .start(), .chat(), .invoke(), .listTools(), .capabilities()"
    - "DirectAdapter.start() 启动最小 WS 传输服务在 AGENT_WS_PORT (默认 28789)，支持 chat.send 和 chat.history RPC"
    - "ChatEvent 联合类型定义了 text_delta / tool_start / tool_result / tool_error / complete / error 六种事件"
    - "AnthropicProvider 实现了 agent-core LLMProvider 接口，可调用 claude-sonnet-4"
    - "DirectAdapter 通过 AgentRunner 实现了 IAgentEngine 接口"
    - "getAgentEngine() 懒加载单例工厂可返回 DirectAdapter 实例"
    - "agent-service.ts 仅保留 getAgentGreeting() 和 AGENT_GREETING，其余代码已删除"
    - "agent-service-v2.ts 文件已删除"
    - "openclaw-bridge.ts 中 sendMessageToAgent 已删除，handleAgentRequest import 已移除"
    - "openclaw-integration.ts 文件已删除"
    - "gateway/protocol.ts 和 gateway/error-codes.ts 中与 OpenClaw 无关的类型已提取到 adapter/shared/"
    - "MIG-01 测试通过 -- 接口编译、工具注册、chat/invoke/start 路径的基本测试均通过"
  artifacts:
    - path: "apps/db-ops-api/src/adapter/types.ts"
      provides: "IAgentEngine 接口 (含 .start()) + ChatEvent + AgentCapabilities + ChatResult + InvokeResult"
      min_lines: 85
      exports: ["IAgentEngine", "ChatEvent", "AgentCapabilities", "ChatResult", "InvokeResult"]
    - path: "apps/db-ops-api/src/adapter/llm-provider.ts"
      provides: "AnthropicProvider implementing @slide/agent-core LLMProvider"
      min_lines: 120
      exports: ["AnthropicProvider"]
    - path: "apps/db-ops-api/src/adapter/direct-adapter.ts"
      provides: "DirectAdapter implementing IAgentEngine, 含 start() WS 传输层实现"
      min_lines: 200
      exports: ["DirectAdapter"]
    - path: "apps/db-ops-api/src/adapter/get-agent-engine.ts"
      provides: "getAgentEngine() 工厂函数 + lazy singleton"
      min_lines: 30
      exports: ["getAgentEngine"]
    - path: "apps/db-ops-api/src/adapter/shared/protocol-types.ts"
      provides: "与 OpenClaw 无关的通用 WS 协议类型（ConnectRequest, ConnectChallenge 等）"
      min_lines: 30
    - path: "apps/db-ops-api/src/adapter/shared/error-codes.ts"
      provides: "与 OpenClaw 无关的通用错误码常量"
      min_lines: 30
  key_links:
    - from: "adapter/types.ts"
      to: "@slide/agent-core"
      via: "import { ToolSchema } from '@slide/agent-core'"
      pattern: "import.*ToolSchema.*from.*@slide/agent-core"
    - from: "adapter/direct-adapter.ts"
      to: "adapter/types.ts"
      via: "implements IAgentEngine"
      pattern: "implements IAgentEngine"
    - from: "adapter/direct-adapter.ts"
      to: "@slide/agent-core"
      via: "import { AgentRunner, ToolRegistry } from '@slide/agent-core'"
      pattern: "import.*from.*@slide/agent-core"
    - from: "adapter/llm-provider.ts"
      to: "@slide/agent-core"
      via: "import { LLMProvider, type Message } from '@slide/agent-core'"
      pattern: "import.*LLMProvider.*from.*@slide/agent-core"
    - from: "adapter/direct-adapter.ts"
      to: "ws"
      via: "import { WebSocketServer, WebSocket } from 'ws'"
      pattern: "import.*WebSocketServer.*from.*ws"
---

<objective>
定义 IAgentEngine 接口契约（含 .start() WS 传输层方法）、实现 DirectAdapter（包装 @slide/agent-core 的 AgentRunner）、接入 AnthropicProvider LLM 实现，完成死代码清理和共享层提取。

Purpose: 这是 Phase 108 的基础层。接口契约是所有适配器的共同基础。IAgentEngine.start() 保证适配器在 server.ts 启动时就能准备好 WS 传输层——OpenClawAdapter 启动 Gateway，DirectAdapter 启动最小 WS 服务。DirectAdapter 是默认 Agent 引擎的适配器包装。死代码清理防止后续迁移时遗留 import 断裂。

Output:
- adapter/types.ts — IAgentEngine 接口定义（ChatEvent, AgentCapabilities, ChatResult, InvokeResult, start()）
- adapter/llm-provider.ts — AnthropicProvider（包装 Anthropic SDK 实现 @slide/agent-core 的 LLMProvider）
- adapter/direct-adapter.ts — DirectAdapter（在 AgentRunner 上包装 IAgentEngine 接口，含 start() WS 传输层）
- adapter/get-agent-engine.ts — getAgentEngine() 工厂函数 + 懒加载单例
- adapter/shared/ — 从 gateway/protocol.ts 和 gateway/error-codes.ts 提取的非 OpenClaw 通用类型
- adapter/__tests__/ — 接口契约测试 + DirectAdapter 单元测试
- agent-service.ts — 只保留 getAgentGreeting 和 AGENT_GREETING
- agent-service-v2.ts — 已删除
- openclaw-bridge.ts — 移除 sendMessageToAgent 和 handleAgentRequest import
- openclaw-integration.ts — 已删除
- tsc --noEmit 编译通过
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-agent/108-CONTEXT.md
@.planning/phases/108-agent/108-RESEARCH.md
@.planning/phases/108-agent/108-AI-SPEC.md
@.planning/phases/108-agent/108-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key types consumed by Plan 1 tasks. Extract from existing codebase. -->

From packages/agent-core/src/index.ts:
```typescript
export { ToolRegistry, validateJsonSchema, castToolParams } from "./tool-registry.js";
export { AgentRunner, NoopHook } from "./runner.js";
export type {
  LLMProvider, LLMResponse, LLMCallOptions, StreamCallbacks, ToolCallRequest, Message,
  Tool, ToolRegistry as IToolRegistry, ToolSchema, JsonSchemaProperty,
  AgentHook, AgentHookContext, ToolEvent,
  AgentRunSpec, AgentRunResult,
} from "./types.js";
```

From packages/agent-core/src/types.ts (LLMProvider interface):
```typescript
export interface LLMProvider {
  getDefaultModel(): string;
  chat(messages: Message[], tools: ToolSchema[], options?: LLMCallOptions): Promise<LLMResponse>;
  chatStream(messages: Message[], tools: ToolSchema[], callbacks: StreamCallbacks, options?: LLMCallOptions): Promise<LLMResponse>;
}

export interface ToolSchema {
  name: string;
  description: string;
  parameters: { type: "object"; properties: Record<string, JsonSchemaProperty>; required?: string[] };
}

export interface AgentRunSpec {
  initialMessages: Message[]; tools: ToolRegistry; model: string;
  maxIterations: number; maxToolResultChars: number; temperature?: number; maxTokens?: number;
  hook: AgentHook; contextWindowTokens?: number; /* ... other optional fields */
}

export interface AgentRunResult {
  finalContent: string | null; messages: Message[]; toolsUsed: string[];
  usage: Record<string, number>; stopReason: string; error: string | null; toolEvents: ToolEvent[];
}
```

From packages/agent-core/src/types.ts (AgentHook interface):
```typescript
export interface AgentHook {
  wantsStreaming(): boolean;
  beforeIteration(ctx: AgentHookContext): Promise<void> | void;
  onStream(ctx: AgentHookContext, delta: string): Promise<void> | void;
  onStreamEnd(ctx: AgentHookContext, resuming: boolean): Promise<void> | void;
  beforeExecuteTools(ctx: AgentHookContext): Promise<void> | void;
  emitReasoning(text: string | null): Promise<void> | void;
  emitReasoningEnd(): Promise<void> | void;
  afterIteration(ctx: AgentHookContext): Promise<void> | void;
  finalizeContent(ctx: AgentHookContext, content: string | null): string | null;
}
```

From packages/agent-core/src/runner.ts (AgentRunner constructor + run signature):
```typescript
export class AgentRunner {
  constructor(provider: LLMProvider);
  async run(spec: AgentRunSpec): Promise<AgentRunResult>;
  getProvider(): LLMProvider;
  setProvider(provider: LLMProvider): void;
}
```

From apps/db-ops-api/src/gateway/openclaw-runtime.ts (existing singleton pattern to replicate):
```typescript
let openClawRuntime: { config: OpenClawConfig; tools: ReturnType<typeof createOpenClawTools> } | null = null;
export async function getOpenClawRuntime() {
  if (!openClawRuntime) { openClawRuntime = await initializeOpenClawRuntime(); }
  return openClawRuntime;
}
```

From apps/db-ops-api/server.ts (current imports that will be replaced in Plan 3):
```typescript
// Line 71-72 — replaced with getAgentEngine() in Plan 3
import { startGatewayServer } from '../../src/gateway/server.js';
import { getOpenClawRuntime } from './src/gateway/openclaw-runtime.js';
// Line 60 — preserved through all plans
import { getAgentGreeting } from './src/agent-service.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 IAgentEngine 接口（含 start()）+ ChatEvent 类型 + test scaffold</name>
  <files>
    apps/db-ops-api/src/adapter/types.ts (NEW)
    apps/db-ops-api/src/adapter/__tests__/ia-agent-engine.test.ts (NEW)
    apps/db-ops-api/src/adapter/__tests__/adapter-parity.ts (NEW)
  </files>
  <read_first>
    - packages/agent-core/src/types.ts (LLMProvider, ToolSchema, AgentRunResult interfaces)
    - packages/agent-core/src/index.ts (public exports)
    - .planning/phases/108-agent/108-CONTEXT.md (D-01 through D-05: interface contract)
    - .planning/phases/108-agent/108-RESEARCH.md (Pattern 2: Adapter Event Mapping, code examples for interface)
    - .planning/phases/108-agent/108-AI-SPEC.md (Section 4: Implementation Guidance, ChatEvent type)
  </read_first>
  <action>
    Create apps/db-ops-api/src/adapter/types.ts with:

    1. Define ChatEvent type as discriminated union with six variants:
       - TextDeltaEvent { type: 'text_delta'; delta: string }
       - ToolStartEvent { type: 'tool_start'; toolName: string; args: Record<string, unknown> }
       - ToolResultEvent { type: 'tool_result'; toolName: string; result: unknown }
       - ToolErrorEvent { type: 'tool_error'; toolName: string; error: string }
       - CompleteEvent { type: 'complete'; finalContent?: string }
       - ErrorEvent { type: 'error'; error: string }

    2. Define AgentCapabilities interface with: streaming (boolean), toolCalling (boolean), maxContextTokens (number), supportsCustomSystemPrompt (boolean)

    3. Define ChatResult interface with: finalContent (string | null), usage (Record<string, number> optional)

    4. Define InvokeResult interface with: content (string | null), usage (Record<string, number> optional)

    5. Define IAgentEngine interface with FIVE methods:
       - start(): Promise<void> — 启动 WS 传输层服务。OpenClawAdapter 启动 Gateway WebSocket（port 28789），DirectAdapter 启动最小 WS 服务（AGENT_WS_PORT || 28789）。
          server.ts 在启动后立即调用 engine.start()，确保前端 WebSocket 端口在 Chat 连接前就绪。此方法幂等——重复调用不重复启动。
       - chat(sessionKey: string, message: string, onEvent: (event: ChatEvent) => void): Promise&lt;ChatResult&gt;
       - invoke(sessionKey: string, message: string, systemPrompt?: string): Promise&lt;InvokeResult&gt;
       - listTools(): ToolSchema[] — ToolSchema imported from @slide/agent-core
       - capabilities(): AgentCapabilities

    6. Import ToolSchema from @slide/agent-core (not from tools/types.ts which has a different AnyAgentTool type)

    7. Export all types. Do not export any implementation classes.

    Create apps/db-ops-api/src/adapter/__tests__/adapter-parity.ts with:
    - AdapterParityTestHarness class (pattern from RESEARCH.md code example + AI-SPEC Section 5)
    - Constructor takes two IAgentEngine instances
    - assertToolParity(): sorts both adapters' listTools() by name, compares name/description/parameters with deep equality
    - A noop ChatRecorder that collects ChatEvent[] from an adapter.chat() call
    - Does not require actual LLM calls (uses mock adapters)

    Create apps/db-ops-api/src/adapter/__tests__/ia-agent-engine.test.ts with:
    - A mock adapter implementing IAgentEngine (hardcoded tools, simple chat/invoke returns)
    - Test: new interface compiles with all 5 methods (start, chat, invoke, listTools, capabilities)
    - Test: start() is callable and does not throw
    - Test: listTools() returns correct ToolSchema signature
    - Test: capabilities() returns expected AgentCapabilities shape
    - Test: chat() onEvent callback receives at least 'complete' event type
    - Test: invoke() returns InvokeResult with content string
  </action>
  <acceptance_criteria>
    - apps/db-ops-api/src/adapter/types.ts exists and exports IAgentEngine, ChatEvent (6 variants), AgentCapabilities, ChatResult, InvokeResult
    - IAgentEngine interface has exactly 5 methods: start, chat, invoke, listTools, capabilities
    - ToolSchema is imported from @slide/agent-core, not from tools/types.ts
    - apps/db-ops-api/src/adapter/__tests__/adapter-parity.ts exports AdapterParityTestHarness class
    - apps/db-ops-api/src/adapter/__tests__/ia-agent-engine.test.ts has at least 6 test cases (including start())
    - ALL tests pass: `npx vitest run apps/db-ops-api/src/adapter/__tests__/ --reporter=verbose` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run apps/db-ops-api/src/adapter/__tests__/ -t "IAgentEngine" --reporter=verbose</automated>
  </verify>
  <done>
    IAgentEngine 接口编译通过，6 个测试全部通过（含 start() 方法）。ChatEvent 联合类型完整定义了 6 个事件变体。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 AnthropicProvider + DirectAdapter（含 start() WS 传输层） + getAgentEngine() 工厂</name>
  <files>
    apps/db-ops-api/src/adapter/llm-provider.ts (NEW)
    apps/db-ops-api/src/adapter/direct-adapter.ts (NEW)
    apps/db-ops-api/src/adapter/get-agent-engine.ts (NEW)
    apps/db-ops-api/src/adapter/__tests__/direct-adapter.test.ts (NEW)
  </files>
  <read_first>
    - apps/db-ops-api/src/adapter/types.ts (just created in Task 1 — IAgentEngine with start(), ChatEvent interfaces)
    - packages/agent-core/src/types.ts (LLMProvider, AgentHook, AgentRunSpec, ToolSchema, Tool interfaces)
    - packages/agent-core/src/runner.ts (AgentRunner constructor signature, run() signature)
    - packages/agent-core/src/tool-registry.ts (ToolRegistry.register(), getDefinitions(), execute())
    - .planning/phases/108-agent/108-RESEARCH.md (DirectAdapter chat() implementation pattern, factory pattern, Option B WS transport)
    - .planning/phases/108-agent/108-AI-SPEC.md (Section 4 — AnthropicProvider pattern, DirectAdapter pattern)
    - .planning/phases/108-agent/108-CONTEXT.md (D-15 factory function, D-16 constructor injection, D-25 Option B)
    - apps/db-ops-api/src/gateway/openclaw-runtime.ts (existing getOpenClawRuntime singleton pattern — replicate for getAgentEngine)
    - apps/db-ops-api/src/gateway/server.ts (lines 193-328 — existing WS auth/connect pattern for reference)
  </read_first>
  <action>
    Create apps/db-ops-api/src/adapter/llm-provider.ts:

    1. Implement AnthropicProvider class conforming to LLMProvider interface from @slide/agent-core
    2. getDefaultModel(): reads ANTHROPIC_MODEL env var, defaults to 'claude-sonnet-4-20250929'
    3. chat(): calls Anthropic SDK messages.create() with messages + tools, returns LLMResponse with content/toolCalls/finishReason/usage
    4. chatStream(): calls Anthropic SDK messages.stream(), pipes onContentDelta to callbacks, returns final LLMResponse
    5. Read apiKey from process.env.ANTHROPIC_API_KEY
    6. Import Anthropic SDK from @anthropic-ai/sdk (existing dependency)
    7. Only implement Anthropic provider (not OpenAI/Ollama — those can be added later per research recommendation)
    8. Include try/catch with Error wrapping for SDK failures

    Create apps/db-ops-api/src/adapter/direct-adapter.ts:

    1. Class DirectAdapter implements IAgentEngine
    2. Constructor takes { tools: ToolRegistry; llmProvider: LLMProvider; sessionHistory?: Map&lt;string, Message[]&gt; }
    3. Internal fields: runner (AgentRunner), registry (ToolRegistry), provider (LLMProvider), sessions (Map), wsServer (WebSocketServer | null)
    4. start() implementation (per D-25 Option B — minimal WS transport):
       - Guard: if (this.wsServer) return (幂等)
       - Read port from process.env.AGENT_WS_PORT, default to 28789
       - Create WebSocketServer instance on that port
       - On 'connection' event:
         - Read incoming JSON messages
         - Dispatch by message type:
           - 'chat.send': call this.chat(msg.params.sessionKey, msg.params.message, onEvent), stream events back to WS client
           - 'chat.history': return empty history or read from chatDatabaseService (stub for now)
           - Stream events (text_delta, tool_start, etc.) as JSON over WS
         - Handle errors: send { type: 'error', error: message } to client
         - Handle WebSocket close/disconnect gracefully
       - Store reference in this.wsServer
       - Log startup: `console.log('[DirectAdapter] WS transport listening on port ${port}')`

    5. chat() implementation:
       - Lookup or create session history from this.sessions Map (system message + prior history)
       - Push user message to session history
       - Create streaming AgentHook that maps to onEvent calls:
         - onStream → onEvent({ type: 'text_delta', delta })
         - beforeExecuteTools → onEvent({ type: 'tool_start', toolName, args }) for each tool call
         - afterIteration → onEvent({ type: 'tool_result', toolName, result }) or tool_error for each tool event
       - Call runner.run() with AgentRunSpec (contextWindowTokens: 200000, maxIterations: 10, maxToolResultChars: 20000, temperature: 0)
       - Push assistant response to session history
       - On completion: onEvent({ type: 'complete', finalContent })
       - On catch: onEvent({ type: 'error', error: message }) + rethrow
       - Return ChatResult

    6. invoke() implementation:
       - Single-turn non-streaming: build messages array (system prompt + user message)
       - Call runner.run() with NoopHook, maxIterations: 5
       - Return InvokeResult

    7. listTools(): returns this.registry.getDefinitions()
    8. capabilities(): returns { streaming: true, toolCalling: true, maxContextTokens: 200000, supportsCustomSystemPrompt: true }

    Create apps/db-ops-api/src/adapter/get-agent-engine.ts:
    1. Module-level let engine: IAgentEngine | null = null
    2. getAgentEngine(): loads tools from catalog.ts (platform tools), creates ToolRegistry, creates AnthropicProvider, returns DirectAdapter instance
    3. For Plan 1, hardcode to return DirectAdapter (feature flags and adapter selection come in Plan 3)
    4. Same pattern as existing getOpenClawRuntime() in openclaw-runtime.ts

    Create apps/db-ops-api/src/adapter/__tests__/direct-adapter.test.ts:
    1. Test: DirectAdapter compiles and can be instantiated with mock ToolRegistry + mock LLMProvider
    2. Test: DirectAdapter.start() creates a WebSocketServer (verify port binding)
    3. Test: DirectAdapter.chat() with a mock hook produces events
    4. Test: DirectAdapter.invoke() returns InvokeResult
    5. Test: listTools() returns registered tools
    6. Test: capabilities() returns expected shape
    7. Mock LLMProvider that returns hardcoded responses (no real SDK calls)
    8. Mock AgentRunner dependency — or use real AgentRunner with mock LLMProvider
  </action>
  <acceptance_criteria>
    - apps/db-ops-api/src/adapter/llm-provider.ts exports AnthropicProvider implementing LLMProvider
    - AnthropicProvider reads ANTHROPIC_MODEL env var with fallback 'claude-sonnet-4-20250929'
    - apps/db-ops-api/src/adapter/direct-adapter.ts exports DirectAdapter implementing IAgentEngine
    - DirectAdapter.start() 创建 WebSocketServer 在 AGENT_WS_PORT || 28789，幂等（重复调用不创建第二个 server）
    - DirectAdapter.chat() 产生至少一个 'complete' 事件 via onEvent
    - DirectAdapter.invoke() 返回 { content, usage } 其中 content 是字符串
    - apps/db-ops-api/src/adapter/get-agent-engine.ts exports getAgentEngine async function
    - getAgentEngine() 返回 IAgentEngine 实例，重复调用返回同一实例
    - tsc --noEmit passes: `npx tsc --noEmit --project apps/db-ops-api/tsconfig.json` exits 0
    - DirectAdapter tests pass: `npx vitest run apps/db-ops-api/src/adapter/__tests__/direct-adapter.test.ts --reporter=verbose` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --project apps/db-ops-api/tsconfig.json && npx vitest run apps/db-ops-api/src/adapter/__tests__/direct-adapter.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    DirectAdapter 可通过 getAgentEngine() 工厂创建。AnthropicProvider 读取环境变量配置 LLM。start() 创建 WS 传输服务在默认端口 28789。DirectAdapter.chat() 产生流式事件序列，invoke() 返回完成结果。全部测试通过。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 死代码清理 + 共享层提取</name>
  <files>
    apps/db-ops-api/src/agent-service.ts (MODIFY)
    apps/db-ops-api/src/agent-service-v2.ts (DELETE)
    apps/db-ops-api/src/gateway/openclaw-bridge.ts (MODIFY)
    apps/db-ops-api/src/gateway/openclaw-integration.ts (DELETE)
    apps/db-ops-api/src/adapter/shared/protocol-types.ts (NEW)
    apps/db-ops-api/src/adapter/shared/error-codes.ts (NEW)
  </files>
  <read_first>
    - apps/db-ops-api/src/agent-service.ts (full file — identify exports to keep: getAgentGreeting + AGENT_GREETING)
    - apps/db-ops-api/src/gateway/openclaw-bridge.ts (identify exports to keep: initializeAgentRunner, isAgentRunnerInitialized)
    - apps/db-ops-api/src/gateway/protocol.ts (full — identify non-OpenClaw-specific types)
    - apps/db-ops-api/src/gateway/error-codes.ts (full — identify non-OpenClaw-specific error constants)
    - apps/db-ops-api/src/gateway/chat-methods.ts (lines 10-20 — check which imports from openclaw-bridge survive)
    - apps/db-ops-api/server.ts (line 60 — confirms getAgentGreeting is the only surviving import from agent-service)
    - .planning/phases/108-agent/108-CONTEXT.md (D-14, D-26, D-31: exactly which code to remove)
    - .planning/phases/108-agent/108-RESEARCH.md (Assumptions A2, A3 about dead code verification)
  </read_first>
  <action>
    (A) Trim agent-service.ts:
    - Delete everything EXCEPT: getAgentGreeting(), AGENT_GREETING constant, and any imports they depend on
    - Specifically remove: ALL_TOOL_DEFINITIONS, handleAgentRequest, classifyIntent, OPS_SYSTEM_PROMPTS, executeTool, createBaseTools, and all tool implementations (db_health_check, db_slow_queries, etc.)
    - Remove imports that were only used by deleted code: OpenAI, pi-agent-core, pi-coding-agent imports, instanceDatabaseService, metricsDatabaseService, databaseService, llmService
    - Keep import for toolCatalog if getAgentGreeting still needs it (verify first — grep what getAgentGreeting accesses)
    - After trimming, agent-service.ts should be ~20-50 lines

    (B) Delete agent-service-v2.ts:
    - Use Bash `rm apps/db-ops-api/src/agent-service-v2.ts`

    (C) Clean openclaw-bridge.ts:
    - Remove the import `import { handleAgentRequest, type AgentEvent as SlideAgentEvent } from '../agent-service.js'`
    - Remove the entire sendMessageToAgent function (lines 58-140)
    - Keep: initializeAgentRunner(), isAgentRunnerInitialized(), AgentEvent type, generateMessageId()
    - After cleanup, openclaw-bridge.ts should only export AgentEvent, initializeAgentRunner, isAgentRunnerInitialized

    (D) Delete openclaw-integration.ts:
    - Use Bash `rm apps/db-ops-api/src/gateway/openclaw-integration.ts`

    (E) Extract shared protocol types from gateway/protocol.ts:
    - Read gateway/protocol.ts (current 6611 bytes / ~150 lines)
    - Identify types that are not OpenClaw-specific: ConnectRequest, ConnectChallenge, protocol version constants, message envelope types
    - Create adapter/shared/protocol-types.ts with these extracted types
    - DO NOT modify gateway/protocol.ts — just create the shared file (Plan 2 will handle migration)
    - Export all extracted types from adapter/shared/protocol-types.ts

    (F) Extract shared error codes from gateway/error-codes.ts:
    - Read gateway/error-codes.ts (current 2842 bytes / ~110 lines)
    - Identify error code constants not specific to OpenClaw (e.g., INVALID_REQUEST, SESSION_NOT_FOUND)
    - Create adapter/shared/error-codes.ts with extracted constants/helpers
    - DO NOT modify gateway/error-codes.ts

    (G) Verify:
    - Run grep to confirm no remaining imports reference deleted exports:
      grep --include="*.ts" -rn "handleAgentRequest\|classifyIntent\|OPS_SYSTEM_PROMPTS\|ALL_TOOL_DEFINITIONS\|executeTool" apps/db-ops-api/src/
    - Run grep to confirm no remaining imports for agent-service-v2:
      grep --include="*.ts" -rn "agent-service-v2" apps/db-ops-api/src/
    - Run grep to confirm no remaining imports for openclaw-integration:
      grep --include="*.ts" -rn "openclaw-integration" apps/db-ops-api/src/
    - Ensure agent-service-v2.ts file no longer exists
    - Ensure openclaw-integration.ts file no longer exists
  </action>
  <acceptance_criteria>
    - agent-service.ts exports ONLY getAgentGreeting and AGENT_GREETING (verify with grep -n "^export" agent-service.ts)
    - agent-service.ts no longer imports OpenAI, pi-agent-core, pi-coding-agent, pi-coding-agent
    - agent-service-v2.ts does not exist (ls returns "No such file")
    - openclaw-bridge.ts no longer imports from agent-service.ts
    - openclaw-bridge.ts no longer exports sendMessageToAgent
    - openclaw-integration.ts does not exist
    - adapter/shared/protocol-types.ts exists and exports non-OpenClaw-specific types
    - adapter/shared/error-codes.ts exists and exports non-OpenClaw-specific error constants/helpers
    - grep for handleAgentRequest, classifyIntent, ALL_TOOL_DEFINITIONS, executeTool returns zero results outside deleted files
    - tsc --noEmit compiles cleanly: `npx tsc --noEmit --project apps/db-ops-api/tsconfig.json` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --project apps/db-ops-api/tsconfig.json && grep -rn "handleAgentRequest\|classifyIntent\|OPS_SYSTEM_PROMPTS\|ALL_TOOL_DEFINITIONS\|executeTool" apps/db-ops-api/src/ 2>&1 | grep -v "Binary\|agent-service-v2\|agent-service-v2.ts" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>
    死代码清理完成。agent-service.ts 从 861 行缩减到 ~30 行（仅 getAgentGreeting + AGENT_GREETING）。agent-service-v2.ts 和 openclaw-integration.ts 已删除。openclaw-bridge.ts 精简到只保留初始化方法。共享层提取到 adapter/shared/。全量编译通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IAgentEngine interface callers -> DirectAdapter.chat() | Platform code (server.ts, chat-methods.ts) calls the interface from WebSocket request handlers. Adapter runs in-process. |
| DirectAdapter -> LLMProvider -> Anthropic SDK | LLM API calls go outbound to Anthropic. API key from env var. |
| DirectAdapter -> @slide/agent-core AgentRunner | AgentRunner manages LLM-tool loop entirely in-process. No cross-boundary data flow. |
| DirectAdapter.start() -> WS port 28789 | DirectAdapter opens a WebSocket server on AGENT_WS_PORT. Incoming connections can send chat.send RPC messages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Spoofing | DirectAdapter | mitigated | DirectAdapter does not expose a separate network port in Plan 1. Port binding in start() is guarded by idempotency check. In Plan 1, server.ts still starts Gateway separately — DirectAdapter.start() is only called in test context. |
| T-108-02 | Tampering | DirectAdapter.chat() | mitigated | onEvent callback is provided by the platform caller — the adapter cannot inject arbitrary events. Event types are constrained by the ChatEvent discriminated union (TypeScript compile-time check). |
| T-108-03 | Information Disclosure | AnthropicProvider | mitigated | API key read from process.env.ANTHROPIC_API_KEY at runtime. Never logged, never included in error messages. Error wrapping in AnthropicProvider replaces SDK error details with generic `LLM call failed: ${message}`. |
| T-108-04 | Denial of Service | AgentRunner | mitigated | AgentRunner has built-in maxIterations=10, maxToolResultChars=20000, contextWindowTokens=200000. DirectAdapter sets these in AgentRunSpec. No unbounded loops. |
| T-108-05 | Elevation of Privilege | getAgentEngine() factory | accepted | The factory returns a singleton instance shared across all request handlers. All handlers run in the same Node.js process with the same privileges. This matches the existing getOpenClawRuntime() pattern. RBAC is enforced at the API layer, not the adapter layer. |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | Plan 1 installs no new packages. @slide/agent-core is a workspace package (already compiled). Anthropic SDK is an existing dependency. Verify via `npm ls @anthropic-ai/sdk` — must resolve to a known version. |
</threat_model>

<verification>
1. `npx tsc --noEmit --project apps/db-ops-api/tsconfig.json` — TypeScript compilation passing
2. `npx vitest run apps/db-ops-api/src/adapter/__tests__/ --reporter=verbose` — All adapter tests passing
3. `grep -rn "handleAgentRequest\|classifyIntent\|ALL_TOOL_DEFINITIONS\|executeTool\|openclaw-integration\|agent-service-v2" apps/db-ops-api/src/` — Zero dead code references remaining (excluding agent-service-v2.ts which is already deleted)
4. `ls apps/db-ops-api/src/agent-service-v2.ts apps/db-ops-api/src/gateway/openclaw-integration.ts 2>&1` — Both files confirmed deleted
5. `grep -n "^export" apps/db-ops-api/src/agent-service.ts` — Only getAgentGreeting and AGENT_GREETING exported
</verification>

<success_criteria>
1. IAgentEngine 接口定义完整（含 .start() 方法），编译通过
2. AnthropicProvider 实现 LLMProvider，可以创建实例
3. DirectAdapter 通过 getAgentEngine() 工厂可实例化，start() 启动 WS 传输层，chat() 产生事件序列，invoke() 返回结果
4. agent-service.ts 从 861 行缩减到仅保留 getAgentGreeting 和 AGENT_GREETING
5. agent-service-v2.ts 和 openclaw-integration.ts 已删除
6. openclaw-bridge.ts 精简到只保留初始化方法（sendMessageToAgent 删除）
7. adapter/shared/ 包含从 gateway/ 提取的非 OpenClaw 通用类型和错误码
8. 全量 TypeScript 编译通过 (tsc --noEmit)
9. 所有测试通过 (vitest)
</success_criteria>

<output>
Create .planning/phases/108-agent/108-01-SUMMARY.md when done
</output>
