---
phase: 113-ai-cron-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/sql/migrations/010_add_task_description_log_columns.sql
  - apps/db-ops-api/src/cron/types.ts
  - apps/db-ops-api/src/cron/cron-job-service.ts
autonomous: true
requirements:
  - D-01 (seed data)
  - D-03 (log columns)
  - D-05 (task_description column)
must_haves:
  truths:
    - "cron_jobs 表有 task_description 列（替代 handler 列）"
    - "cron_job_logs 表有 JSON 迹列（tools_used, tool_events, usage, stop_reason, duration_ms, partial_trace）"
    - "13 条种子数据包含 NL 任务描述"
    - "CronJobConfig 接口使用 task_description 替代 handler"
    - "CronJobLog 接口包含 JSON 迹字段"
    - "CronJobDatabaseService 新增 createJob/deleteJob 方法，expandLog 支持迹参数"
    - "所有 SELECT handler 的查询替换为 task_description"
  artifacts:
    - path: "apps/db-ops-api/sql/migrations/010_add_task_description_log_columns.sql"
      provides: "handler→task_description 迁移 + log 扩展 + 13 NL 种子"
      contains: "ALTER TABLE cron_jobs DROP COLUMN handler"
    - path: "apps/db-ops-api/src/cron/types.ts"
      provides: "更新后的 CronJobConfig + CronJobLog + AgentRunResult 类型"
      contains: "task_description: string"
    - path: "apps/db-ops-api/src/cron/cron-job-service.ts"
      provides: "expandLog + createJob + deleteJob + handler→task_description 查询更新"
      contains: "completeLog(logId, status, summary, error?, trace?)"
  key_links:
    - from: "cron-job-service.ts"
      to: "types.ts"
      via: "import { CronJobConfig, CronJobLog }"
    - from: "010 migration"
      to: "cron-job-service.ts"
      via: "查询使用新列"
---

<objective>
Schema 迁移 + 类型定义 + 数据库服务层扩展

Purpose: 为 AI Agent Cron 准备数据库基础设施。替换 handler 列为 task_description（NL 文本），扩展 cron_job_logs 支持 Agent 执行迹存储，更新类型定义，在 cron-job-service 中新增 createJob/deleteJob 方法。

Output: 迁移 SQL、更新后的 types.ts、更新后的 cron-job-service.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-ai-cron-agent/113-CONTEXT.md
@.planning/phases/113-ai-cron-agent/113-RESEARCH.md
@.planning/phases/113-ai-cron-agent/113-PATTERNS.md

@apps/db-ops-api/src/cron/types.ts
@apps/db-ops-api/src/cron/cron-job-service.ts
@apps/db-ops-api/sql/migrations/009_add_cron_jobs_tables.sql

<interfaces>
<!-- 当前 CronJobConfig: handler 字段将被替换为 task_description -->
interface CronJobConfig {
  id: number; name: string; handler: string;    // handler → task_description
  cron_expr: string; enabled: boolean; timezone: string;
  description: string | null;
  last_run_at: string | null; next_run_at: string | null;
  last_result: string | null; timeout_seconds: number;
  retry_count: number; created_at: string; updated_at: string;
}

<!-- 当前 CronJobLog -->
interface CronJobLog {
  id: number; job_id: number; started_at: string; finished_at: string | null;
  status: CronJobStatus; result_summary: string | null; error_message: string | null;
}

<!-- 当前 completeLog 签名 -->
completeLog(logId: number, status: string, summary?: string, errorMessage?: string): Promise<boolean>

<!-- 当前 updateJob 签名 — 需要添加 task_description -->
updateJob(id: number, data: Partial<Pick<CronJobConfig, 'cron_expr' | 'enabled' | 'timezone' | 'description' | 'timeout_seconds' | 'retry_count'>>): Promise<boolean>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL migration — task_description + log columns + NL seed data</name>
  <files>
    apps/db-ops-api/sql/migrations/010_add_task_description_log_columns.sql
  </files>
  <read_first>
    apps/db-ops-api/sql/migrations/009_add_cron_jobs_tables.sql
  </read_first>
  <action>
    创建 migration 文件 apps/db-ops-api/sql/migrations/010_add_task_description_log_columns.sql，包含以下三个部分（均在 START TRANSACTION ... COMMIT 内）：

    1. ALTER TABLE cron_jobs — DROP COLUMN handler, ADD COLUMN task_description TEXT NOT NULL AFTER name
    2. ALTER TABLE cron_job_logs —
      MODIFY COLUMN status ENUM('running','success','error','skipped','timeout','partial') NOT NULL DEFAULT 'running',
      ADD COLUMN result LONGTEXT AFTER result_summary,
      ADD COLUMN tools_used JSON AFTER result,
      ADD COLUMN tool_events JSON AFTER tools_used,
      ADD COLUMN usage JSON AFTER tool_events,
      ADD COLUMN stop_reason VARCHAR(50) AFTER usage,
      ADD COLUMN duration_ms INT AFTER stop_reason,
      ADD COLUMN error_trace TEXT AFTER duration_ms,
      ADD COLUMN partial_trace LONGTEXT AFTER error_trace
    3. 13 条 UPDATE cron_jobs SET task_description = '...' WHERE name = '...' 语句（从 RESEARCH.md 附录的 NL 任务描述复制，保持 13 个 handler 的中文 name 不变，任务描述使用英文，与 009 migration 的 INSERT 中 name 完全匹配）

    header 格式和 START TRANSACTION / COMMIT 模式与 009 保持一致。

    不要包含新建表或 INSERT 语句——表已存在，只更新数据。
    种子数据中前 3 个任务（TopSQL 自动分析、告警 RCA 分析、故障自动诊断）保持 enabled=false（原先受 ENABLE_AUTO_AI_ANALYSIS 控制，现在明确关闭）。
    使用 COMMIT 结束，不带 ROLLBACK（迁移是批量非破坏性的）。
  </action>
  <verify>
    <automated>grep -c 'task_description TEXT NOT NULL' apps/db-ops-api/sql/migrations/010_add_task_description_log_columns.sql && grep -c 'DROP COLUMN handler' apps/db-ops-api/sql/migrations/010_add_task_description_log_columns.sql && grep -c "COMMIT" apps/db-ops-api/sql/migrations/010_add_task_description_log_columns.sql</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在，包含 START TRANSACTION 和 COMMIT
    - 包含 ALTER TABLE cron_jobs DROP COLUMN handler / ADD COLUMN task_description
    - 包含 ALTER TABLE cron_job_logs MODIFY COLUMN status / ADD COLUMN tools_used / tool_events / usage / stop_reason / duration_ms / error_trace / partial_trace
    - 包含 13 条 UPDATE cron_jobs SET task_description 语句
    - 前 3 条 UPDATE 的 WHERE name 匹配 009 seed 中 enabled=false 的三个任务名（TopSQL 自动分析、告警 RCA 分析、故障自动诊断）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update types.ts — replace handler with task_description, expand CronJobLog, add AgentRunResult</name>
  <files>
    apps/db-ops-api/src/cron/types.ts
  </files>
  <read_first>
    apps/db-ops-api/src/cron/types.ts
  </read_first>
  <action>
    编辑 apps/db-ops-api/src/cron/types.ts，做以下精确修改：

    1. 扩展 CronJobStatus 枚举：在 'success' | 'error' | 'running' | 'skipped' 后添加 'timeout' | 'partial'
    2. 完全删除 HandlerName 类型（第 9-22 行整个 export type HandlerName = ...）
    3. CronJobConfig 接口：将 handler: string 替换为 task_description: string（位置在 name 之后，cron_expr 之前）
    4. CronJobLog 接口：添加以下新字段（在 error_message 之后、右花括号之前）：
      - result: string | null
      - tools_used: string[] | null
      - tool_events: any[] | null
      - usage: Record<string, number> | null
      - stop_reason: string | null
      - duration_ms: number | null
      - error_trace: string | null
      - partial_trace: string | null
    5. 在 CronJobParam 接口之后，文件末尾，添加新接口：
      ```typescript
      export interface AgentRunResult {
        finalContent: string | null;
        error: string | null;
        toolsUsed: any[];
        toolEvents: any[];
        usage: Record<string, number>;
        stopReason: string;
        durationMs: number;
      }
      ```

    不要修改 CronJobParam 接口。不要修改文件的其他部分。
  </action>
  <verify>
    <automated>grep -c 'task_description: string' apps/db-ops-api/src/cron/types.ts && grep -c 'tools_used' apps/db-ops-api/src/cron/types.ts && grep -c 'AgentRunResult' apps/db-ops-api/src/cron/types.ts && ! grep -q 'HandlerName' apps/db-ops-api/src/cron/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - CronJobStatus 包含 'timeout' 和 'partial'
    - HandlerName 类型已删除
    - CronJobConfig.handler 替换为 CronJobConfig.task_description
    - CronJobLog 包含 result, tools_used, tool_events, usage, stop_reason, duration_ms, error_trace, partial_trace
    - 文件末尾有 AgentRunResult 接口
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update cron-job-service.ts — expand completeLog, add createJob/deleteJob, handler→task_description 查询</name>
  <files>
    apps/db-ops-api/src/cron/cron-job-service.ts
  </files>
  <read_first>
    apps/db-ops-api/src/cron/cron-job-service.ts
  </read_first>
  <action>
    编辑 apps/db-ops-api/src/cron/cron-job-service.ts，做以下修改：

    1. 所有 SELECT 查询：将 handler, 替换为 task_description,（共 3 处：getJobs() 第 35 行、getEnabledJobs() 第 57 行、getJobById() 第 80 行）

    2. completeLog() 方法（第 240-255 行）：扩展签名和 SQL 更新语句。
       - 新签名：completeLog(logId: number, status: string, summary?: string, errorMessage?: string, trace?: { tools_used?: string[]; tool_events?: any[]; usage?: Record<string, number>; stop_reason?: string; duration_ms?: number; partial_trace?: string }): Promise<boolean>
       - SQL UPDATE 增加列：tools_used = ?, tool_events = ?, usage = ?, stop_reason = ?, duration_ms = ?, partial_trace = ?
       - JSON 字段在传参时使用 JSON.stringify() 序列化
       - 所有 trace 字段在 undefined 时传 null

    3. updateJob() 方法（第 101 行）：在 updateJob 的 Partial Pick 泛型参数中加上 'task_description'。
       在方法体内添加对应的更新逻辑（与 cron_expr 等字段相同的 pattern）：
       ```
       if (data.task_description !== undefined) {
         updates.push('task_description = ?');
         values.push(data.task_description);
       }
       ```

    4. 新增 createJob() 方法（在 updateJob 之后、toggleJob 之前）：
       - 签名：async createJob(data: { name: string; task_description: string; cron_expr: string; timezone?: string; description?: string; timeout_seconds?: number }): Promise<number>
       - INSERT INTO cron_jobs (name, task_description, cron_expr, timezone, description, timeout_seconds) VALUES (?, ?, ?, ?, ?, ?)
       - 默认 timezone: 'Asia/Shanghai', 默认 timeout_seconds: 300
       - 返回 insertId

    5. 新增 deleteJob() 方法（在 toggleJob 之后、updateRunResult 之前）：
       - 签名：async deleteJob(id: number): Promise<boolean>
       - DELETE FROM cron_jobs WHERE id = ? 返回 affectedRows > 0

    6. getLogs() 方法（第 260-285 行）：SELECT 列中添加 result, tools_used, tool_events, usage, stop_reason, duration_ms, error_trace, partial_trace（在 error_message 之后按同样顺序）

    保持所有其他代码不变，包括单例导出 export const cronJobService。
  </action>
  <verify>
    <automated>grep -c "task_description" apps/db-ops-api/src/cron/cron-job-service.ts && grep -c "createJob" apps/db-ops-api/src/cron/cron-job-service.ts && grep -c "deleteJob" apps/db-ops-api/src/cron/cron-job-service.ts</automated>
  </verify>
  <acceptance_criteria>
    - 所有 SELECT handler 替换为 task_description（共 3 处）
    - completeLog 接受可选的 trace 参数，更新 JSON 列
    - updateJob 支持 task_description 字段更新
    - createJob 方法存在，执行 INSERT 返回 insertId
    - deleteJob 方法存在，执行 DELETE 返回 boolean
    - getLogs SELECT 包含所有新 JSON 列
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → MySQL | 直接执行 ALTER TABLE — 需要在执行前确保无并发的 handler 读取 |
| cron-job-service → MySQL | 参数化查询防止注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering | migration SQL | mitigate | migration 在 START TRANSACTION/COMMIT 内，原子执行 |
| T-113-02 | Information Disclosure | cron job logs | mitigate | cron-job-service 所有路由已有 verifyToken + requirePermission 中间件 |
</threat_model>

<verification>
- migration SQL 语法通过 mysql 语法校验（ALTER TABLE + UPDATE + COMMIT）
- types.ts 编译无类型错误（tsc --noEmit）
- cron-job-service.ts 编译无类型错误（tsc --noEmit）
</verification>

<success_criteria>
- 迁移文件包含列变更 + 13 条 NL 种子数据
- 类型定义完全反映新 schema
- 数据库服务支持完整 CRUD（create/read/update/delete）+ 扩展迹写入
</success_criteria>

<output>
Create `.planning/phases/113-ai-cron-agent/113-01-SUMMARY.md` when done
</output>
