---
phase: 113-ai-cron-agent
plan: 03
type: execute
wave: 2
depends_on:
  - 113-01
  - 113-02
files_modified:
  - apps/db-ops-api/src/cron/cron-manager.ts
  - apps/db-ops-api/server.ts
  - apps/db-ops-api/src/cron/cron-job-handlers.ts
  - apps/db-ops-api/src/adapter/get-agent-engine.ts
  - apps/db-ops-api/src/__tests__/cron-eval.test.ts
autonomous: true
requirements:
  - D-01 (create API — free creation)
  - D-03 (write logs with trace)
  - D-05 (delete handlers)
  - D-09 (cron:view/cron:manage on POST/DELETE)
  - D-07 (manual trigger via CronExecutor)
must_haves:
  truths:
    - "CronManager 使用 CronExecutor.execute() 替代 getHandler(name)()"
    - "cron-job-handlers.ts 文件已删除"
    - "POST /api/cron/jobs 创建新任务（requirePermission cron:manage）"
    - "DELETE /api/cron/jobs/:id 删除任务（requirePermission cron:manage）"
    - "PUT /api/cron/jobs/:id 支持 task_description 更新"
    - "POST /api/cron/jobs/:id/run 使用 CronExecutor 替代 getHandler"
    - "CronManager.reload() 不再过滤 handlerNames"
    - "eval 测试验证执行迹完整性和安全性"
  artifacts:
    - path: "apps/db-ops-api/src/cron/cron-manager.ts"
      provides: "更新后的 CronManager — CronExecutor 驱动、删除 handler 引用"
      contains: "cronExecutor.execute(config.id, config.task_description)"
    - path: "apps/db-ops-api/server.ts"
      provides: "POST/DELETE 路由 + PUT/run 更新 + getHandler import 移除"
      contains: "fastify.post('/api/cron/jobs'"
    - path: "apps/db-ops-api/src/__tests__/cron-eval.test.ts"
      provides: "结构完整性和安全 eval 测试"
      contains: "expect(log.tools_used).toBeNonEmptyArray()"
  key_links:
    - from: "cron-manager.ts"
      to: "cron-executor.ts"
      via: "import { CronExecutor }"
    - from: "cron-manager.ts"
      to: "cron-job-service.ts"
      via: "completeLog(..., trace)"
    - from: "server.ts"
      to: "cron-manager.ts"
      via: "const cronManager = new CronManager(jobService, cronExecutor)"
---

<objective>
CronManager 重写 + API 路由 + 删除旧 handler + 集成测试

Purpose: 将 CronManager 从 getHandler 分发表切换到 AgentRunner 驱动的 CronExecutor。添加 POST/DELETE API 路由支持任务创建和删除。删除 cron-job-handlers.ts。添加集成 eval 测试验证全链路。

Output: 更新后的 cron-manager.ts、server.ts、删除 cron-job-handlers.ts、新增 cron-eval.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-ai-cron-agent/113-CONTEXT.md
@.planning/phases/113-ai-cron-agent/113-RESEARCH.md
@.planning/phases/113-ai-cron-agent/113-PATTERNS.md

@apps/db-ops-api/src/cron/cron-manager.ts
@apps/db-ops-api/server.ts (第 3659-3837 行: CronManager 初始化 + API 路由)
@apps/db-ops-api/src/cron/cron-executor.ts (Plan 02 新建)
@apps/db-ops-api/src/cron/cron-job-service.ts (Plan 01 更新后)
@apps/db-ops-api/src/cron/types.ts (Plan 01 更新后)

<interfaces>
<!-- CronManager 当前构造函数签名 — 需要添加 cronExecutor 参数 -->
constructor(jobService: CronJobDatabaseService)

<!-- CronExecutor.execute() 签名 (Plan 02 定义) -->
execute(jobId: number, taskDescription: string, timeoutSeconds: number): Promise<AgentRunResult>

<!-- AgentRunResult (Plan 01 types.ts 已添加) -->
interface AgentRunResult {
  finalContent: string | null;
  error: string | null;
  toolsUsed: any[];
  toolEvents: any[];
  usage: Record<string, number>;
  stopReason: string;
  durationMs: number;
}

<!-- CronJobDatabaseService (Plan 01 更新后) 新增方法 -->
createJob(data: { name, task_description, cron_expr, timezone?, description?, timeout_seconds? }): Promise<number>
deleteJob(id: number): Promise<boolean>
completeLog(logId, status, summary?, error?, trace?): Promise<boolean>

<!-- current server.ts imports (line 70-72) -->
import { CronJobDatabaseService, cronJobService } from './src/cron/cron-job-service';
import { getHandler } from './src/cron/cron-job-handlers';

<!-- current server.ts CronManager init (line 3660) -->
const cronManager = new CronManager(cronJobService);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite cron-manager.ts — CronExecutor 集成 + 删除 handler 引用</name>
  <files>
    apps/db-ops-api/src/cron/cron-manager.ts
  </files>
  <read_first>
    apps/db-ops-api/src/cron/cron-manager.ts
    apps/db-ops-api/src/cron/cron-job-service.ts (更新后的, 含 createJob/deleteJob/expandLog)
    apps/db-ops-api/src/cron/cron-executor.ts (Plan 02 新建)
    apps/db-ops-api/src/cron/types.ts (Plan 01 更新后)
  </read_first>
  <action>
    编辑 apps/db-ops-api/src/cron/cron-manager.ts：

    1. 移除导入：
       - 删除 `import { getHandler, handlerNames } from './cron-job-handlers';`
       - 删除 `import { CronJobConfig } from './types';`（如果已存在，保留）

    2. 添加导入：
       - import { CronExecutor } from './cron-executor';
       - import { AgentRunResult } from './types';

    3. 构造函数签名变更（第 28 行）：
       当前: constructor(jobService: CronJobDatabaseService)
       改为: constructor(jobService: CronJobDatabaseService, cronExecutor: CronExecutor)
       将 cronExecutor 保存为私有成员：private cronExecutor: CronExecutor

    4. reload() 方法（第 46-73 行）：
       - 移除 handlerNames 验证块（第 55-64 行）
       - 移除 validJobs 过滤，直接使用 enabledJobs
       - logs 输出从 `${validJobs.length} 个任务已调度（共 ${enabledJobs.length} 个已启用）`
         改为 `${enabledJobs.length} 个任务已调度`

    5. executeJob() 方法（第 113-148 行）：
       用 RESEARCH.md 和 PATTERNS.md 中的新模式替换：
       - 保留 runningFlags 并发守卫
       - 保留 startLog(logId) 
       - 保留 startTime = Date.now()
       - 替换 handler 调用为：
         ```
         const result = await this.cronExecutor.execute(
           config.id,
           config.task_description,
           config.timeout_seconds || 300,
         );
         ```
       - 结果状态判断：
         ```
         const durationMs = Date.now() - startTime;
         const status = result.error ? 'error'
           : result.stopReason === 'max_iterations' ? 'partial'
           : 'success';
         ```
       - completeLog 调用增加 trace 参数：
         ```
         await this.jobService.completeLog(logId, status,
           result.finalContent || '执行完成',
           result.error || undefined,
           {
             tools_used: result.toolsUsed,
             tool_events: result.toolEvents,
             usage: result.usage,
             stop_reason: result.stopReason,
             duration_ms: durationMs,
           },
         );
         ```
       - 保留 updateRunResult
       - 保留 catch 块中的错误日志管理（error 状态下不传 trace）
       - 保留 finally 中的 runningFlags.delete

    确保完成修改后文件编译通过，所有 import 路径正确。
    不要在 reload() 或 executeJob() 中保留任何 handlerNames、getHandler 或 task_description 字段访问错误的旧引用。
  </action>
  <verify>
    <automated>! grep -q 'getHandler' apps/db-ops-api/src/cron/cron-manager.ts && ! grep -q 'handlerNames' apps/db-ops-api/src/cron/cron-manager.ts && grep -q 'cronExecutor' apps/db-ops-api/src/cron/cron-manager.ts && grep -q 'task_description' apps/db-ops-api/src/cron/cron-manager.ts</automated>
  </verify>
  <acceptance_criteria>
    - 不再导入 getHandler / handlerNames
    - 构造函数接受 CronExecutor 参数
    - reload() 不过滤 handlerNames
    - executeJob() 使用 cronExecutor.execute() 并传递 task_description
    - completeLog 传递 AgentRunResult 迹数据（tools_used, tool_events, usage, stop_reason, duration_ms）
    - catch 块处理 error/partial/timeout 等状态
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update server.ts — POST/DELETE 路由 + PUT/run 更新 + 删除 handler 导入</name>
  <files>
    apps/db-ops-api/server.ts
  </files>
  <read_first>
    apps/db-ops-api/server.ts (第 3659-3837 行: CronManager 初始化 + 全部 cron 路由)
  </read_first>
  <action>
    编辑 apps/db-ops-api/server.ts：

    A) 导入修改（文件顶部附近）：
       - 删除 import { getHandler } from './src/cron/cron-job-handlers';（第 72 行）
       - 添加 import { CronExecutor } from './src/cron/cron-executor';

    B) CronManager 初始化（第 3660-3661 行）：
       从 `const cronManager = new CronManager(cronJobService);`
       改为：
       ```
       import { AgentRunner } from '@slide/agent-core';
       import { AnthropicProvider } from './src/adapter/llm-provider.js';
       import { loadPlatformTools } from './src/adapter/get-agent-engine.js';
       
       const cronProvider = new AnthropicProvider();
       const cronRunner = new AgentRunner(cronProvider);
       const cronTools = await loadPlatformTools();
       const cronExecutor = new CronExecutor(cronRunner, cronTools, cronProvider);
       const cronManager = new CronManager(cronJobService, cronExecutor);
       ```

       注意：loadPlatformTools() 目前不是从 get-agent-engine.ts 导出的。
       有两种方式：
       a) 在 get-agent-engine.ts 中导出 loadPlatformTools（推荐——避免重复工具注册逻辑）
       b) 直接在 server.ts 中内联创建 CronExecutor

       方式 a 实现：在 get-agent-engine.ts 中 export async function loadPlatformTools() — 当前它是 module-private async function。需要将 export 关键字添加到函数定义。同时添加：
       ```
       export async function createLLMProvider(): Promise<LLMProvider> { ... }
       ```
       并在 get-agent-engine.ts 中导出这两个函数。

       实际修改：
       1. 编辑 apps/db-ops-api/src/adapter/get-agent-engine.ts：
          - 在 `async function loadPlatformTools()` 前加 export 关键字
          - 在 `async function createLLMProvider()` 前加 export 关键字
          - 添加 import type { LLMProvider } from '@slide/agent-core'（如果它还没有类型级别导入的话）
       2. 在 server.ts 中：
          ```
          import { AgentRunner } from '@slide/agent-core';
          import { AnthropicProvider } from './src/adapter/llm-provider.js';
          import { loadPlatformTools } from './src/adapter/get-agent-engine.js';
          import { CronExecutor } from './src/cron/cron-executor.js';
          
          const cronProvider = new AnthropicProvider();
          const cronRunner = new AgentRunner(cronProvider);
          const cronTools = await loadPlatformTools();
          const cronExecutor = new CronExecutor(cronRunner, cronTools, cronProvider);
          const cronManager = new CronManager(cronJobService, cronExecutor);
          ```

         注意：使用 `new AnthropicProvider()` 作为 cron 执行的默认 provider。这与 get-agent-engine 的 fallback 行为一致（当 DB provider 不可用时使用 AnthropicProvider）。如果需要 DB 配置的 provider，可以使用 `createLLMProvider()` 代替。

    C) 新增 POST /api/cron/jobs 路由（在 PUT 之前，第 3707 行之前）：
       路由模式参考 PATTERNS.md 第 532-567 行：
       - preHandler: [verifyToken, requirePermission('cron:manage')]
       - 验证 body.name, body.task_description, body.cron_expr 非空
       - 验证 cron 表达式：new CronJob(body.cron_expr, () => {})
       - 调用 cronJobService.createJob({ ... })
       - 调用 cronManager.reload()
       - reply.code(201).send({ id, message: '创建成功' })

    D) 新增 DELETE /api/cron/jobs/:id 路由（在 /run 之前）：
       路由模式参考 PATTERNS.md 第 572-589 行：
       - preHandler: [verifyToken, requirePermission('cron:manage')]
       - 检查任务存在（getJobById）
       - 调用 cronJobService.deleteJob(Number(id))
       - 调用 cronManager.reload()
       - reply.send({ message: '删除成功' })

    E) 修改 PUT /api/cron/jobs/:id 路由（第 3708-3748 行）：
       - 在 updateJob 的 body 参数中添加 task_description: body.task_description

    F) 修改手动触发路由 POST /api/cron/jobs/:id/run（第 3778-3801 行）：
       将 handler 调用方式从 getHandler 改为 cronExecutor.execute：
       ```
       const result = await cronExecutor.execute(
         config.id,
         config.task_description,
         config.timeout_seconds || 300,
       );
       const status = result.error ? 'error' : 'success';
       await cronJobService.completeLog(logId, status,
         result.finalContent || '手动触发执行成功',
         result.error || undefined,
         {
           tools_used: result.toolsUsed,
           tool_events: result.toolEvents,
           usage: result.usage,
           stop_reason: result.stopReason,
           duration_ms: Date.now() - startTime,
         },
       );
       reply.send({ logId, status });
       ```

       注意：手动触发路由也需要 startTime 变量（参考 cron-manager.ts 的模式）。
       在 startLog 之前添加 const startTime = Date.now();

    G) 验证 PUT 路由的 import：server.ts 顶部已有 `import { CronJob } from 'cron';` — 在 cron 路由段中通过 `new CronJob(body.cron_expr, () => {})` 验证表达式（第 3721 行存在）。
       确认 CronJob 在 server.ts 上下文中可用（它已通过顶部的 import { CronJob } from 'cron' 导入）。
  </action>
  <verify>
    <automated>grep -c "fastify\.post.*/api/cron/jobs" server.ts && grep -c "fastify\.delete.*/api/cron/jobs/:id" server.ts && grep -c "cronExecutor" server.ts && ! grep -q "getHandler" server.ts</automated>
  </verify>
  <acceptance_criteria>
    - server.ts 不再导入 getHandler
    - get-agent-engine.ts 导出 loadPlatformTools
    - 创建 POST /api/cron/jobs 路由（requirePermission cron:manage）
    - 创建 DELETE /api/cron/jobs/:id 路由（requirePermission cron:manage）
    - PUT 路由支持 task_description 更新
    - 手动触发路由使用 CronExecutor.execute()
    - CronManager 使用 CronExecutor 初始化
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete cron-job-handlers.ts + create cron-eval.test.ts</name>
  <files>
    apps/db-ops-api/src/cron/cron-job-handlers.ts
    apps/db-ops-api/src/__tests__/cron-eval.test.ts
  </files>
  <read_first>
    apps/db-ops-api/src/cron/types.ts (更新后的 CronJobLog 完整字段)
    apps/db-ops-api/src/cron/cron-completion-tool.ts
    apps/db-ops-api/src/__tests__/cron-executor.test.ts (测试文件模式)
  </read_first>
  <action>
    A) 删除 cron-job-handlers.ts：
       直接删除文件 /Users/max/Coding/39-Slide/apps/db-ops-api/src/cron/cron-job-handlers.ts。
       （server.ts 和 cron-manager.ts 中已移除对该文件的导入 — Task 1 和 Task 2 已完成）

    B) 创建 apps/db-ops-api/src/__tests__/cron-eval.test.ts：
       集成 eval 测试，使用 vitest，包含以下测试用例：

       describe('cron-eval: structure and safety checks')
       
       it('tool slide_complete_cron is registered in catalog', () => {
         检查 toolCatalog.has('slide_complete_cron') 为 true
       })

       it('slide_complete_cron has correct parameter schema', () => {
         验证 status 是 ['success', 'failure', 'partial']
         验证 summary 是 required string
         验证 details 是 optional object
       })

       it('cron-completion-tool source calls toolCatalog.register', () => {
         readFileSync 检查 cron-completion-tool.ts 包含 toolCatalog.register
       })

       it('cron-job-handlers.ts has been deleted', () => {
         验证文件 apps/db-ops-api/src/cron/cron-job-handlers.ts 不存在
       })

       it('cron-manager.ts no longer references getHandler or handlerNames', () => {
         readFileSync 检查 cron-manager.ts 不包含 'getHandler' 或 'handlerNames'
       })

       it('cron-manager.ts references CronExecutor and task_description', () => {
         readFileSync 检查 cron-manager.ts 包含 'CronExecutor' 和 'task_description'
       })

       it('server.ts no longer imports getHandler', () => {
         readFileSync 检查 server.ts 不包含 'cron-job-handlers'
       })

       it('types.ts has task_description field instead of handler', () => {
         readFileSync 检查 types.ts 包含 'task_description: string'
         readFileSync 检查 types.ts 不包含 'export type HandlerName'
       })

       it('types.ts CronJobLog has trace fields', () => {
         readFileSync 检查 types.ts 包含 'tools_used' 和 'tool_events'
       })

       it('cron-job-service.ts has createJob and deleteJob methods', () => {
         readFileSync 检查 cron-job-service.ts 包含 'createJob' 和 'deleteJob'
       })

       it('cron-job-service.ts queries task_description instead of handler', () => {
         readFileSync 检查 cron-job-service.ts SELECT 查询包含 'task_description' 而不是 'handler'
       })
  </action>
  <verify>
    <automated>! test -f apps/db-ops-api/src/cron/cron-job-handlers.ts && cd apps/db-ops-api && npx vitest run src/__tests__/cron-eval.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - cron-job-handlers.ts 文件已被删除
    - cron-eval.test.ts 包含所有上述测试用例
    - 所有测试通过（npx vitest run）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → CRUD routes | POST/PUT/DELETE 请求需验证 token 和 cron:manage 权限 |
| CronManager → CronExecutor | Agent 执行结果可能很大，需控制写入日志的数据量 |
| GET → log viewer | 迹数据包含工具调用详情，需 cron:view 权限 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering | POST /api/cron/jobs | mitigate | requirePermission('cron:manage') + 字段验证 (name/task_description/cron_expr 非空) + cron 表达式格式校验 |
| T-113-02 | Tampering | DELETE /api/cron/jobs/:id | mitigate | requirePermission('cron:manage') + 前置 getJobById 404 检查 |
| T-113-03 | Denial of Service | executeJob partial_trace | mitigate | result.finalContent 和 partial_trace 使用 LONGTEXT 列，但写入前通过 JSON.stringify 限制大小 |
| T-113-04 | Tampering | cron expression validation | mitigate | new CronJob() 验证，无效表达式返回 400 |
| T-113-05 | Information Disclosure | cron job logs | mitigate | GET 路由均有 requirePermission('cron:view') |

## Package Legitimacy — No new packages added

D-07 说明：手动触发路由使用 CronExecutor (AgentRunner) 而不是 Gateway sendGatewayChat。
</threat_model>

<verification>
- cron-job-handlers.ts 已删除（文件不存在）
- server.ts 和 cron-manager.ts 不再引用 getHandler/handlerNames
- npx vitest run src/__tests__/cron-eval.test.ts --reporter=verbose 全部通过
- npx tsc --noEmit 无类型错误
</verification>

<success_criteria>
- CronManager 完全通过 CronExecutor 驱动（无 handler 引用残留）
- 完整的 CRUD API 路由（GET/POST/PUT/DELETE/toggle/run/logs）
- 手动触发使用 AgentRunner 执行
- 13 个硬编码 handler 文件已被删除
- 集成 eval 测试全部通过
</success_criteria>

<output>
Create `.planning/phases/113-ai-cron-agent/113-03-SUMMARY.md` when done
</output>
