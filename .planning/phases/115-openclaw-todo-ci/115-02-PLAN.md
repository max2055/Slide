---
phase: 115-openclaw-todo-ci
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/ui/routing/bindings.ts
  - frontend/src/app/ui/routing/resolve-route.ts
  - frontend/src/app/ui/routing/bound-account-read.ts
  - frontend/src/app/src/config/types.ts
  - frontend/src/app/src/auto-reply/envelope.ts
  - frontend/src/app/src/auto-reply/commands-registry.ts
  - frontend/src/app/src/auto-reply/inbound-debounce.ts
  - frontend/src/app/src/auto-reply/status.ts
  - frontend/src/app/src/auto-reply/commands-registry-list.ts
  - frontend/src/app/src/auto-reply/command-auth.ts
  - frontend/src/app/src/auto-reply/chunk.ts
  - frontend/src/app/src/auto-reply/commands-registry.types.ts
  - frontend/src/app/src/auto-reply/dispatch.ts
  - frontend/src/app/src/auto-reply/command-status-builders.ts
  - frontend/src/app/src/auto-reply/skill-commands.ts
  - frontend/src/app/src/auto-reply/command-detection.runtime-types.ts
  - frontend/src/app/src/auto-reply/commands-registry-normalize.ts
  - frontend/src/app/src/auto-reply/command-detection.ts
  - frontend/src/app/src/auto-reply/stage-sandbox-media.test-harness.ts
  - frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.test-harness.ts
  - frontend/src/app/src/auto-reply/reply/commands.test-harness.ts
  - frontend/src/app/src/auto-reply/reply/directive-handling.params.ts
  - frontend/src/app/src/auto-reply/reply/queue.test-helpers.ts
  - frontend/src/app/src/infra/update-startup.ts
  - frontend/src/app/src/auto-reply/heartbeat-reply-payload.ts
  - frontend/src/app/src/auto-reply/reply/reply-delivery.ts
  - frontend/src/app/src/auto-reply/reply/agent-runner-helpers.ts
  - frontend/src/app/src/auto-reply/reply/agent-runner-payloads.ts
  - frontend/src/app/src/auto-reply/reply/followup-runner.ts
  - frontend/src/app/src/auto-reply/reply/agent-runner-execution.ts
  - frontend/src/app/src/auto-reply/reply/streaming-directives.ts
  - frontend/src/app/src/auto-reply/reply/block-reply-pipeline.ts
  - frontend/src/app/src/auto-reply/reply/dispatch-from-config.ts
  - frontend/src/app/src/auto-reply/reply/reply-media-paths.ts
  - frontend/src/app/src/auto-reply/reply/block-reply-coalescer.ts
  - frontend/src/app/src/auto-reply/reply/dispatch-acp-delivery.ts
  - frontend/vite.config.js
autonomous: true
requirements:
  - D-06
  - D-09
user_setup: []

must_haves:
  truths:
    - "Three dead routing files (bindings.ts, resolve-route.ts, bound-account-read.ts) are deleted from the source tree"
    - "Auto-reply / infra files compile successfully — they no longer import from a non-existent config/types.openclaw.js path"
    - "Vite alias `openclaw/plugin-sdk/reply-payload` is renamed to `@agent/plugin-sdk/reply-payload` consistently across the codebase"
    - "No remaining broken import paths point to the deleted `config/` directory"
  artifacts:
    - path: "frontend/src/app/ui/routing/bindings.ts"
      provides: "Deleted dead routing file"
      min_lines: 0
    - path: "frontend/src/app/ui/routing/resolve-route.ts"
      provides: "Deleted dead routing file"
      min_lines: 0
    - path: "frontend/src/app/ui/routing/bound-account-read.ts"
      provides: "Deleted dead routing file"
      min_lines: 0
    - path: "frontend/src/app/src/config/types.ts"
      provides: "New type definition file replacing the deleted config/types.openclaw.js"
    - path: "frontend/vite.config.js"
      provides: "Renamed Vite alias from openclaw/plugin-sdk to @agent/plugin-sdk"
      contains: "@agent/plugin-sdk/reply-payload"
  key_links:
    - from: "frontend/src/app/src/auto-reply/*.ts"
      to: "frontend/src/app/src/config/types.ts"
      via: "Import path changed from ../config/types.openclaw.js to ../config/types.js"
      pattern: "config/types"
    - from: "frontend/vite.config.js"
      to: "all auto-reply/reply files"
      via: "Vite alias resolution for @agent/plugin-sdk/reply-payload"
      pattern: "@agent/plugin-sdk"
---

<objective>
Delete dead routing files, restore broken OpenClawConfig imports in live files, and rename the Vite alias to a neutral name.

Purpose: D-06 deletes routing code that imports the already-deleted config/types.openclaw.js. The auto-reply and infra files (20+) are still actively used but have broken imports from that same deleted config directory — these need a type replacement, not deletion. D-09 renames the `openclaw/plugin-sdk/reply-payload` Vite alias to `@agent/plugin-sdk/reply-payload`, updating all imports across the codebase.

Output:
- routing/bindings.ts, resolve-route.ts, bound-account-read.ts deleted
- New `frontend/src/app/src/config/types.ts` with minimal OpenClawConfig type stub
- All auto-reply/infra imports updated from `../config/types.openclaw.js` to `../config/types.js`
- Vite alias renamed in vite.config.js + all import paths updated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-openclaw-todo-ci/115-CONTEXT.md
@.planning/phases/115-openclaw-todo-ci/115-RESEARCH.md

<interfaces>
Current Vite alias (from frontend/vite.config.js):
```
'openclaw/plugin-sdk/reply-payload': fileURLToPath(new URL('./src/app/src/plugin-sdk/reply-payload.ts', import.meta.url))
```

Files using Vite alias (12 files — all under frontend/src/app/src/auto-reply/):
```
heartbeat-reply-payload.ts:  import { hasOutboundReplyContent } from "openclaw/plugin-sdk/reply-payload"
reply-delivery.ts:           import { resolveSendableOutboundReplyParts } from "openclaw/plugin-sdk/reply-payload"
agent-runner-helpers.ts:     } from "openclaw/plugin-sdk/reply-payload"
agent-runner-payloads.ts:    import { resolveSendableOutboundReplyParts } from "openclaw/plugin-sdk/reply-payload"
followup-runner.ts:          } from "openclaw/plugin-sdk/reply-payload"
agent-runner-execution.ts:   } from "openclaw/plugin-sdk/reply-payload"
streaming-directives.ts:     import { hasOutboundReplyContent } from "openclaw/plugin-sdk/reply-payload"
block-reply-pipeline.ts:     import { resolveSendableOutboundReplyParts } from "openclaw/plugin-sdk/reply-payload"
dispatch-from-config.ts:     import { resolveSendableOutboundReplyParts } from "openclaw/plugin-sdk/reply-payload"
reply-media-paths.ts:        } from "openclaw/plugin-sdk/reply-payload"
block-reply-coalescer.ts:    import { resolveSendableOutboundReplyParts } from "openclaw/plugin-sdk/reply-payload"
dispatch-acp-delivery.ts:    import { hasOutboundReplyContent } from "openclaw/plugin-sdk/reply-payload"
```

Files importing OpenClawConfig from ../config/types.openclaw.js (non-routing, 20+ files):
```
frontend/src/app/src/auto-reply/envelope.ts
frontend/src/app/src/auto-reply/commands-registry.ts
...
```
(Executor must scan all files with `grep -l 'types.openclaw' --include='*.ts' frontend/src/` to get the full list.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead routing files</name>
  <files>
    frontend/src/app/ui/routing/bindings.ts
    frontend/src/app/ui/routing/resolve-route.ts
    frontend/src/app/ui/routing/bound-account-read.ts
  </files>
  <read_first>
    frontend/src/app/ui/routing/bindings.ts (verify it imports from deleted config/types.openclaw.js)
  </read_first>
  <action>
    Delete three dead routing files per D-06:
    1. `frontend/src/app/ui/routing/bindings.ts` — imports `OpenClawConfig` from `../config/types.openclaw.js`, a file that no longer exists. This file is NOT imported by any other module (confirmed by grep).
    2. `frontend/src/app/ui/routing/resolve-route.ts` — same broken import, no other imports.
    3. `frontend/src/app/ui/routing/bound-account-read.ts` — same broken import, no other imports.

    Before deleting, verify with `grep -rn "routing/bindings\|routing/resolve-route\|routing/bound-account-read" frontend/src/ --include="*.ts" | grep -v "routing/"` that NO other file imports from these files.

    After deletion, confirm all three files are gone with `test -f` returning 1 for each.
  </action>
  <verify>
    <automated>
      test ! -f frontend/src/app/ui/routing/bindings.ts && \
      test ! -f frontend/src/app/ui/routing/resolve-route.ts && \
      test ! -f frontend/src/app/ui/routing/bound-account-read.ts && \
      echo "ROUTING_DELETED"
    </automated>
    Expected: "ROUTING_DELETED"
  </verify>
  <acceptance_criteria>
    - `bindings.ts`, `resolve-route.ts`, `bound-account-read.ts` all deleted
    - No other files in `frontend/src/` import from these deleted files
  </acceptance_criteria>
  <done>Three dead routing files deleted, no broken imports remain.</done>
</task>

<task type="auto">
  <name>Task 2: Create config/types.ts type stub and fix OpenClawConfig imports in live files</name>
  <files>
    frontend/src/app/src/config/types.ts  (NEW)
    frontend/src/app/src/auto-reply/envelope.ts
    frontend/src/app/src/auto-reply/commands-registry.ts
    frontend/src/app/src/auto-reply/inbound-debounce.ts
    frontend/src/app/src/auto-reply/status.ts
    frontend/src/app/src/auto-reply/commands-registry-list.ts
    frontend/src/app/src/auto-reply/command-auth.ts
    frontend/src/app/src/auto-reply/chunk.ts
    frontend/src/app/src/auto-reply/commands-registry.types.ts
    frontend/src/app/src/auto-reply/dispatch.ts
    frontend/src/app/src/auto-reply/command-status-builders.ts
    frontend/src/app/src/auto-reply/skill-commands.ts
    frontend/src/app/src/auto-reply/command-detection.runtime-types.ts
    frontend/src/app/src/auto-reply/commands-registry-normalize.ts
    frontend/src/app/src/auto-reply/command-detection.ts
    frontend/src/app/src/auto-reply/stage-sandbox-media.test-harness.ts
    frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.test-harness.ts
    frontend/src/app/src/auto-reply/reply/commands.test-harness.ts
    frontend/src/app/src/auto-reply/reply/directive-handling.params.ts
    frontend/src/app/src/auto-reply/reply/queue.test-helpers.ts
    frontend/src/app/src/infra/update-startup.ts
  </files>
  <read_first>
    frontend/src/app/src/auto-reply/envelope.ts
    frontend/src/app/src/auto-reply/commands-registry.ts
  </read_first>
  <action>
    **Step 1: Scan all files with broken OpenClawConfig imports**

    Run `grep -rl 'types.openclaw' frontend/src/ --include="*.ts" | grep -v node_modules` to get the complete list. Separate into:
    - Routing files under `ui/routing/` → already deleted in Task 1 (ignore)
    - All other files → need import fix

    **Step 2: Analyze actual OpenClawConfig type usage**

    For each non-routing file, check what properties of OpenClawConfig are accessed. Common patterns used across auto-reply files:
    - `cfg?.agents?.defaults` (envelope.ts:70)
    - `cfg` passed as parameter to other openclaw config consumers
    - Type-only usage (no property access, just type annotations)

    **Step 3: Create `frontend/src/app/src/config/types.ts`** with an appropriate OpenClawConfig stub

    Create the directory `frontend/src/app/src/config/` if it does not exist. Create `types.ts` that exports `OpenClawConfig` as a type that satisfies all extant usage patterns. Best approaches (use judgment):
    - If property access is minimal (just `agents`, `channels`, etc.), create an explicit interface with those properties typed as `unknown`
    - If usage is diverse or pass-through, use `Record<string, any>` with a comment explaining this is a backward-compatibility stub

    **Step 4: Update all non-routing imports**

    For each file importing from `"../config/types.openclaw.js"`, change to `"../config/types.js"` (the new file). Use sed or a similar batch approach:

    For files at `frontend/src/app/src/auto-reply/*.ts`:
    ```
    s|\.\./config/types\.openclaw\.js|../config/types.js|g
    ```
    For files at `frontend/src/app/src/auto-reply/reply/*.ts`:
    ```
    s|\.\./\.\./config/types\.openclaw\.js|../../config/types.js|g
    ```
    For `frontend/src/app/src/infra/update-startup.ts`:
    Adjust relative path as needed to match `../config/types.js`

    Verify each update by searching for `types.openclaw` and confirming zero remaining occurrences in non-routing files.
  </action>
  <verify>
    <automated>
      test -f frontend/src/app/src/config/types.ts && \
      ! grep -r 'types\.openclaw' frontend/src/ --include='*.ts' | grep -v 'routing/' | grep -v node_modules | head -1
    </automated>
    Expected: `types.ts` exists, and `grep` for `types.openclaw` in non-routing files returns empty (exit code 1)
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/src/config/types.ts` exists and exports `OpenClawConfig`
    - Zero occurrences of `types.openclaw` remain outside the deleted `routing/` directory
    - Frontend TypeScript compilation (`cd frontend && npx tsc --noEmit`) must pass (or if no tsc script, at minimum no import errors for `config/types.openclaw`)
  </acceptance_criteria>
  <done>All config/types.openclaw.js imports replaced with config/types.js. New type stub file created.</done>
</task>

<task type="auto">
  <name>Task 3: Rename Vite alias `openclaw/plugin-sdk` to `@agent/plugin-sdk` and update all imports</name>
  <files>
    frontend/vite.config.js
    frontend/src/app/src/auto-reply/heartbeat-reply-payload.ts
    frontend/src/app/src/auto-reply/reply/reply-delivery.ts
    frontend/src/app/src/auto-reply/reply/agent-runner-helpers.ts
    frontend/src/app/src/auto-reply/reply/agent-runner-payloads.ts
    frontend/src/app/src/auto-reply/reply/followup-runner.ts
    frontend/src/app/src/auto-reply/reply/agent-runner-execution.ts
    frontend/src/app/src/auto-reply/reply/streaming-directives.ts
    frontend/src/app/src/auto-reply/reply/block-reply-pipeline.ts
    frontend/src/app/src/auto-reply/reply/dispatch-from-config.ts
    frontend/src/app/src/auto-reply/reply/reply-media-paths.ts
    frontend/src/app/src/auto-reply/reply/block-reply-coalescer.ts
    frontend/src/app/src/auto-reply/reply/dispatch-acp-delivery.ts
  </files>
  <read_first>
    frontend/vite.config.js
  </read_first>
  <action>
    **Step 1: Update Vite alias in vite.config.js**

    In `frontend/vite.config.js`, change:
    ```
    'openclaw/plugin-sdk/reply-payload': fileURLToPath(new URL('./src/app/src/plugin-sdk/reply-payload.ts', import.meta.url))
    ```
    to:
    ```
    '@agent/plugin-sdk/reply-payload': fileURLToPath(new URL('./src/app/src/plugin-sdk/reply-payload.ts', import.meta.url))
    ```

    The key changes: `openclaw/plugin-sdk/` → `@agent/plugin-sdk/`.

    **Step 2: Update all import paths across frontend**

    Find all files importing from the old alias: `grep -rn '"openclaw/plugin-sdk' frontend/src/ --include="*.ts" --include="*.js" | grep -v node_modules`

    For each file, replace `"openclaw/plugin-sdk/reply-payload"` with `"@agent/plugin-sdk/reply-payload"`.

    Use a single sed command across the whole tree:
    ```
    find frontend/src/ -name '*.ts' -o -name '*.js' | xargs sed -i '' 's|openclaw/plugin-sdk/reply-payload|@agent/plugin-sdk/reply-payload|g'
    ```
    (The `-i ''` is for macOS sed; use `-i` without `''` on Linux.)

    **Step 3: Verify**

    Confirm zero occurrences of `openclaw/plugin-sdk` remain in frontend/src:
    ```
    grep -rn 'openclaw/plugin-sdk' frontend/src/ --include='*.ts' --include='*.js' | grep -v node_modules
    ```
    Should return empty (exit code 1).
  </action>
  <verify>
    <automated>
      grep -c '@agent/plugin-sdk/reply-payload' frontend/vite.config.js && \
      ! grep -r 'openclaw/plugin-sdk' frontend/src/ --include='*.ts' --include='*.js' | grep -v node_modules | head -1
    </automated>
    Expected: `grep -c` returns at least 1 (alias exists), and `grep -r` for old alias returns empty
  </verify>
  <acceptance_criteria>
    - `vite.config.js` contains `'@agent/plugin-sdk/reply-payload'` (not the old name)
    - Zero occurrences of `openclaw/plugin-sdk` remain in `frontend/src/`
    - All 12+ auto-reply files use the new import path
  </acceptance_criteria>
  <done>Vite alias renamed. All imports updated. Zero traces of the old alias remain in source files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan deletes dead code, fixes import paths, and renames a Vite alias. No new trust boundaries are introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-02 | Tampering | Vite alias rename | accept | Alias name change is purely a build-time concern. No runtime impact. Module resolution unchanged. |
</threat_model>

<verification>
- 3 routing files deleted, no downstream import errors
- config/types.ts exists with valid OpenClawConfig stub
- Zero occurrences of `types.openclaw` or `openclaw/plugin-sdk` in frontend/src/ source files
- `cd frontend && npx tsc --noEmit` passes (or at minimum, no unresolved module errors)
</verification>

<success_criteria>
- All dead routing code is removed
- Auto-reply / infra files compile without import errors
- Vite alias uses neutral name `@agent/plugin-sdk/reply-payload`
- Frontend build (`cd frontend && npm run build`) completes without errors
</success_criteria>

<output>
Create `.planning/phases/115-openclaw-todo-ci/115-02-SUMMARY.md` when done
</output>
