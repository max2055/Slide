---
phase: 115-openclaw-todo-ci
plan: 04
type: execute
wave: 3
depends_on:
  - 115-02
  - 115-03
files_modified:
  - frontend/src/app/i18n/locales/zh-CN.ts
  - frontend/src/app/ui/direct-gateway.ts
  - frontend/src/app/ui/types/chat-types.ts
  - frontend/src/app/ui/views/chat.ts
  - frontend/src/app/src/chat/canvas-render.ts
  - frontend/src/app/src/protocol/CLAUDE.md
  - apps/db-ops-api/server.ts
  - package.json
  - apps/db-ops-api/src/alert-rca-service.ts
  - apps/db-ops-api/src/ai-agent-bridge.ts
  - apps/db-ops-api/src/chat-handler.ts
  - apps/db-ops-api/src/fault-diagnosis-service.ts
  - apps/db-ops-api/src/tools/policy.ts
  - apps/db-ops-api/src/tools/orchestrator.ts
  - apps/db-ops-api/src/tools/catalog.ts
  - apps/db-ops-api/src/tools/types.ts
  - apps/db-ops-api/src/tools/generated/llm-config/index.ts (if still exists — deleted by Plan 01, skip)
  - apps/db-ops-api/src/llm/provider-catalog.ts
autonomous: true
requirements:
  - D-07
  - D-08
  - D-10
  - D-11
  - D-12
  - D-13
user_setup: []

must_haves:
  truths:
    - "No `OpenClaw`/`openclaw` references remain in code comments, i18n files, or user-facing strings"
    - "server.ts health endpoint no longer returns `gateway_version` field"
    - "server.ts DOC_TITLES no longer lists OPENCLAW-*.html entries"
    - "Root package.json no longer has `gateway:start` or `gateway:stop` scripts"
    - "`__openclaw` session key marker is either removed or confirmed as unused"
    - "protocol/CLAUDE.md is removed or updated to reflect current DirectAdapter architecture"
  artifacts:
    - path: "apps/db-ops-api/server.ts"
      provides: "Cleaned health endpoint and doc titles"
      not_contains: "gateway_version\|OPENCLAW-"
    - path: "package.json"
      provides: "No gateway scripts"
      not_contains: "gateway:start\|gateway:stop"
    - path: "frontend/src/app/ui/direct-gateway.ts"
      provides: "File with OpenClaw comment removed"
      not_contains: "OpenClaw-style"
    - path: "frontend/src/app/ui/types/chat-types.ts"
      provides: "Comment no longer references OpenClaw"
      not_contains: "Copied from OpenClaw"
  key_links:
    - from: "frontend/src/app/i18n/locales/zh-CN.ts"
      to: "section headers"
      via: "OpenClaw 菜单组 → neutral comment"
      pattern: "菜单组"
    - from: "apps/db-ops-api/server.ts"
      to: "health endpoint"
      via: "gateway_version field removed"
      pattern: "gateway_version"
---

<objective>
Clean sweep: replace all remaining `OpenClaw`/`openclaw` text references in comments, i18n, and files, clean server.ts and package.json, remove `__openclaw` markers, and handle misc cleanup items.

Purpose: Per D-07/D-08, all OpenClaw naming in comments and text must use neutral aliases. D-10/D-11 clean health endpoint fields and package.json scripts. D-12 checks `__openclaw` markers. D-13 tidies misc files (protocol doc, file comments).

Output:
- Zero `OpenClaw`/`openclaw` references in source comments, i18n, or text (excluding functional property names like `includeInOpenClawGroup` in tool-catalog.ts)
- server.ts: `gateway_version` removed from health response, `OPENCLAW-*` entries removed from DOC_TITLES
- package.json: `gateway:start`/`gateway:stop` scripts removed (if they exist)
- `__openclaw` markers in chat.ts and canvas-render.ts assessed and either removed or left with a comment
- protocol/CLAUDE.md deleted or rewritten to reflect DirectAdapter
- direct-gateway.ts: OpenClaw comment replaced with neutral text
- chat-types.ts: "Copied from OpenClaw" comment replaced
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-openclaw-todo-ci/115-CONTEXT.md
@.planning/phases/115-openclaw-todo-ci/115-RESEARCH.md

<interfaces>
Known OpenClaw reference locations (pre-confirmed by grep):

**i18n (frontend/src/app/i18n/locales/zh-CN.ts):**
- Line 169: `// OpenClaw 菜单组` (comment, not user-facing)
- Line 210: `// OpenClaw 菜单组` (comment, not user-facing)

**Frontend comments:**
- frontend/src/app/ui/direct-gateway.ts:154 — `// Parse OpenClaw-style session key: agent:<agentId>:<actualKey> → actualKey`
- frontend/src/app/ui/types/chat-types.ts:3 — `* Copied from OpenClaw ui/src/ui/types/chat-types.ts`

**`__openclaw` markers:**
- frontend/src/app/ui/views/chat.ts:~1946 — session key marker (DirectAdapter: still produces? Check at execution)
- frontend/src/app/src/chat/canvas-render.ts:~148 — URL path `/__openclaw__/canvas/documents/`

**Backend `复用 OpenClaw` comments (all in apps/db-ops-api/src/):**
- alert-rca-service.ts:128
- ai-agent-bridge.ts:5
- chat-handler.ts:6,13
- fault-diagnosis-service.ts:81
- tools/policy.ts:4,293,457
- tools/orchestrator.ts:4
- tools/catalog.ts:4,18
- tools/types.ts:4,33,89,95,109,150,202
- llm/provider-catalog.ts:5

**server.ts:**
- L196 — query includes 'gateway_version' in SQL
- L202 — returns `gateway: config.gateway_version || ''`
- L221-223 — OPENCLAW-RELATIONSHIP.html, OPENCLAW-COMPARISON.html, DE-OPENCLAW.html in DOC_TITLES

**protocol/CLAUDE.md:** References Gateway protocol docs — outdated since migration to DirectAdapter.

**Key exclusion:** `includeInOpenClawGroup` property in `frontend/src/app/src/agents/tool-catalog.ts` is a functional property name, NOT a comment. Per D-07, `gateway`/`Gateway` is preserved. This property should be kept as-is unless renaming it wouldn't break runtime behavior (executor's judgment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean i18n comments, code comments, and tool-catalog OpenClaw references</name>
  <files>
    frontend/src/app/i18n/locales/zh-CN.ts
    frontend/src/app/ui/direct-gateway.ts
    frontend/src/app/ui/types/chat-types.ts
    frontend/src/app/src/agents/tool-catalog.ts
    apps/db-ops-api/src/alert-rca-service.ts
    apps/db-ops-api/src/ai-agent-bridge.ts
    apps/db-ops-api/src/chat-handler.ts
    apps/db-ops-api/src/fault-diagnosis-service.ts
    apps/db-ops-api/src/tools/policy.ts
    apps/db-ops-api/src/tools/orchestrator.ts
    apps/db-ops-api/src/tools/catalog.ts
    apps/db-ops-api/src/tools/types.ts
    apps/db-ops-api/src/llm/provider-catalog.ts
  </files>
  <read_first>
    frontend/src/app/i18n/locales/zh-CN.ts (lines 165-215)
    frontend/src/app/ui/direct-gateway.ts (read_line: 150-160)
    frontend/src/app/src/agents/tool-catalog.ts
  </read_first>
  <action>
    Per D-07/D-08, replace or remove all `OpenClaw`/`openclaw` text references in comments and i18n files. Follow D-07 principle: `openclaw`/`OpenClaw` → neutral aliases (NOT `slide`).

    **Step 1: i18n zh-CN.ts**
    - Lines 169 and 210: Change `// OpenClaw 菜单组` to `// 导航菜单组`
    - These are section-comment markers, not user-facing text — no translation needed

    **Step 2: Frontend comments**
    - `direct-gateway.ts:154`: Change "Parse OpenClaw-style session key: ..." → "Parse session key (agent format): agent:<agentId>:<actualKey> → actualKey"
    - `chat-types.ts:3`: Change "* Copied from OpenClaw ui/src/ui/types/chat-types.ts" → "* Derived from upstream chat-types.ts"
    - `tool-catalog.ts`: For the `includeInOpenClawGroup` property (lines 36, 102, 110, 118, 126, 134, 142, 150, 158, 166, 174):
      - This is a FUNCTIONAL property name, not just a comment. Renaming it would change serialization/behavior.
      - **Executor judgment call**: If this property is only used internally (no serialization), rename it to `includeInGroup` or `legacyGroup`. If it's wire-formatted or persisted, leave the name but add a comment explaining it's legacy naming.
      - Default action: Leave the property name as-is, add a comment `// legacy OpenClaw naming — kept for backward compatibility` above the first usage.

    **Step 3: Backend "复用 OpenClaw" comments**
    All backend files listed above have comments like `* 复用 OpenClaw 的 ... 设计模式`. Replace each with neutral language:
    - `复用 OpenClaw 的 XX 设计模式` → `复用 XX 设计模式 (from upstream)`
    - `通过 OpenClaw Agent 执行` → `通过 Agent 执行`
    - `或 OpenClawAdapter` → `或 OpenClawAdapter` (keep — this references a real adapter class name)
    - `OpenClaw 风格` → `上游风格`

    Specific replacements:
    - `alert-rca-service.ts:128`: `通过 OpenClaw Agent 执行分析` → `通过 Agent 执行分析`
    - `ai-agent-bridge.ts:5`: `或 OpenClawAdapter` → keep (references real class name); `不再直接调用 sendGatewayChat` → keep
    - `chat-handler.ts:6,13`: References `DirectAdapter or OpenClawAdapter` → keep (OpenClawAdapter is a real class); update surrounding prose if it implies OpenClaw is current
    - `fault-diagnosis-service.ts:81`: `通过 OpenClaw Agent 执行诊断` → `通过 Agent 执行诊断`
    - `tools/policy.ts:4,293,457`: `复用 OpenClaw 的 tool-policy.ts 设计模式` → `复用上游 tool-policy 设计模式` (and similar for other lines)
    - `tools/orchestrator.ts:4`: `复用 OpenClaw 的 tool orchestration 机制` → `复用上游 tool orchestration 机制`
    - `tools/catalog.ts:4,18`: `复用 OpenClaw 的 tool-catalog 和 tool-policy 设计模式` → `复用上游 tool-catalog 和 tool-policy 设计模式`; `复用 OpenClaw string-coerce.ts 模式` → `复用上游 string-coerce.ts 模式`
    - `tools/types.ts:4,33,89,95,109,150,202`: Multiple references — replace `OpenClaw 风格` with `上游风格`, `OpenClaw 的 AnyAgentTool 接口` with `上游 AnyAgentTool 接口`, etc.
    - `llm/provider-catalog.ts:5`: `复用 OpenClaw provider-catalog 模式` → `复用上游 provider-catalog 模式`

    **Step 4: Full sweep scan**
    After making the known replacements, run a final sweep to catch any remaining OpenClaw references:
    ```
    grep -rn "OpenClaw\|openclaw" apps/db-ops-api/src/ frontend/src/ --include="*.ts" --include="*.js" --include="*.md" | grep -v node_modules | grep -v "\.git/"
    ```
    If any remain that were not caught, handle them with the same treatment. Skip dead files that Plan 01 already deleted (llm-config/index.ts, configure_llm.ts).
  </action>
  <verify>
    <automated>
      grep -rn "OpenClaw\|openclaw" apps/db-ops-api/src/ frontend/src/ --include="*.ts" --include="*.js" --include="*.md" | grep -v node_modules | grep -v "\.git/" | grep -v "includeInOpenClawGroup"
    </automated>
    Expected: Empty (exit code 1) — no OpenClaw/openclaw references remain in source files, except for `includeInOpenClawGroup` (functional property name) and references to `OpenClawAdapter` class name (real class reference)
  </verify>
  <acceptance_criteria>
    - i18n zh-CN.ts `OpenClaw 菜单组` comments replaced with neutral text
    - direct-gateway.ts and chat-types.ts comments cleaned
    - tool-catalog.ts `includeInOpenClawGroup`: either renamed or annotated with legacy comment
    - All backend "复用 OpenClaw" comments replaced with neutral "复用上游" phrasing
    - Full grep sweep confirms no remaining OpenClaw/openclaw text in any source file (excluding functional property names and real class references like `OpenClawAdapter`)
  </acceptance_criteria>
  <done>Zero OpenClaw/openclaw references remain in source code comments and text (excluding functional names and adapter class references).</done>
</task>

<task type="auto">
  <name>Task 2: Clean server.ts, package.json, __openclaw markers, protocol doc</name>
  <files>
    apps/db-ops-api/server.ts
    package.json
    frontend/src/app/ui/views/chat.ts
    frontend/src/app/src/chat/canvas-render.ts
    frontend/src/app/src/protocol/CLAUDE.md
  </files>
  <read_first>
    apps/db-ops-api/server.ts (lines 190-225 — health endpoint + DOC_TITLES)
    package.json
    frontend/src/app/ui/views/chat.ts (grep for __openclaw to find the exact lines)
  </read_first>
  <action>
    Per D-10, D-11, D-12, D-13:

    **server.ts (D-10):**
    a. Health endpoint (L191-206):
       - Remove `'gateway_version'` from the SQL query's config_key IN clause (L196)
       - Remove `gateway: config.gateway_version || ''` from the response object (L202)
       - The response should still include `version`, just not `gateway`
       - Also remove `gateway: ''` from the catch block fallback (L205)
    b. DOC_TITLES (L221-223):
       - Remove three entries: `'OPENCLAW-RELATIONSHIP.html'`, `'OPENCLAW-COMPARISON.html'`, `'DE-OPENCLAW.html'`
       - Note: These are just title map entries. The actual HTML files in `docs/slide/` remain on disk (documentation cleanup is deferred per CONTEXT.md "Deferred Ideas").

    **package.json (D-11):**
    - Check root `package.json` for `"gateway:start"` and `"gateway:stop"` scripts. The research says these may already be removed. If they exist, delete both lines. If not present, no action needed.
    - Do NOT remove any other scripts.

    **`__openclaw` markers (D-12):**
    a. `chat.ts`: Search for `__openclaw` in the file. The RESEARCH.md says DirectAdapter no longer produces `__openclaw` session keys, so the parsing code is dead. Remove the entire `__openclaw` parsing branch. If the marker is in a session key extraction function, simplify it to not check for the `__openclaw` prefix.
    b. `canvas-render.ts`: Search for `__openclaw`/`__openclaw__` URL path. RESEARCH.md flags this as risky because it generates `/__openclaw__/canvas/documents/...` URLs. Check if the backend route that serves this path still exists.
       - If the backend route exists: Keep the `__openclaw__` path but replace it with `__canvas__` or a neutral path prefix, AND update the corresponding backend route.
       - If the backend route was removed: The URL prefix generates broken links. Replace `__openclaw__` with `__canvas__` in the frontend URL generation AND add a check — if no backend route serves it, remove the URL generation entirely.
       - **Executor discretion**: This requires checking whether canvas functionality is still operational. If uncertain, replace the path prefix to a neutral name and add a comment noting the backend route may need updating.

    **protocol/CLAUDE.md (D-13):**
    - This file documents the old Gateway protocol (references `docs/gateway/protocol.md`, `src/gateway/protocol/schema.ts`, etc.)
    - If the protocol/schema.ts, index.ts, and connect-error-details.ts files in the same directory are still in use (check with `grep -rn "from.*protocol" frontend/src/ --include="*.ts" | grep -v node_modules`), the CLAUDE.md should be UPDATED rather than deleted:
      - Rewrite to reflect that the protocol now applies to DirectAdapter, not Gateway
      - Remove references to `gateway/protocol.md`, `gateway/bridge-protocol.md`, `gateway/protocol/schema.ts`
      - Simplify: describe the current protocol state (DirectAdapter WS handshake, schema types, client-info format)
    - If the protocol files themselves are unused, delete the entire CLAUDE.md
    - **Executor's judgment**: Default to updating the doc if the schema files are still imported.
  </action>
  <verify>
    <automated>
      ! grep -q "gateway_version" apps/db-ops-api/server.ts && \
      ! grep -q "OPENCLAW-" apps/db-ops-api/server.ts && \
      ! grep -q "__openclaw" frontend/src/app/ui/views/chat.ts && \
      ! grep -q '__openclaw__' frontend/src/app/src/chat/canvas-render.ts && \
      echo "CLEANUP_DONE"
    </automated>
    Expected: "CLEANUP_DONE" — no gateway_version, OPENCLAW-*, or __openclaw markers remain in specified files
  </verify>
  <acceptance_criteria>
    - server.ts health endpoint does not reference `gateway_version` in query or response
    - server.ts DOC_TITLES does not contain `OPENCLAW-*` entries
    - Root `package.json` does not have `gateway:start`/`gateway:stop` scripts (or was already clean)
    - `chat.ts` no longer parses `__openclaw` session key markers
    - `canvas-render.ts` either uses neutral URL path or has updated handling (replaced `__openclaw__` with neutral prefix)
    - `protocol/CLAUDE.md` updated to reflect DirectAdapter state (or deleted if schema files unused)
  </acceptance_criteria>
  <done>server.ts, package.json, chat.ts, canvas-render.ts, and protocol/CLAUDE.md cleaned per D-10 through D-13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan renames text references, removes unused code paths, and updates documentation. No new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-04 | Tampering | canvas-render.ts URL path | accept | Replacing `__openclaw__` URL prefix with neutral `__canvas__`. Risk is limited to broken canvas links if backend route doesn't match. |
</threat_model>

<verification>
- Zero `OpenClaw`/`openclaw` references remain in source code (excluding functional names and class references)
- server.ts: no gateway_version field or OPENCLAW-* doc entries
- chat.ts: no __openclaw parsing code
- canvas-render.ts: no __openclaw URL prefix
- `cd frontend && npx tsc --noEmit` passes
- `cd apps/db-ops-api && npx tsc --noEmit` passes
</verification>

<success_criteria>
- All OpenClaw naming in comments, i18n, and text replaced with neutral aliases
- server.ts health endpoint and doc titles cleaned
- package.json gateway scripts removed (or already clean)
- __openclaw markers removed from chat.ts and canvas-render.ts
- protocol/CLAUDE.md updated or removed
</success_criteria>

<output>
Create `.planning/phases/115-openclaw-todo-ci/115-04-SUMMARY.md` when done
</output>
