---
phase: 115-openclaw-todo-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/src/tools/generated/slide-self-mgmt/configure_llm.ts
  - apps/db-ops-api/src/tools/generated/slide-self-mgmt/index.ts
  - apps/db-ops-api/src/tools/generated/llm-config/index.ts
  - apps/db-ops-api/src/tools/generated/llm-config/llm-config-tools.test.ts
  - apps/db-ops-api/CLAUDE.md
autonomous: true
requirements:
  - D-04
  - D-05
user_setup: []

must_haves:
  truths:
    - "Agent LLM config tools (configure_llm.ts + llm-config/index.ts) are no longer present in the source tree"
    - "LLM config management is only accessible via REST API /api/llm/configs, not through Agent tools"
    - "CLAUDE.md no longer references self-mgmt empty-shell tools or llm-config"
    - "slide-self-mgmt/index.ts still exports checkStatusTool and completeAnalysis (not broken by the deletion)"
  artifacts:
    - path: "apps/db-ops-api/src/tools/generated/slide-self-mgmt/configure_llm.ts"
      provides: "Deleted file"
      min_lines: 0
    - path: "apps/db-ops-api/src/tools/generated/llm-config/index.ts"
      provides: "Deleted file"
      min_lines: 0
    - path: "apps/db-ops-api/src/tools/generated/llm-config/llm-config-tools.test.ts"
      provides: "Deleted test file"
      min_lines: 0
    - path: "apps/db-ops-api/src/tools/generated/slide-self-mgmt/index.ts"
      provides: "Removed configureLlmTool export"
      not_contains: "configureLlmTool"
    - path: "apps/db-ops-api/CLAUDE.md"
      provides: "Updated — no references to self-mgmt empty-shell tools or llm-config"
      not_contains: "self-mgmt 空壳工具"
  key_links:
    - from: "CLAUDE.md"
      to: "slide-self-mgmt/index.ts"
      via: "updated tool status section"
      pattern: "完善\|已修复"
---

<objective>
Delete both Agent LLM config tool sets, remove their exports, and update CLAUDE.md

Purpose: D-04 states that Agent LLM config tools (configure_llm.ts + llm-config/index.ts) are replaced by the REST API `/api/llm/configs`. These tools are dead code after the migration. D-05 updates the backend CLAUDE.md to remove the now-outdated section about these tools.

Output:
- configure_llm.ts deleted
- llm-config/index.ts deleted
- llm-config-tools.test.ts deleted
- configureLlmTool export removed from slide-self-mgmt/index.ts
- CLAUDE.md "self-mgmt 空壳工具" section removed/updated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-openclaw-todo-ci/115-CONTEXT.md
@.planning/phases/115-openclaw-todo-ci/115-RESEARCH.md

<interfaces>
From apps/db-ops-api/src/tools/generated/slide-self-mgmt/index.ts:
```
export { checkStatusTool } from './check_status.js';
export { configureLlmTool } from './configure_llm.js';
export { analyzeRunningCronTool } from './complete_analysis.js';
import { checkStatusTool } from './check_status.js';
import { configureLlmTool } from './configure_llm.js';
import { analyzeRunningCronTool } from './complete_analysis.js';
```
Only the `configureLlmTool` export and import lines must be removed. **keep** `checkStatusTool` and `analyzeRunningCronTool` untouched.

From apps/db-ops-api/CLAUDE.md:
## 待修复
- **self-mgmt 空壳工具**: `tools/generated/slide-self-mgmt/check_status.ts` 的 DB/LLM 检查返回占位数据；`configure_llm.ts` 6 个 handler 均为 TODO。已有功能完整的 `llm-config/index.ts` 可用作委托源

## 已修复
- **LLM 配置工具**: `tools/generated/llm-config/index.ts` 6 个工具全部实现，18 个测试

These sections reference files that D-04 deletes. The entire **待修复** section should be revised since the check_status.ts TODO will be fixed by Plan 03, and configure_llm.ts/llm-config/index.ts no longer exist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Agent LLM config tool files</name>
  <files>
    apps/db-ops-api/src/tools/generated/slide-self-mgmt/configure_llm.ts
    apps/db-ops-api/src/tools/generated/llm-config/index.ts
    apps/db-ops-api/src/tools/generated/llm-config/llm-config-tools.test.ts
  </files>
  <read_first>
    apps/db-ops-api/src/tools/generated/slide-self-mgmt/configure_llm.ts
    apps/db-ops-api/src/tools/generated/llm-config/index.ts
  </read_first>
  <action>
    Delete three files per D-04:
    1. `apps/db-ops-api/src/tools/generated/slide-self-mgmt/configure_llm.ts` (288 lines, 6 LLM CRUD handlers — all replaced by REST API `/api/llm/configs`)
    2. `apps/db-ops-api/src/tools/generated/llm-config/index.ts` (464 lines, 6 tool factories — all replaced)
    3. `apps/db-ops-api/src/tools/generated/llm-config/llm-config-tools.test.ts` (230 lines, tests for the deleted tools)

    Before deleting, read configure_llm.ts and verify no other files import from it besides slide-self-mgmt/index.ts (which Task 2 handles). Also verify llm-config-tools.test.ts is the only test file for llm-config/index.ts.

    After deletion, confirm the files no longer exist with `test -f` for each path.
  </action>
  <verify>
    <automated>
      test ! -f apps/db-ops-api/src/tools/generated/slide-self-mgmt/configure_llm.ts && \
      test ! -f apps/db-ops-api/src/tools/generated/llm-config/index.ts && \
      test ! -f apps/db-ops-api/src/tools/generated/llm-config/llm-config-tools.test.ts && \
      echo "FILES_DELETED"
    </automated>
    Expected: "FILES_DELETED"
  </verify>
  <acceptance_criteria>
    - `configure_llm.ts` file no longer exists
    - `llm-config/index.ts` file no longer exists
    - `llm-config-tools.test.ts` file no longer exists
    - No remaining broken imports pointing to these deleted files (verify with grep)
  </acceptance_criteria>
  <done>All three files deleted. No other files have broken imports from these deleted files.</done>
</task>

<task type="auto">
  <name>Task 2: Remove configureLlmTool export from slide-self-mgmt/index.ts</name>
  <files>
    apps/db-ops-api/src/tools/generated/slide-self-mgmt/index.ts
  </files>
  <read_first>
    apps/db-ops-api/src/tools/generated/slide-self-mgmt/index.ts
  </read_first>
  <action>
    Edit `slide-self-mgmt/index.ts` to remove the `configureLlmTool` export and import:

    Current exports (examples):
    ```
    export { checkStatusTool } from './check_status.js';
    export { configureLlmTool } from './configure_llm.js';
    export { analyzeRunningCronTool } from './complete_analysis.js';
    ```

    Remove:
    - `export { configureLlmTool } from './configure_llm.js';`
    - `import { configureLlmTool } from './configure_llm.js';`

    Keep ALL other exports and imports intact (checkStatusTool, analyzeRunningCronTool must remain).

    Also check if any OTHER files in the codebase import from this index file or directly from configure_llm.ts — if found, notify in summary but do NOT modify them (they are edge cases for executor judgment).
  </action>
  <verify>
    <automated>
      grep -c "configureLlmTool" apps/db-ops-api/src/tools/generated/slide-self-mgmt/index.ts
    </automated>
    Expected: exit code 1 (string not found, grep returns 0 matches)
  </verify>
  <acceptance_criteria>
    - `configureLlmTool` no longer appears anywhere in `slide-self-mgmt/index.ts`
    - `checkStatusTool` and `analyzeRunningCronTool` exports and imports remain untouched
  </acceptance_criteria>
  <done>configureLlmTool import and export removed. Other exports intact.</done>
</task>

<task type="auto">
  <name>Task 3: Update backend CLAUDE.md</name>
  <files>
    apps/db-ops-api/CLAUDE.md
  </files>
  <read_first>
    apps/db-ops-api/CLAUDE.md
  </read_first>
  <action>
    Update CLAUDE.md per D-05. Two sections need revision:

    **"待修复" section — self-mgmt 空壳工具:**
    - `configure_llm.ts` handers = TODO → this file is now DELETED per D-04. Remove its mention.
    - `llm-config/index.ts` "可用作委托源" → this file is now DELETED per D-04. Remove this sentence.
    - `check_status.ts` placeholder data → this TODO will be fixed by Plan 03. Keep this entry but rephrase to remove the now-deleted configure_llm/llm-config references.

    **"已修复" section — LLM 配置工具:**
    - The entry "tools/generated/llm-config/index.ts 6 个工具全部实现，18 个测试" references a now-deleted file. This entry should be removed entirely (the LLM config tool implementation is gone; it was replaced by REST API).

    New "待修复" entry should read something like:
    ```
    - **slide-self-mgmt 状态检查**: `check_status.ts` 的 DB/LLM 检查返回占位数据，需参考 database-service 实现真实检查
    ```

    Remove the entire "LLM 配置工具" entry from "已修复".
  </action>
  <verify>
    <automated>
      grep -c "self-mgmt 空壳工具\|llm-config/index.ts" apps/db-ops-api/CLAUDE.md; echo "EXIT:$?"
    </automated>
    Expected: 0 matches (exit 1), both phrases removed
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` no longer contains `self-mgmt 空壳工具` or `llm-config/index.ts`
    - `check_status.ts` mention remains (its TODO will be fixed in Plan 03, not deleted)
    - No references to `configure_llm.ts` as a TODO or fix
  </acceptance_criteria>
  <done>CLAUDE.md cleaned: self-mgmt section rephrased without deleted files, llm-config entry removed from 已修复.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan deletes dead Agent tool code. No new trust boundaries are introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | Agent tool catalog | accept | Deleting tools whose capabilities are fully covered by REST API (`/api/llm/configs`). No new attack surface. |
</threat_model>

<verification>
- All 3 files deleted (configure_llm.ts, llm-config/index.ts, llm-config-tools.test.ts)
- slide-self-mgmt/index.ts no longer exports configureLlmTool
- CLAUDE.md updated: no references to self-mgmt empty shells or llm-config
- `cd apps/db-ops-api && npx tsc --noEmit` passes (backend typecheck)
</verification>

<success_criteria>
- Agent LLM config tools are completely removed from the codebase
- LLM configuration is only accessible through the REST API at `/api/llm/configs`
- Backend typecheck passes with no broken imports
- CLAUDE.md accurately reflects the current state of the codebase
</success_criteria>

<output>
Create `.planning/phases/115-openclaw-todo-ci/115-01-SUMMARY.md` when done
</output>
