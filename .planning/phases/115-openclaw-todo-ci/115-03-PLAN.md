---
phase: 115-openclaw-todo-ci
plan: 03
type: execute
wave: 2
depends_on:
  - 115-01
files_modified:
  - apps/db-ops-api/src/tools/ops/get_instance_summary.ts
  - apps/db-ops-api/src/tools/ops/list_active_alerts.ts
  - apps/db-ops-api/src/report-service.ts
  - apps/db-ops-api/src/tools/generated/slide-self-mgmt/check_status.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
user_setup: []

must_haves:
  truths:
    - "get_instance_summary filters results by the current user's RBAC instance permissions"
    - "list_active_alerts filters results by the current user's RBAC instance permissions"
    - "collectCapacityData() in report-service.ts returns real capacity data (disk_usage, growth_trend) using database-service.ts capacity methods"
    - "check_status.ts performs real DB connection and LLM provider status checks instead of returning placeholder data"
  artifacts:
    - path: "apps/db-ops-api/src/tools/ops/get_instance_summary.ts"
      provides: "RBAC-scoped instance summary tool"
      contains: "requestContext"
    - path: "apps/db-ops-api/src/tools/ops/list_active_alerts.ts"
      provides: "RBAC-scoped alert listing tool"
      contains: "requestContext"
    - path: "apps/db-ops-api/src/report-service.ts"
      provides: "Real capacity data collection"
      contains: "collectCapacityData"
    - path: "apps/db-ops-api/src/tools/generated/slide-self-mgmt/check_status.ts"
      provides: "Real service status check"
      not_contains: "placeholder\|stub\|TODO"
  key_links:
    - from: "get_instance_summary.ts"
      to: "requestContext.userId"
      via: "RBAC permission filter"
      pattern: "user.*permission\|rbac.*filter\|instance.*access"
    - from: "report-service.ts"
      to: "database-service.ts capacity methods"
      via: "collectCapacityData implementation"
      pattern: "getMySQLCapacity\|getPostgresCapacity\|get.*Capacity"
---

<objective>
Fix 4 remaining backend TODO stubs after D-04 eliminated the configure_llm.ts TODO: implement RBAC scope filtering in two ops tools, implement real capacity data collection in report-service.ts, and replace placeholder status checks in check_status.ts.

Purpose: D-01 requires fixing all TODO stubs. D-02 requires RBAC scope derived from the request context. D-03 requires reusing existing database-service.ts capacity methods instead of returning empty data.

Output:
- get_instance_summary.ts: Results filtered by user's instance permissions
- list_active_alerts.ts: Results filtered by user's instance permissions
- report-service.ts collectCapacityData(): Returns real capacity metrics
- check_status.ts: Real DB connection check and LLM provider status
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-openclaw-todo-ci/115-CONTEXT.md
@.planning/phases/115-openclaw-todo-ci/115-RESEARCH.md

<interfaces>
Existing TODO markers in the 4 files:

1. get_instance_summary.ts line 4 (D-01/D-02):
```
// TODO(D-08): Add RBAC user-context scope when available. get_instance_summary data
```

2. list_active_alerts.ts line 4 (D-01/D-02):
```
// TODO(D-08): Add RBAC user-context scope when available. Current: queries all data the service account can see.
```

3. report-service.ts line 608-609 (D-01/D-03):
```
private async collectCapacityData(instanceId: number): Promise<any> {
    // TODO: 实现容量数据收集
```

4. check_status.ts (D-01): No explicit TODO marker, but CLAUDE.md documents it returns placeholder data for DB/LLM checks.

Available capacity methods in database-service.ts (per CONTEXT.md reusable assets):
- getMySQLCapacity(instanceId)
- getPostgresCapacity(instanceId)
- getDamengCapacity(instanceId)
- getOracleCapacity(instanceId)

RBAC pattern reference — existing tools like testConnection use requestContext:
```typescript
// Existing pattern from testConnection tool
const rbacService = requestContext?.rbacService;
const userId = requestContext?.userId;
// Use userId to filter by instance permissions
```

Agent tool pattern (for check_status.ts):
```typescript
import type { AnyAgentTool, ToolResult } from '../../types.js';
import { toolCatalog } from '../../catalog.js';

export const myTool: AnyAgentTool = {
  name: 'slide_my_tool',
  description: '...',
  parameters: { type: 'object', properties: {}, required: [] },
  handler: async (args) => { /* ... */ },
};
toolCatalog.register(myTool);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RBAC scope filtering to get_instance_summary.ts and list_active_alerts.ts</name>
  <files>
    apps/db-ops-api/src/tools/ops/get_instance_summary.ts
    apps/db-ops-api/src/tools/ops/list_active_alerts.ts
  </files>
  <read_first>
    apps/db-ops-api/src/tools/ops/get_instance_summary.ts
    apps/db-ops-api/src/tools/ops/list_active_alerts.ts
  </read_first>
  <action>
    Per D-01/D-02, add RBAC user-context scope filtering to both tools. The pattern:

    1. Read both files. Identify how each currently retrieves instances (likely through `instanceDatabaseService.getAllInstances()` or similar).

    2. Add an optional `requestContext` parameter to the tool factory/function signature (following the pattern used by `testConnection` and other RBAC-aware tools). The type for request context is typically `UserRequestContext` from `../../types.js` or similar — import it from wherever existing tools import it.

    3. In the handler:
       - Get `userId` from `requestContext?.userId`
       - If `userId` is present, use RBAC service to get the set of instance IDs the user has access to
       - Filter the query results to only include instances in the user's allowed set
       - If `userId` is absent (service account / no auth context), return all results (existing behavior for backward compatibility)

    4. The exact RBAC service API may vary. Look for existing RBAC patterns in the codebase:
       - `permissionDatabaseService` or `rbacDatabaseService` — check for methods like `getUserInstances()`, `getUserPermittedInstanceIds()`, or similar
       - Use `grep -rn "getUserInstances\|getUserPermittedInstanceIds\|getUserAccessibleInstanceIds\|instancePermissions\|instanceAccess" apps/db-ops-api/src/ --include="*.ts" | grep -v node_modules | head -10` to find the right method
       - If no direct "get user instance IDs" method exists, check how `instance-level access control` was implemented in Phase 101 (AUTH-04) — there should be a method that returns which instances a user can access

    5. Remove the `TODO(D-08)` comment from both files after implementation.

    6. Verify: both tools should compile and existing tests should pass (tests may not exist for these specific tools, but the build should not break).

    Security note per D-02: Use `requestContext.userId` from the authenticated request context, never from `args.userId` or any client-supplied parameter. This prevents privilege escalation.
  </action>
  <verify>
    <automated>
      grep -c "requestContext\|userId\|permission\|instance.*access" apps/db-ops-api/src/tools/ops/get_instance_summary.ts apps/db-ops-api/src/tools/ops/list_active_alerts.ts
    </automated>
    Expected: At least 1 match in each file for `requestContext` or RBAC-related patterns
  </verify>
  <acceptance_criteria>
    - Both files contain RBAC filtering logic referencing `requestContext`
    - `TODO(D-08)` comment removed from both files
    - Results are filtered by the authenticated user's instance-level permissions
    - Service account/unauthorized calls still return all results (backward compatible)
    - No client-supplied parameters used for authorization decisions
  </acceptance_criteria>
  <done>Both tools filter results by user's RBAC instance permissions per D-02. Service-account fallback preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Implement real capacity data collection in report-service.ts collectCapacityData()</name>
  <files>
    apps/db-ops-api/src/report-service.ts
  </files>
  <read_first>
    apps/db-ops-api/src/report-service.ts (lines 600-620 around collectCapacityData)
    apps/db-ops-api/src/database-service.ts (grep for get*Capacity method signatures — the exact method names, params, and return types)
  </read_first>
  <action>
    Per D-01/D-03, implement `collectCapacityData()` at line 608 to return real data instead of the current unfinished stub.

    The method currently reads:
    ```typescript
    private async collectCapacityData(instanceId: number): Promise<any> {
        // TODO: 实现容量数据收集
    }
    ```

    Implementation:
    1. First, determine the instance's `db_type` by reading the instance info (use `instanceDatabaseService.getInstanceById` or equivalent — verify the import exists in report-service.ts).

    2. Based on `db_type`, call the appropriate capacity method from database-service.ts:
       - `mysql` → `databaseService.getMySQLCapacity(instanceId)`
       - `postgresql` → `databaseService.getPostgresCapacity(instanceId)`
       - `dmdb` (Dameng) → `databaseService.getDamengCapacity(instanceId)`
       - `oracle` → `databaseService.getOracleCapacity(instanceId)`
       - default → return a reasonable empty state (e.g., `{ disk_usage: 0, growth_trend: 'unknown' }`)

    3. Read the capacity method signatures in database-service.ts first to understand their return types. They likely return disk usage, growth trend, and related capacity metrics.

    4. The return should match what the capacity report template expects (check how the existing code uses `capacityData` at lines 212, 239, 243, 487, 493, 501).

    5. Handle errors gracefully — if the capacity query fails (connection issue, unsupported DB type), return safe defaults rather than throwing.

    6. Remove the `// TODO: 实现容量数据收集` comment.

    7. Verify the implementation doesn't break existing report rendering. The capacity template at lines 239-243 expects `disk_usage` and `growth_trend` fields.
  </action>
  <verify>
    <automated>
      grep -A5 "private async collectCapacityData" apps/db-ops-api/src/report-service.ts
    </automated>
    Expected: The method body contains calls to one or more `get*Capacity` methods, not just a TODO comment
  </verify>
  <acceptance_criteria>
    - `collectCapacityData()` calls database-service.ts capacity methods based on the instance's `db_type`
    - The `// TODO` comment is removed
    - Returns `{ disk_usage, growth_trend }` compatible with the capacity report template
    - Handles unknown db_type and errors gracefully with safe defaults
  </acceptance_criteria>
  <done>collectCapacityData() returns real capacity data by dispatching to the correct database-service.ts capacity method per db_type.</done>
</task>

<task type="auto">
  <name>Task 3: Fix check_status.ts to perform real status checks</name>
  <files>
    apps/db-ops-api/src/tools/generated/slide-self-mgmt/check_status.ts
  </files>
  <read_first>
    apps/db-ops-api/src/tools/generated/slide-self-mgmt/check_status.ts
  </read_first>
  <action>
    Per D-01, fix `check_status.ts` to return real status data instead of placeholder values. The CLAUDE.md documents that DB connection and LLM provider checks return placeholder data.

    1. Read the full file to understand the current structure. It likely has a `ServiceStatus` interface and checks for:
       - Frontend service status
       - Backend service status
       - Database connection status
       - LLM provider status

    2. Replace **database connection check** — instead of returning hardcoded/"running" status:
       - Import `databaseService` (likely already imported — verify) 
       - Call `databaseService.checkConnectionAlive()` or attempt a lightweight query like `SELECT 1`
       - Return actual connection status, response time, and any error message

    3. Replace **LLM provider check** — instead of returning placeholder:
       - Import `getAllProviders` from `../../../llm/provider-catalog.js` (likely already imported)
       - For each configured provider, attempt a minimal connectivity check (ping the API endpoint or check the provider config validity)
       - Return actual provider status instead of hardcoded "running"
       - Handle the case where no LLM providers are configured (return "stopped" or "not configured" gracefully)

    4. Keep existing service checks (frontend/backend port checks) — they may already be functional.

    5. Remove any placeholder comments or stub data.

    6. Verify: `npx tsc --noEmit` in apps/db-ops-api passes.
  </action>
  <verify>
    <automated>
      grep -n "databaseService\|checkConnection\|provider\|getAllProviders" apps/db-ops-api/src/tools/generated/slide-self-mgmt/check_status.ts | head -5
    </automated>
    Expected: File contains references to actual service checks (database ping, LLM provider connectivity)
  </verify>
  <acceptance_criteria>
    - Database connection check performs a real connectivity test (SELECT 1 or checkConnectionAlive)
    - LLM provider check iterates over configured providers and returns per-provider status
    - No placeholder data or stub responses remain
    - Backend typecheck passes
  </acceptance_criteria>
  <done>check_status.ts performs real DB connection and LLM provider status checks instead of returning placeholder data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent tool handler → Backend DB | RBAC scope filtering prevents tools from returning data for instances the user doesn't have permission to access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03 | Elevation of Privilege | get_instance_summary / list_active_alerts | mitigate | Per D-02: Use `requestContext.userId` (authenticated context) — never a client-supplied parameter. Filter results by the user's instance-level permissions via RBAC service. |
</threat_model>

<verification>
- Both tools compile with RBAC filtering logic
- report-service.ts collectCapacityData() calls real capacity methods
- check_status.ts performs real connectivity checks
- `cd apps/db-ops-api && npx tsc --noEmit` passes
- Existing tests pass: `cd apps/db-ops-api && npx vitest run` (but note: 70+ backend tests are known to fail in the current state — this plan only ensures no NEW failures are introduced)
</verification>

<success_criteria>
- get_instance_summary.ts: Results filtered by user's RBAC instance permissions
- list_active_alerts.ts: Results filtered by user's RBAC instance permissions
- report-service.ts: collectCapacityData() dispatches to real capacity methods per db_type
- check_status.ts: DB connection and LLM provider checks return real status
- No TODO comments remain in any of the 4 files
</success_criteria>

<output>
Create `.planning/phases/115-openclaw-todo-ci/115-03-SUMMARY.md` when done
</output>
