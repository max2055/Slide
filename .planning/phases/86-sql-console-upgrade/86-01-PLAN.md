---
phase: 86-sql-console-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/openclaw/ui/views/__tests__/sql-console.test.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All new components have corresponding test stubs before implementation begins"
    - "Vitest can discover and run tests in sql-console.test.ts"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest configuration for LitElement components"
      min_lines: 10
    - path: "frontend/package.json"
      provides: "Updated devDependencies with vitest and @open-wc/testing"
      contains: "vitest" and "@open-wc/testing"
    - path: "frontend/src/openclaw/ui/views/__tests__/sql-console.test.ts"
      provides: "Test stubs for all 7 requirements"
      min_lines: 60
  key_links:
    - from: "frontend/package.json"
      to: "frontend/vitest.config.ts"
      via: "vitest devDependency"
      pattern: "vitest"
    - from: "frontend/vitest.config.ts"
      to: "sql-console.test.ts"
      via: "test configuration"
      pattern: "\\.test\\.ts"
---

<objective>
Create test infrastructure for all SQL Console Upgrade components.

Purpose: Establish Vitest configuration and test stubs BEFORE any implementation code is written. This ensures every feature has a corresponding test skeleton that will be filled in during implementation.

Output: Vitest config, updated package.json with test dependencies, test stub file with describe blocks for all 7 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/86-sql-console-upgrade/86-VALIDATION.md
@.planning/phases/86-sql-console-upgrade/86-RESEARCH.md
@frontend/package.json
@frontend/vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Create Vitest configuration and update package.json</name>
  <files>
    frontend/vitest.config.ts
    frontend/package.json
  </files>
  <read_first>
    frontend/package.json (current devDependencies section - needs vitest and @open-wc/testing added)
    frontend/vite.config.js (existing Vite config - vitest config should extend it)
  </read_first>
  <action>
    1. Create `frontend/vitest.config.ts` with the following exact content:

    ```typescript
    import { defineConfig } from 'vitest/config';
    import { fileURLToPath, URL } from 'node:url';

    export default defineConfig({
      resolve: {
        alias: {
          '@': fileURLToPath(new URL('./src', import.meta.url)),
          '@openclaw/ui': fileURLToPath(new URL('./src/openclaw/ui', import.meta.url)),
        },
      },
      test: {
        environment: 'jsdom',
        globals: true,
        include: ['src/**/*.test.ts'],
        setupFiles: [],
        css: true,
      },
    });
    ```

    2. Update `frontend/package.json`:
       - Add to `scripts`: `"test": "vitest run"` and `"test:watch": "vitest"`
       - Add to `devDependencies`:
         - `"vitest": "^3.1.3"`
         - `"@open-wc/testing": "^4.0.0"`
         - `"jsdom": "^26.1.0"`

    Keep all existing entries unchanged. Do not modify any other part of package.json.
  </action>
  <verify>
    <automated>cd /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/frontend && npm install --dry-run 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/vitest.config.ts` exists and is readable
    - `frontend/vitest.config.ts` contains `test.environment: 'jsdom'` and `test.globals: true`
    - `frontend/package.json` contains `"test": "vitest run"` in scripts
    - `frontend/package.json` has `vitest` in devDependencies
    - `frontend/package.json` has `@open-wc/testing` in devDependencies
    - `frontend/package.json` has `jsdom` in devDependencies
  </acceptance_criteria>
  <done>
    Vitest config created and package.json updated with test dependencies.
  </done>
</task>

<task type="auto">
  <name>Create test stubs for all 7 SQL Console requirements</name>
  <files>
    frontend/src/openclaw/ui/views/__tests__/sql-console.test.ts
  </files>
  <read_first>
    frontend/vite.config.ts (created in previous task - vitest config that will run these tests)
    86-RESEARCH.md (validation architecture section for per-requirement test mappings)
    86-VALIDATION.md (per-task verification map)
  </read_first>
  <action>
    Create `frontend/src/openclaw/ui/views/__tests__/sql-console.test.ts` with describe/it stubs for all 7 requirements. Each describe block must contain a correct empty-state assertion that initially fails (RED) — this is standard TDD practice. Use Vitest + `@open-wc/testing` fixtures.

    Content structure:

    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('SQL Console Upgrade', () => {
      describe('SQLC-01: Schema-driven autocomplete', () => {
        it('provides table and column names from loaded schema in autocomplete suggestions', () => {
          expect(false).toBe(true); // RED — implement in Plan 02
        });

        it('filters completions based on cursor context (FROM clause, after JOIN, etc.)', () => {
          expect(false).toBe(true); // RED — implement in Plan 02
        });
      });

      describe('SQLC-02: Sortable result table', () => {
        it('sorts column ascending on first click, descending on second, unsorted on third', () => {
          expect(false).toBe(true); // RED — implement in Plan 03
        });

        it('places null values at end regardless of sort direction', () => {
          expect(false).toBe(true); // RED — implement in Plan 03
        });
      });

      describe('SQLC-03: Client-side pagination', () => {
        it('shows correct page of results based on currentPage and pageSize', () => {
          expect(false).toBe(true); // RED — implement in Plan 03
        });

        it('renders all rows when pageSize is "All"', () => {
          expect(false).toBe(true); // RED — implement in Plan 03
        });
      });

      describe('SQLC-04: CSV export', () => {
        it('generates CSV string with column headers as first row', () => {
          expect(false).toBe(true); // RED — implement in Plan 03
        });

        it('escapes cells containing commas, double-quotes, and newlines per RFC 4180', () => {
          expect(false).toBe(true); // RED — implement in Plan 03
        });

        it('writes null values as empty string in exported CSV', () => {
          expect(false).toBe(true); // RED — implement in Plan 03
        });
      });

      describe('SQLC-05: Multi-tab editor', () => {
        it('creates and switches between tabs preserving independent editor content', () => {
          expect(false).toBe(true); // RED — implement in Plan 02
        });

        it('persists tabs across page reload via localStorage', () => {
          expect(false).toBe(true); // RED — implement in Plan 02
        });

        it('shows confirmation dialog when closing a tab with non-empty SQL', () => {
          expect(false).toBe(true); // RED — implement in Plan 02
        });
      });

      describe('SQLC-06: Query history', () => {
        it('displays history items with SQL preview, instance, duration, row count, and timestamp', () => {
          expect(false).toBe(true); // RED — implement in Plan 04
        });

        it('filters history items by SQL text search', () => {
          expect(false).toBe(true); // RED — implement in Plan 04
        });

        it('loads next batch on infinite scroll', () => {
          expect(false).toBe(true); // RED — implement in Plan 04
        });
      });

      describe('SQLC-07: EXPLAIN visualization', () => {
        it('normalizes MySQL EXPLAIN FORMAT=JSON to unified PlanNode format', () => {
          expect(false).toBe(true); // RED — implement in Plan 05
        });

        it('renders EXPLAIN plan as collapsible tree view with operation, rows, cost', () => {
          expect(false).toBe(true); // RED — implement in Plan 05
        });

        it('renders EXPLAIN plan as flat sortable table when toggle is Table view', () => {
          expect(false).toBe(true); // RED — implement in Plan 05
        });
      });
    });
    ```

    Make sure the directory `frontend/src/openclaw/ui/views/__tests__/` exists before creating the file.
  </action>
  <verify>
    <automated>cd /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/frontend && npx vitest run --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/openclaw/ui/views/__tests__/sql-console.test.ts` exists
    - File contains `describe('SQLC-01'`, `describe('SQLC-02'`, `describe('SQLC-03'`, `describe('SQLC-04'`, `describe('SQLC-05'`, `describe('SQLC-06'`, `describe('SQLC-07'`
    - File has at least 60 lines
    - `npx vitest run` discovers all tests (they will fail with `expected false to be true` — TDD RED state is intentional)
    - Vitest reports the correct total test count (16 tests)
  </acceptance_criteria>
  <done>
    Test stubs created for all 7 requirements. Vitest discovers and runs 16 tests (all RED).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No user-facing features in this plan. Test infrastructure only — no trust boundaries apply.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | — | — | accept | Infrastructure-only plan. No production code or user data involved. |
</threat_model>

<verification>
- `cd frontend && npm install` succeeds
- `cd frontend && npx vitest run` discovers 16 tests (all expected to fail — RED state)
- All test stub files are tracked in git
</verification>

<success_criteria>
1. Vitest config is functional — `npx vitest run` discovers and reports tests
2. All 16 test stubs exist across 7 describe blocks
3. `frontend/package.json` has `vitest`, `@open-wc/testing`, `jsdom` in devDependencies
4. `npm run test` script is defined
</success_criteria>

<output>
After completion, create `.planning/phases/86-sql-console-upgrade/86-01-SUMMARY.md`
</output>
