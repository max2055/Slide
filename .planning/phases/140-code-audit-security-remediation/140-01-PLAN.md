---
phase: 140-code-audit-security-remediation
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SEC140-01, SEC140-02, SEC140-03]
files_modified:
  - apps/db-ops-api/src/security/database-target-policy.ts
  - apps/db-ops-api/src/instance-database-service.ts
  - apps/db-ops-api/server.ts
  - apps/db-ops-api/package.json
  - pnpm-lock.yaml
  - apps/db-ops-api/src/security/database-target-policy.test.ts
  - apps/db-ops-api/src/auth/security-boundaries.test.ts
---

# Phase 140 Plan 01：关闭 SSRF、品牌越权与依赖高危漏洞

<objective>
为数据库连接建立可配置且防 DNS rebinding 的目标授权边界，关闭品牌配置垂直越权，并升级生产依赖直到 High/Critical 为零或有可验证的限时处置。
</objective>

<tasks>
<task type="auto">
  <name>Task 1: 数据库目标策略与连接固定</name>
  <action>实现 DB_ALLOWED_CIDRS、各 adapter 端口允许集、IP/DNS 解析校验和 pinned lookup/地址连接。默认拒绝环回、链路本地、元数据、保留/组播地址；生产环境未配置允许网段时拒绝任意新目标。所有 test/create/update/reload 路径复用同一策略。错误只返回稳定 reason code。</action>
  <verify><automated>pnpm --filter slide-api vitest run src/security/database-target-policy.test.ts</automated></verify>
  <done>授权内网 fixture 通过；私网绕过、IPv4/IPv6、混合 DNS、rebind、非法端口全部拒绝且连接函数未调用。</done>
</task>
<task type="auto">
  <name>Task 2: 品牌写权限与审计</name>
  <action>PUT branding 增加 admin 权限、strictBody 和结构化审计；GET 保持登录可读。测试 viewer/DBA/admin 矩阵以及未知字段、无效值和持久化结果。</action>
  <verify><automated>pnpm --filter slide-api vitest run src/auth/security-boundaries.test.ts -t "branding"</automated></verify>
  <done>非管理员为 403 且零写入；管理员成功并产生不含配置 secret 的审计事件。</done>
</task>
<task type="auto">
  <name>Task 3: 生产依赖升级</name>
  <action>升级 Fastify/插件及受影响传递依赖，处理 v5 兼容变化；验证 Content-Type schema、错误处理、CORS、REST/WS、构建和锁文件。禁止使用 audit ignore 掩盖 High/Critical。</action>
  <verify><automated>pnpm audit --prod &amp;&amp; pnpm --filter slide-api typecheck &amp;&amp; pnpm --filter slide-api test</automated></verify>
  <done>生产依赖 High/Critical 为零，完整 API 测试通过。</done>
</task>
</tasks>

<success_criteria>输出 140-01-SUMMARY.md，记录 red/green 测试、依赖变化、残余风险和精确命令。</success_criteria>
