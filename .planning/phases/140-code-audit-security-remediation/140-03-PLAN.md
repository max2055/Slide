---
phase: 140-code-audit-security-remediation
plan: 03
type: execute
wave: 3
depends_on: [140-02]
autonomous: true
requirements: [SEC140-07, SEC140-08, SEC140-09]
---

# Phase 140 Plan 03：密钥迁移、告警与安全门禁

<objective>把密钥和安全事件纳入可迁移、可告警、不可静默回归的生产机制，并完成 Phase 级复验。</objective>

<tasks>
<task type="auto">
  <name>Task 1: 版本化认证加密迁移</name>
  <action>定义 v2 envelope（key id、nonce、ciphertext、tag），使用严格 32-byte key 和 AES-256-GCM；保留 v1 CBC 只读兼容并在成功读取后原子重写 v2。任何环境缺失有效 JWT/加密 key 均失败，测试回滚、错误 key、篡改和混合版本。</action>
  <verify><automated>pnpm --filter slide-api vitest run src/config/security-config.test.ts src/credential-rotation.test.ts</automated></verify>
  <done>旧凭据无损迁移，新密文篡改拒绝，日志/证据不含 key 或明文。</done>
</task>
<task type="auto">
  <name>Task 2: 安全事件与告警</name>
  <action>refresh replay、SSRF 拒绝、品牌越权、审批伪造/重放、限流和 fatal shutdown 产生类型化安全事件；接入现有通知/审计通道并去重限噪。</action>
  <verify><automated>pnpm --filter slide-api vitest run src/auth/security-boundaries.test.ts src/audit/audit-log.test.ts</automated></verify>
  <done>事件含 actor/resource/request/reason，不含 secret；重复攻击按窗口聚合。</done>
</task>
<task type="auto">
  <name>Task 3: CI 门禁与最终复验</name>
  <action>CI 增加 production audit、secret scan、安全负向测试和审计 finding 矩阵；High/Critical、测试 skip 或证据缺失均失败。执行完整 tests/typecheck/build/API/browser/runtime UAT，生成 REVIEW、EVIDENCE-MATRIX 和 VERIFICATION。</action>
  <verify><automated>pnpm audit --prod &amp;&amp; pnpm lint &amp;&amp; pnpm -r typecheck &amp;&amp; pnpm -r test &amp;&amp; pnpm build</automated></verify>
  <done>全部 Phase 140 要求有当前 commit 证据，VERIFICATION 明确 PASS/FAIL。</done>
</task>
</tasks>

<success_criteria>输出 140-03-SUMMARY.md、140-REVIEW.md、140-EVIDENCE-MATRIX.md、140-VERIFICATION.md，并同步 ROADMAP/STATE。</success_criteria>
