---
phase: 140-code-audit-security-remediation
plan: 02
type: execute
wave: 2
depends_on: [140-01]
autonomous: true
requirements: [SEC140-04, SEC140-05, SEC140-06]
---

# Phase 140 Plan 02：审批凭证、错误边界与 HTTP 防护

<objective>让危险 SQL 只能由持久审批事实授权执行，并统一客户端错误、脱敏日志和 HTTP 纵深防御。</objective>

<tasks>
<task type="auto">
  <name>Task 1: 持久审批执行凭证</name>
  <action>执行器接收服务端构造的审批授权对象，并在副作用前验证 approval id、operation id、instance id、SQL SHA-256、approved/executing 状态、reviewer 和有效期；批准只能原子消费一次。移除任意非空 approvedOperationId 放行语义和遗留 SELECT 正则。</action>
  <verify><automated>pnpm --filter slide-api vitest run src/approval-service.test.ts src/operations/sql-boundary.test.ts</automated></verify>
  <done>伪造、错实例、改 SQL、重放、过期和并发批准均零副作用。</done>
</task>
<task type="auto">
  <name>Task 2: 统一错误与进程失败策略</name>
  <action>建立 public error DTO 和 Fastify error handler，客户端只见 reason code；日志集中脱敏。连接测试、登录和 SQL 路径不再返回底层 message。uncaughtException/unhandledRejection 记录后进入有界 shutdown 并退出。</action>
  <verify><automated>pnpm --filter slide-api vitest run src/security/public-dto.test.ts src/auth/security-boundaries.test.ts</automated></verify>
  <done>响应/日志 secret 扫描为零，fatal fixture 确认进程退出且不继续接收请求。</done>
</task>
<task type="auto">
  <name>Task 3: HTTP 安全基线</name>
  <action>注册 helmet、登录 IP+用户名限流、显式 1 MiB bodyLimit、环境化 CORS 和 HTML CSP。生产 CORS 缺失 fail-closed；代理信任必须显式配置。</action>
  <verify><automated>pnpm --filter slide-api vitest run src/security/http-boundary.test.ts</automated></verify>
  <done>安全头、429、413、CORS allow/deny 和 HTML CSP 均由行为测试证明。</done>
</task>
</tasks>

<success_criteria>输出 140-02-SUMMARY.md，并完成 API/runtime 负向验证。</success_criteria>
