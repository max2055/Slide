---
phase: 106-指标采集可配置化
plan: 03
type: execute
wave: 2
depends_on:
  - 106-01
files_modified:
  - apps/db-ops-api/src/alert-evaluator.ts
  - apps/db-ops-api/src/alert-engine.ts
  - apps/db-ops-api/src/metric-database-service.ts
  - apps/db-ops-api/server.ts
autonomous: true
requirements:
  - MET-03
  - MET-02

must_haves:
  truths:
    - "Alert evaluator getMetricValue() merges fixed columns + JSON metrics_data column for metric lookup"
    - "Alert engine syncRulesFromRegistry() and alert rule save validates metric_name exists in metric_definitions and is_collected=true"
    - "metric:write permission code exists and POST/PUT/DELETE /api/metrics/registry routes use requirePermission('metric:write')"
    - "Delete metric with alert rule references returns 400 with clear error message"
    - "metric-database-service.deleteMetric checks alert_rules references before deleting"
    - "metrics-database-service.recordMetrics handles metrics_data field and writes it to the JSON column"
  artifacts:
    - path: "apps/db-ops-api/src/alert-evaluator.ts"
      provides: "getMetricValue merges fixed + JSON columns"
      min_lines: 40
    - path: "apps/db-ops-api/src/metric-database-service.ts"
      provides: "deleteMetric with ref check, new deleteMetricWithRefCheck method"
      min_lines: 20
    - path: "apps/db-ops-api/server.ts"
      provides: "Route permission changes (metric:write), delete ref check in route handler"
---

<objective>
实现告警端到端集成：alert-evaluator 合并 JSON 列取值 (D-15)、告警规则保存时验证 (D-14)、metric:write 权限路由保护 (D-16)、删除指标时检查告警引用 (D-17)、metrics-database-service 写入 JSON 列 (D-05)。

Purpose: 确保动态指标能正常参与告警评估，自定义指标的创建/编辑/删除有权限保护，指标删除不会留下悬空告警引用。
Output: 修改 4 个文件，新增权限验证逻辑和引用检查
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<interfaces>
<!-- Key contracts this plan depends on -->

From `apps/db-ops-api/src/alert-evaluator.ts` (existing):
```typescript
function getMetricValue(metricName: string, metrics: any): number {
  if (!metrics) return 0;
  const val = metrics[metricName];
  return val != null ? Number(val) : 0;
}

async function evaluateAllRules(): Promise<
  Array<{ rule: AlertRule; instanceId: number; instanceName: string;
           currentValue: number; thresholdUsed: number; }>
>
```

From `apps/db-ops-api/src/alert-engine.ts` (existing):
```typescript
class AlertEngine {
  async syncRulesFromRegistry(): Promise<{ created: number; updated: number }>;
}

export const alertEngine = new AlertEngine();
```

From `apps/db-ops-api/src/metric-database-service.ts` (existing):
```typescript
class MetricDatabaseService {
  async deleteMetric(id: string): Promise<{ success: boolean; reason?: string; error?: string }>
  // reason: 'builtin' | 'not_found'
}
```

From server.ts (existing metric routes at line 2963-3053):
```typescript
fastify.get('/api/metrics/registry', { preHandler: [verifyToken, requirePermission('metric:view')] }, ...)
fastify.post('/api/metrics/registry', { preHandler: [verifyToken, requirePermission('metric:manage')] }, ...)
fastify.put('/api/metrics/registry/:id', { preHandler: [verifyToken, requirePermission('metric:manage')] }, ...)
fastify.delete('/api/metrics/registry/:id', { preHandler: [verifyToken, requirePermission('metric:manage')] }, ...)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alert evaluator JSON column merge + alert rule validation</name>
  <files>
    apps/db-ops-api/src/alert-evaluator.ts
    apps/db-ops-api/src/alert-engine.ts
  </files>
  <behavior>
    - getMetricValue('cpu_usage', { cpu_usage: 75, metrics_data: {} }) === 75
    - getMetricValue('custom_metric', { cpu_usage: 75, metrics_data: { custom_metric: 42 } }) === 42
    - getMetricValue('custom_metric', { cpu_usage: 75 }) === 0
    - getMetricValue('nonexistent', {}) === 0
    - getMetricValue('custom_metric', { metrics_data: { custom_metric: '50' } }) === 50  (string to number)
    - syncRulesFromRegistry skips metrics where is_collected=false
    - syncRulesFromRegistry skips metrics where threshold_template is null
  </behavior>
  <action>
    修改 `apps/db-ops-api/src/alert-evaluator.ts` 中的 `getMetricValue()` 函数:

    将现有实现:
    ```typescript
    function getMetricValue(metricName: string, metrics: any): number {
      if (!metrics) return 0;
      const val = metrics[metricName];
      return val != null ? Number(val) : 0;
    }
    ```

    替换为:
    ```typescript
    function getMetricValue(metricName: string, metrics: any): number {
      if (!metrics) return 0;
      // 1. Check fixed columns first (fast path, type-safe)
      if (metricName in metrics && typeof metrics[metricName] === 'number') {
        return metrics[metricName];
      }
      // 2. Fallback: check JSON-extracted metrics_data (D-15)
      if (metrics.metrics_data && typeof metrics.metrics_data === 'object') {
        const val = metrics.metrics_data[metricName];
        if (val != null) return Number(val);
      }
      // 3. Last resort: direct property access (catches any string-value cases)
      const val = metrics[metricName];
      return val != null ? Number(val) : 0;
    }
    ```
    — 函数先检查固定列（快速路径），再检查 metrics_data JSON 列（动态指标），最后兜底直接属性访问。不破坏现有行为。

    修改 `apps/db-ops-api/src/alert-engine.ts` 中的 `syncRulesFromRegistry()` 方法:
    - 现有的 `if (!m.is_collected || !m.threshold_template) continue;` 已经正确处理了 `is_collected=false` 的跳过
    - 不需要额外修改 — `syncRulesFromRegistry` 已经隐式验证了 metric_name 存在（它遍历 metricRegistry.getAll() 得到的都是有效指标）
    - **但是** 需要额外为 D-14 的要求添加: 在 `syncRulesFromRegistry` 中，当遍历 `metrics` 时，额外打印一条日志:
      ```typescript
      // 如果存在设置 is_collected=false 但已经有告警规则的指标，提醒用户
      ```
    - 实际 D-14 的前端/后端双重验证主要在 alert rule 保存时触发。在 server.ts 的 Task 2 中处理 alert-rules 的 POST/PUT 验证。这里只处理 `syncRulesFromRegistry` 的增强。
  </action>
  <verify>
    <automated>cd apps/db-ops-api && npx vitest run src/__tests__/alert-evaluator-merge.test.ts --reporter verbose 2>&1</automated>
  </verify>
  <done>getMetricValue merges fixed + JSON columns, backward compatible with existing fixed-column path (D-15). syncRulesFromRegistry verified.</done>
</task>

<task type="auto">
  <name>Task 2: metric:write route protection + delete ref check + route updates</name>
  <files>
    apps/db-ops-api/src/metric-database-service.ts
    apps/db-ops-api/server.ts
  </files>
  <action>
    本 task 修改 2 个文件实现 4 个子目标: D-16 (metric:write 权限), D-17 (删除指标引用检查), D-14 告警规则验证 (后端部分), D-05 JSON 列写入。

    **A) MetricDatabaseService 添加 deleteWithRefCheck 方法 (`src/metric-database-service.ts`)**

    在 `deleteMetric` 方法后追加新方法:
    ```typescript
    /**
     * 删除指标定义前检查告警规则引用 (D-17)
     * @returns {success:boolean, reason?:string, referencedBy?:any[], error?:string}
     *   reason='has_alerts' 时 referencedBy 包含引用的告警规则
     */
    async deleteMetricWithRefCheck(id: string): Promise<{
      success: boolean;
      reason?: string;
      referencedBy?: Array<{ id: number; name: string }>;
      error?: string;
    }> {
      const pool = this.getPool();
      if (!pool) return { success: false, error: '数据库未连接' };

      try {
        // 检查是否为内置指标
        const [rows] = await pool.execute(
          'SELECT is_builtin FROM metric_definitions WHERE id = ?', [id]
        ) as any;
        if (rows.length === 0) return { success: false, reason: 'not_found' };
        if (rows[0].is_builtin) return { success: false, reason: 'builtin' };

        // 检查告警规则引用
        const [alertRules] = await pool.execute(
          'SELECT id, name FROM alert_rules WHERE metric_name = ?', [id]
        ) as any;
        if (alertRules && alertRules.length > 0) {
          return {
            success: false,
            reason: 'has_alerts',
            referencedBy: alertRules.map((r: any) => ({ id: r.id, name: r.name })),
          };
        }

        // 无引用，执行删除
        await pool.execute(
          'DELETE FROM metric_definitions WHERE id = ? AND is_builtin = FALSE', [id]
        );
        return { success: true };
      } catch (error: any) {
        console.error('删除指标失败:', error);
        return { success: false, error: error.message };
      }
    }
    ```

    **B) 修改 server.ts 路由和权限**

    **B1) POST/PUT/DELETE /api/metrics/registry 路由权限改为 metric:write (D-16)**

    找到 3 条写路由:
    - 第 2997 行 `POST /api/metrics/registry` — `requirePermission('metric:manage')` => `requirePermission('metric:write')`
    - 第 3018 行 `PUT /api/metrics/registry/:id` — `requirePermission('metric:manage')` => `requirePermission('metric:write')`
    - 第 3035 行 `DELETE /api/metrics/registry/:id` — `requirePermission('metric:manage')` => `requirePermission('metric:write')`

    **B2) DELETE route 换用 deleteMetricWithRefCheck (D-17)**

    修改第 3035-3053 行的 DELETE handler:
    现有:
    ```typescript
    fastify.delete('/api/metrics/registry/:id', { preHandler: [verifyToken, requirePermission('metric:manage')] }, async (request, reply) => {
      try {
        const { id } = request.params as any;
        const result = await metricDatabaseService.deleteMetric(id);
        ...
      } ...
    });
    ```

    替换为:
    ```typescript
    fastify.delete('/api/metrics/registry/:id', { preHandler: [verifyToken, requirePermission('metric:write')] }, async (request, reply) => {
      try {
        const { id } = request.params as any;
        const result = await metricDatabaseService.deleteMetricWithRefCheck(id);
        if (!result.success) {
          if (result.reason === 'builtin') {
            reply.code(403).send({ error: '预定义指标不可删除' });
            return;
          }
          if (result.reason === 'has_alerts') {
            reply.code(400).send({
              error: '该指标被告警规则引用，无法删除',
              referencedBy: result.referencedBy,
            });
            return;
          }
          if (result.reason === 'not_found') {
            reply.code(404).send({ error: '指标未找到' });
            return;
          }
          reply.code(400).send(result);
          return;
        }
        await metricRegistry.refreshFromDB();
        monitorCollector.refreshSchedule();
        reply.send({ success: true });
      } catch (error: any) {
        reply.code(500).send({ error: error.message });
      }
    });
    ```

    **B3) POST/PUT route 加 SQL 校验 (D-12 第二层)**

    在 `POST /api/metrics/registry` 和 `PUT /api/metrics/registry/:id` handler 中，如果 body 包含 `collection_sql`:
    ```typescript
    const body = request.body as any;
    // SQL 白名单校验 (D-12)
    if (body.collection_sql) {
      const { sqlValidator } = await import('./src/sql-validator.js');
      const validation = sqlValidator.validateSqlIsSelectOnly(body.collection_sql);
      if (!validation.valid) {
        reply.code(400).send({ error: `SQL 验证失败: ${validation.error}` });
        return;
      }
    }
    ```
    在 POST 路由中，保留原有必填字段检查，在其后插入 SQL 校验。

    **B4) 告警规则保存验证 (D-14 后端)**

    在 server.ts 中找到 `POST /api/alert-rules` 和 `PUT /api/alert-rules/:id` 的 handler。
    在 handler 开头插入:
    ```typescript
    const body = request.body as any;
    // D-14: 验证 metric_name 存在于 metric_definitions 且 is_collected=true
    if (body.metric_name) {
      const def = metricRegistry.getById(body.metric_name);
      if (!def) {
        reply.code(400).send({ error: `指标 "${body.metric_name}" 不存在` });
        return;
      }
      if (!def.is_collected) {
        reply.code(400).send({ error: `指标 "${body.metric_name}" 未启用采集，无法创建告警规则` });
        return;
      }
    }
    ```
    如果要找 alert-rules 路由，搜索 `/api/alert-rules`。如找不到该路径，说明 alert rules 也可能通过其他路由管理，搜索 `/api/alert-rules` 或 `alert_rule` 模式。
    实际上 alert_rules 的 CRUD 在 `alert-database-service.ts` 中。查找 server.ts 中 `/api/alert-rules` 或 `alertRule`:
    如果不存在独立路由，则 syncRulesFromRegistry 会处理创建，但这是内部的。D-14 说"告警规则保存时双重验证 metric_name 必须存在于 metric_definitions"，这暗示 alert rules 有手动创建/编辑路由。如果找不到独立 route，D-14 验证可以在 `alert-database-service.ts` 的 create/update 方法中添加。

    稳妥做法：先 grep 确认 server.ts 中 `/api/alert-rule` 的路径。如不存在，就在 `alert-database-service.ts` 的 createAlertRule/updateAlertRule 方法中添加验证，import metricRegistry 进行检查。无论哪种方式，validator 逻辑相同。

    **[实现提示]** 在执行前先确认 server.ts 是否有独立的 alert-rule 写路由。若无，选择 alert-database-service.ts 方案。

    **B5) 更新 metrics-database-service.ts 以支持 JSON 列写入**

    修改 `src/metrics-database-service.ts` 的 `recordMetrics` 方法:
    - 在 data 参数 interface 追加 `metrics_data?: Record<string, number>`
    - INSERT SQL 末尾追加 `, metrics_data` 列及其对应的 `?` placeholder
    - VALUES 数组末尾追加 `data.metrics_data ? JSON.stringify(data.metrics_data) : null`
    - 注意: 该 INSERT 有 34 个绑定参数，追加 metrics_data 后变为 35 个。VALUES 数组长度必须匹配。
    - 同时更新 `getRealtimeMetrics` 和 `getHistoricalMetrics` 的 SELECT 查询，新增 `metrics_data` 字段
  </action>
  <verify>
    <automated>cd apps/db-ops-api && npx tsc --noEmit src/metric-database-service.ts server.ts src/metrics-database-service.ts src/alert-database-service.ts 2>&1 | head -20</automated>
  </verify>
  <done>metric:write permissions on write routes (D-16). Delete checks alert_rules references before deletion (D-17). Alert rule save validates metric_name exists + is_collected (D-14). metrics-database-service records metrics_data JSON column (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API route -> metric-database-service | Metric write operations require metric:write permission |
| Alert rule save -> metric_registry | Validation ensures metric exists and is collected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-08 | Elevation of Privilege | server.ts metric routes | mitigate | requirePermission('metric:write') on POST/PUT/DELETE (D-16) |
| T-106-09 | Tampering | metric-database-service.deleteMetricWithRefCheck | mitigate | Pre-deletion alert_rules reference check prevents orphaned alerts (D-17) |
| T-106-10 | Tampering | alert rule save | mitigate | Backend validates metric_name exists + is_collected=true before save (D-14) |
</threat_model>

<verification>
1. `npx tsc --noEmit src/metric-database-service.ts src/metrics-database-service.ts server.ts` — no TS errors
2. Verify POST/PUT/DELETE /api/metrics/registry routes use `requirePermission('metric:write')`
3. Verify deleteMetricWithRefCheck queries alert_rules before delete
4. Verify alerts-rule save validates metric_name through metricRegistry
</verification>

<success_criteria>
- getMetricValue() in alert-evaluator.ts merges fixed columns with metrics_data JSON
- syncRulesFromRegistry correctly skips is_collected=false
- POST/PUT/DELETE /api/metrics/registry use `metric:write` permission
- DELETE /api/metrics/registry/:id uses deleteMetricWithRefCheck that checks alert_rules
- metrics-database-service.recordMetrics writes metrics_data to JSON column
- Alert rule save validates metric_name exists + is_collected
</success_criteria>

<output>
Create `.planning/phases/106-指标采集可配置化/106-03-SUMMARY.md` when done
</output>
