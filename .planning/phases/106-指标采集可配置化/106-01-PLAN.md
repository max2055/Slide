---
phase: 106-指标采集可配置化
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/src/collectors/registry.ts
  - apps/db-ops-api/src/collectors/base-provider.ts
  - apps/db-ops-api/sql/schema.sql
  - apps/db-ops-api/src/sql-validator.ts
  - apps/db-ops-api/src/metric-database-service.ts
autonomous: true
requirements:
  - MET-02
  - MET-01

must_haves:
  truths:
    - "Generic Registry<T> abstraction exists and can be used for Provider registration"
    - "MetricProvider interface + BaseMetricProvider abstract class exist in collectors/base-provider.ts"
    - "metric_definitions table has new columns: collection_sql, value_type, category, updated_by"
    - "metrics_history table has new JSON column: metrics_data"
    - "metric:write permission code exists in permissions seed data"
    - "node-sql-parser based sql-validator.ts correctly rejects non-SELECT statements"
    - "MetricDatabaseService.createMetric/updateMetric handles collection_sql, value_type, category, updated_by"
  artifacts:
    - path: "apps/db-ops-api/src/collectors/registry.ts"
      provides: "Generic Registry<T> class with register/enable/disable/list/get"
      min_lines: 40
    - path: "apps/db-ops-api/src/collectors/base-provider.ts"
      provides: "MetricProvider interface + BaseMetricProvider abstract class"
      min_lines: 30
    - path: "apps/db-ops-api/src/sql-validator.ts"
      provides: "validateSqlIsSelectOnly() SQL whitelist function using node-sql-parser"
      min_lines: 30
    - path: "apps/db-ops-api/sql/schema.sql"
      provides: "ALTER TABLE migration statements for metric_definitions and metrics_history"
      contains: "metrics_data JSON"
---

<objective>
创建 Phase 106 的后端基础设施层：Generic Registry<T> 抽象、BaseMetricProvider 基类、数据库迁移（metric_definitions + metrics_history 新列）、SQL 白名单验证器、MetricDatabaseService CRUD 增强。

Purpose: 完成所有后续 plan 依赖的基础组件。Registry 是 Phase 107/108 的架构基础；DB 迁移和 MetricDatabaseService 增强为数据驱动采集铺路；SQL 验证器确保自定义 SQL 安全。
Output: 5 个文件（3 新建 + 2 修改），包含完整的类型定义和接口
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<interfaces>
<!-- Existing interfaces the executor needs — extracted from codebase -->

From `apps/db-ops-api/src/metric-database-service.ts`:
```typescript
interface MetricDefinitionRow {
  id: string;
  name: string;
  description: string | null;
  unit: string;
  db_types: string;       // JSON string
  aggregation: string;
  default_interval: number;
  threshold_template: string | null;  // JSON string
  is_collected: boolean;
  is_builtin: boolean;
  created_at: Date;
  updated_at: Date;
}

class MetricDatabaseService {
  async createMetric(data: {
    id: string; name: string; description?: string; unit: string;
    db_types: string[]; aggregation: string; default_interval: number;
    threshold_template?: { warning: number; error: number; critical: number };
    is_collected?: boolean;
  }): Promise<{ success: boolean; error?: string }>

  async updateMetric(id: string, data: {
    name?: string; description?: string; unit?: string; db_types?: string[];
    aggregation?: string; default_interval?: number;
    threshold_template?: { warning: number; error: number; critical: number };
    is_collected?: boolean;
  }): Promise<{ success: boolean; error?: string }>

  async deleteMetric(id: string): Promise<{ success: boolean; reason?: string; error?: string }>
  async getAllMetrics(): Promise<MetricDefinitionRow[]>
  async getMetricById(id: string): Promise<MetricDefinitionRow | null>
}
```

From `apps/db-ops-api/src/database-service.ts`:
```typescript
interface DatabaseConnection {
  id: number; name: string;
  config: DatabaseConfig;
  pool: mysql.Pool | null;
  pgClient: PgClient | null;
  oracleConnection: oracledb.Connection | null;
  oraclePool: oracledb.Pool | null;
  dmConnection: dmdb.Connection | null;
  connected: boolean;
  db_type: string;
  deltaCounter?: { queries: number; commits: number; rollbacks: number; ...; timestamp: number };
  pgDeltaCounter?: { xactCommit: number; xactRollback: number; ...; timestamp: number };
  oracleDeltaCounter?: { executes: number; commits: number; timestamp: number };
}
```

From `apps/db-ops-api/src/metric-registry.ts` (MetricRegistry uses MetricDatabaseService):
```typescript
interface MetricDefinition {
  id: string; name: string; description: string; unit: string;
  db_types: string[]; aggregation: 'avg' | 'max' | 'min' | 'sum' | 'last';
  default_interval: number;
  threshold_template: { warning: number; error: number; critical: number };
  is_collected: boolean; is_builtin: boolean;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Generic Registry<T> + BaseMetricProvider</name>
  <files>
    apps/db-ops-api/src/collectors/registry.ts
    apps/db-ops-api/src/collectors/base-provider.ts
    apps/db-ops-api/src/__tests__/collectors/registry.test.ts
  </files>
  <behavior>
    - Registry<T> can register items by name, retrieve by name, list all
    - Registry<T> can enable/disable items (enabled defaults to true)
    - Registry<T> get(name) returns T | undefined for unknown name
    - BaseMetricProvider abstract class has: name, supportedDbTypes, enabled, consecutiveFailures, collect(), resetFailures()
    - MetricProvider interface is exported and used by BaseMetricProvider
  </behavior>
  <action>
    创建 `apps/db-ops-api/src/collectors/registry.ts` — 通用 `Registry<T extends { readonly name: string }>` 类。

    实现（注册时间测试后）:
    - `class Registry<T extends { readonly name: string }>` with:
      - `private items = new Map<string, { provider: T; enabled: boolean; consecutiveFailures: number }>()`
      - `register(provider: T): void` — 添加到 Map
      - `enable(name: string): void` — 设置 enabled=true
      - `disable(name: string): void` — 设置 enabled=false, 重置 consecutiveFailures
      - `get(name: string): T | undefined` — 返回 provider 实例
      - `list(): T[]` — 返回所有已注册 provider（无论 enabled）
      - `listEnabled(): T[]` — 仅返回 enabled=true 的 provider
      - `recordFailure(name: string): number` — 递增 consecutiveFailures，返回新值
      - `resetFailures(name: string): void` — 重置 consecutiveFailures=0
      - `getProvidersByDbType(dbType: string): T[]` — 根据 supportedDbTypes 过滤
    - 导出 `type RegistryItem<T> = { provider: T; enabled: boolean; consecutiveFailures: number }`

    另创建 `apps/db-ops-api/src/collectors/base-provider.ts`:
    - 导出 `interface MetricProvider`:
      ```typescript
      interface MetricProvider {
        readonly name: string;
        readonly supportedDbTypes: string[];
        enabled: boolean;
        consecutiveFailures: number;
        collect(instance: DatabaseConnection, metricDef: MetricDefinition): Promise<number | null>;
        describeSchema?(instanceId: number): Promise<string>;
        resetFailures(): void;
      }
      ```
    - 导出 `abstract class BaseMetricProvider implements MetricProvider`:
      ```typescript
      abstract class BaseMetricProvider implements MetricProvider {
        abstract readonly name: string;
        abstract readonly supportedDbTypes: string[];
        enabled = true;
        consecutiveFailures = 0;
        abstract collect(instance: DatabaseConnection, metricDef: MetricDefinition): Promise<number | null>;
        async describeSchema?(instanceId: number): Promise<string> { return ''; }
        resetFailures(): void { this.consecutiveFailures = 0; }
      }
      ```
    - 导入 `DatabaseConnection` from `../database-service.js`, `MetricDefinition` from `../metric-registry.js`
    - 注意: BaseMetricProvider.describeSchema 用 optional method 声明（?号语法），非必须实现的子类可以不实现

    创建测试文件 `apps/db-ops-api/src/__tests__/collectors/registry.test.ts`:
    - 测试 Registry 注册、获取、列表
    - 测试 enable/disable 切换
    - 测试 recordFailure 计数器
    - 测试 getProvidersByDbType 过滤
  </action>
  <verify>
    <automated>cd apps/db-ops-api && npx vitest run src/__tests__/collectors/registry.test.ts --reporter verbose 2>&1</automated>
  </verify>
  <done>Registry<T> and BaseMetricProvider created, factory-reset tests pass. CollectorRegistry singleton not yet created (Plan 02 creates it).</done>
</task>

<task type="auto">
  <name>Task 2: SQL schema migration — metric_definitions + metrics_history</name>
  <files>
    apps/db-ops-api/sql/schema.sql
  </files>
  <action>
    在 `apps/db-ops-api/sql/schema.sql` 文件末尾追加 Phase 106 的 schema 变更。

    在文件末尾（标记 phase 106 影响）依次添加:

    1. **metric_definitions 表追加列**：
    ```sql
    -- ============================================
    -- Phase 106: 指标采集可配置化 - Schema 变更
    -- ============================================

    -- metric_definitions 新列
    ALTER TABLE metric_definitions
      ADD COLUMN `collection_sql` TEXT DEFAULT NULL COMMENT '自定义采集 SQL（用户自定义指标用）' AFTER `threshold_template`,
      ADD COLUMN `value_type` ENUM('gauge', 'counter', 'histogram') NOT NULL DEFAULT 'gauge' COMMENT '指标值类型' AFTER `collection_sql`,
      ADD COLUMN `category` VARCHAR(50) DEFAULT NULL COMMENT '指标分类标签' AFTER `value_type`,
      ADD COLUMN `updated_by` INT UNSIGNED DEFAULT NULL COMMENT '最后修改人用户 ID' AFTER `category`,
      ADD INDEX `idx_category` (`category`);

    -- metrics_history 追加 JSON 列
    ALTER TABLE metrics_history
      ADD COLUMN `metrics_data` JSON DEFAULT NULL COMMENT '动态指标数据（自定义指标写入此列，与固定列双轨并行）' AFTER `is_estimated`;
    ```

    2. **权限种子数据追加 `metric:write`**：
    在现有 `permissions` 表的 INSERT IGNORE 中（约第 1223 行附近），找到 `metric:manage` 所在的位置，在其后追加：
    ```sql
    ('metric:write',  '管理自定义指标',  '创建/编辑/删除自定义指标定义', 'metric', 'write'),
    ```
    注意不要重复插入已有行，找到 seed 的 INSERT 语句块添加。

    3. **DBA 角色权限追加 `metric:write`**：
    在 `metric:manage` 条目之后的 role_permissions seed 中找到 DBA 的 `metric:manage`，如果 DBA 的 role_permissions 中 `metric:manage` 后面没有 `metric:write`，追加它。DBA 需要 `metric:view` + `metric:manage` + `metric:write`（因 `metric:manage` 不等于 `metric:write` 的粗粒度）。

    使用 INSERT IGNORE 模式确保幂等：
    ```sql
    INSERT IGNORE INTO role_permissions (role_id, permission_id)
    SELECT r.id, p.id FROM roles r, permissions p
    WHERE r.name = 'dba' AND p.code = 'metric:write';
    ```
  </action>
  <verify>
    <automated>grep -c 'metrics_data JSON' apps/db-ops-api/sql/schema.sql && grep -c "metric:write" apps/db-ops-api/sql/schema.sql</automated>
  </verify>
  <done>schema.sql has ALTER TABLE migration for both tables and metric:write permission seed. SQL is idempotent (ADD COLUMN IF NOT EXISTS via ALTER TABLE will fail on re-run, so wrapper in application code needed at runtime).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create SQL validator + enhance MetricDatabaseService CRUD</name>
  <files>
    apps/db-ops-api/src/sql-validator.ts
    apps/db-ops-api/src/metric-database-service.ts
  </files>
  <behavior>
    - validateSqlIsSelectOnly('SELECT 1') returns { valid: true }
    - validateSqlIsSelectOnly('DROP TABLE users') returns { valid: false, error: '...' }
    - validateSqlIsSelectOnly('INSERT INTO t VALUES(1)') returns { valid: false }
    - validateSqlIsSelectOnly('DELETE FROM t') returns { valid: false }
    - validateSqlIsSelectOnly('ALTER TABLE t DROP COLUMN c') returns { valid: false }
    - validateSqlIsSelectOnly('invalid sql !!!') returns { valid: false, error: '...' }
    - MetricDatabaseService.createMetric with collection_sql, value_type, category succeeds
    - MetricDatabaseService.updateMetric with collection_sql updates correctly
  </behavior>
  <action>
    创建 `apps/db-ops-api/src/sql-validator.ts`:

    - 安装 node-sql-parser: `cd apps/db-ops-api && npm install node-sql-parser`
    - 实现 `validateSqlIsSelectOnly(sql: string): { valid: boolean; error?: string }`:
      - 导入 `{ Parser } from 'node-sql-parser'`
      - 使用 `new Parser().astify(sql)` 解析 SQL
      - 检查所有 statement 的 `type` === 'select' / 'SELECT'
      - 非 SELECT 返回错误信息 `"不允许的 SQL 操作: {type}。仅支持 SELECT 查询"`
      - parse 异常返回 `"SQL 语法错误: {message}"`
    - 导出该函数

    修改 `apps/db-ops-api/src/metric-database-service.ts`:

    1. **MetricDefinitionRow interface 追加新字段**：
    ```typescript
    export interface MetricDefinitionRow {
      // ... keep existing fields
      collection_sql: string | null;
      value_type: 'gauge' | 'counter' | 'histogram';
      category: string | null;
      updated_by: number | null;
    }
    ```

    2. **createMetric(data) 中处理新字段**：
      - data 参数追加可选字段:
        ```typescript
        data: { ...existing...; collection_sql?: string; value_type?: string; category?: string; }
        ```
      - INSERT SQL 增加列: `collection_sql, value_type, category`
      - VALUES 对应位置处理，collection_sql 使用 `data.collection_sql || null`, value_type 使用 `data.value_type || 'gauge'`, category 使用 `data.category || null`
      - **不要动 is_builtin 的 FALSE 逻辑**

    3. **updateMetric(id, data) 中处理新字段**：
      - data 参数追加 `collection_sql?: string; value_type?: string; category?: string;`
      - 在现有的 if-else set 逻辑后追加：
        ```typescript
        if (data.collection_sql !== undefined) { updates.push('collection_sql = ?'); values.push(data.collection_sql); }
        if (data.value_type !== undefined) { updates.push('value_type = ?'); values.push(data.value_type); }
        if (data.category !== undefined) { updates.push('category = ?'); values.push(data.category); }
        ```
      - 注意该方法使用 inline string building 而非 prepared statement，所以需要在 pairs.map 中处理字符串转义。现有的转义逻辑 (`'${value.replace(/'/g, "\\'")}'`) 已经能处理这些 text/varchar 类型。

    4. **更新 token 字段（原 created_by/updated_by 不用改，现有 created_at 和 updated_at 是 auto 的）**
      - 追加 `updated_by` 字段到 updateMetric 的 data 参数:
        ```typescript
        data: { ...existing...; updated_by?: number; }
        ```
      - updateMetric 的 if block 追加:
        ```typescript
        if (data.updated_by !== undefined) { updates.push('updated_by = ?'); values.push(data.updated_by); }
        ```
  </action>
  <verify>
    <automated>cd apps/db-ops-api && npx vitest run src/__tests__/sql-validator.test.ts --reporter verbose 2>&1 && echo "---" && npx tsc --noEmit src/sql-validator.ts src/metric-database-service.ts 2>&1 | head -20</automated>
  </verify>
  <done>sql-validator.ts correctly rejects non-SELECT statements using node-sql-parser AST. MetricDatabaseService create/update handles collection_sql, value_type, category, updated_by (D-07, D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-input-SQL -> backend | Custom SQL from metric_definitions crosses from UI to backend SQL validator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering | sql-validator.ts | mitigate | node-sql-parser AST check ensures type=SELECT only |
| T-106-02 | Information Disclosure | sql-validator.ts | mitigate | Read-only DB account for custom SQL execution (Plan 02) |
| T-106-03 | Elevation of Privilege | metric-database-service.ts | mitigate | metric:write permission on creation (Plan 03 adds route guard) |
</threat_model>

<verification>
1. `cd apps/db-ops-api && npx vitest run src/__tests__/collectors/registry.test.ts --reporter verbose` — Registry tests pass
2. `cd apps/db-ops-api && npx vitest run src/__tests__/sql-validator.test.ts --reporter verbose` — SQL validator tests pass
3. Manually verify schema.sql ALTER TABLE statements are syntactically correct
4. `npx tsc --noEmit src/sql-validator.ts src/metric-database-service.ts` — no TypeScript errors
</verification>

<success_criteria>
- Registry<T> and BaseMetricProvider exist and are typed correctly
- schema.sql has Phase 106 changes (collection_sql, value_type, category, updated_by, metrics_data JSON)
- metric:write permission seeded
- sql-validator.ts rejects non-SELECT statements
- MetricDatabaseService.createMetric/updateMetric handles new fields
- Collector singleton not yet created (delegated to Plan 02 where CollectorRegistry wraps Registry<MetricProvider>)
</success_criteria>

<output>
Create `.planning/phases/106-指标采集可配置化/106-01-SUMMARY.md` when done
</output>
