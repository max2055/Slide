---
phase: 121-product-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/ui/views/ai-settings.ts
  - frontend/src/app/ui/views/llm-config.ts
  - frontend/src/app/ui/views/scoring-settings.ts
  - frontend/src/app/ui/views/appearance-settings.ts
  - frontend/src/app/ui/views/branding-settings.ts
  - frontend/src/app/ui/views/users-management.ts
  - frontend/src/app/ui/views/rbac-page.ts
  - frontend/src/app/ui/views/cron-jobs-settings.ts
autonomous: true
requirements: [UI-CONSISTENCY-02]
user_setup: []
must_haves:
  truths:
    - "No occurrences of forbidden patterns (`.card`, `.modal-overlay`, `class=\"btn primary\"`, `.badge`, `.tag`, `.status-badge`) in any settings view file"
    - "Build passes after any fixes applied"
    - "Each of the 8 settings pages follows Template A or Template B consistently"
  artifacts:
    - path: frontend/src/app/ui/views/ai-settings.ts
      provides: "Settings page — no forbidden CSS patterns, follows Template A"
    - path: frontend/src/app/ui/views/llm-config.ts
      provides: "Settings page — no forbidden CSS patterns"
    - path: frontend/src/app/ui/views/scoring-settings.ts
      provides: "Settings page — no forbidden CSS patterns"
    - path: frontend/src/app/ui/views/appearance-settings.ts
      provides: "Settings page — no forbidden CSS patterns"
    - path: frontend/src/app/ui/views/branding-settings.ts
      provides: "Settings page — no forbidden CSS patterns"
    - path: frontend/src/app/ui/views/users-management.ts
      provides: "Settings page — no forbidden CSS patterns"
    - path: frontend/src/app/ui/views/rbac-page.ts
      provides: "Settings page — no forbidden CSS patterns"
    - path: frontend/src/app/ui/views/cron-jobs-settings.ts
      provides: "Settings page — no forbidden CSS patterns"
  key_links:
    - from: "Template A pages (ai-settings, llm-config, scoring-settings, appearance-settings, branding-settings)"
      to: "app-card"
      via: "import in component — cards use <app-card> not .card"
    - from: "Template B pages (users-management, rbac-page, cron-jobs-settings)"
      to: "app-card / app-dialog / app-badge"
      via: "import in component — cards/dialogs/badges use shared components"
---

<objective>
验证并修复 8 个设置/管理页面的模板一致性。检查禁止的 CSS 模式、共享组件使用、设计 token 合规。不是重新执行，是验证收尾。

Purpose: 确保 Phase 120 的 UI 统一工作没有遗漏，防止回退。
Output: Grep guardrail 结果、修复后的页面文件、通过 build。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/app/ui/views/ai-settings.ts
@frontend/src/app/ui/views/llm-config.ts
@frontend/src/app/ui/views/scoring-settings.ts
@frontend/src/app/ui/views/appearance-settings.ts
@frontend/src/app/ui/views/branding-settings.ts
@frontend/src/app/ui/views/users-management.ts
@frontend/src/app/ui/views/rbac-page.ts
@frontend/src/app/ui/views/cron-jobs-settings.ts

/Users/max/Coding/40-Slide/CLAUDE.md (shared components table and rules)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run grep guardrails across 8 settings views, fix violations</name>
  <read_first>
    @frontend/src/app/ui/views/ai-settings.ts
    @frontend/src/app/ui/views/llm-config.ts
    @frontend/src/app/ui/views/scoring-settings.ts
    @frontend/src/app/ui/views/appearance-settings.ts
    @frontend/src/app/ui/views/branding-settings.ts
    @frontend/src/app/ui/views/users-management.ts
    @frontend/src/app/ui/views/rbac-page.ts
    @frontend/src/app/ui/views/cron-jobs-settings.ts
  </read_first>
  <action>
    Run the following grep guardrails across ALL 8 settings view files. For each violation found, fix the file. Repeat until zero violations.

    Forbidden pattern 1 — `.card` CSS class (must use `<app-card>` component):
    Run: `grep -n 'class="card"' frontend/src/app/ui/views/ai-settings.ts frontend/src/app/ui/views/llm-config.ts frontend/src/app/ui/views/scoring-settings.ts frontend/src/app/ui/views/appearance-settings.ts frontend/src/app/ui/views/branding-settings.ts frontend/src/app/ui/views/users-management.ts frontend/src/app/ui/views/rbac-page.ts frontend/src/app/ui/views/cron-jobs-settings.ts`
    If found: Replace `<div class="card">` or `<div class="card ...">` with `<app-card variant="default">` (wrap content). For card-body pattern, use `<app-card>` with proper slot structure. Import `../components/app-card.js` if not already imported.

    Forbidden pattern 2 — `.modal-overlay` (must use `<app-dialog>`):
    Run: `grep -n 'modal-overlay' frontend/src/app/ui/views/*.ts`
    If found: Replace modal dialog pattern with `<app-dialog>` component. Import `../components/app-dialog.js` if needed.

    Forbidden pattern 3 — `class="btn primary"` (space separator — must be `class="btn-primary"` hyphenated):
    Run: `grep -n 'class="btn primary"' frontend/src/app/ui/views/ai-settings.ts frontend/src/app/ui/views/llm-config.ts frontend/src/app/ui/views/scoring-settings.ts frontend/src/app/ui/views/appearance-settings.ts frontend/src/app/ui/views/branding-settings.ts frontend/src/app/ui/views/users-management.ts frontend/src/app/ui/views/rbac-page.ts frontend/src/app/ui/views/cron-jobs-settings.ts`
    If found: Replace `class="btn primary"` with `class="btn-primary"`.

    Forbidden pattern 4 — `.badge`, `.tag`, `.status-badge` CSS classes (must use `<app-badge>`):
    Run: `grep -n 'class="[^"]*badge[^"]*"\|class="[^"]*tag[^"]*"' frontend/src/app/ui/views/ai-settings.ts frontend/src/app/ui/views/llm-config.ts frontend/src/app/ui/views/scoring-settings.ts frontend/src/app/ui/views/appearance-settings.ts frontend/src/app/ui/views/branding-settings.ts frontend/src/app/ui/views/users-management.ts frontend/src/app/ui/views/rbac-page.ts frontend/src/app/ui/views/cron-jobs-settings.ts | grep -v 'sharedBtnStyles\|shared-btn-styles\|app-badge'`
    If found: Replace badge/tag divs or spans with `<app-badge variant="ok|danger|warn|muted">`. Import `../components/app-badge.js` if needed.

    Forbidden pattern 5 — `.empty-state` or `暂无数据` empty state div (must use `<app-empty-state>`):
    Run: `grep -n 'empty-state\|暂无数据\|暂无' frontend/src/app/ui/views/ai-settings.ts frontend/src/app/ui/views/llm-config.ts frontend/src/app/ui/views/scoring-settings.ts frontend/src/app/ui/views/appearance-settings.ts frontend/src/app/ui/views/branding-settings.ts frontend/src/app/ui/views/users-management.ts frontend/src/app/ui/views/rbac-page.ts frontend/src/app/ui/views/cron-jobs-settings.ts`
    This is a softer check — if a page has a hand-rolled empty state div with "暂无数据", replace with `<app-empty-state title="..." description="...">`. Import `../components/app-empty-state.js`.

    For each fix:
    - Add the necessary import at the top of the file (e.g., `import "../components/app-card.js"` for app-card, etc.)
    - For `<app-dialog>`, also import sharedBtnStyles if the dialog has buttons
    - Do NOT change any other code — only fix the forbidden patterns
    - After each file, double-check the fix didn't break the structure
  </action>
  <acceptance_criteria>
    1. `grep -rn 'class="card"\|class="btn primary"\|modal-overlay' frontend/src/app/ui/views/ai-settings.ts frontend/src/app/ui/views/llm-config.ts frontend/src/app/ui/views/scoring-settings.ts frontend/src/app/ui/views/appearance-settings.ts frontend/src/app/ui/views/branding-settings.ts frontend/src/app/ui/views/users-management.ts frontend/src/app/ui/views/rbac-page.ts frontend/src/app/ui/views/cron-jobs-settings.ts` returns zero lines
    2. `grep -rn 'class="[^"]*badge[^"]*"\|class="[^"]*tag[^"]*"' frontend/src/app/ui/views/[aislburc]*.ts | grep -v 'sharedBtnStyles\|shared-btn-styles\|app-badge' | grep -v '//.*' | wc -l` returns 0
    3. All new imports are present in files that had fixes — verified by `grep -c 'import.*app-card\|import.*app-dialog\|import.*app-badge\|import.*app-empty-state'` on each fixed file
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/max/Coding/40-Slide && grep -rn 'class="card"\|class="btn primary"\|modal-overlay' frontend/src/app/ui/views/ai-settings.ts frontend/src/app/ui/views/llm-config.ts frontend/src/app/ui/views/scoring-settings.ts frontend/src/app/ui/views/appearance-settings.ts frontend/src/app/ui/views/branding-settings.ts frontend/src/app/ui/views/users-management.ts frontend/src/app/ui/views/rbac-page.ts frontend/src/app/ui/views/cron-jobs-settings.ts; echo "EXIT:$?"</automated>
  </verify>
  <done>All grep guardrails pass with zero violations across 8 settings view files. Any violations found have been fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify page checklist + build</name>
  <read_first>
    @frontend/src/app/ui/views/settings-shell.ts
    @frontend/src/app/ui/views/ai-settings.ts
    @frontend/src/app/ui/views/llm-config.ts
    @frontend/src/app/ui/views/scoring-settings.ts
    @frontend/src/app/ui/views/appearance-settings.ts
    @frontend/src/app/ui/views/branding-settings.ts
    @frontend/src/app/ui/views/users-management.ts
    @frontend/src/app/ui/views/rbac-page.ts
    @frontend/src/app/ui/views/cron-jobs-settings.ts
  </read_first>
  <action>
    1. Verify each page confirms to its template by checking the following criteria in each file:

    **Template A** (ai-settings, llm-config, scoring-settings, appearance-settings, branding-settings):
    - `max-width: 800px` set on `:host` or a wrapper element — verify presence of `max-width` with value `800` or `var(--settings-max-width)` or similar
    - Page header with `<h1>` and optional `<p>` subtitle — check `.page-header` pattern
    - Content in `<app-card>` containers — verify at least one `app-card` import or usage
    - Uses `sharedBtnStyles` if buttons are present — grep for `sharedBtnStyles` or `shared-btn-styles`
    - Uses design tokens for spacing/radius/font-size (`var(--space-*)`, `var(--radius-*)`, `var(--text-*)`) — grep for any hardcoded `px` value (soft check, only fix obvious holdouts like `font-size: 13px` or `padding: 16px` if they can be cleanly replaced)

    **Template B** (users-management, rbac-page, cron-jobs-settings):
    - Data tables use `<app-data-table>` — verify import or usage
    - Dialogs use `<app-dialog>` — verify import or usage
    - Forms use `<app-form-field>` — verify import or usage
    - Action buttons use `btn-sm` or `btn-primary` classes
    - Empty states use `<app-empty-state>` — verify import or usage

    For any page that fails a checklist item, fix it. For example:
    - If `max-width` is missing on a Template A page, add `:host { display: block; max-width: 800px; }` to the component styles
    - If `sharedBtnStyles` is missing but the page has buttons, add the import
    - If a hardcoded `px` value for a token that already has a `var(--space-*)` or `var(--radius-*)` equivalent, replace it

    2. Run the build:
    `cd /Users/max/Coding/40-Slide/frontend && npm run build 2>&1`
    If build fails, read the error output, fix the issue, and rebuild until pass.

    3. Record the checklist results as a comment in the plan output (SUMMARY.md):
    - Page-by-page: Template assignment, pass/fail per checklist item
    - Total violations found and fixed in Task 1
    - Build result (pass/fail)
  </action>
  <acceptance_criteria>
    1. `cd /Users/max/Coding/40-Slide/frontend && npm run build 2>&1` exits with code 0
    2. Each Template A page has `max-width: 800px` on `:host` — `grep -l 'max-width.*800' frontend/src/app/ui/views/ai-settings.ts frontend/src/app/ui/views/llm-config.ts frontend/src/app/ui/views/scoring-settings.ts frontend/src/app/ui/views/appearance-settings.ts frontend/src/app/ui/views/branding-settings.ts | wc -l` equals 5
    3. File check: Each of the 8 pages' imports include the shared component types they should use (app-card for Template A, app-dialog/app-data-table/app-empty-state for Template B)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/max/Coding/40-Slide/frontend && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>Build passes. All 8 pages verified against their template checklist. Page-by-page results recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan performs static analysis and build verification — no runtime boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-SC | Tampering | npm installs | accept | No new packages installed — build uses existing dependencies |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds
- Grep guardrails zero violations
- Page checklist all 8 pages confirmed
</verification>

<success_criteria>
- Build passes
- Zero `.card`, `.modal-overlay`, `btn primary`, `.badge` (non-app-badge) violations
- All 8 setting pages follow their designated template
</success_criteria>

<output>
Create `.planning/phases/121-product-polish/121-03-SUMMARY.md` when done
</output>
