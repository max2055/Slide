---
phase: 121-product-polish
plan: 02
type: execute
wave: 4
depends_on: [04]
files_modified:
  - .planning/phases/121-product-polish/121-GOLDEN-FLOW-VERIFICATION.md
autonomous: false
requirements: [GOLDEN-FLOW-01, POLISH-01]
user_setup:
  - service: "Database instance management"
    why: "Need at least 1 reachable database instance to verify golden flow step 1"
    env_vars: []
    dashboard_config:
      - task: "Add a reachable DB instance via '数据库实例' page, test connection"
  - service: "Cron Jobs"
    why: "Need at least 1 enabled cron job to verify metric collection (step 2)"
    dashboard_config:
      - task: "Go to '定时任务' settings page, enable at least one cron job (e.g., '容量数据采集')"
  - service: "LLM Provider"
    why: "Need LLM provider configured and enabled for AI RCA (step 6)"
    dashboard_config:
      - task: "Go to 'LLM 配置' settings page, add and enable a Provider"
  - service: "Alert Rule"
    why: "Need an alert rule with low threshold to trigger an alert for steps 4-5"
    dashboard_config:
      - task: "Create an alert rule with a low threshold (e.g., health_score < 10) and set up monitoring"
must_haves:
  truths:
    - "A golden flow verification checklist exists with 10 steps covering: instance -> metrics -> health_score -> alert -> event -> RCA -> notification -> resolution -> history -> health center"
    - "Each step has Operation, Expected Result, Actual Result, and Status columns"
    - "Document marks the notification step as deferred/incomplete (notification closure not implemented)"
    - "Document can be used as a manual test plan by a human operator"
  artifacts:
    - path: .planning/phases/121-product-polish/121-GOLDEN-FLOW-VERIFICATION.md
      provides: "Manual verification checklist for Slide's core DBA workflow"
  key_links: []
---

<objective>
将 Slide 最核心的 DBA 链路形成可重复的手动验证 checklist。降级为手动文档而非 Playwright E2E，原因：当前环境 4/5 实例不可达，13 个 Cron Job 全禁用。等环境恢复后再写 Playwright 脚本。

Purpose: 让用户可以按步骤验证产品核心链路是否正常工作，为后续自动化 E2E 测试提供测试用例。
Output: `.planning/phases/121-product-polish/121-GOLDEN-FLOW-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/app/ui/views/health-center.ts
@frontend/src/app/ui/views/settings-shell.ts
.apps/db-ops-api/src/consistency-checker.ts

<interfaces>
For guidance on the exact API endpoints, DB tables, and UI views involved in each step:

Step 1 — Instance: Add instance via UI at path /api/database/instances
Step 2 — Metrics: Collected by cron into metrics_history table (see schema.sql lines 113-125)
Step 3 — Health: health_check_history table (schema.sql line 163)
Step 4 — Alert: alert_rules config (schema.sql line 258), alerts table, alert-evaluator process
Step 5 — Event: alert_events + alert_event_members (schema.sql lines 330-365)
Step 6 — AI RCA: ai_analysis table, alert-rca-service.ts
Step 7 — Notification: notification_records table (schema.sql line 712) — known gap
Step 8 — Resolution: alert_events status changes
Step 9 — History: Health center page views from 121-01b/121-04
Step 10 — Consistency: GET /api/health/consistency endpoint from 121-01a
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create golden flow verification checklist document</name>
  <read_first>
    @frontend/src/app/ui/views/health-center.ts
    @frontend/src/app/ui/views/settings-shell.ts
    .apps/db-ops-api/sql/schema.sql (for table references in the checklist)
  </read_first>
  <action>
    Create the file `.planning/phases/121-product-polish/121-GOLDEN-FLOW-VERIFICATION.md`.

    The document should have the following structure:

    # Slide 金牌用户故事 E2E 验证清单

    **Phase:** 121
    **创建日期:** {current date}
    **目标:** 端到端验证 DBA 核心链路：实例添加 → 指标采集 → 健康评分 → 告警触发 → 事件聚合 → AI 根因分析 → 通知 → 用户处置 → 历史追溯 → 闭环健康检查

    **前置条件：**
    - [ ] 至少 1 个数据库实例已添加且状态为 active（测试连接通过）
    - [ ] 至少 1 个 Cron Job 已启用（如「容量数据采集」）
    - [ ] 至少 1 个 LLM Provider 已配置且 enabled（用于 AI RCA）
    - [ ] 至少 1 条告警规则阈值设置在可触发范围内
    - [ ] 系统 API 可通过浏览器正常访问

    **已知限制：**
    - 通知闭环未实现（见 Phase 121 通知 deferred 说明）
    - 当前环境 4/5 实例不可达，部分步骤可能因环境条件跳过

    ## 验证步骤

    | # | 步骤名称 | 操作说明 | 预期结果 | 验证方法 | 通过条件 | 实际结果 | 状态 | 备注 |
    |---|----------|---------|---------|---------|---------|---------|------|------|
    |   |          |         |         |         |         |         |      |      |

    Each row should be filled out with the following content:

    **Step 1: 实例可用性**
    - 操作: 打开「数据库实例」页面，查看已添加实例列表
    - 预期: 至少 1 个实例状态为 active，health_status 不是 unknown
    - 验证方法: 浏览器打开 http://localhost:5173 → 导航到「数据库实例」
    - 通过条件: 看到至少一个实例 status=active，health_status 有值
    - 实际结果: (留空由执行者填写)
    - 状态: (留空)
    - 备注: 如实例不可达，请先添加可达测试实例

    **Step 2: 指标采集**
    - 操作: 等待 Cron 触发采集（最长 5 分钟），或手动触发采集
    - 预期: metrics_history 表有新记录，recorded_at 在最近 10 分钟内
    - 验证方法: 查询 SQL: SELECT COUNT(*) as cnt, MAX(recorded_at) as latest FROM metrics_history WHERE instance_id = ?
    - 通过条件: cnt > 0 且 latest 在 10 分钟内
    - 实际结果: (留空)
    - 状态: (留空)
    - 备注: 如 Cron 未运行，在「定时任务」页面启用「容量数据采集」

    **Step 3: 健康评分**
    - 操作: 打开实例详情页（点击实例名称），查看「健康评分」概览
    - 预期: health_score 显示基于维度的真实计算值（非硬编码 100），health_check_history 中有多条记录
    - 验证方法: 浏览器打开 Instances → 选择一个实例 → 查看概览页
    - 通过条件: health_score 正常显示且有历史记录
    - 实际结果: (留空)
    - 状态: (留空)
    - 备注: —

    **Step 4: 告警触发**
    - 操作: 设置低阈值告警规则（如 health_score < 90），等待评估周期触发，或手动创建测试告警
    - 预期: alerts 表出现新 status=open 的记录
    - 验证方法: 打开「告警列表」页面，查看是否有新告警
    - 通过条件: 至少有 1 条 open 状态告警
    - 实际结果: (留空)
    - 状态: (留空)
    - 备注: 可先在告警规则页面创建低阈值规则

    **Step 5: 事件聚合**
    - 操作: 同一实例关联告警自动（或等待）聚合到 alert_events
    - 预期: alert_events 表中有新事件，且 alert_event_members 中有对应成员记录
    - 验证方法: 打开「告警事件」视图，查看事件列表
    - 通过条件: 事件正确聚合了告警成员，members 计数对得上
    - 实际结果: (留空)
    - 状态: (留空)
    - 备注: 事件聚合可能需要 10 分钟窗口

    **Step 6: AI 根因分析**
    - 操作: 打开已触发告警的详情，触发 AI RCA 分析，或等待自动分析
    - 预期: ai_analysis 表生成分析结果，有根因和建议
    - 验证方法: 打开告警详情页 (Alert Detail Modal) → 检查 AI 分析选项卡
    - 通过条件: 看到 AI 生成的根因分析和修复建议
    - 实际结果: (留空)
    - 状态: (留空)
    - 备注: 需要 LLM Provider 正常可用

    **Step 7: 通知记录**
    - 操作: 检查 notification_records 表是否有与此告警关联的记录
    - 预期: 当前为空壳（通知闭环 deferred），notification_records 可能为空或无发送配置
    - 验证方法: 查询 SQL: SELECT * FROM notification_records WHERE alert_id = ?
    - 通过条件: 当前标记为 known gap — 无需通知记录
    - 实际结果: (留空)
    - 状态: (留空 — 标记为 DEFERRED)
    - 备注: 通知发送链路（钉钉/企微/飞鹰/webhook）将在下个 phase 实现。此步骤仅为记录 gap

    **Step 8: 用户处置**
    - 操作: 手动关闭告警、resolve 关联事件
    - 预期: alert 状态变更为 resolved 或 closed；alert_event 状态同步变更
    - 验证方法: 在告警列表点击关闭 → 查看状态变更；在事件列表点击 resolve
    - 通过条件: 告警和事件状态同步变更
    - 实际结果: (留空)
    - 状态: (留空)
    - 备注: 注意 event_member 状态一致性（121-01a 一致性检查覆盖此场景）

    **Step 9: 历史可追溯**
    - 操作: 查看已关闭告警的历史记录和事件时间线
    - 预期: 可以在告警历史页看到已关闭告警，事件时间线完整
    - 验证方法: 在告警列表切到历史视图，点击事件查看时间线
    - 通过条件: 历史的告警记录存在且可展开查看详情
    - 实际结果: (留空)
    - 状态: (留空)
    - 备注: —

    **Step 10: 闭环健康检查**
    - 操作: 打开「设置 → 闭环健康」页面
    - 预期: health center 展示各检查项结果，对应步骤的检查项应显示 pass 或相应的状态
    - 验证方法: 浏览器打开 Settings → 闭环健康 tab
    - 通过条件: 页面正常加载，实例/指标/告警/事件/Cron 等检查项显示正确的状态
    - 实际结果: (留空)
    - 状态: (留空)
    - 备注: 某些检查项因环境限制可能显示 warn 或 fail，属于正常情况

    ## 汇总

    | 类别 | 通过 | 失败 | 跳过 | 备注 |
    |------|------|------|------|------|
    | 实例/采集 | — | — | — | |
    | 告警/事件/处置 | — | — | — | |
    | AI 分析 | — | — | — | |
    | 通知 | — | — | DEFERRED | 下个 phase 实现 |
    | 闭环检查 | — | — | — | |

    **测试环境信息：**
    - 后端版本: (填写 commit SHA)
    - 前端版本: (填写 commit SHA)
    - 测试日期: (填写)
    - 测试人: (填写)

    **输出格式要求：**
    - 所有「(留空)」字段保持空白，由执行者填写
    - 状态列使用: `[x] pass | [ ] fail-[原因] | [ ] skip-[原因] | [ ] deferred`
    - 备注列记录执行过程中的观测值（如：实际告警数、延迟时间等）
    - 文档头部添加前置条件 checklist，执行前逐项勾选
  </action>
  <acceptance_criteria>
    1. File exists at `.planning/phases/121-product-polish/121-GOLDEN-FLOW-VERIFICATION.md`
    2. Document has 10 steps in a table with columns: #, 步骤名称, 操作说明, 预期结果, 验证方法, 通过条件, 实际结果, 状态, 备注
    3. Step 7 (notification) is explicitly marked as DEFERRED with explanation
    4. Document lists prerequisites and known limitations
    5. Summary table at the end with pass/fail/skip/deferred counts
    6. All 10 steps are filled with operation/expected/verification content (actual result and status left blank for manual execution)
  </acceptance_criteria>
  <verify>
    <automated>ls /Users/max/Coding/40-Slide/.planning/phases/121-product-polish/121-GOLDEN-FLOW-VERIFICATION.md && grep -c 'DEFERRED\|deferred' /Users/max/Coding/40-Slide/.planning/phases/121-product-polish/121-GOLDEN-FLOW-VERIFICATION.md && grep -c '|.*Step 10\|# 步骤名称' /Users/max/Coding/40-Slide/.planning/phases/121-product-polish/121-GOLDEN-FLOW-VERIFICATION.md</automated>
  </verify>
  <done>121-GOLDEN-FLOW-VERIFICATION.md created with 10-step checklist, prerequisites, known limitations, and summary table</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan produces a documentation artifact — no runtime changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-SC | Tampering | npm installs | accept | No packages installed |
</threat_model>

<verification>
- 121-GOLDEN-FLOW-VERIFICATION.md exists with all 10 steps
- Step 7 notification is marked deferred per CONTEXT.md
- Prerequisites checklist and summary table are present
</verification>

<success_criteria>
- Golden flow verification checklist created with 10 steps
- Prerequisites clearly documented
- Known gaps (notification) explicitly marked
- Document can be executed by a human operator
</success_criteria>

<output>
Create `.planning/phases/121-product-polish/121-02-SUMMARY.md` when done
</output>
