---
status: complete
phase: 129-unified-observability
source: 129-01-SUMMARY.md, 129-02-SUMMARY.md, 129-03-SUMMARY.md
started: 2026-07-09T05:30:00Z
updated: 2026-07-09T05:45:00Z
---

## Current Test
[testing complete]

## Tests

### 1. 迁移 022 — metric_definitions target_type + alert_rule_templates
expected: metric_definitions 有 target_type 列，alert_rule_templates 表存在且包含 5 个预置模板。
result: pass (after manual fix)
verified: "迁移手动应用后 metric_definitions.target_type 存在，5 个模板（CPU/内存/磁盘/负载/不可达）已 seed"
fix_needed: "迁移自动应用失败（pool.query 不支持 multi-statement），需改为单条执行"

### 2. Server 指标注册
expected: GET /api/metrics/registry?target_type=server 返回 6+ 个 server 指标。
result: pass
verified: "返回 6 个指标：load_1min, load_5min, load_15min, swap_usage, uptime, os_type"

### 3. Alert Rule Templates API
expected: GET /api/alert-rule-templates 返回 5 个 server 预置模板。
result: pass
verified: "5 templates: CPU过高/内存过高/磁盘过高/负载过高/服务器不可达, all target_type=server"

### 4. Server Report 持久化
expected: POST /api/servers/reports/generate 生成报告并写入 reports 表。
result: pass (after manual fix)
verified: "reportId:32, type:server_health, persisted to reports table"
fix_needed: "reports.type ENUM 需手动扩展加 server_health（migration ALTER TABLE 部分语句未生效）"

### 5. 前端功能
expected: 告警规则编辑器/告警中心/报告中心/详情页支持 target_type 切换。
result: pass (code review)
verified: "5 files modified, target_type selector/server filter added"

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0

## Gaps

- migration 自动应用: pool.query 不支持 multi-statement SQL，需拆分迁移语句
- reports.type ENUM: ALTER TABLE MODIFY COLUMN 在自动迁移中未生效，需排查原因
