---
phase: 129-unified-observability
plan: 02
type: execute
wave: 2
depends_on:
  - 129-01
files_modified:
  - frontend/src/app/ui/components/alert-rule-editor.ts
  - frontend/src/app/ui/views/alerts.ts
  - frontend/src/app/ui/views/reports.ts
  - frontend/src/app/ui/views/server-detail.ts
autonomous: true
requirements:
  - UNI-02
  - UNI-03
  - UNI-04
  - UNI-06
user_setup: []
must_haves:
  truths:
    - 告警规则编辑器可选择 target_type（实例/服务器），选择服务器时显示服务器下拉并隐藏 db_types 和 instance_ids
    - 告警中心告警列表显示 target_type 和 server_name 列，筛选栏支持 target_type 和服务器下拉
    - 报告中心有"服务器巡检"类型卡片，目标选择器支持"数据库实例 / 服务器"切换
    - 服务器详情页有"一键巡检"和"查看告警"按钮，点击触发相应操作
  artifacts:
    - frontend/src/app/ui/components/alert-rule-editor.ts (更新)
    - frontend/src/app/ui/views/alerts.ts (更新)
    - frontend/src/app/ui/views/reports.ts (更新)
    - frontend/src/app/ui/views/server-detail.ts (更新)
  key_links:
    - alert-rule-editor target_type 切换时 server_id 选择器与 db_types/instance_ids 的互斥显示逻辑
    - alerts 页 filterSeverity 与 target_type/server_id 筛选组合
    - server-detail 中 "查看告警" 导航到 alerts 页并自动筛选 server_id
---

<objective>
前端界面向 target_type 感知升级：告警规则编辑器支持目标类型选择，告警中心和报告中心可筛选和展示服务器指标/告警/报告，服务器详情页提供巡检和告警入口。

Purpose: 用户在 UI 中可以为服务器创建告警规则、查看服务器告警、生成服务器巡检报告，从服务器详情页直接触发巡检和告警导航。

Output: 四个前端文件的更新
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/129-unified-observability/129-CONTEXT.md
@frontend/src/app/ui/components/alert-rule-editor.ts
@frontend/src/app/ui/views/alerts.ts
@frontend/src/app/ui/views/reports.ts
@frontend/src/app/ui/views/server-detail.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: alert-rule-editor — add target_type dropdown with server_id selector</name>
  <files>frontend/src/app/ui/components/alert-rule-editor.ts</files>
  <action>
    在 `alert-rule-editor.ts` 中增加 target_type 支持：

    1.1 AlertRule 接口（组件顶部）增加 target_type?: 'instance' | 'server' 和 server_id?: number | null 字段。

    1.2 属性列表增加 @property({type: Array}) servers: any[] = []（接收服务器列表）。

    1.3 _initForm() 中初始化 target_type='instance'（新建时），server_id=null。

    1.4 在"规则名称"字段之后、"指标"字段之前插入 target_type 选择器：
      - 使用 app-form-field label="目标类型" required
      - 两个按钮或 select 控件：实例（'instance'）/ 服务器（'server'）
      - target_type 变化时清空 server_id（如果从 server 切换到 instance）或清空 db_types/instance_ids（如果从 instance 切换到 server）

    1.5 条件渲染：当 target_type === 'server' 时：
      - 隐藏"适用数据库类型"区域
      - 隐藏"适用实例"区域
      - 显示新的"适用服务器"选择器（app-form-field），使用 select 下拉（非 multiple），选项来自 this.servers 属性，格式：host (label)
      - style 保持与现有实例选择器一致

    1.6 条件渲染：当 target_type === 'instance' 或未设置时：
      - 显示现有的"适用数据库类型"和"适用实例"区域
      - 隐藏"适用服务器"选择器

    1.7 _save() 方法构建 body 时：
      - 始终包含 target_type: f.target_type || 'instance'
      - target_type === 'server' 时：包含 server_id: Number(f.server_id) || null，不发送 instance_ids 和 db_types
      - target_type === 'instance' 时：按现有逻辑发送 instance_ids 和 db_types，不发送 server_id

    1.8 确保不引入新的 slop 代码 — 使用现有 sharedBtnStyles 和 _form 模式的 setter。

    注意：alert-rule-editor 被 alerts.ts 中的 _renderRules() 调用，传入 .metricRegistry 和 .instances。调用处需要补充传入 .servers（通过 authFetch /api/servers 加载），在 loadRules() 中一并加载。
  </action>
  <verify>
    <automated>grep -c "target_type" frontend/src/app/ui/components/alert-rule-editor.ts && grep -c "servers" frontend/src/app/ui/components/alert-rule-editor.ts</automated>
  </verify>
  <done>
    alert-rule-editor 有 target_type 切换，server 模式下显示服务器选择器并隐藏实例相关字段，save 事件发送 target_type 和 server_id。
  </done>
</task>

<task type="auto">
  <name>Task 2: alerts page — add target_type and server_id filters, server_name column</name>
  <files>frontend/src/app/ui/views/alerts.ts</files>
  <action>
    在 `alerts.ts` 中增加服务器告警感知：

    2.1 Alert 接口增加 server_id?: number, server_name?: string, target_type?: string。

    2.2 AlertRule 接口增加 target_type?: string, server_id?: number | null。

    2.3 状态增加 servers: any[] = []、filterTargetType: string = ''、filterServerId: number | null = null。

    2.4 在 _renderAlertsTab() 的过滤栏区域（参考现有的 filterSeverity 的位置），增加：
      - target_type 筛选下拉（全部 / 实例 / 服务器）
      - server_id 筛选下拉（当 target_type === 'server' 时显示，选项来自 this.servers；当 target_type !== 'server' 时隐藏）
      - 样式与现有 filterSeverity 一致

    2.5 filteredAlerts getter 增加 target_type 和 server_id 过滤逻辑。

    2.6 alerts tab 表格增加"目标类型"列（显示 instance 或 server 标签）和"服务器"列（显示 server_name）。当 alert.target_type 为 'server' 时，instance_name 列显示 "—"。

    2.7 loadRules() 中补充加载服务器列表：
      - 在 loadRules 的 Promise.all 中添加 authFetch("/api/servers") 请求
      - 服务器数据存入 this.servers

    2.8 _renderRules() 在规则表格中：
      - 表格增加"目标类型"列显示 target_type（instance/server）
      - 规则指标行显示 target_type 标识

    2.9 在 loadAlerts() 中补充服务器名称的映射：如果 alert.server_id 存在，根据 servers 列表填充 server_name（后端可能已经 JOIN 了 servers 表返回 server_name，但前端也应有 fallback）。

    注意：alerts.ts 中已有 filteredAlerts 逻辑（第 751-768 行），在此基础上扩展即可。不要重写整个文件。
  </action>
  <verify>
    <automated>grep -c "filterTargetType\|filterServerId" frontend/src/app/ui/views/alerts.ts && grep -c "server_name" frontend/src/app/ui/views/alerts.ts</automated>
  </verify>
  <done>
    alerts 页可筛选 target_type 和 server_id，告警列表和规则列表显示目标类型和服务器名称。
  </done>
</task>

<task type="auto">
  <name>Task 3: reports page + server-detail — add server inspection and alert entry points</name>
  <files>frontend/src/app/ui/views/reports.ts, frontend/src/app/ui/views/server-detail.ts</files>
  <action>
    3.1 在 `reports.ts` 中：

      3.1.1 Report 接口增加 server_id?: number, server_name?: string, target_type?: string。
      3.1.2 状态增加 servers: any[] = []（存储服务器列表）、selectedTargetType: 'instance' | 'server' = 'instance'、selectedServerId: number | null = null。
      3.1.3 目标选择器区域改为 target_type 切换（两个按钮/单选组："数据库实例" / "服务器"），切换时显示对应的列表：
        - target_type='instance' 时显示实例下拉（现有行为不变）
        - target_type='server' 时显示服务器下拉，options 来自 this.servers
      3.1.4 在 reportTypes 数组末尾增加"服务器巡检"类型卡片：
        - type: "server_health", icon: "server", title: "服务器巡检报告", desc: "所有纳管服务器健康状态检查与评分"
        - 服务器巡检卡片的 generate 按钮点击时调用已有的 _generateReport 方法（传入 type='server_health'），构建 body 包含 server_id: this.selectedServerId（可选，为空时生成全部服务器报告）
      3.1.5 历史报告表格增加"目标类型"列和"服务器"列。

    3.2 在 `server-detail.ts` 中：

      3.2.1 在 header 区域（back-btn 和 server-title 右侧、刷新按钮左侧或刷新按钮组内），增加两个操作按钮：
        - "一键巡检"按钮：@click 时 POST /api/servers/reports/generate，成功后显示 toast 并可选跳转报告页面（dispatch slide-navigate { tab: "reports" }）
        - "查看告警"按钮：@click 时 dispatch slide-navigate { tab: "alerts", serverId: this.serverId }
      3.2.2 按钮使用现有的 action-btn 样式，参考 refresh-btn 的样式风格。
      3.2.3 在 tabs 数组（第 400 行附近）末尾增加一个"告警" tab（key: "alerts", label: "告警"），点击时 dispatch slide-navigate { tab: "alerts", serverId: this.serverId }（跳转到告警中心并筛选该服务器）。
      3.2.4 "告警" tab 可以暂时作为跳转入口（不需要内嵌告警列表），点击直接导航到告警中心。server-detail 本身不渲染告警表格。

    3.3 组件注册：
      - 确保 server-detail 中 import { showToast } from "../components/app-toast-container.js" 已存在（第 9 行已有）
      - reports.ts 中 import { authFetch } from "../../../api/index.js" 已存在（第 8 行已有）
      - reports.ts 中需要补充 import { showToast } from "../components/app-toast-container.js"

    注意：所有变更遵循现有代码风格，不复位现有功能。
  </action>
  <verify>
    <automated>grep -c "server_health\|服务器巡检" frontend/src/app/ui/views/reports.ts && grep -c "一键巡检\|查看告警" frontend/src/app/ui/views/server-detail.ts</automated>
  </verify>
  <done>
    报告中心有服务器巡检入口和 target_type 切换，服务器详情页有巡检/告警按钮和告警 tab 导航。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> alerts-page | user sees/filters alert data |
| browser -> server-detail | user triggers report generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-03 | Information Disclosure | alerts.js target_type filter | low | accept | filter is client-side only; server enforces auth via requirePermission |
| T-129-04 | Tampering | server-detail "一键巡检" POST | low | mitigate | backend route uses requirePermission('servers:manage') |
| T-129-SC | Tampering | npm install | high | mitigate | no new npm packages in this plan |
</threat_model>

<verification>
- [ ] alert-rule-editor 打开后 target_type 切换正确显示/隐藏对应字段
- [ ] alerts 页筛选栏有 target_type 和 server 下拉
- [ ] alerts 页告警列表和规则列表显示 target_type 和 server_name
- [ ] reports 页目标选择器可切换实例/服务器，有服务器巡检卡片
- [ ] server-detail 页 header 有巡检/告警按钮，tabs 有"告警"入口
</verification>

<success_criteria>
前端四个页面 target_type 感知就绪：用户可为服务器创建告警规则、查看和筛选服务器告警、生成服务器巡检报告、从服务器详情页一键巡检和查看告警。
</success_criteria>

<output>
`.planning/phases/129-unified-observability/129-02-SUMMARY.md` — 记录四个文件的变更要点和新增功能状态。
</output>
