---
phase: 109-nanobot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agent-core/src/types.ts
  - packages/agent-core/src/session.ts
  - packages/agent-core/src/runner.ts
  - packages/agent-core/src/index.ts
autonomous: true
requirements: [MIG-02]

must_haves:
  truths:
    - "agent-core has a Session class with addMessage(), get_history() with token-budget slicing"
    - "agent-core has a SessionManager with get_or_create(), save(), JSONL persistence"
    - "AgentRunner.requestModel() applies NANOBOT_LLM_TIMEOUT_S for non-streaming calls, skips wall-clock timeout for streaming"
    - "Streaming requests have NANOBOT_STREAM_IDLE_TIMEOUT_S applied at provider level"
    - "Timeout errors return error_kind='timeout', not thrown exceptions"
    - "New types (SessionEntry, SessionMetadata) exported from @slide/agent-core"
    - "SessionManager uses safe_key() for filename mapping, path under ~/.slide/sessions/"
    - "Test files exist for Session and timeout — npx vitest run passes"
  artifacts:
    - path: packages/agent-core/src/session.ts
      provides: Session + SessionManager classes
      min_lines: 250
    - path: packages/agent-core/src/types.ts
      provides: SessionEntry, SessionMetadata types
      contains: "SessionEntry"
    - path: packages/agent-core/src/runner.ts
      provides: Timeout layering in requestModel()
      contains: "NANOBOT_LLM_TIMEOUT_S"
    - path: packages/agent-core/src/__tests__/session.test.ts
      provides: Session + SessionManager tests
      min_lines: 20
  key_links:
    - from: packages/agent-core/src/runner.ts
      to: nanobot-reference/nanobot/agent/runner.py (lines 610-739)
      via: Timeout layering pattern — LLM timeout + stream idle timeout distinction
    - from: packages/agent-core/src/session.ts
      to: nanobot-reference/nanobot/session/manager.py
      via: Session dataclass + SessionManager port
    - from: packages/agent-core/src/session.ts
      to: packages/agent-core/src/types.ts
      via: SessionEntry type definition

---

<objective>
Port nanobot Session management and timeout layering to agent-core TypeScript.

**Purpose:** Foundation for all subsequent subsystems. Session management replaces DirectAdapter's in-memory Map, providing JSONL-persisted sessions with metadata, token-budget history slicing, and checkpoint storage. Timeout layering prevents stuck LLM calls from freezing the pipeline.

**Output:** `packages/agent-core/src/session.ts` (NEW), `packages/agent-core/src/types.ts` (MODIFY), `packages/agent-core/src/runner.ts` (MODIFY), `packages/agent-core/src/index.ts` (MODIFY), `packages/agent-core/src/__tests__/session.test.ts` (NEW)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-nanobot/109-CONTEXT.md
@.planning/phases/109-nanobot/109-RESEARCH.md
@.planning/phases/108-agent/108-01-SUMMARY.md
@.planning/phases/108-agent/108-02-SUMMARY.md
@.planning/phases/108-agent/108-03-SUMMARY.md

<interfaces>
Existing types from packages/agent-core/src/types.ts:
- Message { role, content, tool_calls, tool_call_id, name, reasoning_content, thinking_blocks }
- LLMResponse { content, finishReason, toolCalls, usage, errorKind, ... }
- LLMCallOptions { model, temperature, maxTokens, reasoningEffort }
- AgentRunSpec { initialMessages, tools, model, maxIterations, hook, llmTimeoutS, ... }

Existing runner.ts checkpoint mechanism:
- emitCheckpoint(spec, payload) one-way callback (line 792-799)
- No restore logic

Existing runner.ts completion states:
- The final assistant message is appended via appendFinalMessage()
- The over-limit recovery uses buildLengthRecoveryMessage()
- Empty content retries and length recovery in main loop

Existing DirectAdapter session pattern (will be replaced in Plan 109-03):
- `private sessions: Map<string, Message[]>` — in-memory only, no persistence
- `private systemPrompt: string` — hardcoded DEFAULT_SYSTEM_PROMPT
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port nanobot Session + SessionManager to TypeScript</name>
  <files>
    - packages/agent-core/src/types.ts (APPEND SessionEntry, SessionMetadata types)
    - packages/agent-core/src/session.ts (CREATE)
    - packages/agent-core/src/index.ts (APPEND exports)
    - packages/agent-core/src/__tests__/session.test.ts (CREATE)
  </files>
  <read_first>
    - nanobot-reference/nanobot/session/manager.py (Session dataclass lines 80-332, SessionManager lines 333-704)
    - packages/agent-core/src/types.ts (existing types to extend)
    - packages/agent-core/src/index.ts (export pattern)
  </read_first>
  <action>
    Create Session + SessionManager port from nanobot session/manager.py.

    1. **types.ts**: Add `SessionEntry` interface (extends Message-like fields with timestamp, metadata, reasoning_content, thinking_blocks, tool_calls, tool_call_id, name). Add `SessionMetadata` interface (`{ [key: string]: unknown; _last_summary?: { text: string; last_active: string }; runtime_checkpoint?: Record<string, unknown> }`). Export both.

    2. **session.ts**: Create `Session` class with:
       - Fields: `key: string`, `messages: SessionEntry[]`, `created_at: Date`, `updated_at: Date`, `metadata: SessionMetadata`, `last_consolidated: number`
       - `addMessage(role, content, extra?): void` — pushes message with timestamp, calls sessionManager.save() if manager set
       - `getHistory(maxMessages, maxTokens?): SessionEntry[]` — slices from last_consolidated, aligns to first user turn, drops orphan tool results at front, applies token-budget truncation from tail using estimateMessageTokens per nanobot pattern (use simple char-based estimate: ~4 chars per token). Per D-07 port.
       - `clear(): void`
       - `retainRecentLegalSuffix(maxMessages: number): void` — per nanobot Session method
       - `enforceFileCap(maxMessages: number): boolean` — per nanobot Session method

    3. **session.ts**: Create `SessionManager` class with:
       - `workspace: string` path constructor param
       - `cache: Map<string, Session>` in-memory LRU (cap at 100 sessions)
       - `sessionsDir: string` = `{workspace}/.slide/sessions/`
       - `safeKey(key: string): string` — per nanobot filenam-safe hash
       - `getOrCreate(key: string): Session` — checks cache, loads from JSONL if exists, creates new if not
       - `_load(key: string): Session | null` — reads JSONL file, parses messages, reassembles Session. Handle corrupt files with _repair per nanobot pattern.
       - `_sessionPayload(session: Session): object` — serialize for JSONL
       - `save(session: Session, fsync?: boolean): void` — atomic write with tmp + replace pattern
       - `flushAll(): number` — save all dirty sessions
       - `invalidate(key: string): void` — remove from cache
       - `deleteSession(key: string): boolean` — remove from disk + cache
       - `listSessions(): string[]` — list all session files in dir

    4. **Test file session.test.ts**: Create in agent-core test directory. Test:
       - Session.addMessage() creates entry with timestamp
       - Session.getHistory() respects maxMessages limit
       - SessionManager.getOrCreate() returns same session for same key
       - SessionManager.save() writes JSONL to disk
       - SessionManager._load() reads back saved session

    5. **index.ts**: Add `export { Session, SessionManager } from "./session.js"` and re-export types.

    Per D-04: port from nanobot Python, keep TS idioms (Map not dict, async/await, etc.). Per D-09: evaluate existing session-manager.ts pattern only for JSONL format reference — do NOT use its EventEmitter or parentId DAG pattern; this Session is fresh from nanobot.

    Use `~/.slide/sessions/` as root sessions dir. Pass workspace path in constructor. Do NOT import any db-ops-api code.
  </action>
  <verify>
    <automated>
      cd /Users/max/Coding/39-Slide && npx tsc --noEmit --project packages/agent-core/tsconfig.json 2>&1 | head -20 && echo "TSC OK" && npx vitest run packages/agent-core/src/__tests__/session.test.ts 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit --project packages/agent-core/tsconfig.json` compiles without errors
    - `npx vitest run packages/agent-core/src/__tests__/session.test.ts` passes
    - `Session.addMessage()` produces entries with `timestamp` field
    - `Session.getHistory(5)` returns max 5 messages, aligned to user turn
    - `SessionManager.getOrCreate("test-key")` returns same Session object for same key
    - SessionManager writes `.jsonl` files to `~/.slide/sessions/` directory
    - SessionManager.load() from disk reproduces all messages with correct ordering
    - Types are exported from `@slide/agent-core`: import { Session, SessionManager, SessionEntry, SessionMetadata } works
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add timeout layering to AgentRunner</name>
  <files>
    - packages/agent-core/src/runner.ts (MODIFY requestModel logic)
    - packages/agent-core/src/types.ts (APPEND LLMCallOptions fields)
    - packages/agent-core/src/__tests__/runner-timeout.test.ts (CREATE)
  </files>
  <read_first>
    - nanobot-reference/nanobot/agent/runner.py lines 610-739 (timeout layering pattern)
    - packages/agent-core/src/runner.ts (current requestModel flow around line 400-450)
    - packages/agent-core/src/types.ts (LLMResponse interface, LLMCallOptions interface)
  </read_first>
  <action>
    Add timeout layering to AgentRunner's requestModel logic. Per D-05 and D-06.

    1. **types.ts**: Add `timeoutS?: number` and `streamIdleTimeoutS?: number` to `LLMCallOptions`.

    2. **runner.ts**: In the requestModel method (the private method that calls this.provider.chat/chatStream):

    a. Determine LLM timeout:
       ```
       let timeoutS = spec.llmTimeoutS
       if timeoutS === undefined:
           const raw = process.env['NANOBOT_LLM_TIMEOUT_S'] || '300'
           timeoutS = parseFloat(raw) || 300
       if timeoutS <= 0: timeoutS = undefined
       ```

    b. Determine if streaming:
       ```
       const wantsStreaming = hook.wantsStreaming()
       ```

    c. KEY: Only apply wall-clock timeout for non-streaming:
       ```
       const outerTimeoutS = wantsStreaming ? undefined : timeoutS
       ```

    d. Wrap the provider call with a timeout Promise:
       - For non-streaming: use Promise.race with a delay rejection
       - For streaming: no wall-clock timeout (already has provider-level idle timeout)

    e. Timeout error handling:
       - On timeout (check error name or custom signal), return `LLMResponse` with:
         - `content: "Error calling LLM: timed out after {timeoutS}s"` (or "stream stalled" for streaming timeout)
         - `finishReason: "error"`
         - `errorKind: "timeout"`
         - `toolCalls: []`, `usage: {}`, `shouldExecuteTools: false`, `hasToolCalls: false`

    f. Pass `streamIdleTimeoutS` through to provider in LLMCallOptions if set. The nanobot pattern has NANOBOT_STREAM_IDLE_TIMEOUT_S environment variable (default 120s) handled at provider level, not runner. Add the env var name to the provider call options.

    3. **Test file runner-timeout.test.ts**: Create in agent-core test directory. Create a MockLLMProvider that:
       - `chat()` hangs indefinitely (never resolves)
       - `chatStream()` hangs indefinitely
       Test with AgentRunner:
       - Non-streaming call with short llmTimeoutS returns error_kind="timeout"
       - Streaming call with short llmTimeoutS does NOT timeout (wall-clock timeout skipped)
       - Error kind is "timeout", not an exception

    Per research Pitfall 6: Do NOT apply wall-clock timeout to streaming requests. Only streaming has its own idle timeout.
  </action>
  <verify>
    <automated>
      cd /Users/max/Coding/39-Slide && npx tsc --noEmit --project packages/agent-core/tsconfig.json 2>&1 | head -10 && echo "TSC OK" && npx vitest run packages/agent-core/src/__tests__/runner-timeout.test.ts 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - Non-streaming AgentRunner with `llmTimeoutS: 0.1` against a hanging provider returns `error_kind: "timeout"` — no exception
    - Streaming AgentRunner with `llmTimeoutS: 0.1` against a hanging provider does NOT timeout (wants_streaming skips wall-clock timeout)
    - `process.env['NANOBOT_LLM_TIMEOUT_S'] = '10'` is read as default when spec.llmTimeoutS undefined
    - `process.env['NANOBOT_STREAM_IDLE_TIMEOUT_S'] = '120'` is passed through to LLMCallOptions.streamIdleTimeoutS
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-core → filesystem (JSONL) | Session JSONL files written to ~/.slide/sessions/. No user-facing write path. |
| agent-core → env vars | NANOBOT_LLM_TIMEOUT_S and NANOBOT_STREAM_IDLE_TIMEOUT_S read from process.env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Tampering | Session JSONL files | mitigate | Atomic write with tmp + replace pattern. No user-facing metadata write path at this layer. |
| T-109-02 | Denial of Service | AgentRunner timeout | mitigate | maxIterations=10 default + 300s LLM timeout prevents unbounded invocation. Streaming idle timeout at provider level. |
</threat_model>

<verification>
```bash
# Compile check
cd /Users/max/Coding/39-Slide && npx tsc --noEmit --project packages/agent-core/tsconfig.json

# Test check - all new tests pass
npx vitest run packages/agent-core/src/__tests__/
```
</verification>

<success_criteria>
- agent-core compiles cleanly with new session module and timeout changes
- Session.getHistory() returns correctly sliced messages with token budget
- SessionManager persists/loads sessions from JSONL files
- Timeout returns error_kind="timeout" for non-streaming, skipped for streaming
- All new tests pass
- Existing tests in apps/db-ops-api/src/adapter/__tests__/ still pass (no breakage)
</success_criteria>

<output>
Create `.planning/phases/109-nanobot/109-01-SUMMARY.md` when done
</output>
