---
phase: 100
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/server.ts
  - frontend/src/openclaw/ui/icons.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
user_setup: []
must_haves:
  truths:
    - "Unauthenticated GET requests to /api/database/instances return 401 instead of data"
    - "Unauthenticated GET requests to /api/alerts return 401 instead of data"
    - "Unauthenticated GET requests to /api/metrics/:instanceId return 401 instead of data"
    - "Unauthenticated GET requests to /api/chat/history return 401 instead of data"
    - "eyeOff icon renders correctly on login page password visibility toggle"
    - "eyeOff icon renders correctly in overview, config-form, and config pages"
  artifacts:
    - path: "apps/db-ops-api/server.ts"
      provides: "Auth middleware on 4 unprotected GET routes"
      key_patterns:
        - "preHandler: [verifyToken]" on routes /api/alerts, /api/metrics/:instanceId, /api/database/instances, /api/chat/history
    - path: "frontend/src/openclaw/ui/icons.ts"
      provides: "eyeOff SVG with rendering attributes"
      key_patterns:
        - "fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\""
  key_links:
    - from: "server.ts L388-401"
      to: "verifyToken function (L85)"
      via: "fastify route { preHandler: [verifyToken] }"
    - from: "icons.ts L440"
      to: "shield icon pattern (L489)"
      via: "identical SVG attribute set"
---

<objective>
在 4 个未受保护的 GET 路由上添加 auth 中间件，并修复 eyeOff 图标 SVG 属性缺失导致的登录页渲染崩溃。

Purpose: 消除生产环境信息泄露风险（4 个 API 路由目前无需认证即可访问数据库实例、告警、指标和聊天历史），并修复登录页运行时崩溃（eyeOff 图标不可见导致 JavaScript 异常）。

Output: server.ts 路由认证加固 + icons.ts eyeOff 图标渲染修复
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/100-安全紧急修复/100-RESEARCH.md

server.ts verifyToken 中间件 (L85):
```typescript
async function verifyToken(request: any, reply: any) {
  const authHeader = request.headers.authorization;
  if (!authHeader || !authHeader.startsWith('Bearer ')) {
    return reply.code(401).send({ error: '未提供认证令牌' });
  }
  const token = authHeader.split(' ')[1];
  try { /* jwt.verify(token, JWT_SECRET) */ } catch { /* 401 */ }
}
```

已有带 auth 中间件的路由模式：
```typescript
fastify.get('/api/users', { preHandler: [verifyToken, requirePermission('admin:*')] }, handler);
fastify.post('/api/alerts/:id/read', { preHandler: [verifyToken, requirePermission('alert:manage')] }, handler);
```

shield 图标（icons.ts:489）—— 正确的 SVG 属性参考模式：
```typescript
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
```
</context>

<interfaces>
verifyToken (server.ts:85) — Fastify preHandler, 接收 `(request, reply)`, 发送 401 或继续

eyeOff SVG template (icons.ts:439-449) — Lit html\`...\` template literal, 4 个 `<path>` 元素在 `<svg>` 内部
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 为 4 个 GET 路由添加 `preHandler: [verifyToken]`</name>
  <files>
    apps/db-ops-api/server.ts
  </files>
  <read_first>
    apps/db-ops-api/server.ts
    Read L394 (GET /api/database/instances), L538 (GET /api/alerts), L573 (GET /api/metrics/:instanceId), L584 (GET /api/chat/history)
    确认每个路由注册行当前没有 `preHandler` 参数
    Read L548 附近的 POST /api/alerts/:id/read 路由作为已保护路由的参考模式
  </read_first>
  <action>
    在 server.ts 中，对以下 4 个 fastify.get 路由添加 `{ preHandler: [verifyToken] }` 作为第二个参数，紧跟路由路径之后：

    1. L394: `fastify.get('/api/database/instances', ...)` → `fastify.get('/api/database/instances', { preHandler: [verifyToken] }, async (request, reply) => {`
    2. L538: `fastify.get('/api/alerts', ...)` → `fastify.get('/api/alerts', { preHandler: [verifyToken] }, async (request, reply) => {`
    3. L573: `fastify.get('/api/metrics/:instanceId', ...)` → `fastify.get('/api/metrics/:instanceId', { preHandler: [verifyToken] }, async (request, reply) => {`
    4. L584: `fastify.get('/api/chat/history', ...)` → `fastify.get('/api/chat/history', { preHandler: [verifyToken] }, async (request, reply) => {`

    严格遵循以下规则：
    - 只添加 `{ preHandler: [verifyToken] }`，不加 `requirePermission`（D-01 要求）
    - 不修改路由 handler 函数体
    - 不修改已存在的 auth 路由（如 L548 POST /api/alerts/:id/read、L556 DELETE /api/alerts）
    - 不改动任何 import 语句（verifyToken 已在文件内 L85 定义）
  </action>
  <acceptance_criteria>
    - server.ts 中每个目标路由的第二行变为 `{ preHandler: [verifyToken] },`
    - 通过 grep 验证：`grep -c "preHandler: \[verifyToken\]" apps/db-ops-api/server.ts` 结果增加 4
    - 不通过 grep 验证：4 个路由 `/api/database/instances`, `/api/alerts`, `/api/metrics/:instanceId`, `/api/chat/history` 的 fastify.get 调用中均包含 preHandler
    - curl 验证（需启动后端）：对 4 个路由分别发无 token 请求，返回 HTTP 401
  </acceptance_criteria>
  <verify>
    <automated>grep -E "fastify\.(get|post|put|delete|patch)\('/api/(database/instances|alerts|metrics/[^']+|chat/history)'" apps/db-ops-api/server.ts | while read -r line; do case "$line" in *preHandler*) ;; *) echo "MISSING: $line";; esac; done; echo "OK"</automated>
  </verify>
  <done>4 个路由的 fastify.get 调用均包含 `{ preHandler: [verifyToken] }`，grep 确认无遗漏</done>
</task>

<task type="auto">
  <name>Task 2: 修复 eyeOff 图标 SVG 渲染属性</name>
  <files>
    frontend/src/openclaw/ui/icons.ts
  </files>
  <read_first>
    frontend/src/openclaw/ui/icons.ts
    Read L439-449 (eyeOff 图标定义) — 确认当前 `<svg>` 元素缺少渲染属性
    Read L489 (shield 图标) — 确认目标属性模式
  </read_first>
  <action>
    在 frontend/src/openclaw/ui/icons.ts 中，修改 L440 的 `<svg>` 元素：

    当前：`<svg viewBox="0 0 24 24">`
    修改为：`<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">`

    规则：
    - 属性加在 `<svg>` 上，**不是**加在每个 `<path>` 上
    - 保持所有 4 个 `<path>` 元素和它们的 d 属性不变
    - 不修改同文件中其他图标（只修 eyeOff，批量修复留给 Phase 102）
    - 不修改 eye 图标（L432-437），它没有渲染问题
  </action>
  <acceptance_criteria>
    - icons.ts 中 eyeOff 的 `<svg>` 元素包含完整属性：`fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"`
    - 4 个 `<path>` 元素内容未变更
    - 登录页（login-gate.ts）、概览页（overview.ts）、配置表单（config-form.node.ts）、配置页（config.ts）中 eyeOff 使用不受影响
    - 浏览器验证（需手动）：打开登录页，点击密码可见性切换按钮，eyeOff 图标正常显示
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'eyeOff: html`' frontend/src/openclaw/ui/icons.ts && grep -A1 'eyeOff: html`' frontend/src/openclaw/ui/icons.ts | grep -E 'fill="none".*stroke="currentColor"'</automated>
  </verify>
  <done>eyeOff SVG 元素包含所有 5 个渲染属性（fill, stroke, stroke-width, stroke-linecap, stroke-linejoin）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Backend API | 4 GET routes currently allow unauthenticated data access (read: database instances, alerts, metrics, chat history) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Information Disclosure | GET /api/database/instances (server.ts L394) | mitigate | Add preHandler: [verifyToken] per D-01. Authenticated users only. References ASVS V2.1.1 (Authentication) and V4.1.1 (Access Control). |
| T-100-02 | Information Disclosure | GET /api/alerts (server.ts L538) | mitigate | Same mitigation as T-100-01. |
| T-100-03 | Information Disclosure | GET /api/metrics/:instanceId (server.ts L573) | mitigate | Same mitigation as T-100-01. |
| T-100-04 | Information Disclosure | GET /api/chat/history (server.ts L584) | mitigate | Same mitigation as T-100-01. |

No STRIDE threats from SEC-02 (icon rendering — client-side only, no data flow change).
</threat_model>

<verification>
1. `grep -c "preHandler: \[verifyToken\]" apps/db-ops-api/server.ts` — 结果至少 4 处（原有 + 新增 4 处）
2. 对 4 个路由分别执行 `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/alerts` 返回 401
3. 登录页密码可见性切换后 eyeOff 图标可见
</verification>

<success_criteria>
1. 4 个 GET 路由全部添加 `preHandler: [verifyToken]`
2. eyeOff 图标 `<svg>` 元素包含完整渲染属性
3. 所有修改与原代码风格一致，无多余变更
</success_criteria>

<output>
After completion, create `.planning/phases/100-安全紧急修复/100-01-SUMMARY.md`
</output>
