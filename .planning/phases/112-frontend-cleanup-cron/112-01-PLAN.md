---
phase: 112-frontend-cleanup-cron
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "frontend/src/openclaw/" (renamed to frontend/src/app/)
  - "frontend/src/main.ts"
  - "frontend/src/vite.config.js"
  - "frontend/src/openclaw/protocol/" (deleted)
  - "frontend/src/openclaw/ui/app-gateway.ts" (deleted)
  - "frontend/src/openclaw/ui/views/cron.ts" (deleted)
  - "frontend/src/openclaw/ui/views/overview.ts" (deleted)
  - "frontend/src/openclaw/ui/views/overview-attention.ts" (deleted)
  - "frontend/src/openclaw/ui/views/overview-cards.ts" (deleted)
  - "frontend/src/openclaw/ui/views/overview-event-log.ts" (deleted)
  - "frontend/src/openclaw/ui/views/overview-hints.ts" (deleted)
  - "frontend/src/openclaw/ui/views/overview-log-tail.ts" (deleted)
  - "frontend/src/openclaw/ui/views/usage.ts" (deleted)
  - "frontend/src/openclaw/ui/views/usage-metrics.ts" (deleted)
  - "frontend/src/openclaw/ui/views/usage-query.ts" (deleted)
  - "frontend/src/openclaw/ui/views/usage-render-details.ts" (deleted)
  - "frontend/src/openclaw/ui/views/usage-render-overview.ts" (deleted)
  - "frontend/src/openclaw/ui/views/usageTypes.ts" (deleted)
  - "frontend/src/openclaw/ui/views/skills.ts" (deleted)
  - "frontend/src/openclaw/ui/views/skills-grouping.ts" (deleted)
  - "frontend/src/openclaw/ui/views/skills-shared.ts" (deleted)
  - "frontend/src/openclaw/ui/views/config.ts" (deleted)
  - "frontend/src/openclaw/ui/views/config-form.ts" (deleted)
  - "frontend/src/openclaw/ui/views/config-form.analyze.ts" (deleted)
  - "frontend/src/openclaw/ui/views/config-form.node.ts" (deleted)
  - "frontend/src/openclaw/ui/views/config-form.render.ts" (deleted)
  - "frontend/src/openclaw/ui/views/config-form.shared.ts" (deleted)
  - "frontend/src/openclaw/ui/views/exec-approval.ts" (deleted)
  - "frontend/src/openclaw/ui/views/unavailable-page.ts" (deleted)
  - "frontend/src/openclaw/ui/navigation.ts"
  - "frontend/src/openclaw/ui/app-render.ts"
  - "frontend/src/openclaw/ui/app.ts"
  - "frontend/src/openclaw/ui/app-lifecycle.ts"
  - "frontend/src/openclaw/ui/app-settings.ts"
  - "frontend/src/openclaw/ui/embed-sandbox.ts"
  - "frontend/src/openclaw/ui/controllers/control-ui-bootstrap.ts"
  - "frontend/src/openclaw/ui/chat/slash-commands.ts"
  - "frontend/src/openclaw/i18n/locales/en.ts"
  - "frontend/src/openclaw/i18n/locales/de.ts"
  - "frontend/src/openclaw/i18n/locales/fr.ts"
  - "frontend/src/openclaw/i18n/locales/tr.ts"
  - "frontend/src/openclaw/i18n/locales/pt-BR.ts"
  - "frontend/src/openclaw/i18n/locales/zh-CN.ts"
  - "frontend/src/openclaw/i18n/locales/zh-TW.ts"
  - "frontend/src/openclaw/i18n/locales/es.ts"
  - "frontend/src/openclaw/i18n/locales/uk.ts"
  - "frontend/src/openclaw/i18n/locales/pl.ts"
  - "frontend/src/openclaw/i18n/locales/ko.ts"
  - "frontend/src/openclaw/i18n/locales/ja-JP.ts"
  - "frontend/src/openclaw/i18n/locales/id.ts"
autonomous: true
requirements: []

must_haves:
  truths:
    - "frontend/src/openclaw/ no longer exists — directory renamed to frontend/src/app/"
    - "frontend/src/main.ts imports from ./app/ not ./openclaw/"
    - "protocol/ directory does not exist anywhere under frontend/src/"
    - "app-gateway.ts does not exist anywhere under frontend/src/"
    - "No placeholder view files for cron/skills/usage/config/overview/exec-approval exist under views/"
    - "No unavailable-page.ts exists under frontend/src/"
    - "navigation.ts no longer lists dead tabs (overview, usage, cron, skills, config) in TAB_GROUPS or TAB_PATHS"
    - "app-render.ts no longer renders placeholder sections for deleted tabs"
    - "app.ts and app-lifecycle.ts import initChatClient from direct-gateway.ts not app-gateway.ts"
    - "@openclaw/protocol alias removed from vite.config.js; remaining @openclaw aliases updated to @slide/app"
    - "All 13 i18n locale files have dead Gateway/navigation keys removed"
  artifacts:
    - path: "frontend/src/app/"
      provides: "Renamed openclaw directory"
    - path: "frontend/src/main.ts"
      contains: "from './app/"
    - path: "frontend/src/app/ui/navigation.ts"
      contains: "TAB_GROUPS with no overview/usage/cron/skills/config in openclaw group"
    - path: "frontend/src/app/ui/app-render.ts"
      contains: "no ${state.tab === 'overview'|'usage'|'cron'|'skills'|'config' placeholder renders"
    - path: "frontend/vite.config.js"
      contains: "@slide/app" and no "@openclaw/protocol"
  key_links:
    - from: "frontend/src/main.ts"
      to: "app/styles.css + app/ui/app.js"
      via: "import"
      pattern: "./app/"
    - from: "frontend/src/app/ui/app.ts"
      to: "app/ui/direct-gateway.ts"
      via: "import { initChatClient }"
      pattern: "initChatClient"
    - from: "frontend/src/app/ui/app-lifecycle.ts"
      to: "app/ui/direct-gateway.ts"
      via: "import { initChatClient }"
      pattern: "initChatClient"
---

<objective>
Rename `frontend/src/openclaw/` to `frontend/src/app/`, delete all dead placeholder views and protocol files from Gateway removal, update all consumers (imports, navigation, rendering, i18n).

Purpose: Clean up legacy OpenClaw directory structure and remove all placeholder/dead code left after Phase 111 Gateway simplification.
Output: Clean, consistent `app/` directory structure with no dead views, no protocol files, no broken imports, and no stale navigation entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-frontend-cleanup-cron/112-CONTEXT.md
@.planning/phases/112-frontend-cleanup-cron/112-RESEARCH.md

**Phase 111 SUMMARY (key decisions that caused this cleanup):**
@.planning/phases/111-gateway-simplify/111-01-SUMMARY.md
@.planning/phases/111-gateway-simplify/111-02-SUMMARY.md
@.planning/phases/111-gateway-simplify/111-03-SUMMARY.md
@.planning/phases/111-gateway-simplify/111-04-SUMMARY.md

<interfaces>
From frontend/src/main.ts (pre-rename entry point):
```typescript
import './openclaw/styles.css';
import './openclaw/ui/app.js';
```

From frontend/src/vite.config.js (aliases):
```javascript
'@openclaw/src': fileURLToPath(new URL('./src/openclaw/src', import.meta.url)),
'@openclaw/ui': fileURLToPath(new URL('./src/openclaw/ui', import.meta.url)),
'@openclaw/protocol': fileURLToPath(new URL('./src/openclaw/protocol', import.meta.url))
```

From navigation.ts:
- TAB_GROUPS: openclaw group = ["overview", "sessions", "usage", "cron", "agents", "skills"] — sessions + agents kept, others removed
- TAB_PATHS includes all tabs including dead ones
- TAB_TYPE includes all tab names

From app-render.ts:
- Lazy imports: lazyAgents (views/agents.ts), lazySessions (views/sessions.ts)
- Direct imports: renderOverview from views/overview.ts
- Inline placeholders at lines 634-638 for usage, cron; line 741 for skills; line 489 for config; line 505 for overview; line 795 for config/appearance/system
- Tab render branches for llm-config (line 762), ai-settings (line 765), scoring-settings (line 768)

From app.ts + app-lifecycle.ts:
```typescript
import { initChatClient } from "./app-gateway.ts";
```

Surviving `@openclaw/src` alias consumers (3 files, after deletion):
- embed-sandbox.ts (15 lines)
- chat/slash-commands.ts (slash-command only)
- controllers/control-ui-bootstrap.ts (65 lines)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename openclaw/ to app/ + update all import paths</name>
  <files>
    frontend/src/openclaw/ (renamed to frontend/src/app/)
    frontend/src/main.ts
    frontend/vite.config.js
  </files>
  <read_first>
    frontend/src/main.ts
    frontend/vite.config.js
  </read_first>
  <action>
    Rename the `frontend/src/openclaw/` directory to `frontend/src/app/` using `git mv` (not `mv` — preserves git history). After rename:

    1. Update `frontend/src/main.ts`: change `import './openclaw/styles.css'` to `import './app/styles.css'` and `import './openclaw/ui/app.js'` to `import './app/ui/app.js'`.

    2. Update `frontend/vite.config.js`: change the 3 alias paths from `./src/openclaw/src` → `./src/app/src`, `./src/openclaw/ui` → `./src/app/ui`. Remove the `@openclaw/protocol` alias entirely (the protocol/ directory is deleted in Task 2). Rename `@openclaw` prefix to `@slide/app` for the remaining two aliases:
       - `'@openclaw/src'` → `'@slide/app/src'`
       - `'@openclaw/ui'` → `'@slide/app/ui'`

    3. Update the 3 surviving files that import from `@openclaw/` aliases (these files survive the deletion in Task 2):
       - `frontend/src/app/ui/embed-sandbox.ts`: Change `from "@openclaw/src/"` → `from "@slide/app/src/"`
       - `frontend/src/app/ui/chat/slash-commands.ts`: Change `from "@openclaw/src/"` → `from "@slide/app/src/"`
       - `frontend/src/app/ui/controllers/control-ui-bootstrap.ts`: Change `from "@openclaw/src/"` → `from "@slide/app/src/"`

    4. Verify there are zero broken imports by running `cd frontend && npm run build` after all changes. If the build fails due to any remaining `openclaw` references, fix them.
  </action>
  <verify>
    <automated>
      test -d frontend/src/app/ &amp;&amp; test ! -d frontend/src/openclaw/ &amp;&amp; grep -q "from './app/" frontend/src/main.ts &amp;&amp; grep -q "@slide/app" frontend/vite.config.js
    </automated>
  </verify>
  <done>
    - `frontend/src/app/` exists, `frontend/src/openclaw/` does not exist
    - `frontend/src/main.ts` imports from `./app/` not `./openclaw/`
    - `frontend/vite.config.js` has `@slide/app/src` and `@slide/app/ui` aliases, no `@openclaw/protocol`
    - 3 surviving files use `@slide/app/` instead of `@openclaw/` alias
    - `cd frontend && npm run build` completes without import errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete dead files + update consumers (navigation, rendering, lifecycle)</name>
  <files>
    frontend/src/app/protocol/ (delete directory)
    frontend/src/app/ui/app-gateway.ts (delete)
    frontend/src/app/ui/views/cron.ts (delete)
    frontend/src/app/ui/views/overview.ts (delete)
    frontend/src/app/ui/views/overview-attention.ts (delete)
    frontend/src/app/ui/views/overview-cards.ts (delete)
    frontend/src/app/ui/views/overview-event-log.ts (delete)
    frontend/src/app/ui/views/overview-hints.ts (delete)
    frontend/src/app/ui/views/overview-log-tail.ts (delete)
    frontend/src/app/ui/views/usage.ts (delete)
    frontend/src/app/ui/views/usage-metrics.ts (delete)
    frontend/src/app/ui/views/usage-query.ts (delete)
    frontend/src/app/ui/views/usage-render-details.ts (delete)
    frontend/src/app/ui/views/usage-render-overview.ts (delete)
    frontend/src/app/ui/views/usageTypes.ts (delete)
    frontend/src/app/ui/views/skills.ts (delete)
    frontend/src/app/ui/views/skills-grouping.ts (delete)
    frontend/src/app/ui/views/skills-shared.ts (delete)
    frontend/src/app/ui/views/config.ts (delete)
    frontend/src/app/ui/views/config-form.ts (delete)
    frontend/src/app/ui/views/config-form.analyze.ts (delete)
    frontend/src/app/ui/views/config-form.node.ts (delete)
    frontend/src/app/ui/views/config-form.render.ts (delete)
    frontend/src/app/ui/views/config-form.shared.ts (delete)
    frontend/src/app/ui/views/exec-approval.ts (delete)
    frontend/src/app/ui/views/unavailable-page.ts (delete)
    frontend/src/app/ui/navigation.ts
    frontend/src/app/ui/app-render.ts
    frontend/src/app/ui/app.ts
    frontend/src/app/ui/app-lifecycle.ts
    frontend/src/app/ui/app-settings.ts
  </files>
  <read_first>
    frontend/src/app/ui/navigation.ts
    frontend/src/app/ui/app-render.ts
    frontend/src/app/ui/app.ts
    frontend/src/app/ui/app-lifecycle.ts
    frontend/src/app/ui/app-gateway.ts
  </read_first>
  <action>
    Use `git rm` to delete all listed dead files (protocol/ directory, app-gateway.ts, all placeholder views). Then update consumer files:

    1. **`navigation.ts`** (per D-09, D-11):
       - Remove from `TAB_GROUPS` → openclaw group: `"overview"`, `"usage"`, `"cron"`, `"skills"` (keep `"sessions"` and `"agents"`)
       - Remove from `Tab` type union: `"overview"`, `"usage"`, `"cron"`, `"skills"` (keep sessions/agents)
       - Remove from `TAB_PATHS`: overview, usage, cron, skills paths and their entries
       - Remove from `PATH_TO_TAB`: same tabs
       - Add new tab `"cron-jobs"` to settings group: `"cron-jobs"` in TAB_GROUPS settings.tabs, add to Tab type, add to TAB_PATHS (`cron-jobs: "/cron-jobs"`), add `iconForTab` case returning `"loader"`, add `titleForTab` and `subtitleForTab` entries.
       - Add to `TAB_REQUIRED_PERMISSIONS`: `'cron-jobs': 'cron:view'`
       - Remove entries from `iconForTab`: overview, usage, cron, skills

    2. **`app-render.ts`** (per D-09):
       - Remove `import { renderOverview } from "./views/overview.ts"` (line 43)
       - Keep `import "./views/llm-config.ts"` at line 22 unchanged (it is a real view, not a placeholder)
       - Remove the lazy agents/sessions: keep `lazyAgents` and `lazySessions` (they are kept per D-09, D-10)
       - Remove the `tab === "overview"` render block (lines 505-549) and its `sessionsCount`, `cronNext`, `cronEnabled`, `cronStatus`, `cronJobs`, `usageResult`, `sessionsResult`, `skillsReport`, `overviewLogLines`, `showGatewayPassword` prop extraction
       - Remove the `tab === "usage"` inline placeholder (lines 634-635)
       - Remove the `tab === "cron"` inline placeholder (lines 637-638) — this slot will be replaced in Plan 03
       - Remove the `tab === "skills"` inline placeholder (line 741-742)
       - Remove the `tab === "config"` from the combined condition at line 795 (keep `|| state.tab === "appearance" || state.tab === "system"` only)
       - Keep ALL real business view renders (sessions, agents, dashboard, instances-db, instance-detail, alerts, schema, indexes, llm-config, ai-settings, scoring-settings, sql-console, approval, reports, metric-registry, events, docs, users, rbac, chat)
       - Note: The `cron-jobs` tab render branch will be added in Plan 03, not here.

    3. **`app.ts`** (per D-04):
       - Change `import { initChatClient } from "./app-gateway.ts"` (line 10) to `import { initChatClient } from "./direct-gateway.ts"`
       - Ensure `initChatClient(this)` call still works with the DirectGatewayClient import

    4. **`app-lifecycle.ts`** (per D-04):
       - Change `import { initChatClient } from "./app-gateway.ts"` (line 1) to `import { initChatClient } from "./direct-gateway.ts"`
       - Ensure the `initChatClient(host)` call at line 53 still works — DirectGatewayClient's initChatClient has the same signature

    5. **`app-settings.ts`**: Verify no imports reference deleted files. The file should already be clean from Phase 111 (D-01 applied there).

    6. Verify `cd frontend && npm run build` passes before committing. Fix any remaining broken imports.
  </action>
  <verify>
    <automated>
      test ! -d frontend/src/app/protocol/ &amp;&amp; test ! -f frontend/src/app/ui/app-gateway.ts &amp;&amp; test ! -f frontend/src/app/ui/views/cron.ts &amp;&amp; test ! -f frontend/src/app/ui/views/unavailable-page.ts &amp;&amp; grep -q "initChatClient" frontend/src/app/ui/direct-gateway.ts &amp;&amp; grep -q "cron-jobs" frontend/src/app/ui/navigation.ts &amp;&amp; ! grep -qE '"overview"|"usage"|"cron"|"skills"|"config"' frontend/src/app/ui/navigation.ts
    </automated>
  </verify>
  <done>
    - protocol/ directory deleted
    - app-gateway.ts deleted
    - 24+ placeholder view files deleted (cron, overview/usage/skills/config/exec-approval families + unavailable-page)
    - app.ts + app-lifecycle.ts import initChatClient from direct-gateway.ts
    - navigation.ts has no overview/usage/cron/skills/config tabs; has cron-jobs tab in settings group
    - app-render.ts has no overview/usage/cron/skills/config placeholder renders
    - `cd frontend && npm run build` passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean i18n locale files of dead Gateway/navigation keys</name>
  <files>
    frontend/src/app/i18n/locales/en.ts
    frontend/src/app/i18n/locales/de.ts
    frontend/src/app/i18n/locales/fr.ts
    frontend/src/app/i18n/locales/tr.ts
    frontend/src/app/i18n/locales/pt-BR.ts
    frontend/src/app/i18n/locales/zh-CN.ts
    frontend/src/app/i18n/locales/zh-TW.ts
    frontend/src/app/i18n/locales/es.ts
    frontend/src/app/i18n/locales/uk.ts
    frontend/src/app/i18n/locales/pl.ts
    frontend/src/app/i18n/locales/ko.ts
    frontend/src/app/i18n/locales/ja-JP.ts
    frontend/src/app/i18n/locales/id.ts
  </files>
  <read_first>
    frontend/src/app/ui/navigation.ts (to know which tab keys are dead)
    frontend/src/app/i18n/locales/en.ts (reference locale to identify dead keys)
  </read_first>
  <action>
    Per D-06: precisely delete dead Gateway/navigation translation keys from all 13 locale files. Only delete keys that correspond to removed Gateway views/features. Keep all keys for active Slide views/settings.

    Dead keys to remove from all locale files:
    - `tabs.overview`, `subtitles.overview` — overview tab deleted
    - `tabs.usage`, `subtitles.usage` — usage tab deleted
    - `tabs.cron`, `subtitles.cron` — cron tab removed (replaced by cron-jobs)
    - `tabs.skills`, `subtitles.skills` — skills tab deleted
    - `tabs.config`, `subtitles.config` — config tab deleted
    - Any other i18n keys whose only consumer is a deleted view (e.g., `overview.*`, `usage.*`, `cron.*`, `skills.*`, `config.*`, `exec-approval.*`)

    Keep ALL keys for: chat, dashboard, instances-db, instance-detail, alerts, schema, indexes, reports, metric-registry, events, users, rbac, sql-console, approval, llm-config, ai-settings, scoring-settings, sessions, agents, docs, login, common, nav groups, etc.

    Strategy: read the reference locale (en.ts). For each key that matches a deleted tab's namespace, check if it's used by any surviving code. Delete only confirmed dead keys. Do NOT delete keys shared across multiple views.

    Also clean the dead `@openclaw/src/protocol` import from `chat/slash-commands.ts` (per D-08):
    - Remove `import type { CommandEntry, CommandsListResult } from "@openclaw/src/protocol/index.ts"` (line 2)
    - Replace with local type stubs if the types are still referenced in the file, or inline the type
    - Verify the file still compiles

    After all changes, verify: `cd frontend && npm run build` passes.
  </action>
  <verify>
    <automated>
      cd frontend &amp;&amp; npm run build 2>&amp;1 | tail -5
    </automated>
  </verify>
  <done>
    - No `tabs.overview`, `tabs.usage`, `tabs.cron`, `tabs.skills`, `tabs.config` keys exist in any locale file
    - No `subtitles.overview` etc. dead subtitle keys exist
    - chat/slash-commands.ts compiles without `@openclaw/src/protocol` import
    - `cd frontend && npm run build` passes with zero errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan is pure frontend deletion/rename/refactor with no new backend interactions. No new trust boundaries introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Denial of Service | build system | mitigate | Verify `npm run build` after each task — broken imports would cause build failure, caught before deployment |
| T-112-02 | Tampering | git history | accept | `git mv` preserves history for the 109 files under `app/`; future `git blame` needs `--follow` (known limitation, documented in RESEARCH.md Pitfall 1) |
</threat_model>

<verification>
- `frontend/src/openclaw/` no longer exists, `frontend/src/app/` exists
- `cd frontend && npm run build` passes with zero errors
- All placeholder views are removed from disk
- navigation.ts has clean tab groups with no dead entries
- app-render.ts renders only real views, no placeholder stubs for deleted tabs
- i18n files have only active keys
- chat/slash-commands.ts compiles with no @openclaw imports
</verification>

<success_criteria>
1. Directory renamed, build passes
2. All 24+ placeholder view files + protocol/ + app-gateway.ts deleted from disk
3. navigation.ts and app-render.ts no longer reference deleted views/tabs
4. app.ts and app-lifecycle.ts import from direct-gateway.ts not app-gateway.ts
5. i18n files have dead keys removed
6. chat/slash-commands.ts protocol import cleaned
7. `npm run build` succeeds end-to-end
</success_criteria>

<output>
Create `.planning/phases/112-frontend-cleanup-cron/112-01-SUMMARY.md` when done
</output>
