---
phase: 112-frontend-cleanup-cron
plan: 03
type: execute
wave: 2
depends_on: ["112-01", "112-02"]
files_modified:
  - "frontend/src/app/ui/views/cron-jobs-settings.ts" (new)
  - "frontend/src/app/ui/app-render.ts"
  - "frontend/src/app/ui/navigation.ts"
  - "frontend/src/app/ui/app-settings.ts"
  - "frontend/src/app/i18n/locales/en.ts"
  - "frontend/src/app/i18n/locales/de.ts"
  - "frontend/src/app/i18n/locales/fr.ts"
  - "frontend/src/app/i18n/locales/tr.ts"
  - "frontend/src/app/i18n/locales/pt-BR.ts"
  - "frontend/src/app/i18n/locales/zh-CN.ts"
  - "frontend/src/app/i18n/locales/zh-TW.ts"
  - "frontend/src/app/i18n/locales/es.ts"
  - "frontend/src/app/i18n/locales/uk.ts"
  - "frontend/src/app/i18n/locales/pl.ts"
  - "frontend/src/app/i18n/locales/ko.ts"
  - "frontend/src/app/i18n/locales/ja-JP.ts"
  - "frontend/src/app/i18n/locales/id.ts"
autonomous: false
requirements: []

must_haves:
  truths:
    - "Settings page shows a new '定时任务' tab with the loader icon, alongside AI Settings, LLM Config, Scoring Settings"
    - "Cron job list table renders with columns: Status, Name, Description, Cron Expression, Next Run, Last Run, Last Result, Actions"
    - "Toggle switch enables/disables each cron job with optimistic UI update"
    - "Double-clicking cron expression cell opens inline edit with text input, save/cancel buttons, and next-5-run preview"
    - "Common template dropdown (every minute / every 5 min / every hour / daily / daily at 08:00) populates the expression input"
    - "Trigger Now button shows confirmation dialog before sending POST"
    - "Post-trigger, frontend polls GET /api/cron/jobs/:id/logs?limit=1 every 3 seconds for up to 30 seconds"
    - "Logs button expands a sub-row showing last 20 execution logs with started_at, finished_at, duration, status, result, error"
    - "All error states handled: API load failure, toggle failure, expression validation, trigger failure, log load failure"
  artifacts:
    - path: "frontend/src/app/ui/views/cron-jobs-settings.ts"
      provides: "LitElement <cron-jobs-settings> with full cron management UI"
      exports: ["CronJobsSettings"]
    - path: "frontend/src/app/ui/app-render.ts"
      contains: "<cron-jobs-settings> render branch for tab === 'cron-jobs'"
    - path: "frontend/src/app/ui/navigation.ts"
      contains: "cron-jobs in settings group TAB_GROUPS"
    - path: "frontend/src/app/i18n/locales/en.ts"
      contains: "cron-jobs translation keys"
  key_links:
    - from: "cron-jobs-settings.ts"
      to: "/api/cron/jobs"
      via: "authFetch()"
    - from: "cron-jobs-settings.ts"
      to: "/api/cron/jobs/:id/toggle"
      via: "PATCH"
    - from: "cron-jobs-settings.ts"
      to: "/api/cron/jobs/:id/run"
      via: "POST"
    - from: "cron-jobs-settings.ts"
      to: "/api/cron/jobs/:id/logs"
      via: "GET"
---

<objective>
Create the frontend cron job management UI: a Settings tab with job list table, enable/disable toggle, cron expression editor with live preview, manual trigger, and execution log viewer.

Purpose: Provide a visual management interface for the DB-backed cron system, so operators can enable/disable/reconfigure cron jobs without editing code or restarting the server.
Output: Working `<cron-jobs-settings>` LitElement integrated into the Settings page, fully wired with i18n and backend API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-frontend-cleanup-cron/112-CONTEXT.md (D-17 through D-23)
@.planning/phases/112-frontend-cleanup-cron/112-RESEARCH.md (Architecture Patterns, Code Examples)
@.planning/phases/112-frontend-cleanup-cron/112-UI-SPEC.md (complete UI contract)

**Existing patterns (reference implementations):**
@.planning/phases/111-gateway-simplify/111-04-SUMMARY.md (view placeholder handling)
@.planning/phases/93-ai-agent-ops-assistant/93-01-SUMMARY.md (AI Settings tab pattern — <ai-settings-page>)

<interfaces>
**From app-render.ts** (after Plan 01 cleanup):
- Settings tabs rendered at lines for llm-config, ai-settings, scoring-settings
- Pattern: `${state.tab === "cron-jobs" ? html\`<cron-jobs-settings>\`\`\` : nothing}`
- The `cron-jobs` tab type must be added to navigation.ts Tab type

**From navigation.ts** (after Plan 01):
- TAB_GROUPS settings group currently includes: `["users", "rbac", "appearance", "llm-config", "ai-settings", "scoring-settings", "cron-jobs"]`
- Tab type union includes `"cron-jobs"`

**API endpoints (from Plan 02):**
- GET /api/cron/jobs — list all jobs
- PUT /api/cron/jobs/:id — update job (cron_expr, enabled, timezone, etc.)
- POST /api/cron/jobs/:id/toggle — body: { enabled: boolean }
- POST /api/cron/jobs/:id/run — manual trigger
- GET /api/cron/jobs/:id/logs?limit=20&offset=0 — execution logs

**Existing authFetch pattern** (from ai-settings.ts):
```typescript
const response = await fetch('/api/...', {
  method: 'GET',
  headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
});
```

**Existing toggle switch CSS** (from ai-settings.ts `.cfg-toggle` pattern):
- 40px x 22px slider: `.cfg-toggle { position:relative; width:40px; height:22px; ... }`
- `.cfg-toggle input[type="checkbox"] { display:none }`
- `.cfg-toggle .slider { ... }`

**Cron expression preview pattern** (from RESEARCH.md):
```typescript
import { CronJob } from 'cron'; // frontend usage for preview only
function getNextRunPreviews(expression: string): string[] | string { ... }
```
Note: `cron` npm package usage on frontend is via a bundled helper or a small inline computation. Since `cron` is a Node.js package, the frontend cannot directly import it. For the live preview, use a lightweight cron parser library or compute via the backend. Alternative: send a GET /api/cron/jobs/preview?expr=... to validate and get next dates from the backend. For simplicity in this plan, pass the expression to the backend validation endpoint or compute preview with a simple cron-parser bundled for frontend.

Actually, since the backend already has `cron` installed and the PUT route validates the expression, we can create a lightweight GET /api/cron/jobs/preview endpoint that takes a `?expr=` query param and returns the next 5 dates as ISO strings. The frontend calls this endpoint for live preview. This avoids bundling the `cron` npm package (which is Node-specific) on the frontend side.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cron-jobs-settings.ts LitElement with full management UI</name>
  <files>
    frontend/src/app/ui/views/cron-jobs-settings.ts (new)
  </files>
  <read_first>
    frontend/src/app/ui/views/ai-settings.ts (reference for LitElement pattern, authFetch, toggle switch)
    .planning/phases/112-frontend-cleanup-cron/112-UI-SPEC.md (full interaction contract)
    .planning/phases/112-frontend-cleanup-cron/112-CONTEXT.md (D-17 through D-23)
  </read_first>
  <action>
    Create `frontend/src/app/ui/views/cron-jobs-settings.ts` as a custom LitElement `<cron-jobs-settings>` registered with `@customElement('cron-jobs-settings')`.

    Follow the `ai-settings.ts` LitElement pattern exactly:
    - `import { LitElement, html, css } from "lit"`
    - `import { customElement, state } from "lit/decorators.js"`
    - Use `authFetch` from the existing API utility (same pattern as ai-settings.ts)
    - Static styles with `css` tagged template
    - `render()` method returning HTML template

    **Component state:**
    - `@state() jobs: CronJobConfig[]` — full job list
    - `@state() loading: boolean` — initial loading state
    - `@state() error: string | null` — load error state
    - `@state() editingJobId: number | null` — which row has inline cron editor open
    - `@state() editValue: string` — current value in cron expression editor
    - `@state() previewDates: string[] | null` — next 5 run preview from backend
    - `@state() previewError: string | null` — preview validation error
    - `@state() expandedJobId: number | null` — which row has logs sub-row expanded
    - `@state() jobLogs: Map<number, CronJobLog[]>` — logs per job
    - `@state() logsLoading: boolean`
    - `@state() triggerDialogOpen: boolean`
    - `@state() triggerJobId: number | null`
    - `@state() triggerJobName: string | null`
    - `@state() triggerRunning: boolean`
    - `@state() pollingJobIds: Set<number>` — jobs being polled after trigger

    **Types to define locally:**
    ```typescript
    interface CronJobConfig {
      id: number; name: string; handler: string; cron_expr: string;
      enabled: boolean; timezone: string; description: string | null;
      last_run_at: string | null; next_run_at: string | null;
      last_result: string | null; timeout_seconds: number;
    }
    interface CronJobLog {
      id: number; job_id: number; started_at: string; finished_at: string | null;
      status: string; result_summary: string | null; error_message: string | null;
    }
    ```

    **Methods:**
    - `connectedCallback()` — call `loadCronJobs()`
    - `async loadCronJobs()` — GET `/api/cron/jobs`, set jobs state, handle error
    - `async toggleJob(id, enabled)` — POST `/api/cron/jobs/:id/toggle` with `{ enabled }`. Optimistic: toggle immediately in UI state. On error: revert + show toast.
    - `async updateCronExpr(id, expr)` — PUT `/api/cron/jobs/:id` with `{ cron_expr: expr }`. Call `loadCronJobs()` on success. On error: show inline error.
    - `async getPreview(expr)` — GET `/api/cron/jobs/preview?expr=${encodeURIComponent(expr)}`. Returns `{ dates: string[] }` or error. If backend preview endpoint not available, implement a simple client-side cron parser using regex for the 5 standard presets (5-field cron).
    - `async triggerJob(id)` — POST `/api/cron/jobs/:id/run`. On success: close dialog, start polling. On error: show error in dialog.
    - `async loadLogs(jobId)` — GET `/api/cron/jobs/:id/logs?limit=20`. Store in jobLogs map.
    - `pollJobStatus(jobId)` — setInterval every 3s, GET `/api/cron/jobs/:id/logs?limit=1`, check if latest log has non-'running' status. Stop polling when complete or after 30 seconds (10 iterations).

    **Render structure (following 112-UI-SPEC exactly):**
    1. **Loading state**: full-width centered "加载中..." text (same as ai-settings.ts)
    2. **Error state**: centered error block with "加载定时任务失败" header + "重试" button
    3. **Table** with the following structure:
       - `<table class="table">` with `<thead>` and `<tbody>`
       - Columns: Status (48px dot), Name (1fr), Description (2fr), Cron Expression (180px monospace), Next Run (140px), Last Run (140px), Last Result (80px badge), Actions (120px toggle + buttons)

    **Per-row behavior:**
    - **Status dot**: green circle (`--ok`) if enabled, gray circle (`--muted`) if disabled
    - **Name**: semibold, `--text-sm`. When disabled, use `--muted` color.
    - **Description**: `--text-sm`, `--muted` color
    - **Cron Expression**: monospace font, `--text-xs`. Double-click to enter edit mode (inline). In edit mode: text input + save (checkmark icon) + cancel (x icon) + below: preview list or error text.
    - **Next Run / Last Run**: relative time display using a simple helper function (format relative to now). If null, show "—"
    - **Last Result badge**: colored pill. Green (`--ok`) for 'success', red (`--danger`) for 'error', yellow (`--warn`) for 'skipped', gray (`--muted`) for null/'running'
    - **Actions column**: toggle switch (`.cfg-toggle` pattern) + "立即执行" ghost button + "日志" ghost button

    **Cron expression editor (inline, per UI-SPEC Interaction 4):**
    - Triggered by double-click on the cell or clicking an edit icon
    - Text input with monospace font, `--text-xs`
    - Below input: live preview of next 5 execution times (from backend preview endpoint or client-side)
    - Common template dropdown next to input with 5 presets: every minute, every 5 min, every hour, daily midnight, daily 08:00
    - Save icon button sends PUT; Cancel icon button reverts
    - Invalid expression: input border turns `--danger`, show inline error

    **Manual trigger dialog (per UI-SPEC Interaction 5):**
    - "立即执行" button opens confirmation dialog
    - Dialog title: "手动触发"
    - Body: "确认立即执行「{jobName}」?"
    - "取消" and "确认执行" buttons
    - On confirm: button shows loading state, POST sent, on success close dialog + start polling
    - On error: keep dialog open, show error message

    **Run log sub-row (per UI-SPEC Interaction 6):**
    - Click "日志" button to expand sub-row below current row
    - Expanded content shows log table header + last 20 log rows
    - Columns: started_at, finished_at, duration (computed), status badge, result_summary (truncated)
    - If status is 'error', show error_message in red text
    - "加载更多" button at bottom for pagination (offset-based)
    - Loading state: "加载中..." text in expanded area
    - Empty state: "暂无执行记录"

    **Styling:**
    - Reuse project CSS variables from base.css exclusively (--text-*, --space-*, --radius-*, --color-*, --bg, --card, --border, --muted, --accent, --ok, --warn, --danger)
    - Reuse `.cfg-toggle` CSS pattern from ai-settings.ts for toggle switches
    - Reuse `.table`, `.table-head`, `.table-row` patterns (define inline if not available in global CSS)
    - Table rows: flat `--card` background, `--bg-hover` on hover, no alternating colors
    - Status pill: `display:inline-flex; align-items:center; gap:4px; padding:2px 8px; border-radius:var(--radius-xs); font-size:var(--text-xs)`
    - Inline cron editor: `font-family:var(--mono); font-size:var(--text-xs)`
    - Dialog/modal: overlay with centered card (.md-preview-dialog pattern)
    - Toast: fixed top-right, auto-dismiss after 3 seconds

    **Toast system:**
    Since the component may not have access to a global toast system, implement a simple local toast:
    ```typescript
    @state() private toast: { message: string; type: 'success' | 'error' } | null = null;
    private showToast(message: string, type: 'success' | 'error' = 'success') {
      this.toast = { message, type };
      setTimeout(() => this.toast = null, 3000);
    }
    ```
    Render at bottom-right of the component: `position:fixed; bottom:24px; right:24px; ...`

    **Backend preview endpoint note:**
    Add a companion change: In server.ts (Plan 02), add a simple GET /api/cron/jobs/preview endpoint that accepts `?expr=` query param and returns next 5 dates using the backend's `cron` package:
    ```typescript
    fastify.get('/api/cron/jobs/preview', { preHandler: [verifyToken, requirePermission('cron:view')] }, async (request, reply) => {
      const { expr } = request.query as { expr: string };
      try {
        const job = new CronJob(expr, () => {});
        const dates = job.nextDates(5).map((d: Date) => d.toISOString());
        return { dates };
      } catch {
        return reply.code(400).send({ error: 'Invalid cron expression', dates: [] });
      }
    });
    ```
    Add this route to server.ts alongside the other cron routes (it must be added by this task since the frontend depends on it).
  </action>
  <verify>
    <automated>
      test -f frontend/src/app/ui/views/cron-jobs-settings.ts &amp;&amp; grep -q "customElement.*cron-jobs-settings" frontend/src/app/ui/views/cron-jobs-settings.ts &amp;&amp; grep -q "loadCronJobs" frontend/src/app/ui/views/cron-jobs-settings.ts &amp;&amp; grep -q "/api/cron/jobs" frontend/src/app/ui/views/cron-jobs-settings.ts &amp;&amp; grep -q "preview" frontend/src/app/ui/views/cron-jobs-settings.ts
    </automated>
  </verify>
  <done>
    - LitElement `<cron-jobs-settings>` created in `frontend/src/app/ui/views/cron-jobs-settings.ts`
    - Implements all 6 API interactions: load, toggle, update expression, trigger, preview, logs
    - Has loading, error, and empty states for each async operation
    - Toggle uses optimistic UI with revert on error
    - Cron expression editor has inline edit with preview + presets dropdown
    - Manual trigger has confirmation dialog + polling
    - Log viewer has expandable sub-row with pagination
    - Uses project CSS variables only (no hardcoded colors/fonts/spacing)
    - Toast system for success/error feedback
    - GET /api/cron/jobs/preview endpoint added to server.ts alongside other cron routes
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire cron-jobs-settings into Settings tab + add i18n keys</name>
  <files>
    frontend/src/app/ui/app-render.ts
    frontend/src/app/ui/navigation.ts
    frontend/src/app/i18n/locales/en.ts
    frontend/src/app/i18n/locales/de.ts
    frontend/src/app/i18n/locales/fr.ts
    frontend/src/app/i18n/locales/tr.ts
    frontend/src/app/i18n/locales/pt-BR.ts
    frontend/src/app/i18n/locales/zh-CN.ts
    frontend/src/app/i18n/locales/zh-TW.ts
    frontend/src/app/i18n/locales/es.ts
    frontend/src/app/i18n/locales/uk.ts
    frontend/src/app/i18n/locales/pl.ts
    frontend/src/app/i18n/locales/ko.ts
    frontend/src/app/i18n/locales/ja-JP.ts
    frontend/src/app/i18n/locales/id.ts
  </files>
  <read_first>
    frontend/src/app/ui/app-render.ts (lines around settings tab renders, e.g., ai-settings, scoring-settings)
    frontend/src/app/ui/navigation.ts (TAB_GROUPS settings group)
    frontend/src/app/i18n/locales/en.ts (reference for existing i18n key patterns)
  </read_first>
  <action>
    Per D-17 and D-11:

    1. **`frontend/src/app/ui/navigation.ts`**:
       - Verify `"cron-jobs"` is in the `Tab` type union (should have been added in Plan 01 Task 2; if not, add it now)
       - Verify `"cron-jobs"` is in `settings` group `TAB_GROUPS`
       - Verify `TAB_PATHS` has `'cron-jobs': '/cron-jobs'`
       - Verify `iconForTab` has case `'cron-jobs': return 'loader'`
       - Verify `TAB_REQUIRED_PERMISSIONS` has `'cron-jobs': 'cron:view'`

    2. **`frontend/src/app/ui/app-render.ts`**:
       - Add `import "./views/cron-jobs-settings.ts"` alongside the other settings imports (near line 23-24 where ai-settings/scoring-settings are imported)
       - Add a new render branch after the scoring-settings block (or in the settings area near line 770):
         ```typescript
         ${state.tab === "cron-jobs"
           ? html`<cron-jobs-settings></cron-jobs-settings>`
           : nothing}
         ```
       - Verify the import path resolves correctly: `./views/cron-jobs-settings.ts`

    3. **i18n files** — Add cron-jobs translation keys to ALL 13 locale files. Each file gets these new keys:

       **zh-CN.ts** (Chinese reference):
       ```
       tabs.cron-jobs: "定时任务"
       subtitles.cron-jobs: "管理定时采集和分析任务"
       cron-jobs.table.name: "任务名称"
       cron-jobs.table.description: "描述"
       cron-jobs.table.cron-expr: "Cron 表达式"
       cron-jobs.table.next-run: "下次执行"
       cron-jobs.table.last-run: "上次执行"
       cron-jobs.table.last-result: "结果"
       cron-jobs.table.actions: "操作"
       cron-jobs.table.status-enabled: "已启用"
       cron-jobs.table.status-disabled: "已停用"
       cron-jobs.action.trigger: "立即执行"
       cron-jobs.action.logs: "日志"
       cron-jobs.action.edit: "编辑"
       cron-jobs.action.save: "保存"
       cron-jobs.action.cancel: "取消"
       cron-jobs.confirm-trigger.title: "手动触发"
       cron-jobs.confirm-trigger.body: "确认立即执行「{jobName}」？"
       cron-jobs.confirm-trigger.confirm: "确认执行"
       cron-jobs.confirm-trigger.cancel: "取消"
       cron-jobs.toast.trigger-success: "已触发执行"
       cron-jobs.toast.trigger-failed: "触发失败：{reason}"
       cron-jobs.toast.save-success: "保存成功"
       cron-jobs.toast.save-failed: "保存失败：{reason}"
       cron-jobs.toast.toggle-failed: "更新任务状态失败"
       cron-jobs.error.load-failed: "加载定时任务失败"
       cron-jobs.error.load-retry: "重试"
       cron-jobs.error.invalid-expr: "Cron 表达式无效"
       cron-jobs.preset.every-min: "每分钟"
       cron-jobs.preset.every-5min: "每5分钟"
       cron-jobs.preset.every-hour: "每小时"
       cron-jobs.preset.every-day: "每天（午夜）"
       cron-jobs.preset.every-day-8am: "每天（08:00）"
       cron-jobs.result.ok: "成功"
       cron-jobs.result.error: "失败"
       cron-jobs.result.skipped: "跳过"
       cron-jobs.result.na: "未执行"
       cron-jobs.logs.title: "执行记录"
       cron-jobs.logs.started: "开始时间"
       cron-jobs.logs.finished: "结束时间"
       cron-jobs.logs.duration: "耗时"
       cron-jobs.logs.status: "状态"
       cron-jobs.logs.result: "结果摘要"
       cron-jobs.logs.error: "错误信息"
       cron-jobs.logs.show-more: "加载更多"
       cron-jobs.logs.empty: "暂无执行记录"
       cron-jobs.logs.loading: "加载中..."
       cron-jobs.expression.preview: "接下来 5 次执行时间"
       cron-jobs.expression.presets: "常用模板"
       ```

       For **en.ts** (English), use English versions of the same keys. For example:
       ```
       tabs.cron-jobs: "Cron Jobs"
       subtitles.cron-jobs: "Manage scheduled collection and analysis tasks"
       cron-jobs.table.name: "Name"
       cron-jobs.table.description: "Description"
       cron-jobs.table.cron-expr: "Cron Expression"
       [...follow the same pattern with English translations]
       ```

       For the other 11 locale files (de, fr, tr, pt-BR, zh-TW, es, uk, pl, ko, ja-JP, id):
       - Copy the exact same key structure from en.ts or zh-CN.ts
       - For the translation values, use the English text as fallback for all non-zh locale files (de, fr, tr, pt-BR, es, uk, pl, ko, ja-JP, id)
       - For zh-TW, use a close approximation of zh-CN but understand it may not be perfectly accurate — use zh-CN values as basis
       - The key structure is what matters most; translations can be refined later

    4. **Verify build**:
       ```bash
       cd frontend && npm run build
       ```
       Fix any compilation issues (missing imports, wrong paths, etc.).
  </action>
  <verify>
    <automated>
      grep -q "cron-jobs-settings" frontend/src/app/ui/app-render.ts &amp;&amp; grep -q "cron-jobs" frontend/src/app/ui/navigation.ts &amp;&amp; grep -q "tabs.cron-jobs" frontend/src/app/i18n/locales/zh-CN.ts &amp;&amp; grep -q "cron-jobs" frontend/src/app/i18n/locales/en.ts
    </automated>
  </verify>
  <done>
    - app-render.ts imports and renders `<cron-jobs-settings>` for tab === 'cron-jobs'
    - navigation.ts has 'cron-jobs' in settings group Tab type, TAB_PATHS, iconForTab, and TAB_REQUIRED_PERMISSIONS
    - All 13 i18n locale files have the full cron-jobs key set
    - `cd frontend && npm run build` passes with zero errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → backend API | Frontend fetches from /api/cron/* over HTTP with JWT auth header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-09 | Information Disclosure | Cron expression display | mitigate | All data is backend-authorized with `cron:view` permission; JWT auth on all API calls |
| T-112-10 | Elevation of Privilege | Manual trigger via UI | mitigate | Backend `requirePermission('cron:manage')` on POST /api/cron/jobs/:id/run — frontend just sends auth'd request |
| T-112-11 | Tampering | Cron expression input | mitigate | Backend validates expression via try-catch `new CronJob()` before persisting |
</threat_model>

<verification>
- Settings page shows the "定时任务" tab when user has `cron:view` permission
- Job list loads and displays all 13 jobs
- Toggle switch changes state optimistically and persists
- Inline cron expression editor shows preview and validates input
- Manual trigger flow: dialog → POST → polling → result update
- Log viewer shows execution history with expandable sub-row
- All error states render correctly (load failure, toggle failure, validation failure, trigger failure)
- `cd frontend && npm run build` passes
- Backend preview endpoint `GET /api/cron/jobs/preview?expr=* * * * *` returns next 5 dates or error
</verification>

<success_criteria>
1. `<cron-jobs-settings>` created as LitElement with full CRUD UI
2. Integrated into Settings tab page via navigation.ts + app-render.ts
3. All 13 i18n locale files updated with cron-jobs keys
4. Toggle, cron edit, manual trigger, and log viewer all functional
5. GET /api/cron/jobs/preview endpoint added to backend
6. `npm run build` passes with zero errors
7. New Settings tab visible and functional in the frontend
</success_criteria>

<output>
Create `.planning/phases/112-frontend-cleanup-cron/112-03-SUMMARY.md` when done
</output>
