---
status: partial
phase: 124-server-registration
source: 124-01-SUMMARY.md, 124-02-SUMMARY.md
started: 2026-07-08T09:00:00Z
updated: 2026-07-08T23:30:00Z
---

## Current Test

number: 3
name: Navigation Sidebar — "运维" Group with "服务器管理" Tab
expected: |
  Left sidebar shows "运维" group. "服务器管理" tab above "数据库管理".
  Clicking it navigates to the server management page.
awaiting: user visual confirmation (frontend live at :5173)

## Tests

### 1. Cold Start Smoke Test
expected: Kill running server, start from scratch — server boots without errors, migration completes, health check returns 200
result: issue
reported: "后端进程是 7月7日启动的旧版本，没有 /api/servers 路由（404）；迁移 019/020 未在数据库执行，servers/server_metrics 表不存在（500）"
severity: blocker
resolution: "重启后端进程 + 执行迁移 019 (servers 表) 和 020 (server_metrics 表)。重启后 /api/servers → 200, /api/servers/metrics/summary → 200。根因是环境未同步，非代码缺陷"
verified: 2026-07-08

### 2. Backend Coverage Summary (auto-verified)
expected: |
  - Servers table migration (019_add_servers_table.sql) with proper DDL, permissions, role assignments
  - ServerDatabaseService with full CRUD + SSH test + key rotation + credential encryption
  - 7 server API routes (list, detail, create, update, delete, test-connection, rotate-key) with requirePermission middleware
  - credential_encrypted stripped from all GET responses
result: pass
verified: "带 admin token 实测：GET /api/servers → 200 [], POST /api/servers/test-connection 带完整字段 → {success:false,message:'连接失败：Timed out...'}（测试主机不可达，符合预期）。UNIQUE(host,port) 约束、权限种子、角色分配均已落库"
verified_at: 2026-07-08

### 3. Navigation Sidebar — "运维" Group with "服务器管理" Tab
expected: Left sidebar shows "运维" group (renamed from "数据库运维"). Under it, "服务器管理" tab appears above "数据库管理". Clicking it navigates to the server management page.
result: pending

### 4. Server List Page — Shows Servers with Status Indicators
expected: Server list page shows table with columns: status badge (green/online, grey/offline, red/error, amber/unreachable), host (with label as secondary text), OS type badge (CentOS/Ubuntu/etc.), port number, action icons. Shows empty state "暂无服务器" when no servers exist. Page structure aligns with 数据库管理 (instances-db): card with toolbar (search box + right-aligned "+ 添加服务器" button), no page-level title.
result: pending

### 5. Add Server Dialog — All Form Fields
expected: Clicking "+ 添加服务器" opens dialog with: IP/主机名 (required), SSH端口 (default 22), 标签 (optional), 操作系统 dropdown (CentOS/Ubuntu/Debian/RHEL/Other), SSH认证方式 radio (密码/密钥), SSH用户名 (default "root"), 密码 or 私钥 textarea. Shows note "凭据将使用 AES-256-CBC 加密存储".
result: pending

### 6. Test Connection Button — Pass/Fail Result in Toast
expected: In add/edit dialog, "测试连接" button sends test to backend. On success shows toast "连接成功：{host}:{port}". On failure shows toast "连接失败：{error}". Simple pass/fail — no detailed SSH output.
result: pending

### 7. Edit Server Dialog
expected: Clicking edit icon on a server row opens dialog pre-filled with that server's data. Fields same as add dialog. Saving updates the server and refreshes list.
result: pending

### 8. Delete Server with Confirmation
expected: Clicking delete icon shows confirmation "确定要删除服务器 {host} 吗？此操作不可撤销。". Confirming deletes server and shows toast "服务器已删除". List refreshes without the deleted server.
result: pending

### 9. Key Rotation Dialog
expected: Clicking key rotation icon opens dialog with: new credential type selector (密码/密钥), new username input, new credential value input, confirm button. On success shows toast "密钥已轮换".
result: pending

### 10. English i18n Labels
expected: Switching UI to English shows: nav group "Operations" (or similar), tab "Servers", tab "Databases" for instances, subtitles accurate.
result: pending

### 11. Permission Control — Unauthorized Users Cannot Access Server Management
expected: Users without servers:view permission do not see the "服务器管理" tab. Users without servers:manage permission cannot create/edit/delete servers or test connections.
result: pending

## Summary

total: 11
passed: 1
issues: 1 (resolved — environment sync, not code defect)
pending: 9
skipped: 0

## Gaps

<!-- Issues found during review + UAT, all resolved. Documented for traceability. -->

- truth: "后端服务运行最新代码，迁移已执行"
  status: resolved
  reason: "UAT test 1: 后端进程 7月7日启动的旧版本无 /api/servers 路由（404）；迁移 019/020 未执行致 servers/server_metrics 表缺失（500）"
  severity: blocker
  test: 1
  root_cause: "环境未同步——代码和迁移文件都在仓库里，但运行时进程过期 + 数据库 schema 未跑迁移"
  resolution: "重启后端 + 执行迁移 019/020。验证 /api/servers → 200, /api/servers/metrics/summary → 200"
  artifacts: []

- truth: "服务器管理页结构对齐同级数据库管理页（instances-db）"
  status: resolved
  reason: "用户反馈：表格多了'服务器管理'标题；新建按钮风格/位置与数据库管理页不一致"
  severity: major
  test: 4
  root_cause: "124-02 实现偏离 PLAN——PLAN 写的是 .page__header 结构，实际用了 app-card slot=header + toolbar。verification 只 grep 组件引用，未校验页面骨架结构。review 无 UI 规范基准输入，未抓到"
  resolution: "对齐 instances-db（同级运维页）：去掉页面标题，用 div.card + div.toolbar（搜索框 + 右对齐 btn 添加按钮）。中间经历一次错误标杆（users-management 设置页）后纠正。commits: cd58e2b, 452277d, 89f3be2"
  artifacts:
    - path: "frontend/src/app/ui/views/servers-page.ts"
      issue: "RESOLVED — 结构对齐 instances-db"

- truth: "空状态页不出现重复的添加按钮"
  status: resolved
  reason: "用户反馈：空状态页有两个'+ 添加服务器'按钮"
  severity: major
  test: 4
  root_cause: "verification 阶段重构（手写 table → app-card/app-data-table）时，empty-state 分支同时保留了 toolbar 按钮和 app-empty-state slot=actions 按钮"
  resolution: "删除 empty-state slot=actions 按钮，仅保留 toolbar 按钮（后随结构对齐改为单一 toolbar 按钮）。commit: cd58e2b"
  artifacts:
    - path: "frontend/src/app/ui/views/servers-page.ts"
      issue: "RESOLVED — 单一添加按钮"

- truth: "tabs.server-detail i18n 键存在，面包屑显示可读标题"
  status: resolved
  reason: "code review --auto iteration 3 发现：server-detail Tab 类型有效但 tabs.server-detail 键缺失，t() 回退返回原始键字符串，面包屑显示 'tabs.server-detail'"
  severity: blocker
  test: 10
  root_cause: "server-detail Tab 由 Phase 125 引入，但 i18n 键未在 en.ts/zh-CN.ts 添加。titleForTab('server-detail') → t('tabs.server-detail') → 回退到原始键"
  resolution: "en.ts 加 'server-detail': 'Server Detail'，zh-CN.ts 加 'server-detail': '服务器详情'。commit: b672a8c"
  artifacts:
    - path: "frontend/src/app/i18n/locales/en.ts"
    - path: "frontend/src/app/i18n/locales/zh-CN.ts"
