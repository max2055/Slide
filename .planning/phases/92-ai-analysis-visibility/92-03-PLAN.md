---
phase: 92-ai-analysis-visibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/src/ai-analysis-config-service.ts
  - apps/db-ops-api/server.ts
autonomous: true
requirements: ["AI-01"]
user_setup: []

must_haves:
  truths:
    - "GET /api/ai/analysis/recent?instance_id=X&analysis_type=fault_diagnosis&limit=5 returns recent completed analyses"
    - "GET /api/ai/config returns current auto-analysis configuration from system_config"
    - "PUT /api/ai/config saves auto-analysis configuration to system_config"
    - "Auto-analysis config supports: enabled toggle, cron expression, severity filter, instance whitelist, time window"
    - "Config keys follow system_config table pattern with JSON-encoded values"
  artifacts:
    - path: "apps/db-ops-api/src/ai-analysis-config-service.ts"
      provides: "CRUD service for auto-analysis config in system_config table"
      min_lines: 80
    - path: "apps/db-ops-api/server.ts"
      provides: "Adds /api/ai/analysis/recent and /api/ai/config routes"
      exports: ["GET /api/ai/analysis/recent", "GET /api/ai/config", "PUT /api/ai/config"]
  key_links:
    - from: "server.ts"
      to: "ai-analysis-config-service.ts"
      via: "GET/PUT /api/ai/config handlers"
      pattern: "aiAnalysisConfigService"
    - from: "server.ts"
      to: "aiAnalysisDatabaseService.getAnalysisList()"
      via: "GET /api/ai/analysis/recent handler"
      pattern: "getAnalysisList"
---

<objective>
Create the auto-analysis configuration service and add API routes for recent diagnosis queries and config CRUD.

Purpose: Per D-14, the instance detail page needs a dedicated endpoint to query recent diagnoses by instance_id + analysis_type. Per D-15/D-16, auto-analysis scheduling configuration must be stored in system_config and exposed via API. These are independent backend concerns in Wave 1.

Output: ai-analysis-config-service.ts + extended server.ts with new routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-CONTEXT.md (D-14, D-15, D-16)
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-UI-SPEC.md (§5 Auto-Analysis Config Panel for field definitions)
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/ai-analysis-database-service.ts
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/server.ts (lines 1681-1840 for AI analysis routes; lines 140-157 for system_config query pattern)

<interfaces>
From ai-analysis-database-service.ts:
- getAnalysisList(options?: { analysis_type?, instance_id?, status?, related_id?, limit?, offset? }): Promise<AiAnalysisRecord[]>
  — Supports filtering by instance_id + analysis_type. Already has everything needed for the recent endpoint.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ai-analysis-config-service.ts</name>
  <files>
    apps/db-ops-api/src/ai-analysis-config-service.ts
  </files>
  <read_first>
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/ai-analysis-database-service.ts (follow the class + singleton pattern)
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-UI-SPEC.md (§5 for exact config field definitions)
  </read_first>
  <action>
    Create new file apps/db-ops-api/src/ai-analysis-config-service.ts following the same pattern as ai-analysis-database-service.ts.

    **Interface AiAnalysisConfig (matching UI-SPEC §5 form fields):**
    ```typescript
    interface AiAnalysisConfig {
      enabled: boolean;          // default: true — master toggle
      cronExpression: string;    // default: "*/30 * * * *"
      severityLevels: string[];  // default: ["critical", "error", "warning"]
      instanceWhitelist: number[]; // default: [] (empty = all instances)
      timeWindowStart: string;   // default: "00:00"
      timeWindowEnd: string;     // default: "23:59"
    }
    ```

    **DEFAULT_CONFIG** constant with values above.

    **Class AiAnalysisConfigService with methods:**

    1. `private getPool()` and `private isConnected()` — same pattern as ai-analysis-database-service.ts

    2. `async getConfig(): Promise<AiAnalysisConfig>`:
       - Query system_config WHERE config_key IN ('auto_analysis_config')
       - If row exists, parse config_value as JSON and merge with DEFAULT_CONFIG
       - If row does not exist, return DEFAULT_CONFIG
       - On error, return DEFAULT_CONFIG
       - SQL pattern: `SELECT config_key, config_value FROM system_config WHERE config_key = ?`
       - config_value is stored as JSON.stringify(configObject)

    3. `async saveConfig(config: AiAnalysisConfig): Promise<{ success: boolean; error?: string }>`:
       - Validate config:
         - enabled is boolean
         - cronExpression is non-empty string (do NOT validate cron format server-side — client-side validation only per UI-SPEC Edge Cases)
         - severityLevels is array of strings, each in ['critical', 'error', 'warning', 'info']
         - instanceWhitelist is array of numbers
         - timeWindowStart/timeWindowEnd are strings matching HH:MM format
       - Use INSERT ... ON DUPLICATE KEY UPDATE or REPLACE INTO system_config (config_key, config_value) VALUES ('auto_analysis_config', ?)
       - config_value = JSON.stringify(config)

    4. Export singleton: `export const aiAnalysisConfigService = new AiAnalysisConfigService();`

    Use mysql2/promise queries via pool.execute() same as ai-analysis-database-service.ts. Import { dbConnection } from './db-connection.js'.
  </action>
  <acceptance_criteria>
    - File exports AiAnalysisConfig interface with all 6 fields and their defaults
    - File exports aiAnalysisConfigService singleton
    - getConfig() reads from system_config WHERE config_key='auto_analysis_config', returns DEFAULT_CONFIG on missing/error
    - saveConfig() validates fields then writes with INSERT ... ON DUPLICATE KEY UPDATE
    - Config value stored as JSON.stringify(config)
    - Uses dbConnection.getPool() pattern for database access
  </acceptance_criteria>
  <verify>
    <automated>grep -q "AiAnalysisConfig" apps/db-ops-api/src/ai-analysis-config-service.ts && grep -q "getConfig\|saveConfig" apps/db-ops-api/src/ai-analysis-config-service.ts && grep -q "system_config" apps/db-ops-api/src/ai-analysis-config-service.ts && echo "Config service created"</automated>
  </verify>
  <done>
    ai-analysis-config-service.ts created with getConfig() and saveConfig() methods, stores in system_config table, validates input
  </done>
</task>

<task type="auto">
  <name>Task 2: Add API routes for recent diagnosis and config CRUD</name>
  <files>
    apps/db-ops-api/server.ts
  </files>
  <read_first>
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/server.ts (lines 1681-1840 for existing AI analysis routes — add new routes immediately after the existing GET /api/ai/analysis route)
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/ai-analysis-config-service.ts (just created — the service to import)
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/ai-analysis-database-service.ts (getAnalysisList method used by the recent endpoint)
  </read_first>
  <action>
    Add three new routes in server.ts, placed AFTER the existing `fastify.get('/api/ai/analysis', ...)` route (line ~1815) and BEFORE the existing `fastify.delete('/api/ai/analysis/:id', ...)` route (line ~1818).

    **Route 1: GET /api/ai/analysis/recent** (per D-14)
    - preHandler: [verifyToken, requirePermission('ai:view')]
    - Query params: instance_id (number, required), analysis_type (string, required), limit (number, optional, default 5)
    - Calls aiAnalysisDatabaseService.getAnalysisList({ instance_id, analysis_type, status: 'completed', limit })
    - Returns the array of records directly

    **Route 2: GET /api/ai/config** (per D-16)
    - preHandler: [verifyToken, requirePermission('ai:view')]
    - No params required
    - Calls aiAnalysisConfigService.getConfig()
    - Returns the config object

    **Route 3: PUT /api/ai/config** (per D-16)
    - preHandler: [verifyToken, requirePermission('ai:manage')]
    - Body: AiAnalysisConfig fields (enabled, cronExpression, severityLevels, instanceWhitelist, timeWindowStart, timeWindowEnd)
    - Calls aiAnalysisConfigService.saveConfig(body)
    - Returns { success: true, message: '配置已保存' }
    - On validation error returns 400 with error message

    **Imports needed:**
    - Add `import { aiAnalysisConfigService } from './src/ai-analysis-config-service.js';` near other imports

    **Important:**
    - Register the GET /api/ai/analysis/recent route BEFORE the existing GET /api/ai/analysis/:id route to avoid route conflict (the /recent path must be registered before /:id path in Fastify)
    - Actually, looking at the existing routes more carefully: GET /api/ai/analysis (line ~1797) is the list endpoint, GET /api/ai/analysis/:id (line ~1780) is the detail endpoint. Fastify routes are matched by specificity. The /recent route should be registered AFTER /api/ai/analysis/:id to avoid ambiguity, OR register it with full path before the parameterized one. Best practice: register the static /recent path AFTER the parameterized /:id route (since Fastify evaluates routes in order and static paths take precedence in some configurations). Let me look more carefully...

    Actually, in Fastify, routes are evaluated in order of registration. If /api/ai/analysis/:id matches "recent" as the id, that's a problem. To be safe:
    - Add GET /api/ai/analysis/recent BEFORE the GET /api/ai/analysis/:id route (so the static path takes precedence)
    - Move the existing GET /api/ai/analysis/:id route after it if needed

    For safety, place the new recent route immediately after the existing GET /api/ai/analysis list route (line ~1815). This is before the /:id route, so the static path /recent will be registered first and take priority.

    Wait, re-checking the server.ts structure:
    - Line 1755: GET /api/ai/analysis/:id/status
    - Line 1780: GET /api/ai/analysis/:id (detail)
    - Line 1797: GET /api/ai/analysis (list)

    The recent endpoint should be: GET /api/ai/analysis/recent
    But this would match /api/ai/analysis/:id with id="recent". 

    Best approach: register GET /api/ai/analysis/recent BEFORE GET /api/ai/analysis/:id. Since in Fastify routes are registered in order and the router matches the first registered handler, registering the static path first ensures it gets matched for "recent" requests, while the parameterized route catches everything else.

    So: Insert the new recent route right after the list endpoint at line ~1815, BEFORE the `preHandler` array close. Since the :id route at line 1780 is already registered BEFORE, there will be a conflict. 

    Actually, I need to restructure. The safest fix is to insert GET /api/ai/analysis/recent BEFORE the GET /api/ai/analysis/:id route (line 1780). Let me insert it right after the status route at ~1777.

    So the final order will be:
    1. POST /api/ai/analysis (1681)
    2. GET /api/ai/analysis/:id/status (1755)
    3. GET /api/ai/analysis/:id (1780)
    4. GET /api/ai/analysis/recent [NEW — insert here or before the :id route]
    5. GET /api/ai/analysis (1797)
    6. DELETE /api/ai/analysis/:id (1818)
    7. GET /api/ai/config [NEW]
    8. PUT /api/ai/config [NEW]

    Actually, let me just insert GET /api/ai/analysis/recent right before the existing GET /api/ai/analysis/:id. This way the static route is registered before the parameterized one.

    Final decision:
    - Insert GET /api/ai/analysis/recent at line ~1777 (after the status route, before the :id detail route)
    - Insert GET /api/ai/config and PUT /api/ai/config after line ~1830 (after all existing AI analysis routes)

    All routes use the same preHandler auth pattern as existing routes.
  </action>
  <acceptance_criteria>
    - GET /api/ai/analysis/recent registered before GET /api/ai/analysis/:id to avoid route conflict
    - GET /api/ai/analysis/recent accepts instance_id (required), analysis_type (required), limit (optional, default 5) query params
    - GET /api/ai/config returns config object from aiAnalysisConfigService.getConfig()
    - PUT /api/ai/config accepts config body, validates, and calls saveConfig()
    - All three routes have preHandler: [verifyToken, requirePermission('ai:view' or 'ai:manage')]
    - aiAnalysisConfigService imported at top of server.ts
    - No duplicate route registration errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "analysis/recent" apps/db-ops-api/server.ts && grep -q "/api/ai/config" apps/db-ops-api/server.ts && grep -q "aiAnalysisConfigService" apps/db-ops-api/server.ts && echo "Routes added"</automated>
  </verify>
  <done>
    3 new routes added: GET /api/ai/analysis/recent (recent diagnoses), GET /api/ai/config (read config), PUT /api/ai/config (save config)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Frontend writes auto-analysis config via PUT /api/ai/config — untrusted input from browser |
| Client -> API | GET /api/ai/analysis/recent accepts instance_id query param — potential injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04 | Tampering | PUT /api/ai/config | mitigate | Validate config fields in saveConfig(): enabled boolean, cronExpression non-empty string, severityLevels in allowed values, instanceWhitelist number array, timeWindowStart/End match HH:MM |
| T-92-05 | Information Disclosure | GET /api/ai/analysis/recent | mitigate | Requires ai:view permission via preHandler; instance_id is integer from query param (parameterized SQL in getAnalysisList) |
</threat_model>

<verification>
- [ ] ai-analysis-config-service.ts exports AiAnalysisConfig interface and singleton
- [ ] GET /api/ai/analysis/recent route registered
- [ ] GET /api/ai/config route registered
- [ ] PUT /api/ai/config route registered with validation
- [ ] Import added to server.ts
</verification>

<success_criteria>
- Config service reads/writes auto-analysis config to system_config table
- Recent diagnosis endpoint returns filtered results from aiAnalysisDatabaseService.getAnalysisList()
- Config endpoints enable frontend to manage auto-analysis settings
</success_criteria>

<output>
After completion, create `.planning/phases/92-ai-analysis-visibility/92-03-SUMMARY.md`
</output>
