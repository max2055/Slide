---
phase: 92-ai-analysis-visibility
plan: 04
type: execute
wave: 2
depends_on:
  - 92-02
  - 92-03
files_modified:
  - frontend/src/openclaw/ui/views/alerts.ts
autonomous: true
requirements: ["AI-01"]
user_setup: []

must_haves:
  truths:
    - "Alert list shows analysis status badge per alert: '已分析' (green), '分析中' (violet), '分析失败' (danger), or '—' (muted)"
    - "Clicking badge opens modal with full AI analysis result via ai-analysis-result component"
    - "_loadAnalyzedStatuses() logs errors via console.warn"
    - "Settings button opens config modal with toggle, cron, severity, whitelist, time window fields"
  artifacts:
    - path: "frontend/src/openclaw/ui/views/alerts.ts"
      provides: "Updated alert list with status badges, result modals, config panel"
      min_lines: 2400
  key_links:
    - from: "alerts.ts" to: "/api/ai/analysis" via: "_loadAnalyzedStatuses fetch" pattern: "api/ai/analysis"
    - from: "alerts.ts" to: "ai-analysis-result" via: "result modal" pattern: "ai-analysis-result"
    - from: "alerts.ts" to: "/api/ai/config" via: "config panel save" pattern: "api/ai/config"
---

<objective>
Update alerts.ts with analysis status badges (D-11), error logging (D-12), result modal (D-09), and auto-analysis config panel (D-15/D-16).

Output: Updated alerts.ts with badges, result modal, and config panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-CONTEXT.md (D-09, D-11, D-12, D-15, D-16)
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-UI-SPEC.md (§1 Badges, §3 Result Modal, §5 Config Panel)
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/frontend/src/openclaw/ui/views/alerts.ts (full file)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analysis status badges and error logging</name>
  <files>frontend/src/openclaw/ui/views/alerts.ts</files>
  <read_first>
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/frontend/src/openclaw/ui/views/alerts.ts
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-UI-SPEC.md §1
  </read_first>
  <action>
    **1. Change line 669**: replace `analyzedAlertIds: Set<number>` with `analyzedStatuses: Map<number, { status: string; trigger_type: string; result?: any }>`

    **2. Update _loadAnalyzedStatuses()** (lines 824-837):
    - Change `this.analyzedAlertIds = ids` to store Map entries with `{ status: r.status, trigger_type: r.trigger_type, result: r.result }`
    - Add `console.warn('[Alerts] _loadAnalyzedStatuses failed:', err)` in catch block per D-12

    **3. Add state**: `@state() private activeAnalysisRecord: { alert: Alert; record: any } | null = null;`

    **4. Replace inline "分析状态" column** (lines 979-992): replace the entire td content with `this._renderAnalysisBadge(alert.id)`

    **5. Add _renderAnalysisBadge(alertId) method**: check analyzedStatuses map and activeRCAAnalysis for the alert. Return:
    - Running active analysis: `<span class="analysis-badge analysis-badge--running">分析中</span>`
    - Record completed: `<span class="analysis-badge analysis-badge--completed" @click=...>已分析</span>`
    - Record failed: `<span class="analysis-badge analysis-badge--failed" @click=...>分析失败</span>`
    - No record: muted em dash "—"
    - Colors from UI-SPEC §1: completed=#22c55e, running=#d2befc, failed=#b08df5

    **6. Add _openAnalysisResult(alertId)**: look up alert + record, set activeAnalysisRecord

    **7. Add CSS** for .analysis-badge, .analysis-badge--completed, .analysis-badge--running, .analysis-badge--failed with correct colors per UI-SPEC

    **8. Update line 956**: change `analyzedAlertIds.has(alert.id)` to `analyzedStatuses.has(alert.id)`
  </action>
  <acceptance_criteria>
    - analyzedStatuses is Map replacing analyzedAlertIds Set
    - _loadAnalyzedStatuses catch block has console.warn
    - _renderAnalysisBadge exists with 4 states
    - CSS has .analysis-badge variants with UI-SPEC colors
    - Completed/failed badges are clickable with _openAnalysisResult
  </acceptance_criteria>
  <verify>
    <automated>grep -q "analyzedStatuses\|analyzedAlertIds" alerts.ts | head -1 | grep -q Map; grep -q "console.warn.*_loadAnalyzedStatuses" alerts.ts; grep -q "_renderAnalysisBadge\|analysis-badge--completed\|analysis-badge--running\|analysis-badge--failed" alerts.ts; echo OK</automated>
  </verify>
  <done>Status badges and error logging implemented in alerts.ts per D-11/D-12</done>
</task>

<task type="auto">
  <name>Task 2: Analysis result modal and config panel</name>
  <files>frontend/src/openclaw/ui/views/alerts.ts</files>
  <read_first>
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-UI-SPEC.md (§3 Result Modal, §5 Config Panel)
  </read_first>
  <action>
    **A. _renderAnalysisResultModal()**: renders when activeAnalysisRecord is set. Uses `<ai-analysis-result>` with result/analysisType/triggerType/status/errorMessage properties. 720px wide modal, overlay, close via X/backdrop/ESC.

    **B. Config panel state and methods**:
    - Add `@state() private showConfigPanel = false;`
    - Add `@state() private configForm = { enabled: true, cronExpression: '*/30 * * * *', severityLevels: ['critical','error','warning'], instanceWhitelist: [], timeWindowStart: '00:00', timeWindowEnd: '23:59' };`
    - _openConfigPanel(): GET /api/ai/config, populate configForm, set showConfigPanel=true
    - _saveConfig(): PUT /api/ai/config with configForm, show inline feedback
    - _closeConfigPanel(): set showConfigPanel=false

    **C. _renderConfigPanel()**: modal 640px wide with form fields per UI-SPEC §5:
    1. Enable toggle (checkbox styled as toggle)
    2. Cron expression input with help text
    3. Severity checkbox group (critical/error/warning/info)
    4. Instance whitelist (text input for comma-separated IDs + tag pills)
    5. Time window (two time inputs)
    6. Action buttons: "恢复默认" + "保存配置" (.btn.primary)

    **D. Integrate**: add settings button in toolbar next to refresh; call _renderAnalysisResultModal() and _renderConfigPanel() in render().
  </action>
  <acceptance_criteria>
    - _renderAnalysisResultModal() uses ai-analysis-result component
    - _renderConfigPanel() has all 5 form fields + save/reset buttons
    - Settings button exists in toolbar
    - Save calls PUT /api/ai/config
    - Modals close on X and backdrop
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ai-analysis-result\|showConfigPanel\|configForm\|activeAnalysisRecord" alerts.ts; grep -q "/api/ai/config" alerts.ts; echo OK</automated>
  </verify>
  <done>Result modal and config panel implemented in alerts.ts per D-09/D-15/D-16</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries: Client->API (config save)

## STRIDE Register
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-92-06 | Tampering | config panel | mitigate | Server-side validation in ai-analysis-config-service |
</threat_model>

<verification>
- [ ] analyzedStatuses Map replaces analyzedAlertIds Set
- [ ] _loadAnalyzedStatuses has console.warn
- [ ] Badge CSS with UI-SPEC colors
- [ ] Result modal uses ai-analysis-result component
- [ ] Config panel with all fields
</verification>

<success_criteria>
- Status badges for each analysis state in alert list
- Clickable badges open result modal
- Config panel manages auto-analysis settings
</success_criteria>

<output>
After completion, create .planning/phases/92-ai-analysis-visibility/92-04-SUMMARY.md
</output>
