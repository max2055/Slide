---
phase: 92-ai-analysis-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/src/tools/generated/slide-self-mgmt/complete_analysis.ts
  - apps/db-ops-api/src/skills/generated/alert-rca/SKILL.md
  - apps/db-ops-api/src/skills/generated/fault-diagnosis/SKILL.md
  - apps/db-ops-api/src/skills/generated/topsql-analysis/SKILL.md
autonomous: true
requirements: ["AI-01"]
user_setup: []

must_haves:
  truths:
    - "Agent can save Markdown analysis results via slide_complete_analysis tool"
    - "Agent has skill files that define analysis workflows for alerts, faults, and TopSQL"
    - "Each analysis type has a consistent Markdown output format with ## headings"
    - "Agent calls slide_complete_analysis at the end of each analysis workflow"
  artifacts:
    - path: "apps/db-ops-api/src/tools/generated/slide-self-mgmt/complete_analysis.ts"
      provides: "slide_complete_analysis tool accepting { analysisId, markdown }"
      min_lines: 40
    - path: "apps/db-ops-api/src/skills/generated/alert-rca/SKILL.md"
      provides: "Alert RCA skill definition for Agent"
      min_lines: 30
    - path: "apps/db-ops-api/src/skills/generated/fault-diagnosis/SKILL.md"
      provides: "Fault diagnosis skill definition for Agent"
      min_lines: 30
    - path: "apps/db-ops-api/src/skills/generated/topsql-analysis/SKILL.md"
      provides: "TopSQL analysis skill definition for Agent"
      min_lines: 30
  key_links:
    - from: "complete_analysis.ts"
      to: "aiAnalysisDatabaseService.completeAnalysis()"
      via: "call in handler"
      pattern: "completeAnalysis"
    - from: "alert-rca/SKILL.md"
      to: "slide_complete_analysis"
      via: "final tool call in workflow"
      pattern: "slide_complete_analysis"
    - from: "complete_analysis.ts"
      to: "toolCatalog.register()"
      via: "module-level registration"
      pattern: "toolCatalog.register"
---

<objective>
Rewrite the slide_complete_analysis tool to accept Markdown format and create Agent skill files that define analysis workflows for alerts, fault diagnosis, and TopSQL analysis.

Purpose: Per D-01 through D-06, the slide_complete_analysis tool must accept { analysisId, markdown } instead of structured JSON, so Agent's natural Markdown output can be persisted. Three SKILL.md files instruct the Agent on which tools to call and what output format to use.

Output: Working slide_complete_analysis tool + 3 skill files under skills/generated/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-CONTEXT.md
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-RESEARCH.md
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/tools/types.ts
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/tools/generated/slide-self-mgmt/index.ts
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/ai-analysis-database-service.ts
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/skills/generated/check_health/SKILL.md

<interfaces>
From types.ts — AnyAgentTool interface used by complete_analysis.ts:
- name: string — tool name (unique)
- description: string — tool description
- parameters: ToolParameterSchema — JSON Schema object
- handler: ToolHandler — async handler returning ToolResult

From ai-analysis-database-service.ts:
- completeAnalysis(analysisId: number, data: { result: any; usage?: any; duration_ms?: number }): Promise<{ success: boolean; error?: string }>
  — When called with data.result being a Markdown string, it stores via JSON.stringify(data.result)

From index.ts (tool export):
- completeAnalysisTool is exported as named export AND included in slideSelfMgmtTools array
- No changes needed to index.ts — the tool is already registered
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite slide_complete_analysis tool to accept Markdown</name>
  <files>
    apps/db-ops-api/src/tools/generated/slide-self-mgmt/complete_analysis.ts
  </files>
  <read_first>
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/tools/generated/slide-self-mgmt/complete_analysis.ts (current file to be rewritten)
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/ai-analysis-database-service.ts (service interface)
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/tools/types.ts (AnyAgentTool interface)
  </read_first>
  <action>
    Rewrite complete_analysis.ts per D-01, D-02, D-06:

    1. CHANGE the parameters schema from having { analysisId, summary, findings[], recommendations[] } to only { analysisId: number, markdown: string }. Both are required. Drop summary, findings, recommendations parameters entirely.

    2. REWRITE the handler:
       - Receive { analysisId, markdown } from args
       - Call aiAnalysisDatabaseService.completeAnalysis(typedArgs.analysisId, { result: typedArgs.markdown })
       - The markdown string is passed directly as the result. The service does JSON.stringify(result) internally, which stores the Markdown string as a JSON-escaped string in the DB.
       - On success return { success: true, data: { saved: true, analysisId }, summary: '分析结果已保存' }
       - On error return { success: false, error: '保存分析结果失败: ' + error.message }

    3. KEEP all other fields unchanged: name 'slide_complete_analysis', description, group 'db_ops', toolCatalog.register() call.

    4. DO NOT change the export name 'completeAnalysisTool' — it is imported by index.ts.

    The ToolParameterSchema does not have a nested `type` property for items when type is 'string'. For the markdown parameter use { type: 'string', description: '...' }.
  </action>
  <acceptance_criteria>
    - complete_analysis.ts has parameters with only analysisId (number) and markdown (string) as required fields
    - Handler calls aiAnalysisDatabaseService.completeAnalysis(analysisId, { result: markdown })
    - No references to summary, findings, recommendations parameters
    - toolCatalog.register(completeAnalysisTool) is still called
    - completeAnalysisTool is still exported as named export
    - File compiles without TypeScript errors (npx tsc --noEmit passes for this file)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "analysisId.*markdown" apps/db-ops-api/src/tools/generated/slide-self-mgmt/complete_analysis.ts && grep -q "completeAnalysis" apps/db-ops-api/src/tools/generated/slide-self-mgmt/complete_analysis.ts && grep -c "summary\|findings\|recommendations" apps/db-ops-api/src/tools/generated/slide-self-mgmt/complete_analysis.ts | head -1 | xargs test 1 -eq || echo "Only 'markdown' should remain"</automated>
  </verify>
  <done>
    complete_analysis.ts rewritten — accepts { analysisId, markdown }, calls completeAnalysis() with markdown as result, registered with toolCatalog, compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 3 Agent skill files</name>
  <files>
    apps/db-ops-api/src/skills/generated/alert-rca/SKILL.md
    apps/db-ops-api/src/skills/generated/fault-diagnosis/SKILL.md
    apps/db-ops-api/src/skills/generated/topsql-analysis/SKILL.md
  </files>
  <read_first>
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/apps/db-ops-api/src/skills/generated/check_health/SKILL.md (existing pattern)
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-RESEARCH.md (Pattern 2: Existing Skill File Format)
  </read_first>
  <action>
    Create three SKILL.md files under new directories. Follow the exact YAML frontmatter + Markdown body pattern from check_health/SKILL.md.

    Create directories:
    - apps/db-ops-api/src/skills/generated/alert-rca/
    - apps/db-ops-api/src/skills/generated/fault-diagnosis/
    - apps/db-ops-api/src/skills/generated/topsql-analysis/
    Create SKILL.md in each.

    **alert-rca/SKILL.md** (per D-04/D-05):
    - YAML frontmatter: name: alert-rca, description: AI-powered alert root cause analysis using database metrics, metadata.openclaw.emoji: alarm clock emoji
    - Body: # Alert Root Cause Analysis
    - ## Tool Flow: 1. db_health_check 2. db_performance_analysis 3. db_slow_queries
    - ## Output Format with ## Markdown headings:
      - ## 分析摘要 (brief summary)
      - ## 根因分析 (root causes as list)
      - ## 建议操作 (recommendations)
      - ## 关键指标 (key metrics in table)
    - ## Completion: Call slide_complete_analysis with analysisId and full Markdown

    **fault-diagnosis/SKILL.md**:
    - name: fault-diagnosis, description: AI-powered database instance fault diagnosis
    - Body: similar structure with ## headings:
      - ## 诊断概述
      - ## 问题分析
      - ## 修复步骤
      - ## 验证建议
    - Tool flow includes db_health_check, db_active_sessions, db_performance_analysis
    - ## Completion: Call slide_complete_analysis

    **topsql-analysis/SKILL.md**:
    - name: topsql-analysis, description: AI-powered TopSQL analysis for query performance optimization
    - Body: similar structure with ## headings:
      - ## SQL 概述
      - ## 性能分析
      - ## 优化建议
      - ## 预期收益
    - Tool flow includes db_slow_queries, db_performance_analysis, db_health_check
    - ## Completion: Call slide_complete_analysis

    All three skills follow the same template: YAML frontmatter, tool flow, output format with ## Markdown section headings, final call to slide_complete_analysis with analysisId.
  </action>
  <acceptance_criteria>
    - Three directories exist: alert-rca/, fault-diagnosis/, topsql-analysis/ under skills/generated/
    - Each contains a SKILL.md with valid YAML frontmatter (name, description, metadata.openclaw.emoji)
    - Each SKILL.md body has ## Tool Flow, ## Output Format (with ## headings per CONTEXT.md D-05), and ## Completion sections
    - Each mentions slide_complete_analysis as the final save step
    - Each has at least 30 lines
  </acceptance_criteria>
  <verify>
    <automated>for f in alert-rca fault-diagnosis topsql-analysis; do test -f "apps/db-ops-api/src/skills/generated/$f/SKILL.md" && grep -q "slide_complete_analysis" "apps/db-ops-api/src/skills/generated/$f/SKILL.md" || exit 1; done && echo "All 3 skills created"</automated>
  </verify>
  <done>
    3 skill files created — each defines tool call flow, Markdown output format, and final slide_complete_analysis call
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Tool | Agent invokes slide_complete_analysis tool with user-provided data; markdown content originates from LLM output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Spoofing | slide_complete_analysis | mitigate | Tool already requires Agent authentication via Gateway session — no direct user invocation |
| T-92-02 | Tampering | SKILL.md files | accept | Skill files are server-side configuration, only editable via filesystem; read-only at runtime |
</threat_model>

<verification>
- [ ] complete_analysis.ts tool accepts { analysisId, markdown } parameters
- [ ] 3 SKILL.md files exist with proper YAML frontmatter and section structure
</verification>

<success_criteria>
- slide_complete_analysis tool stores Markdown content via aiAnalysisDatabaseService.completeAnalysis()
- Agent has skill definitions to guide analysis workflows for alerts, faults, and TopSQL
- All three skills end with a call to slide_complete_analysis to persist results
</success_criteria>

<output>
After completion, create `.planning/phases/92-ai-analysis-visibility/92-01-SUMMARY.md`
</output>
