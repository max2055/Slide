---
phase: 92-ai-analysis-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/openclaw/ui/views/ai-analysis-result.ts
autonomous: true
requirements: ["AI-01"]
user_setup: []

must_haves:
  truths:
    - "ai-analysis-result component receives result + analysisType as Lit properties (not internal API calls)"
    - "Component renders Markdown content using marked library"
    - "Component shows trigger source tag ('自动分析' / '手动分析') per D-10"
    - "Component handles loading, error, and empty states"
    - "Component uses 'token' key (not 'slide_token')"
    - "Markdown output is sanitized to prevent XSS"
    - "Component renders differently per analysisType (alert_rca, fault_diagnosis, topsql)"
  artifacts:
    - path: "frontend/src/openclaw/ui/views/ai-analysis-result.ts"
      provides: "Data-driven Lit Web Component with @property() interface"
      min_lines: 200
  key_links:
    - from: "ai-analysis-result.ts"
      to: "marked library"
      via: "marked.parse() for Markdown rendering"
      pattern: "marked.parse"
    - from: "ai-analysis-result.ts"
      to: "consumers (alerts.ts, instance-detail.ts)"
      via: "Lit properties (result, analysisType, triggerType, status, loading)"
      pattern: "@property"
---

<objective>
Refactor ai-analysis-result.ts from a self-contained analysis launcher into a data-driven presentational component per D-07 through D-10.

Purpose: The current component manages its own API calls, polling, and token storage. Per D-07 it must accept data via Lit properties so parent components (alerts.ts, instance-detail.ts) control data fetching. Also fix the "slide_token" bug (Research Pitfall 2) and add XSS sanitization.

Output: A reusable, data-driven <ai-analysis-result> Lit Web Component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-CONTEXT.md (D-07, D-08, D-09, D-10)
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-UI-SPEC.md (§2 Unified ai-analysis-result Lit Web Component)
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-RESEARCH.md (Pitfall 2: Token Key Inconsistency, Pitfall 5: Null-Safe Handling)
@/Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/frontend/src/openclaw/ui/views/ai-analysis-result.ts (current file — must read before editing)

<interfaces>
Current ai-analysis-result.ts exports: AIAnalysisResult class (LitElement)
Current props (all @state, all private): analysisId, status, result, errorMessage, pollTimer
New props (all @property, public):
- result: string | null = null
- analysisType: string = "alert_rca"
- triggerType: string = "manual"
- loading: boolean = false
- status: string = "completed"
- errorMessage: string | null = null
- title: string = "AI 分析结果"

Methods to REMOVE: startAnalysis(), _startPolling(), _stopPolling(), _reset()
Methods to ADD: none (pure presentational)

From UI-SPEC §2:
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ai-analysis-result.ts to data-driven component</name>
  <files>
    frontend/src/openclaw/ui/views/ai-analysis-result.ts
  </files>
  <read_first>
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/frontend/src/openclaw/ui/views/ai-analysis-result.ts (current file — read entirely before editing)
    /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/.planning/phases/92-ai-analysis-visibility/92-UI-SPEC.md (especially §2 and §3 for render specifications)
  </read_first>
  <action>
    Full rewrite of ai-analysis-result.ts. Changes described in order of importance:

    **1. Change imports and decorators:**
    - Remove `state` from lit/decorators.js import
    - Add `property` from lit/decorators.js
    - Keep `LitElement, html, css` from lit and `customElement` from decorators
    - Keep `Marked` import from "marked"

    **2. Remove API_BASE constant and getToken() function** — component no longer calls APIs

    **3. Refactor marked initialization:**
    - Create `const marked = new Marked()` at module level (keep existing)
    - If marked.parse supports gfm and breaks options, configure them

    **4. Add XSS sanitization function:**
    - Create `function sanitize(html: string): string` that strips <script>, <iframe>, on* event handlers
    - Minimal approach: use regex to remove <script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script> tags and on\w+= attributes
    - NOTE: do NOT use DOMPurify (not in project dependencies). The esc() pattern from instance-detail.ts is the baseline.

    **5. Redefine the component class with @property() for all inputs:**
    @property({ type: String }) result: string | null = null;
    @property({ type: String }) analysisType: string = "alert_rca";
    @property({ type: String }) triggerType: string = "manual";
    @property({ type: Boolean }) loading: boolean = false;
    @property({ type: String }) status: string = "completed";
    @property({ type: String }) errorMessage: string | null = null;
    @property({ type: String }) title: string = "AI 分析结果";

    **6. Remove all of these:**
    - analysisId, pollTimer, private fields
    - startAnalysis(), _startPolling(), _stopPolling(), _reset() methods
    - disconnectedCallback override (no polling to clean up)

    **7. Keep and update render() method with the following render states:**
    - loading=true → spinner + "AI 分析中，请稍候..."
    - status="running" → animated pulse + "分析中..." (reuse loading state, different text)
    - status="failed" → red error card with error icon + errorMessage
    - status="completed" + result=null → muted text: "分析完成，但暂无结果数据"
    - status="completed" + result string → rendered Markdown via sanitize(marked.parse(result))
    - status="completed" + result object → existing JSON renderer for backward compat

    **8. Add trigger source tag:**
    - Position: top-right of card header, next to close button
    - triggerType="auto" → small pill "自动分析" with --accent-subtle background
    - triggerType="manual" → small pill "手动分析" with --info-subtle background
    - Use UI-SPEC color tokens from §Color Contract

    **9. Add source tag styling from UI-SPEC §Color Contract:**
    - .source-tag.auto → background: rgba(210,190,252,0.15), color: #d2befc
    - .source-tag.manual → background: rgba(59,130,246,0.12), color: #3b82f6

    **10. Markdown rendering rules (from UI-SPEC §2):**
    - Use `marked.parse(result, { async: false })` to convert Markdown to HTML
    - Wrap output in `<div class="result-content">`
    - Apply sanitize() to marked output before inserting
    - Code blocks: --mono font, --bg-elevated background, overflow-x: auto
    - Tables: border-collapse, 100% width, bordered cells with --border
    - Headings: ## -> section headers (--text-md weight 600), ### -> subsection headers

    **11. Null-safe numeric handling (Research Pitfall 5):**
    - In the JSON backward-compat renderer, use `(value ?? 0).toFixed(n)` for numeric formatting

    **12. CSS updates:**
    - Keep existing .card, .card-header, .card-body, .loading-state, .spinner, .error-state, .result-content, .btn styles
    - Add .source-tag class
    - Add .source-tag.auto, .source-tag.manual styles per UI-SPEC colors
    - Add table styling for Markdown-rendered tables
    - Add code block styling
    - Add .result-content h2, h3, ul, ol styles using UI-SPEC typography tokens

    **13. Preserve custom element registration:**
    - Keep `if (!customElements.get("ai-analysis-result"))` registration guard
  </action>
  <acceptance_criteria>
    - Component uses @property() decorators (not @state()) for all 7 input properties
    - No analysisId field, no pollTimer field
    - No startAnalysis(), _startPolling(), _stopPolling(), _reset() methods
    - No getToken() function or "slide_token" string
    - No API_BASE constant
    - Component renders 6 states: loading, running, failed, completed+null, completed+Markdown, completed+JSON
    - Sanitization function exists and strips script/iframe tags and on* handlers
    - Source tag renders for both auto and manual trigger types
    - Table and code block styles included in CSS
    - Null-safe pattern `(value ?? 0)` used in JSON renderer
    - customElements registration guard preserved
  </acceptance_criteria>
  <verify>
    <automated>grep -q "@property" frontend/src/openclaw/ui/views/ai-analysis-result.ts && grep -q "marked.parse" frontend/src/openclaw/ui/views/ai-analysis-result.ts && ! grep -q "slide_token" frontend/src/openclaw/ui/views/ai-analysis-result.ts && ! grep -q "startAnalysis" frontend/src/openclaw/ui/views/ai-analysis-result.ts && grep -q "sanitize" frontend/src/openclaw/ui/views/ai-analysis-result.ts && grep -q "source-tag" frontend/src/openclaw/ui/views/ai-analysis-result.ts && echo "Component refactor verified"</automated>
  </verify>
  <done>
    ai-analysis-result.ts is data-driven — accepts result, analysisType, triggerType, loading, status, errorMessage, title as @property(); has 6 render states; uses "token" key; has XSS sanitization; shows source tags; handles all edge cases from UI-SPEC
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM -> Frontend | Agent-generated Markdown content rendered by marked library — untrusted LLM output displayed in browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03 | Tampering | ai-analysis-result.ts | mitigate | Sanitize marked() output with sanitize() function before innerHTML assignment — strips script/iframe/on* per UI-SPEC Edge Cases |
</threat_model>

<verification>
- [ ] ai-analysis-result.ts uses @property() decorators for all 7 public inputs
- [ ] No "slide_token" reference exists (use grep to verify)
- [ ] No startAnalysis() method exists
- [ ] marked.parse() + sanitize() used for Markdown rendering
- [ ] Source tag (auto/manual) renders in card header
- [ ] 6 render states are implemented
</verification>

<success_criteria>
- ai-analysis-result.ts is a data-driven component usable by any parent via Lit properties
- Token key fixed to "token" matching project convention
- XSS sanitization applied to all rendered Markdown
- All 6 render states (loading, running, failed, completed+null, completed+Markdown, completed+JSON) work
</success_criteria>

<output>
After completion, create `.planning/phases/92-ai-analysis-visibility/92-02-SUMMARY.md`
</output>
