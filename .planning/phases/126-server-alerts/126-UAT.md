---
status: complete
phase: 126-server-alerts
source: 126-01-SUMMARY.md
started: 2026-07-08T23:55:00Z
updated: 2026-07-09T00:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: 重启后端无 `server_id`/`target_type` SQL 错误。
result: pass
verified: "0 Unknown column errors. Migration 021 applied manually + auto-registration fixed (205c65f)."

### 2. Migration 021 — Schema Verification
expected: alert_rules 有 target_type/server_id 列，alerts/alert_events 有 server_id 列。
result: pass
verified: "information_schema confirms all 3 tables have new columns."

### 3. Alert Rule — Server-Scoped Creation
expected: 通过 API 创建 target_type='server' 的告警规则，DB 中正确存储。
result: issue
reported: "API 创建规则后 target_type 始终为 instance，server_id 始终为 null"
severity: blocker
root_cause: "POST/PUT /api/alert-rules 路由未传递 target_type/server_id 到 createAlertRule/updateAlertRule"
fix: "d39e7dc — API 路由补传 target_type/server_id"
reverified: pass — 规则 120 在 DB 中 target_type='server', server_id=3

### 4. Server Alert — Metric Threshold Evaluation
expected: 服务器 CPU > 10%（实际 12.8%）触发 server-scoped 告警。
result: pass
verified: "端到端通过：创建规则 120 → 触发评估 → 生成告警 95793 [WARNING] UAT-E2E-CPU阈值 - 192.168.64.5"
log: "[ServerAlertEvaluator] Created alert: [WARNING] UAT-E2E-CPU阈值 - 192.168.64.5 (server #3)"

### 5. Server Unreachable Detection
expected: 服务器 unreachable >10min 时生成告警。
result: skipped
reason: 当前服务器状态为 online，无法在测试环境触发。checkUnreachable() 代码逻辑已验证正确。

### 6. Server Alert Dedup
expected: findActiveServerAlert() 检测重复告警。
result: pass
verified: "再次触发评估时，对已有活跃告警执行 touch 而非新建。日志显示 dedup 逻辑正常。"

### 7. Alert Engine Integration
expected: ServerAlertEvaluator 在 alert engine cron 循环中正常调用。
result: pass
verified: "Zero Unknown column errors at startup. Manual evaluate triggered 1 server rule evaluation."

## Summary

total: 7
passed: 5
issues: 1 (fixed)
pending: 0
skipped: 1

## Gaps

- truth: "创建 server-scoped 告警规则应正确存储 target_type/server_id"
  status: fixed
  reason: "POST/PUT /api/alert-rules 路由未传递 target_type/server_id 参数"
  severity: blocker
  test: 3
  root_cause: "server.ts API routes missing target_type/server_id in the data payload passed to createAlertRule/updateAlertRule"
  fix: "d39e7dc"
