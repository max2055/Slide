---
phase: 102-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 102-01
files_modified:
  - frontend/src/openclaw/ui/views/query-analysis-tab.ts
  - frontend/src/openclaw/ui/views/instance-detail.ts
  - frontend/src/openclaw/ui/views/event-management.ts
autonomous: false
requirements:
  - UI-05
user_setup: []

must_haves:
  truths:
    - "Structural emoji characters in views are replaced with renderIcon() calls"
    - "Status-message emoji (textual checkmark/cross/warning) are preserved"
    - "Build succeeds with all changes from Plan 1, Plan 2, and Plan 3 combined"
  artifacts:
    - path: "frontend/src/openclaw/ui/views/query-analysis-tab.ts"
      provides: "Tab title emoji replaced: 🔍 -> renderIcon('search'), 📊 -> renderIcon('bar-chart')"
      contains: "renderIcon('search')"
    - path: "frontend/src/openclaw/ui/views/instance-detail.ts"
      provides: "Capacity section emoji replaced: 📦 -> renderIcon('package')"
      contains: "renderIcon('package')"
    - path: "frontend/src/openclaw/ui/views/event-management.ts"
      provides: "Empty state emoji replaced: ⚠️ -> renderIcon('triangle-alert')"
      contains: "renderIcon('triangle-alert')"
  key_links:
    - from: "query-analysis-tab.ts"
      to: "frontend/src/icons.ts"
      via: "renderIcon('search')"
    - from: "instance-detail.ts"
      to: "frontend/src/icons.ts"
      via: "renderIcon('package')"
    - from: "event-management.ts"
      to: "frontend/src/icons.ts"
      via: "renderIcon('triangle-alert')"
---

<objective>
Replace 4 structural emoji characters with `renderIcon()` calls from the shared icon file.

Purpose: Eliminate emoji-as-icon anti-pattern, ensuring consistent styling, theming, and accessibility across all UI elements.

Output: 3 view files modified with emoji replaced by styled SVG icons; human verification of all 6 stat-card views + login page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-UI统一/102-CONTEXT.md
@.planning/phases/102-UI统一/102-RESEARCH.md
@.planning/phases/102-UI统一/102-UI-SPEC.md
@.planning/phases/102-UI统一/102-01-PLAN.md
@.planning/phases/102-UI统一/102-02-PLAN.md

<interfaces>
renderIcon() signature (from icons.ts):
```typescript
export function renderIcon(name: IconName, className = 'icon'): TemplateResult {
  return html`<span class="${className}" aria-hidden="true">${icons[name]}</span>`;
}
```

Import pattern (from Plan 1):
```typescript
import { icons, renderIcon } from '../../icons.js';
```

Emoji replacement map (per D-16, D-17):
| Location | Emoji | Replacement |
|----------|-------|-------------|
| query-analysis-tab.ts:191 | 🔍 | `renderIcon('search')` |
| query-analysis-tab.ts:227 | 📊 | `renderIcon('bar-chart')` |
| instance-detail.ts:2191 | 📦 | `renderIcon('package')` |
| event-management.ts:754 | ⚠️ | `renderIcon('triangle-alert')` |

Preserved emoji (per D-17): ✅ ❌ ⚠️ in textual messages like "连接成功"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 4 structural emoji with renderIcon() calls</name>
  <files>
  frontend/src/openclaw/ui/views/query-analysis-tab.ts
  frontend/src/openclaw/ui/views/instance-detail.ts
  frontend/src/openclaw/ui/views/event-management.ts
  </files>
  <read_first>
  @/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/query-analysis-tab.ts
  @/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/instance-detail.ts
  @/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/event-management.ts
  </read_first>

  <action>
    For each of the 3 files, make the following replacements. Then update the import statement to include `renderIcon`.

    **1. query-analysis-tab.ts:**
    - Line ~191: Change `🔍 查询指纹分析` to `${renderIcon('search')} 查询指纹分析`
    - Line ~227: Change `📊 执行计划` to `${renderIcon('bar-chart')} 执行计划`
    - Update import: add `renderIcon` to the icons import:
      `import { icons, renderIcon } from "../../icons.js"`
      (Note: This file may already have import updated from Plan 1; just verify and add `renderIcon` to the destructuring.)

    **2. instance-detail.ts:**
    - Line ~2191: Change `<div class="capacity-icon">📦</div>` to `<div class="capacity-icon">${renderIcon('package')}</div>`
    - Note: `package` icon must exist in icons.ts. If it does not, check if `package` or a similar icon exists (Lucide has a `package` icon with a box graphic). If not present, add it to `icons.ts` with proper SVG attributes.
    - Update import: add `renderIcon` to the import.

    **3. event-management.ts:**
    - Line ~754: Change `<div class="empty-icon">⚠️</div>` to `<div class="empty-icon">${renderIcon('triangle-alert')}</div>`
    - Update import: add `renderIcon` to the import.

    **4. Verify status-message emoji are preserved (per D-17):**
    After all replacements, grep for emoji characters across all view files. Only these patterns are allowed:
    - `✅` in text like "连接成功", "操作成功"
    - `❌` in text like "连接失败"
    - `⚠️` in text like "警告"
    These are textual status indicators, NOT icon replacements.

    For this check, do `grep -rn '[🔍📊📦]' frontend/src/openclaw/ui/views/ --include='*.ts'` after the changes — it should return empty.
    The ⚠️ emoji check is trickier since it's also used in status messages. Verify only that the structural ⚠️ at event-management.ts:754 is replaced, while textual ⚠️ in status messages remains.
  </action>

  <verify>
    <automated>
      cd /Users/max/Coding/39-Slide/frontend && \
      echo "=== Verify emoji replacements ===" && \
      grep -c "renderIcon.*search" src/openclaw/ui/views/query-analysis-tab.ts && \
      grep -c "renderIcon.*bar-chart" src/openclaw/ui/views/query-analysis-tab.ts && \
      grep -c "renderIcon.*package" src/openclaw/ui/views/instance-detail.ts && \
      grep -c "renderIcon.*triangle-alert" src/openclaw/ui/views/event-management.ts && \
      echo "=== Verify structural emoji removed ===" && \
      # Note: grep with literal emoji chars — these specific structural ones should be gone
      grep -n "🔍\|📊" src/openclaw/ui/views/query-analysis-tab.ts && \
      echo "WARNING: structural emoji still in query-analysis-tab.ts (if no output above, PASS)" || \
      echo "PASS: structural emoji removed" && \
      echo "=== Verify imports include renderIcon ===" && \
      grep "renderIcon" src/openclaw/ui/views/query-analysis-tab.ts && \
      grep "renderIcon" src/openclaw/ui/views/instance-detail.ts && \
      grep "renderIcon" src/openclaw/ui/views/event-management.ts
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "renderIcon.*search" frontend/src/openclaw/ui/views/query-analysis-tab.ts` > 0
    - `grep -c "renderIcon.*bar-chart" frontend/src/openclaw/ui/views/query-analysis-tab.ts` > 0
    - `grep -c "renderIcon.*package" frontend/src/openclaw/ui/views/instance-detail.ts` > 0
    - `grep -c "renderIcon.*triangle-alert" frontend/src/openclaw/ui/views/event-management.ts` > 0
    - No 🔍 or 📊 in query-analysis-tab.ts
    - No 📦 in instance-detail.ts (in the capacity section at least)
    - Status-message emoji (✅ ❌ ⚠️ in text) preserved unchanged
    - All 3 files import `renderIcon` from `../../icons.js`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human verification of UI rendering</name>
  <files>
  (No files modified — verification only)
  </files>
  <read_first>
  (N/A — this is a manual verification checkpoint)
  </read_first>

  <action>
    All automated work from Plans 1, 2, and 3 is complete. The user needs to verify the visual result.

    **Before this checkpoint, the executor MUST:**
    1. Start the frontend dev server (if not running):
       ```
       cd /Users/max/Coding/39-Slide/frontend
       npm run dev &
       ```
       Wait for it to start (check `lsof -ti:5173`).

    2. Run the full verification suite:
       ```
       cd /Users/max/Coding/39-Slide/frontend
       echo "=== Build check ===" && npx vite build 2>&1 | tail -5
       echo "=== Tests ===" && npx vitest run --reporter verbose 2>/dev/null | tail -15
       echo "=== UI-03: No camelCase remnants ===" && grep -rn 'icons\.' src/ --include='*.ts' | grep -v node_modules | grep -v '\.d\.ts' | grep -v 'icons\[' | grep -v 'icons\.ts' | grep -v 'icons\.js' | grep -v 'renderEmojiIcon\|setEmojiIcon'
       echo "=== UI-04: No ov-card references ===" && grep -rn 'ov-card' src/ --include='*.ts' --include='*.css' | grep -v node_modules | grep -v '\.d\.ts'
       echo "=== UI-03: No old import paths ===" && grep -rn 'from.*openclaw/ui/icons\|from.*styles/icons' src/ --include='*.ts'
       ```

    3. Report all results in the checkpoint message.
  </action>

  <what-built>
    All Phase 102 changes: merged icon file with ~80+ icons, 30+ files with updated imports and kebab-case renames, shared `<stat-card>` LitElement component, 6 views migrated from ov-card to stat-card, emoji replaced with renderIcon() calls.
  </what-built>

  <how-to-verify>
    1. Open http://localhost:5173 in a browser
    2. Log in with admin/Tpam1234
    3. Navigate to each of these views and visually confirm stat cards, icons, and no console errors:
       - Login page (verify eye/eyeOff toggle icons work)
       - Dashboard (6 stat cards should render with labels/values/hints)
       - Alerts page (4 stat cards with variant colors)
       - Instances page (4 stat cards with variant colors)
       - Reports page (4 stat cards with variant colors)
       - Schema Management page (4 stat cards with variant colors)
       - Overview page in OpenClaw section (OpenClaw overview cards)
       - Instance Detail -> click any instance (verify icons render, 📦 replaced with SVG package icon)
       - SQL Console -> Query Analysis tab (verify 🔍 and 📊 are now SVG icons)
       - Event Management (verify empty state shows triangle-alert icon instead of ⚠️)
    4. Open browser DevTools console — there should be NO errors
  </how-to-verify>

  <resume-signal>
    Type "approved" if all views render correctly with no console errors. Describe any specific issues found.
  </resume-signal>

  <acceptance_criteria>
    - Login page renders without errors (eye/eyeOff toggle works)
    - Dashboard renders 6 stat cards with correct labels/values
    - Alerts renders 4 stat cards with variant colors
    - Instances renders 4 stat cards with variant colors
    - Reports renders 4 stat cards with variant colors
    - Schema Management renders 4 stat cards with variant colors
    - Overview page renders stat cards correctly
    - Instance Detail: icons and SVG package icon render correctly
    - Query Analysis: 🔍 replaced by search icon, 📊 replaced by bar-chart icon
    - Event Management: empty state shows triangle-alert icon instead of ⚠️
    - No console errors on any page
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderIcon() name parameter | Static string literal in template code; no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-06 | Tampering | renderIcon() name argument | mitigate | Icon names are compile-time string literals, not user input. renderIcon() signature accepts `IconName` union type, enforcing valid keys at compile time. |
| T-102-SC | Tampering | npm dependencies | accept | No new packages installed. |
</threat_model>

<verification>
1. `grep -rn 'from.*openclaw/ui/icons\|from.*styles/icons' frontend/src/ --include='*.ts'` returns empty
2. `grep -rn 'ov-card' frontend/src/ --include='*.ts' --include='*.css'` returns empty
3. `npx vite build` succeeds
4. `npx vitest run` succeeds
5. Human verification confirms all 6 views + login page render correctly
</verification>

<success_criteria>
- All 4 structural emoji replaced with SVG icons (UI-05 complete)
- Status-message emoji preserved as-is (D-17)
- Build and tests pass
- Human verification confirms visual correctness across all affected views
</success_criteria>

<output>
Create `.planning/phases/102-UI统一/102-03-SUMMARY.md` when done
</output>
