---
phase: 135-agent-ws-contracts
plan: 02
type: execute
wave: 2
depends_on: [135-01]
files_modified:
  - apps/db-ops-api/sql/migrations/028_analysis_envelope.sql
  - apps/db-ops-api/src/analysis/analysis-envelope.ts
  - apps/db-ops-api/src/ai-analysis-database-service.ts
  - apps/db-ops-api/src/tools/catalog.ts
  - apps/db-ops-api/src/adapter/types.ts
  - apps/db-ops-api/src/adapter/direct-adapter.ts
  - frontend/src/app/ui/views/ai-analysis-result.ts
  - frontend/src/app/ui/views/agents.ts
  - frontend/src/app/ui/views/agents-panels-overview.ts
  - frontend/src/app/ui/app-render.ts
  - apps/db-ops-api/src/analysis/analysis-envelope.test.ts
  - frontend/e2e/agent-capabilities.spec.ts
autonomous: true
requirements: [ME-05, HI-10, TG-06]
must_haves:
  truths:
    - AI 分析以版本化 AnalysisEnvelope 持久化，结构化数据不再藏在 Markdown JSON code block
    - 旧 Markdown 记录可兼容读取，新增写入必须通过 schema 校验
    - Agent 页面只呈现 DirectAdapter capabilities 声明且真正有 handler 的功能
---

# Phase 135 Plan 02：AnalysisEnvelope 与能力驱动 Agent UI

<objective>
将 AI 输出从脆弱 Markdown 约定升级为可验证、可演进、可追溯的结构化分析，同时清除 Agent 管理页的无效功能面。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-agent-ws-contracts/135-CONTEXT.md
@apps/db-ops-api/src/ai-analysis-database-service.ts
@apps/db-ops-api/src/tools/catalog.ts
@frontend/src/app/ui/views/agents.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 定义 AnalysisEnvelope schema 和迁移策略</name>
  <files>apps/db-ops-api/sql/migrations/028_analysis_envelope.sql, apps/db-ops-api/src/analysis/analysis-envelope.ts, apps/db-ops-api/src/ai-analysis-database-service.ts, apps/db-ops-api/src/analysis/analysis-envelope.test.ts</files>
  <action>
    Envelope 至少包含 schemaVersion、analysisType、subject ResourceRef、conclusions、hypotheses、evidenceRefs、confidence、recommendations、displayMarkdown、model/prompt/tool versions、createdAt；execution_trace 保持独立。用 TypeBox/JSON Schema 验证数量、长度、枚举和引用。新增列与 backfill status，不破坏旧记录；读取器先读 envelope，旧记录转换为 `legacy` view 且不臆造 evidence/confidence。回填只能标记解析成功/失败，禁止把自由文本当可靠事实。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/analysis/analysis-envelope.test.ts</automated></verify>
  <done>合法 envelope round-trip；错误版本/引用/超限拒绝；旧记录仍可显示且明确 legacy。</done>
</task>

<task type="auto">
  <name>Task 2: 重构结构化完成工具和失败语义</name>
  <files>apps/db-ops-api/src/tools/catalog.ts, apps/db-ops-api/src/ai-analysis-database-service.ts, apps/db-ops-api/src/adapter/direct-adapter.ts, apps/db-ops-api/src/analysis/analysis-envelope.test.ts</files>
  <action>
    `slide_complete_analysis` 接受 envelope 对象，不再从 Markdown fenced JSON 解析；服务端补充不可伪造的 actor/run/model/tool provenance。validation failure 是 run failed/partial，不写 completed cache。工具和 DB 写入通过 Operation/idempotency 串联，重复 complete 返回原分析。展示 Markdown 仅为派生视图，结构化字段才是 Data Loom 后续投影输入。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/analysis/analysis-envelope.test.ts -t "completion|provenance|idempotent"</automated></verify>
  <done>无效结构不能落库为完成；重复完成不产生第二条分析；provenance 由服务端生成。</done>
</task>

<task type="auto">
  <name>Task 3: 让 Agent UI 完全由 capabilities 驱动</name>
  <files>apps/db-ops-api/src/adapter/types.ts, apps/db-ops-api/src/adapter/direct-adapter.ts, frontend/src/app/ui/views/agents.ts, frontend/src/app/ui/views/agents-panels-overview.ts, frontend/src/app/ui/app-render.ts, frontend/src/app/ui/views/ai-analysis-result.ts, frontend/e2e/agent-capabilities.spec.ts</files>
  <action>
    capabilities 细化 sessions/files/tools/skills/cron/modelSelection/fallback/reload/edit 等 feature 和 read/write 状态。前端隐藏 unsupported，readonly 显式禁用并给原因；所有可编辑控件必须连接真实 API、dirty/save/error 状态，不允许空回调。分析结果从 Envelope 字段渲染并复用 Phase 132 sanitizer。E2E 枚举所有可见按钮，断言请求、持久化、刷新和失败反馈。
  </action>
  <verify><automated>pnpm --filter slide-frontend exec playwright test e2e/agent-capabilities.spec.ts</automated></verify>
  <done>DirectAdapter 下无“暂不可用”死标签和空操作按钮；支持能力可完成保存与刷新。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 模型伪造 provenance | 服务端覆盖 actor/run/version 字段 |
| Markdown 隐藏恶意结构 | schema 对象单独传输，展示层安全渲染 |
| 能力 UI 诱导无效操作 | capability contract + visible-control E2E |
| 重复完成产生冲突事实 | run-scoped idempotency |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run src/analysis/analysis-envelope.test.ts`
- `pnpm --filter slide-frontend typecheck`
- `pnpm --filter slide-frontend exec playwright test e2e/agent-capabilities.spec.ts`
</verification>

<success_criteria>
1. ME-05 与分析完成失败误报关闭。
2. Data Loom 只能消费 schema-versioned envelope/projection，不解析 Markdown。
3. 输出 `.planning/phases/135-agent-ws-contracts/135-02-SUMMARY.md`。
</success_criteria>

