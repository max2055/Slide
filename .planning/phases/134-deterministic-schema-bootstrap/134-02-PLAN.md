---
phase: 134-deterministic-schema-bootstrap
plan: 02
type: execute
wave: 2
depends_on: [134-01]
files_modified:
  - apps/db-ops-api/src/lifecycle/application-lifecycle.ts
  - apps/db-ops-api/src/lifecycle/worker-lease.ts
  - apps/db-ops-api/server.ts
  - apps/db-ops-api/tests/application-lifecycle.test.ts
  - apps/db-ops-api/tests/bootstrap-upgrade.test.ts
  - docs/OPERATIONS.md
autonomous: true
requirements: [HI-12, HI-01]
must_haves:
  truths:
    - 配置、数据库、迁移和监听器准备完成前不启动任何采集/Cron/告警/清理副作用
    - 未取得 listener 或 worker lease 的进程不产生业务写入
    - 启动失败和 SIGTERM 按逆序释放资源，运行中工作进入安全终态或释放 lease
---

# Phase 134 Plan 02：应用生命周期、重复进程与升级恢复

<objective>
把当前 server.ts 的散落启动副作用收敛为可测试生命周期，确保只有完成 schema 和控制面准备的实例才启动 worker。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-deterministic-schema-bootstrap/134-CONTEXT.md
@apps/db-ops-api/server.ts
@apps/db-ops-api/src/monitor-collector.ts
@apps/db-ops-api/src/cron/cron-manager.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 编排可回滚启动阶段</name>
  <files>apps/db-ops-api/src/lifecycle/application-lifecycle.ts, apps/db-ops-api/server.ts, apps/db-ops-api/tests/application-lifecycle.test.ts</files>
  <action>
    定义阶段：security config -> DB connect -> migrations -> invariants -> HTTP/Agent WS bind -> control-plane ready -> worker lease -> collectors/Cron/alerts/reapers。每个组件实现 start/stop/health；任何阶段失败按逆序 stop。server.ts 只组装依赖，不在模块顶层或 listen 前启动 timer。readiness 在 worker lease 之前不得暗示工作流健康；liveness 与 readiness 分离。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/application-lifecycle.test.ts -t "order|rollback|readiness"</automated></verify>
  <done>注入每一阶段失败时，已启动资源均关闭，未到阶段的副作用调用次数为 0。</done>
</task>

<task type="auto">
  <name>Task 2: 增加 worker lease、重复启动与优雅退出</name>
  <files>apps/db-ops-api/src/lifecycle/worker-lease.ts, apps/db-ops-api/src/lifecycle/application-lifecycle.ts, apps/db-ops-api/server.ts, apps/db-ops-api/tests/application-lifecycle.test.ts</files>
  <action>
    用 MySQL 持久 lease 区分 API listener 与 worker owner，包含 owner、expires/heartbeat 和 fencing token。端口占用进程必须在 worker 启动前退出；多 API replica 时只有持 lease/fencing token 的实例运行 singleton job。SIGTERM/SIGINT 停止接流量、停止领取新工作、等待有界 drain、标记/释放 Operation 和 lease，再关闭 WS/HTTP/DB。超时强制退出但保留可恢复状态。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/application-lifecycle.test.ts -t "duplicate|lease|signal"</automated></verify>
  <done>第二进程 listener 失败时 collector/Cron/alert/health-write spy 全为 0；lease 过期后新进程安全接管。</done>
</task>

<task type="auto">
  <name>Task 3: 空库、历史升级与失败恢复验收</name>
  <files>apps/db-ops-api/tests/bootstrap-upgrade.test.ts, docs/OPERATIONS.md</files>
  <action>
    创建 Phase 124 与 Phase 129 代表性 schema/data fixture，验证升级后行数、FK、密文和业务关联不丢失。覆盖空库启动、migration 中断、checksum mismatch、重复启动、SIGTERM 和下一进程恢复。OPERATIONS 记录 install/inspect/baseline/repair/upgrade/rollback 数据恢复命令、所需环境和故障判据，不提供跳过 invariant 的快捷方式。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/bootstrap-upgrade.test.ts</automated></verify>
  <done>空库和两代历史 fixture 均可重复升级；故障注入后不会带不完整 schema 进入 ready。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 重复进程执行 singleton job | listener 顺序 + fenced worker lease |
| shutdown 中途丢工作 | stop intake + bounded drain + durable state |
| 过期 owner 写入 | fencing token 校验 |
| 运维绕过 schema 检查 | 无 skip-invariant 生产路径 |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run tests/application-lifecycle.test.ts tests/bootstrap-upgrade.test.ts`
- 真实启动两个后端，确认失败进程没有新增业务数据。
- SIGTERM 后端口、WS、timer 和 DB connection 全部释放。
</verification>

<success_criteria>
1. HI-12 关闭并有重复进程数据证据。
2. 新装、升级和恢复使用同一 runner/lifecycle。
3. 输出 `.planning/phases/134-deterministic-schema-bootstrap/134-02-SUMMARY.md`。
</success_criteria>

