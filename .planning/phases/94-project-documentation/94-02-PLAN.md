---
phase: 94-project-documentation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - docs/slide/OPERATIONS.md
  - docs/slide/USER-GUIDE.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "OPERATIONS.md covers full stack deployment: backend (Fastify on :3000), Gateway (OpenClaw on ws://127.0.0.1:28789/ws), frontend (Lit on :5173), and dependency services (MySQL, Elasticsearch, MongoDB, Redis)"
    - "OPERATIONS.md documents startup/shutdown procedures, configuration items from .env, and Gateway runtime mechanisms"
    - "USER-GUIDE.md covers all v1.2 feature modules: instance management, SQL console, alert management, AI analysis, Chat assistant, reports, approval, RBAC, dashboard, AI settings"
    - "USER-GUIDE.md has step-by-step instructions for each module with screenshot references"
    - "USER-GUIDE.md has a FAQ section"
    - "markdownlint passes for both documents"
  artifacts:
    - path: docs/slide/OPERATIONS.md
      provides: "Full-stack deployment and operations documentation"
      contains: "启动, 停止, .env, 配置, Gateway, WebSocket, 依赖"
    - path: docs/slide/USER-GUIDE.md
      provides: "End-user manual for all v1.2 features"
      contains: "实例管理, SQL 控制台, 告警, AI 分析, Chat, FAQ"
  key_links:
    - from: OPERATIONS.md
      to: apps/db-ops-api/server.ts, apps/db-ops-api/.env, apps/db-ops-api/package.json
      via: "Reference to file paths for startup sequence, configuration, and scripts"
    - from: USER-GUIDE.md
      to: frontend/src/openclaw/ui/views/ (actual view components)
      via: "Navigation path references for each feature module"
---

<objective>
编写 OPERATIONS.md（运维文档）和 USER-GUIDE.md（用户手册）。

Purpose: 提供完整运维流程参考和端到端用户指引，覆盖 Slide v1.2 全功能。
Output: docs/slide/OPERATIONS.md, docs/slide/USER-GUIDE.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-project-documentation/94-CONTEXT.md
@.planning/phases/94-project-documentation/94-RESEARCH.md
@.planning/REQUIREMENTS.md

### Interfaces and Reference Context

**Key decision references (per CONTEXT.md):**
- D-04: All docs in Chinese
- D-05: OPERATIONS.md -> 运维人员; USER-GUIDE.md -> DBA 和运维用户
- D-09: USER-GUIDE.md organized by functional module (not by role or task)
- D-10: Detail level: screenshots + step-by-step + FAQ
- D-11: Covers v1.2 full modules: instance management, SQL console, alert management, AI analysis, Chat assistant, reports, approval, RBAC, dashboard, AI settings
- D-12: Single USER-GUIDE.md file (no multi-file split)
- D-13: Full stack operations: db-ops-api backend, OpenClaw Gateway (WebSocket, session management, agent scheduling, channel config), frontend build/deploy, dependency services (MySQL, Elasticsearch, MongoDB, Redis), startup/shutdown, config item descriptions
- D-14: docs/slide/ directory already set up by Plan 01
- D-15: Maintenance strategy referenced
- D-16: Screenshots in docs/slide/assets/screenshots/ (placeholder .gitkeep already exists)

**Source of truth files for content extraction:**

OPERATIONS.md:
- apps/db-ops-api/.env — all 68 lines of configuration keys (DB_, REDIS_, ELASTICSEARCH_, MONGODB_, ANTHROPIC_, OPENAI_, JWT_, ENCRYPTION_KEY, BACKEND_PORT, FRONTEND_PORT, GATEWAY_PORT, ENABLE_AUTO_AI_ANALYSIS, LOG_LEVEL)
- apps/db-ops-api/server.ts — startup sequence: DB init (line 113), LLM init (line 122), CORS (line 125), route registration, Gateway on :28789 (line 3044), monitor collector start (line 3052), alert engine start with 60s interval (line 3058), alert escalation (line 3062), auto analysis cron jobs (lines 3073+)
- apps/db-ops-api/package.json — "dev": "tsx watch server.ts", "start": "tsx server.ts", "gateway:start", "gateway:stop", "test", "smoke" scripts
- apps/db-ops-api/src/gateway/openclaw-runtime.ts — createSlideOpenClawConfig() function, agent configurations (model, workspace, prompt), session settings (maxHistoryMessages: 100, maxHistoryBytes: 1000000), tool allowlist (read/write/edit/bash/grep/find/ls)
- apps/db-ops-api/src/gateway/server.ts — startGatewayServer function signature
- frontend/package.json — "dev": "vite", "build": "vite build", "preview": "vite preview" scripts
- CLAUDE.md (project instructions) — existing commands for kill ports, health check URL
- .openclaw-slide/ — gateway state directory (contains canvas/)

USER-GUIDE.md:
- frontend/src/openclaw/ui/views/ — all view files (dashboard via overview.ts, alerts.ts, instances-db.ts, instance-detail.ts, sql-console.ts, chat.ts, approval-dashboard.ts, rbac-page.ts, reports.ts, ai-analysis-result.ts, ai-settings.ts, schema-management.ts, users-management.ts, config.ts, skills.ts)
- server.ts API routes — the 150+ REST endpoints that back each UI feature

**Grouping by decision:**
D-05: USER-GUIDE.md targets DBA / ops users — focus on "what they see and do" not architecture internals
D-09: Module order should follow user workflow: Dashboard overview -> Instance management -> SQL console -> Alert management -> Chat assistant -> AI analysis -> Reports -> Approval -> RBAC -> AI settings
D-10: Each module section: brief purpose -> step-by-step instructions (numbered) -> screenshot reference -> troubleshooting notes
D-12: Single markdown file, use `##`, `###` hierarchy for modules and sub-sections
D-13: OPERATIONS.md must include OpenClaw Gateway details: WebSocket connection lifecycle, session management, agent scheduling, channel configuration
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write OPERATIONS.md — full-stack operations and deployment document</name>
  <files>
    docs/slide/OPERATIONS.md
  </files>
  <read_first>
    apps/db-ops-api/.env (all 68 lines of configuration keys and their default values)
    apps/db-ops-api/server.ts (lines 1-15 for env setup, lines 111-128 for startup sequence, lines 3031-3067 for Gateway and service startup, lines 3069-3120+ for auto-analysis cron triggers)
    apps/db-ops-api/package.json (scripts: dev, start, gateway:start, gateway:stop, test, smoke, init-db)
    apps/db-ops-api/src/gateway/openclaw-runtime.ts (createSlideOpenClawConfig for agent config defaults)
    apps/db-ops-api/src/gateway/server.ts (startGatewayServer signature)
    frontend/package.json (scripts: dev, build, preview)
    CLAUDE.md (project-level commands: kill ports, health check, credentials)
    frontend/vite.config.ts (vite config if exists)
    .openclaw-slide/ (verify it exists, note its purpose)
  </read_first>
  <action>
    编写 docs/slide/OPERATIONS.md，全部使用中文（D-04），目标受众为运维人员（D-05），语言偏向实操、命令和配置。

    文档结构建议（Claude's Discretion 范围内据 D-13 覆盖）：

    1. **# Slide 运维文档**
       - 开篇概述：Slide v1.2 运维手册，涵盖后端、Gateway、前端的完整运维流程。

    2. **## 系统概览**
       - 系统组件和端口表：
         | 组件 | 技术 | 端口 | 作用 |
         |------|------|------|------|
         | 后端 API | Fastify | 3000 | REST API 服务 |
         | 前端 | Lit + Vite | 5173 | Web UI 开发服务器 |
         | 前端 | Lit (Build) | - | 静态文件（生产） |
         | OpenClaw Gateway | Node.js | 28789 | WebSocket + RPC 代理 |
         | MySQL | MySQL 8 | 3306 | 主数据库 |
         | Elasticsearch | ES 8 | 9200 | 日志/分析 |
         | Redis | Redis 7 | 6379 | 缓存/会话 |
         | MongoDB | MongoDB 7 | 27017 | 灵活存储 |
       - 端口来源：.env 中 BACKEND_PORT=3000, FRONTEND_PORT=5173, GATEWAY_PORT=18790（实际 server.ts 中 Gateway 硬编码端口 28789，注意说明差异）

    3. **## 部署架构**
       - Mermaid flowchart 展示各组件和端口/连接方式
       - 说明开发环境（单机）和生产环境（建议分离）的区别

    4. **## 前置依赖**
       - 各项依赖服务的最低版本要求、安装方式简述、启动检查命令：
         - MySQL 8.x（必需）
         - Elasticsearch 8.x（可选，影响 QAN 和搜索）
         - Redis 7.x（可选，影响会话缓存）
         - MongoDB 7.x（可选，存档场景）
       - Node.js >= 20（项目使用 tsx 执行 TypeScript）

    5. **## 配置项说明**
       - 分组列出 .env 配置项（D-13）：
         - **数据库配置**：DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME — 系统主数据库
         - **Redis 配置**：REDIS_HOST, REDIS_PORT, REDIS_PASSWORD
         - **Elasticsearch 配置**：ELASTICSEARCH_HOST, ELASTICSEARCH_PORT, ELASTICSEARCH_USERNAME, ELASTICSEARCH_PASSWORD
         - **MongoDB 配置**：MONGODB_HOST, MONGODB_PORT, MONGODB_USERNAME, MONGODB_PASSWORD
         - **LLM 配置**：ANTHROPIC_API_KEY, ANTHROPIC_MODEL, OPENAI_API_KEY, OPENAI_MODEL, AZURE_OPENAI_*, OLLAMA_URL, OLLAMA_MODEL, ALIYUN_API_KEY, ALIYUN_MODEL
         - **JWT 配置**：JWT_SECRET_KEY（生产必改，最小 32 字符）, JWT_EXPIRATION_MINUTES（默认 1440 = 24h）
         - **加密密钥**：ENCRYPTION_KEY（生产必改，32 字符）
         - **端口配置**：BACKEND_PORT, FRONTEND_PORT, GATEWAY_PORT
         - **AI 开关**：ENABLE_AUTO_AI_ANALYSIS（默认 false）
         - **日志**：LOG_LEVEL
       - 每个配置项给出作用、默认值、是否必填
       - 引用 apps/db-ops-api/.env 作为权威来源

    6. **## 启动流程**
       - 步骤化说明（D-13）：
         1. 确保依赖服务运行（MySQL + 各可选服务）
         2. `cd apps/db-ops-api && cp .env.example .env`（若无）
         3. 配置 .env 中的数据库连接和 LLM API Key
         4. `cd apps/db-ops-api && npx tsx init-db.ts`（首次初始化数据库表结构）
         5. `cd apps/db-ops-api && npx tsx server.ts`（启动后端，同时启动 Gateway）
         6. `cd frontend && npm run dev`（启动前端开发服务器）
         7. 浏览器访问 http://localhost:5173
         8. 登录凭证：admin / Tpam1234

    7. **## Gateway 运行机制**（D-13 重点）
       - 描述 OpenClaw Gateway 在 Slide 中的角色：
         - WebSocket 服务端（ws://127.0.0.1:28789/ws），由 server.ts 中 startGatewayServer(28789, {}) 启动
         - 自动注册所有 handlers（消息派发、会话管理、Agent 调度）
         - 会话生命周期：客户端连接 -> 认证 -> 创建会话 -> 消息交换 -> 断开
         - Agent 配置：由 createSlideOpenClawConfig() 定义默认 Agent（Slide Database Assistant），model 为 deepseek/deepseek-v4-flash
         - 工具限制：allowlist 为 [], denylist 为 []（未限制）；nodeHost.allowedTools 为 ['read', 'write', 'edit', 'bash', 'grep', 'find', 'ls']
         - 技能配置：额外目录来自 .agents/skills/
         - 关键环境变量：OPENCLAW_STATE_DIR（设为 ~/.openclaw-slide/ 避免冲突）、OPENCLAW_DISABLE_BUNDLED_PLUGINS（设为 1）

    8. **## 后端组件说明**
       - server.ts 启动后各服务的关系：
         - `monitorCollector.start()` — 30s 间隔采集指标
         - `alertEngine.startEvaluationLoop()` — 每 60s 评估告警规则
         - `alertEscalationService.start()` — 升级规则 cron job
         - 自动 AI 分析（ENABLE_AUTO_AI_ANALYSIS=true 时）：
           - TopSQL 自动分析（每 10s 检查）
           - 告警 RCA 自动分析（每 10s 检查新告警）
         - `notificationService.start()` — 暂不自动启动

    9. **## 启停命令**
       - 表格形式列出：
         | 操作 | 命令 |
         |------|------|
         | 启动后端 | `cd apps/db-ops-api && npx tsx server.ts` |
         | 启动前端(dev) | `cd frontend && npm run dev` |
         | 初始化数据库 | `cd apps/db-ops-api && npx tsx init-db.ts` |
         | Gateway 独立启动 | `cd apps/db-ops-api && npm run gateway:start` |
         | Gateway 停止 | `cd apps/db-ops-api && npm run gateway:stop` |
         | 停止后端 | `kill $(lsof -ti:3000)` |
         | 停止前端 | `kill $(lsof -ti:5173)` |
         | 运行测试 | `cd apps/db-ops-api && npm test` |
         | 冒烟测试 | `cd apps/db-ops-api && npm run smoke` |
         | 前端构建 | `cd frontend && npm run build` |
         | 前端预览 | `cd frontend && npm run preview` |

    10. **## 健康检查与日志**
        - 健康检查：`curl http://localhost:3000/api/health` 返回 `{"status":"ok"}`
        - 日志级别由 LOG_LEVEL 控制（默认 INFO）
        - 日志文件位置：当前 stdout（可重定向）

    11. **## 故障排查**
        - 常见问题和解决方案：
          - 端口被占用 -> `lsof -ti:3000 | xargs kill -9`
          - 数据库连接失败 -> 检查 MySQL 是否运行、.env 配置正确
          - Gateway 连接失败 -> 检查 28789 端口是否被占用
          - 前端白屏 -> 检查后端是否已启动、CORS 配置
          - LLM 不可用 -> 检查 API Key 配置

    12. **## 文档维护**（D-15）
        - 简略说明：功能变更时同步更新对应文档，PR review 检查文档

    全文要求：
    - 中文字写，命令和环境变量名保留英文
    - 不复制大段代码文件内容，引用路径（如 `apps/db-ops-api/server.ts` 第 3044 行）
    - 每个章节 3-8 段，足够的实操细节

    最后运行 `npx markdownlint-cli2 docs/slide/OPERATIONS.md` 检查 lint。
  </action>
  <verify>
    <automated>
      test -f docs/slide/OPERATIONS.md && echo "FILE EXISTS"
      wc -c docs/slide/OPERATIONS.md | awk '{print $1}' | xargs echo "FILE BYTES:"
      grep -q '启动流程' docs/slide/OPERATIONS.md && echo "STARTUP OK"
      grep -q 'Gateway' docs/slide/OPERATIONS.md && echo "GATEWAY OK"
      grep -q '配置项' docs/slide/OPERATIONS.md && echo "CONFIG OK"
      grep -q 'DB_HOST' docs/slide/OPERATIONS.md && echo "ENV REFS OK"
      grep -q '健康检查' docs/slide/OPERATIONS.md && echo "HEALTH OK"
      grep -q '故障排查' docs/slide/OPERATIONS.md && echo "TROUBLESHOOT OK"
      npx markdownlint-cli2 docs/slide/OPERATIONS.md 2>/dev/null && echo "LINT PASSED" || echo "LINT ISSUES"
    </automated>
  </verify>
  <acceptance_criteria>
    1. 文件 docs/slide/OPERATIONS.md 存在且 >3000 字符
    2. 包含以下章节：系统概览、部署架构、配置项说明、启动流程、Gateway 运行机制、启停命令、健康检查与日志、故障排查
    3. 配置项说明覆盖所有 .env 中的关键分组（数据库、Redis、ES、MongoDB、LLM、JWT、加密、端口、AI 开关、日志）
    4. 启动流程包含从依赖安装到 UI 登录的完整 8 步
    5. Gateway 运行机制覆盖 WebSocket、会话管理、Agent 配置、工具限制
    6. markdownlint-cli2 通过（$? = 0）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write USER-GUIDE.md — user manual covering all v1.2 features</name>
  <files>
    docs/slide/USER-GUIDE.md
  </files>
  <read_first>
    frontend/src/openclaw/ui/views/ (ls views/ to confirm all view file names, then read 2-3 key view files like overview.ts, alerts.ts, chat.ts to understand UI structure for descriptions)
    apps/db-ops-api/server.ts (inspect 2-3 API route groups to understand data flow details e.g. alerts route at line 440-470, instance routes at line 296-310)
    .planning/phases/94-project-documentation/94-RESEARCH.md (screenshot strategy, pitfalls)
    .planning/ROADMAP.md (Phase 94 success criteria for USER-GUIDE.md)
  </read_first>
  <action>
    编写单文件 docs/slide/USER-GUIDE.md（D-12），全部使用中文（D-04），目标受众为 DBA 和运维用户（D-05）。按功能模块分章（D-09），内容包含截图参考 + 分步骤操作说明 + FAQ（D-10）。

    **关于截图**（按 D-10、D-16，Claude's Discretion 决定数量和内容）：
    - 在 `docs/slide/assets/screenshots/` 中创建占位文本文件来替代实际截图，文件名为 `{module}-{feature}.txt`，内容描述该截图应该在什么页面/状态下截取
    - 在 USER-GUIDE.md 中引用截图时使用格式 `![说明文字](assets/screenshots/{filename}.png)`，同时注明"截图待补充：{具体场景}"
    - 这样文档结构完整可用，用户可后续自行补充截图而无需修改文档结构

    用 D-09 模块顺序，建议排列（Claude's Discretion）：

    **# Slide 用户手册**

    **1. ## 仪表盘**
    - 默认首页，系统概览视图（overview.ts）
    - 展示内容：实例数量、告警统计、最新 AI 分析摘要（ai-stats）、容量趋势图（capacity-trend）
    - 步骤：登录后自动跳转仪表盘 -> 查看各统计卡片 -> 点击卡片跳转详情
    - 截图占位：dashboard-overview.png, dashboard-capacity-trend.png

    **2. ## 实例管理**
    - 实例列表/详情视图：由 instance-database-service.ts 提供数据，instances-db.ts 和 instance-detail.ts 显示
    - 涵盖操作：添加实例、编辑实例、测试连接、查看详情、删除实例
    - 实例详情子页面：指标监控（metrics）、TopSQL、QAN、执行计划（explain）、慢查询（slow-queries）、容量（capacity）、会话（sessions）
    - 步骤：实例管理页 -> 添加实例（选择类型/填连接信息/测试/保存） -> 点击实例 -> 查看详情各 tab
    - 截图占位：instance-list.png, instance-add-form.png, instance-detail.png, instance-metrics.png

    **3. ## SQL 控制台**
    - SQL 执行界面（sql-console.ts），由 sql-executor.ts 提供执行能力
    - 功能：选择实例和数据库 -> 编写 SQL（CodeMirror 编辑器，支持语法高亮/自动补全） -> 执行 -> 查看结果表格 -> 历史查询
    - AI SQL 审核（sql-audit-tab.ts）：SQL 提交前由 LLM 风险检测
    - SQL 审核审批流（approval-flow.ts）：高风险 SQL 需审批后执行
    - 步骤：选择实例 -> 输入 SQL -> 点击执行 -> 查看结果；或点击审核 -> 查看风险建议 -> 提审批
    - 截图占位：sql-console.png, sql-audit-panel.png, sql-history.png

    **4. ## 告警管理**
    - 告警列表视图（alerts.ts），由 alert-engine.ts 每 60s 评估驱动
    - 功能：查看告警列表（筛选/排序） -> 查看告警详情 -> 标记已读/确认
    - AI 告警分析（D-11）：已分析的告警显示"已分析"标记（ai-analysis-result.ts 组件） -> 点击查看 RCA/诊断结果
    - 告警升级（alert-escalation-service.ts）：设置升级规则
    - 维护窗口（maintenance-window-service.ts）：在已知变更期间静默告警
    - 告警静默（alert-silence-service.ts）：临时静音特定告警
    - 告警事件管理（alert-event-service.ts）：事件记录、指派、调查、处理、复盘
    - 步骤：告警列表 -> 查看详情/分析 -> 标记已读；设置页 -> 配置升级规则/维护窗口
    - 截图占位：alert-list.png, alert-detail.png, alert-analysis-result.png, alert-settings.png

    **5. ## AI 分析**
    - AI 分析统一入口（ai-analysis-result.ts），由 ai-agent-bridge.ts 驱动
    - 三种分析类型：RCA（根因分析）、TopSQL 分析、故障诊断
    - 自动分析（ENABLE_AUTO_AI_ANALYSIS=true 时）：TopSQL 每 10s 检查新慢查询 -> 触发分析；告警每 10s 检查新告警 -> 触发 RCA
    - 手动分析：在告警详情页点击"分析"按钮触发
    - AI 分析配置管理功能（ai-settings.ts 和 ai-analysis-config-service.ts）：全局开关、严重级别筛选、实例白名单、时间窗口
    - 截图占位：ai-settings.png, ai-analysis-config.png

    **6. ## Chat 助手**
    - AI Chat 界面（chat.ts），由 Gateway WebSocket 提供实时对话能力
    - 功能：输入问题 -> AI 输出有数据库运维上下文的回答
    - 支持问题类型："哪些实例有问题"、"最近的慢查询"、"当前告警状态"等
    - Agent 能力：由 openclaw-runtime.ts 中配置的"Slide Database Assistant"Agent 驱动，包含 db_health_check、db_performance_analysis 等工具
    - Agent 问候语（getAgentGreeting()，通过 /api/chat/greeting 提供）
    - Agent 选择器（Phase 98）：多个 Agent 时可切换
    - 步骤：进入 Chat 页 -> 输入问题 -> 查看流式回答 -> 点击回答中的回链导航到相关页面
    - 截图占位：chat-interface.png, chat-agent-selector.png

    **7. ## 报表管理**
    - 报表列表和生成（reports.ts），由 report-*.ts 驱动
    - 四种报表类型：健康检查、性能报表、慢查询报表、容量报表
    - 导出格式：PDF / HTML / JSON / MD
    - 步骤：报表页 -> 选择报表类型 -> 配置参数 -> 生成 -> 下载/查看
    - 截图占位：report-list.png, report-generate.png

    **8. ## SQL 审核审批**
    - 审批列表（approval-dashboard.ts）和审批流程（approval-service.ts）
    - 功能：查看待审批列表 -> 查看详情/Diff -> 通过/拒绝 -> 自动执行
    - 批量审批：勾选多条 -> 批量通过/拒绝
    - 步骤：审批页 -> 查看待审批 -> 审查 SQL -> 操作(通过/拒绝) -> 记录历史
    - 截图占位：approval-list.png, approval-review.png

    **9. ## RBAC 权限管理**
    - 用户管理（users-management.ts）、角色权限配置（rbac-page.ts），由 auth/ 和 rbac-api.ts 驱动
    - 功能：用户 CRUD、角色管理（自定义角色 + 权限点绑定）、用户-角色绑定、实例级访问控制
    - 步骤：用户管理 -> 添加/编辑用户 -> 分配角色；角色管理 -> 创建角色 -> 分配权限 -> 绑定用户
    - 截图占位：user-management.png, role-management.png, rbac-config.png

    **10. ## AI 设置**
    - AI 分析配置页（ai-settings.ts）
    - 配置项：全局启用开关、分析严重级别（warning/error/critical）、实例白名单、分析时间窗口
    - 步骤：设置 -> AI 分析 -> 配置 -> 保存
    - 截图占位：ai-settings-page.png

    **11. ## Schema 管理与索引管理**（Claude's Discretion 补充模块）
    - Schema 变更追踪、索引管理（schema-*.ts, index-*.ts）
    - 表结构变更历史、冗余/未使用索引检测
    - 截图占位：schema-changes.png, index-analysis.png

    **12. ## 常见问题 (FAQ)**
    - 按 D-10 要求包含 FAQ 章节：
      - Q: 如何添加新的数据库实例？
        A: 进入实例管理页 -> 点击"添加实例" -> 填写连接信息 -> 测试连接 -> 保存
      - Q: AI 分析结果出现在哪里？
        A: 告警列表有"已分析"标记的告警可点击查看；实例详情页也有诊断摘要
      - Q: 为什么 Chat AI 没有反应？
        A: 检查 LLM 配置是否正确（设置页 -> LLM 配置），或 Gateway 是否正常运行
      - Q: ENABLE_AUTO_AI_ANALYSIS 有什么用？
        A: 设为 true 则自动对新告警和新慢查询触发 AI 分析（将消耗 token）
      - Q: 如何修改登录密码？
        A: 联系管理员可在用户管理页重置
      - Q: 如何配置通知渠道？
        A: 通知页配置（需先完善通知渠道配置 UI）

    全文要求：
    - 每个模块包含：功能简介（2-3 句） + 操作步骤（编号列表，3-8 步） + 截图占位引用 + 备注（可选）
    - 不复制大段代码
    - 使用表格展示复杂结构（如各模块的功能入口位置）

    最后运行 `npx markdownlint-cli2 docs/slide/USER-GUIDE.md` 检查 lint。
  </action>
  <verify>
    <automated>
      test -f docs/slide/USER-GUIDE.md && echo "FILE EXISTS"
      wc -c docs/slide/USER-GUIDE.md | awk '{print $1}' | xargs echo "FILE BYTES:"
      grep -q '仪表盘' docs/slide/USER-GUIDE.md && echo "DASHBOARD OK"
      grep -q '实例管理' docs/slide/USER-GUIDE.md && echo "INSTANCE OK"
      grep -q 'SQL 控制台' docs/slide/USER-GUIDE.md && echo "SQL OK"
      grep -q '告警' docs/slide/USER-GUIDE.md && echo "ALERT OK"
      grep -q 'AI 分析' docs/slide/USER-GUIDE.md && echo "AI ANALYSIS OK"
      grep -q 'Chat' docs/slide/USER-GUIDE.md && echo "CHAT OK"
      grep -q '报表' docs/slide/USER-GUIDE.md && echo "REPORT OK"
      grep -q '审批' docs/slide/USER-GUIDE.md && echo "APPROVAL OK"
      grep -q 'RBAC\|权限' docs/slide/USER-GUIDE.md && echo "RBAC OK"
      grep -q 'AI 设置' docs/slide/USER-GUIDE.md && echo "AI SETTINGS OK"
      grep -q '常见问题\|FAQ' docs/slide/USER-GUIDE.md && echo "FAQ OK"
      grep -q '截图待补充' docs/slide/USER-GUIDE.md && echo "SCREENSHOT PLACEHOLDER OK"
      test -f docs/slide/assets/screenshots/dashboard-overview.txt && echo "SCREENSHOT NOTE FILE EXISTS" || echo "SCREENSHOT NOTE TO CHECK"
      npx markdownlint-cli2 docs/slide/USER-GUIDE.md 2>/dev/null && echo "LINT PASSED" || echo "LINT ISSUES"
    </automated>
  </verify>
  <acceptance_criteria>
    1. 文件 docs/slide/USER-GUIDE.md 存在且 >5000 字符
    2. 覆盖 D-11 全部 10 个功能模块：实例管理、SQL 控制台、告警管理、AI 分析、Chat 助手、报表、审批、RBAC 权限、仪表盘、AI 设置
    3. 包含 FAQ 章节（至少 4 个问答）
    4. 每个模块包含步骤化操作说明
    5. 每个模块有截图占位引用，截图说明文件存在于 assets/screenshots/
    6. markdownlint-cli2 通过（$? = 0）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | This is a documentation-only plan with no code changes, runtime config, or deployment modifications. No trust boundaries apply. |
</threat_model>

<verification>
1. `docs/slide/OPERATIONS.md` 存在，覆盖 D-13 全栈运维内容
2. `docs/slide/USER-GUIDE.md` 存在，覆盖 D-11 所有 10 个功能模块
3. USER-GUIDE.md 包含 FAQ 章节和每个模块的步骤化说明
4. 两个文档的 markdownlint 检查通过
5. 两个文档内容基于实际代码文件路径描述（而非凭空编写）
6. 截图占位文件存在于 docs/slide/assets/screenshots/
</verification>

<success_criteria>
1. docs/slide/OPERATIONS.md 已创建，覆盖启动/停止/配置/Gateway 机制/故障排查
2. docs/slide/USER-GUIDE.md 已创建，覆盖 v1.2 全部 10+ 功能模块
3. USER-GUIDE.md 含 FAQ，每模块有步骤说明和截图占位
4. 两个文档 markdownlint 通过
5. 截图占位文件已创建，方便用户后续补充真实截图
</success_criteria>

<output>
After completion, create `.planning/phases/94-project-documentation/94-02-SUMMARY.md`
</output>
