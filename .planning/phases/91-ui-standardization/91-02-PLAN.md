---
phase: 91-ui-standardization
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/openclaw/styles/base.css
  - frontend/src/openclaw/ui/views/dashboard.ts
  - frontend/src/openclaw/ui/views/approval-dashboard.ts
  - frontend/src/openclaw/ui/views/sql-console.ts
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Global CSS file defines unified --text-* and --space-* design tokens"
    - "Dashboard stat card labels use --text-xs instead of hardcoded 11px"
    - "Dashboard cards use --space-lg for padding instead of hardcoded 16px"
    - "Approval dashboard cards use --text-md for body text instead of hardcoded 14px"
    - "SQL console uses --space-md for consistent card gap instead of hardcoded 12px"
    - "Dark/light theme switching uses new variables correctly"
  artifacts:
    - path: frontend/src/openclaw/styles/base.css
      provides: "Unified typography, spacing, and radius CSS variables"
      contains: "--text-xs, --text-sm, --text-base, --text-md, --text-lg, --text-xl, --text-2xl, --space-xs, --space-sm, --space-md, --space-lg, --space-xl"
    - path: frontend/src/openclaw/ui/views/dashboard.ts
      provides: "Dashboard page using global CSS variables for typography, spacing, and radius"
    - path: frontend/src/openclaw/ui/views/approval-dashboard.ts
      provides: "Approval dashboard using global CSS variables"
    - path: frontend/src/openclaw/ui/views/sql-console.ts
      provides: "SQL console using global CSS variables"
  key_links:
    - from: views/dashboard.ts
      to: styles/base.css
      via: "CSS custom properties in :host/shadow — reassigns global tokens locally"
    - from: views/approval-dashboard.ts
      to: styles/base.css
      via: "var(--text-*), var(--space-*), var(--radius-*) — eliminates hardcoded font-size, gap, padding, border-radius"
---

<objective>
Add unified typography and spacing design tokens to base.css and apply them to the three highest-impact pages (dashboard, approval, sql-console), replacing hardcoded values.

Purpose: Eliminate visual inconsistency across pages. Currently each page hardcodes its own font-size, gap, padding, and border-radius values, causing subtle styling drifts.
Output: Design tokens (--text-xs through --text-2xl, --space-xs through --space-xl) in base.css. Three key pages migrated to use these tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/91-ui-standardization/91-CONTEXT.md
@frontend/src/openclaw/styles/base.css

<interfaces>
Current existing CSS variables in base.css (:root block):
```css
--radius-sm: 6px;
--radius-md: 10px;
--radius-lg: 14px;
--radius-xl: 20px;
--radius-full: 9999px;
--radius: 10px;
--font-body: "Inter", -apple-system, BlinkMacSystemFont, ...;
--mono: "JetBrains Mono", ui-monospace, ...;
--shadow-sm: 0 1px 2px ...
```

A design-token section will be added between the `--theme-switch-x/y` section and `--mono` with --text-* and --space-* variables.

Target patterns in views:
- dashboard.ts: `font-size: 11px`, `font-size: 22px`, `font-size: 12px`, `gap: 20px`, `gap: 12px`, `gap: 6px`, `padding: 16px`
- approval-dashboard.ts: `font-size: 14px`, `font-size: 12px`, `font-size: 11px`, `font-size: 13px`, `font-size: 18px`, `gap: 4px`, `gap: 8px`, `gap: 12px`, `gap: 16px`, `padding: 16px`, `padding: 24px`, `border-radius: 4px`, `border-radius: 6px`, `border-radius: 8px`, `border-radius: 12px`
- sql-console.ts: `font-size: 12px`, `font-size: 11px`, `font-size: 13px`, `font-size: 14px`, `font-size: 15px`, `font-size: 10px`, `gap: 10px`, `gap: 12px`, `gap: 2px`, `padding: 4px 8px` etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unified design tokens (--text-* and --space-*) to base.css</name>
  <files>frontend/src/openclaw/styles/base.css</files>
  <action>
    Insert design token variables into the `:root` block of base.css (and ensure they are available in ALL theme variants). Per D-01 and D-04.

    1. **Add a section to :root** (approximately after line 79 `--theme-switch-y: 50%;` and before `/* Typography */` at line 82):

    ```
      /* Design Tokens - Typography scale */
      --text-xs: 11px;
      --text-sm: 12px;
      --text-base: 13px;
      --text-md: 14px;
      --text-lg: 16px;
      --text-xl: 18px;
      --text-2xl: 22px;

      /* Design Tokens - Spacing scale */
      --space-xs: 4px;
      --space-sm: 8px;
      --space-md: 12px;
      --space-lg: 16px;
      --space-xl: 24px;
    ```

    2. **Do NOT** modify the individual theme overrides (light, openknot, etc.) — these tokens are size/scale values that do NOT change per theme. They are global constants.

    3. Verify the variable names and values match D-01 exactly:
       - xs=11px, sm=12px, base=13px, md=14px, lg=16px, xl=18px, 2xl=22px
       - xs=4px, sm=8px, md=12px, lg=16px, xl=24px

    Do NOT change existing --radius-* values or add new --radius-* variables — they already exist with correct values.
  </action>
  <verify>
    <automated>grep -c '\-\-text-xs: 11px' frontend/src/openclaw/styles/base.css && grep -c '\-\-space-xs: 4px' frontend/src/openclaw/styles/base.css && grep -c '\-\-text-2xl: 22px' frontend/src/openclaw/styles/base.css && grep -c '\-\-space-xl: 24px' frontend/src/openclaw/styles/base.css</automated>
    Verify: All 4 grep commands return exactly 1 line each. The variables exist in a single location (the :root block, not duplicated per theme).
  </verify>
  <done>base.css defines --text-xs, --text-sm, --text-base, --text-md, --text-lg, --text-xl, --text-2xl, --space-xs, --space-sm, --space-md, --space-lg, --space-xl in the :root block.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate dashboard.ts to use global CSS design tokens</name>
  <files>frontend/src/openclaw/ui/views/dashboard.ts</files>
  <action>
    Replace hardcoded font-size, gap, padding, and border-radius values in the `static styles` of dashboard.ts with the new CSS variables. Apply the migration to the `static override styles` block and the class/selector styles only — do NOT modify the LitElement class body or HTML template.

    **Mapping table** (replace all occurrences):

    | Hardcoded value | CSS variable to use |
    |-----------------|---------------------|
    | `font-size: 11px` | `font-size: var(--text-xs)` |
    | `font-size: 12px` | `font-size: var(--text-sm)` |
    | `font-size: 13px` | `font-size: var(--text-base)` |
    | `font-size: 14px` | `font-size: var(--text-md)` |
    | `font-size: 22px` | `font-size: var(--text-2xl)` |
    | `gap: 20px` | `gap: var(--space-xl)` |
    | `gap: 12px` | `gap: var(--space-md)` |
    | `gap: 10px` | `gap: var(--space-md)` (already close to 12) |
    | `gap: 6px` | `gap: var(--space-xs)` |
    | `padding: 16px` | `padding: var(--space-lg)` |
    | `padding: 12px` | `padding: var(--space-md)` |
    | `padding: 8px` | `padding: var(--space-sm)` or `padding: var(--space-sm)` |
    | `padding: 24px` | `padding: var(--space-xl)` |
    | `padding: 0 0 24px 0` | `padding: 0 0 var(--space-xl) 0` |
    | `gap: 8px` | `gap: var(--space-sm)` |
    | `gap: 4px` | `gap: var(--space-xs)` |
    | `border-radius: 4px` | `border-radius: var(--radius-sm)` (if common, else `4px` — use judgment) |
    | `border-radius: 6px` | `border-radius: var(--radius-sm)` |
    | `border-radius: 8px` | `border-radius: var(--radius-md)` (note: existing --radius-md = 10px, so 8px becomes 10px — acceptable per D-01 unification intent) |
    | `border-radius: 12px` | `border-radius: var(--radius-lg)` (note: existing --radius-lg = 14px) |

    **Specific selectors to find and replace in dashboard.ts**:
    - `.dashboard-grid { gap: 20px; ... }` → gap: var(--space-xl)
    - `.dashboard__stat-cards { gap: 12px; }` → gap: var(--space-md)
    - `.ov-card { gap: 6px; padding: 16px; ... }` → gap: var(--space-xs); padding: var(--space-lg)
    - `.ov-card__label { font-size: 11px; ... }` → font-size: var(--text-xs)
    - `.ov-card__value { font-size: 22px; ... }` → font-size: var(--text-2xl)
    - `.ov-card__hint { font-size: 12px; ... }` → font-size: var(--text-sm)
    - `.ov-card__hint span { gap: 4px; ... }` → gap: var(--space-xs)

    Browse the FULL file to find ALL instances of the mapped values and replace them. Skip values inside non-style contexts (HTML templates, template literals that are not CSS, string values used in JavaScript logic).

    Do NOT modify `--border-radius: var(--radius-md)` if already using variables. Only replace hardcoded values.

    Scope: Only dashboard.ts styles. Do not touch the HTML templates or TypeScript logic.
  </action>
  <verify>
    <automated>cd /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    Manual: Verify dashboard renders correctly with unified spacing. Check that stat cards, grid layout, and hint text all use the new variables.
    <br/>
    Automated check: grep -c 'font-size: 11px' frontend/src/openclaw/ui/views/dashboard.ts || true — ideally 0 (but may have hardcoded values in HTML templates which are fine).
  </verify>
  <done>dashboard.ts static styles no longer contain hardcoded font-size (11px, 12px, 13px, 14px, 22px) or gap/padding values that correspond to the new design tokens. Replaced with var(--text-*), var(--space-*), var(--radius-*).</done>
</task>

<task type="auto">
  <name>Task 3: Migrate approval-dashboard.ts and sql-console.ts to use global CSS design tokens</name>
  <files>frontend/src/openclaw/ui/views/approval-dashboard.ts, frontend/src/openclaw/ui/views/sql-console.ts</files>
  <action>
    Apply the same CSS variable migration as Task 2 to approval-dashboard.ts and sql-console.ts.

    **For approval-dashboard.ts**: Replace hardcoded values using the mapping table from Task 2. Key selectors to look for:
    - `.tabs { gap: 4px; ... }` → gap: var(--space-xs)
    - `.tabs { .tab { font-size: 14px; ... }` → font-size: var(--text-md)
    - `.card { border-radius: var(--radius-md, 8px); padding: 16px; ... }` → padding: var(--space-lg); remove the fallback, use `var(--radius-md)` directly
    - `.badge { font-size: 11px; ... }` → font-size: var(--text-xs)
    - `.btn { font-size: 13px; ... }` → font-size: var(--text-base)
    - `.empty, .loading { padding: 40px; ... }` → padding: 40px (keep — no space variable maps to 40px)
    - `.detail-title { font-size: 18px; ... }` → font-size: var(--text-xl)
    - `.dialog-split { gap: 32px; ... }` → keep 32px (no exact space variable)
    - `.meta-row { font-size: 13px; ... }` → font-size: var(--text-base)
    - `.modal-title { font-size: 18px; ... }` → font-size: var(--text-xl)
    - `.detail-sql-panel .cm-scroller { font-size: 13px; ... }` → font-size: var(--text-base)
    - All `gap: 12px` → gap: var(--space-md)
    - All `gap: 8px` → gap: var(--space-sm)
    - All `gap: 6px` → gap: var(--space-xs)
    - All `padding: 16px` → padding: var(--space-lg)
    - All `padding: 8px 12px` → padding: var(--space-sm) var(--space-md)
    - All `border-radius: 12px` → border-radius: var(--radius-lg)
    - All `border-radius: 8px` → border-radius: var(--radius-md)
    - All `border-radius: 6px` → border-radius: var(--radius-sm)
    - All `border-radius: 4px` → border-radius: var(--radius-sm) (if it's a main card/button action, else keep 4px for badges/tags — use judgment)

    **For sql-console.ts**: Apply the same mapping. Key selectors:
    - `:host { gap: 12px; ... }` → gap: var(--space-md)
    - `.tab { font-size: 12px; gap: 4px; padding: 4px 10px; border-radius: var(--radius-sm, 6px); ... }` → font-size: var(--text-sm); gap: var(--space-xs); remove fallback in --radius-sm
    - `.tab.active { ... }` → update border-radius fallback if present
    - `.btn { font-size: 13px; ... }` → font-size: var(--text-base)
    - `.toolbar { gap: 10px; ... }` → gap: var(--space-md)
    - `.toolbar select { font-size: 13px; ... }` → font-size: var(--text-base)
    - `.shortcut { font-size: 11px; ... }` → font-size: var(--text-xs)
    - `.main-area { gap: 12px; ... }` → gap: var(--space-md)
    - `.browser { border-radius: 8px; ... }` → border-radius: var(--radius-md)
    - `.browser-header { font-size: 12px; ... }` → font-size: var(--text-sm)
    - `.schema-name { font-size: 12px; ... }` → font-size: var(--text-sm)
    - `.col-name { font-size: 11px; ... }` → font-size: var(--text-xs)
    - `.col-type { font-size: 10px; ... }` → font-size: 10px (keep — no variable for 10px)
    - `.results { border-radius: 8px; ... }` → border-radius: var(--radius-md)
    - `.results-header { font-size: 12px; ... }` → font-size: var(--text-sm)
    - `.results th { font-size: 12px; ... }` → font-size: var(--text-sm)
    - All `gap: 10px` → gap: var(--space-md)
    - All `gap: 2px` → gap: 2px (keep — no variable maps)
    - All `border-radius: 12px` → border-radius: var(--radius-lg)
    - All `border-radius: 8px` → border-radius: var(--radius-md)
    - All `border-radius: 4px` → border-radius: var(--radius-sm)
    - All `border-radius: 3px` → border-radius: var(--radius-sm) (close enough, unify)
    - All `padding: 6px 16px` → padding: var(--space-sm) var(--space-lg)
    - All `padding: 6px 10px` → padding: var(--space-sm) var(--space-md)
    - All `padding: 4px 8px` → padding: var(--space-xs) var(--space-sm)
    - All `padding: 8px 12px` → padding: var(--space-sm) var(--space-md)
    - All `padding: 8px 14px` → padding: var(--space-sm) var(--space-md)

    Skip: Values inside URL strings, HTML attributes, JavaScript logic, or template expressions (${\}).

    Browse each file fully and replace ALL eligible hardcoded values. Do not modify OpenClaw-native sub-modules (like config-form.*, markdown-sidebar.ts).
  </action>
  <verify>
    <automated>cd /Users/max/Library/CloudStorage/OneDrive-个人/03-Coding/39-Slide/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    Manual: Visit http://localhost:5173/dashboard, http://localhost:5173/approval, http://localhost:5173/sql-console — verify pages render correctly with consistent spacing and typography.
  </verify>
  <done>approval-dashboard.ts and sql-console.ts static styles use --text-*, --space-*, --radius-* variables instead of hardcoded font-size, gap, padding, border-radius values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → DOM | CSS variables cascade into component shadow DOM, no cross-origin data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-03 | T (Tampering) | base.css | mitigate | CSS variable changes are cosmetic only — no runtime logic affected |
| T-91-04 | I (Info Disclosure) | CSS fallback values | accept | var(--*) without fallback in older browsers falls back to empty/undefined, causing minor visual glitch only — acceptable for a tool's UI |
</threat_model>

<verification>
1. Verify all 12 CSS variables exist in base.css: `grep --text-x\|--space-x base.css` shows all 12
2. `cd frontend && npx tsc --noEmit` must pass
3. grep dashboard.ts for `font-size: 11px` and `font-size: 22px` — ideally 0
4. grep approval-dashboard.ts for `font-size: 14px` — ideally 0 (all replaced with var(--text-md))
5. Visual confirmation that dark/light theme switching works on all three pages
</verification>

<success_criteria>
- base.css defines 12 unified design tokens (7 typography + 5 spacing)
- Dashboard stat cards use unified typography and spacing tokens
- Approval dashboard uses consistent spacing and badge sizes
- SQL console uses unified gap/spacing values
- All three pages compile without TypeScript errors
- Visual appearance is consistent (no layout shifts, broken spacing, or font-size regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/91-ui-standardization/91-02-SUMMARY.md`
</output>
