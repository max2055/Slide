---
status: complete
phase: 125-ssh-metrics
source: 125-01-SUMMARY.md, 125-02-SUMMARY.md
started: 2026-07-08T23:30:00Z
updated: 2026-07-08T23:55:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running server/service. Clear ephemeral state. Start the application from scratch (backend + frontend). Server boots without errors, migration 020 completes, and GET /api/health returns live data. Server list page loads at http://localhost:5173 without console errors.
result: pass

### 2. Server List — Metric Badges
expected: Server list page displays real-time CPU, Memory, Disk badges for each server. Badges are color-coded: green (< 50%), amber (50-80%), red (> 80%). Each server row also shows a "Last Collection" timestamp in relative time.
result: pass

### 3. Server List — Detail Button Navigation
expected: Each server row has a detail button. Clicking it navigates to the server detail page.
result: pass
reason: Host click navigation replaced by detail button (phase 124 adjustment)

### 4. 服务器详情 — 概览标签页
expected: 打开服务器详情页，默认显示"概览"标签页。页面展示 2x2 网格的 4 张摘要卡片：CPU 使用率 %、内存使用率 % 及 used/total、磁盘使用率 %、Load average。
result: pass (after fix)
fix: "cc34a06 (app.ts 导航 serverId) + 6071d12 (CentOS OS 归一化) + c486f27 (server-detail safety net)"

### 5. 服务器详情 — 指标标签页（图表）
expected: 切换到"指标"标签页显示 ECharts 趋势图。范围选择按钮（1h/6h/24h/7d/30d）可切换时间范围。
result: pass

### 6. 服务器详情 — 配置标签页
expected: 切换到"配置"标签页显示服务器配置信息。返回按钮可回到服务器列表。
result: pass

### 7. Metric API — 最新指标值
expected: GET /api/servers/:id/metrics 返回最新指标数据。
result: pass
verified: "返回 19 个指标 (cpu_usage=12.8, memory_usage=24.43, disk_usage_root=44, load_1min=0.09 等)"

### 8. Metric API — 历史范围
expected: GET /api/servers/:id/metrics/history?range=... 返回时间序列数据。
result: pass

### 9. Metric API — 一键采集
expected: POST /api/servers/:id/collect 触发即时采集。
result: pass
verified: "返回 {success:true, metrics_count:19}"

### 10. 服务器状态自动转换
expected: 采集连续失败 3 次后状态转 "unreachable"，成功后转 "online"。
result: pass
verified: "服务器 192.168.64.5 初始为 unreachable，采集成功后转为 online"

### 11. SSH 主机密钥验证
expected: 存储 host_key_fingerprint 的服务器连接时验证指纹匹配。
result: pass
verified: "CR-01 修复已实现 SHA-256 指纹比对 (c9e1eca)"

## Summary

total: 11
passed: 10
issues: 1 (已修复)
pending: 0
skipped: 0

## Gaps

- truth: "打开服务器详情页应展示概览标签页的摘要卡片和状态信息"
  status: fixed
  reason: "两个根因：(1) app.ts 导航处理器不识别 serverId 字段 — cc34a06 (2) server-metric-provider 仅 osType==='linux' 匹配 — 6071d12"
  severity: blocker
  test: 4
  root_cause: "app.ts 读取 e.detail.id 而非 serverId + provider 仅匹配 'linux'"
  fix: "cc34a06 (导航), 6071d12 (OS), c486f27 (safety net)"
