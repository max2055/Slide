---
phase: 133-operation-sql-safety
plan: 02
type: execute
wave: 2
depends_on: [133-01]
files_modified:
  - apps/db-ops-api/src/approval-service.ts
  - apps/db-ops-api/src/auth/approval-flow.ts
  - apps/db-ops-api/src/audit/audit-log.ts
  - apps/db-ops-api/src/operations/operation-service.ts
  - apps/db-ops-api/server.ts
  - frontend/src/app/ui/views/approval-dashboard.ts
  - frontend/src/app/ui/views/sql-console.ts
  - frontend/src/app/ui/components/operation-timeline.ts
  - apps/db-ops-api/src/operations/approval-concurrency.test.ts
  - frontend/e2e/operation-lifecycle.spec.ts
autonomous: true
requirements: [HI-02, ME-07, TG-02]
must_haves:
  truths:
    - 竞争审核最多一个请求能认领并触发目标副作用
    - 审批、Operation、SQL 历史和审计共享 correlation lineage
    - 页面刷新后展示服务端状态、时间线、错误和合法动作
---

# Phase 133 Plan 02：原子审批、审计血缘与 Operation UI

<objective>
把审批从非原子顺序调用改为可认领、可恢复的 Operation 工作流，并让用户看到真实执行状态与恢复选项。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-operation-sql-safety/133-CONTEXT.md
@apps/db-ops-api/src/approval-service.ts
@frontend/src/app/ui/views/approval-dashboard.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 实现原子认领与崩溃恢复</name>
  <files>apps/db-ops-api/src/approval-service.ts, apps/db-ops-api/src/auth/approval-flow.ts, apps/db-ops-api/src/operations/operation-service.ts, apps/db-ops-api/src/operations/approval-concurrency.test.ts</files>
  <action>
    审核批准使用条件 UPDATE/CAS 从 pending -> claimed 并绑定 reviewer、lease 和 Operation，affectedRows != 1 立即返回 already_claimed。执行前落 running 事件，执行后在事务中写终态、审批事件和 SQL history。外部数据库无法提供 exactly-once 时，用持久幂等键和可观测 lease；崩溃恢复只有在可证明未执行时重试，否则进入 unknown 并要求人工核对。拒绝、取消和超时走相同迁移表。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/operations/approval-concurrency.test.ts</automated></verify>
  <done>20 个并发批准只有一次 driver execute、一个 success 终态，其余获得确定冲突响应。</done>
</task>

<task type="auto">
  <name>Task 2: 串联审批、历史、审计和补偿信息</name>
  <files>apps/db-ops-api/src/audit/audit-log.ts, apps/db-ops-api/src/approval-service.ts, apps/db-ops-api/src/operations/operation-service.ts, apps/db-ops-api/server.ts</files>
  <action>
    统一写入 approval_request_id、operation_id、correlation_id、actor/policy decision。补偿信息必须区分 verified compensation、manual recovery、not available；没有已验证逆操作时不得显示“可回滚”。增加 Operation list/detail/events/cancel/retry API，动作由状态与权限共同决定。retry 创建新 attempt/event，不覆盖历史。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/operations -t "lineage|compensation|retry"</automated></verify>
  <done>任一审批执行可从 API 一跳追到请求、策略、SQL 历史、审计事件和恢复说明。</done>
</task>

<task type="auto">
  <name>Task 3: 构建 Operation 时间线和真实反馈</name>
  <files>frontend/src/app/ui/components/operation-timeline.ts, frontend/src/app/ui/views/approval-dashboard.ts, frontend/src/app/ui/views/sql-console.ts, frontend/e2e/operation-lifecycle.spec.ts</files>
  <action>
    使用共享组件规则实现 Operation 状态/事件时间线；审批和 SQL 页面都链接同一详情。按钮按服务端 capabilities/actions 渲染，点击后等待 API 结果并刷新，不本地模拟成功。unknown、failed、cancelled 和 lease recovery 使用不同说明；刷新/重连后仍可恢复。测试包含并发冲突、网络失败、取消不可用和人工核对状态。
  </action>
  <verify><automated>pnpm --filter slide-frontend exec playwright test e2e/operation-lifecycle.spec.ts</automated></verify>
  <done>用户可判断谁批准、是否执行、最终结果、失败原因和下一合法动作。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 双击/并发批准 | CAS claim + affectedRows gate |
| 崩溃后重复外部写 | lease + idempotency + unknown 人工核对 |
| 非授权取消/重试 | ActorContext 与服务端 action capability |
| 虚假回滚承诺 | 仅 verified compensation 标记可回滚 |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run src/operations/approval-concurrency.test.ts`
- `pnpm --filter slide-frontend typecheck`
- `pnpm --filter slide-frontend exec playwright test e2e/operation-lifecycle.spec.ts`
</verification>

<success_criteria>
1. HI-02 与 ME-07 关闭并有并发/崩溃证据。
2. 刷新后 UI 不丢失 Operation 终态和血缘。
3. 输出 `.planning/phases/133-operation-sql-safety/133-02-SUMMARY.md`。
</success_criteria>

