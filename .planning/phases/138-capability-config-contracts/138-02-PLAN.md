---
phase: 138-capability-config-contracts
plan: 02
type: execute
wave: 2
depends_on: [138-01, 132-01]
files_modified:
  - apps/db-ops-api/sql/migrations/034_versioned_config.sql
  - apps/db-ops-api/src/config/config-release-service.ts
  - apps/db-ops-api/src/http/schemas.ts
  - apps/db-ops-api/src/http/openapi.ts
  - apps/db-ops-api/server.ts
  - frontend/src/api/generated.ts
  - frontend/src/app/ui/navigation.ts
  - frontend/src/app/ui/app-events.ts
  - frontend/src/app/ui/views/cron-jobs-settings.ts
  - frontend/src/app/ui/views/reports.ts
  - frontend/src/app/ui/views/metric-registry.ts
  - frontend/src/app/ui/views/alerts.ts
  - apps/db-ops-api/tests/config-release.test.ts
  - frontend/e2e/config-release.spec.ts
autonomous: true
requirements: [LO-01, OPT-03, ME-05]
must_haves:
  truths:
    - 高风险配置经过 draft、validate、publish、rollback，失败发布不改变 effective version
    - REST DTO 以 TypeBox/OpenAPI 为事实源，前端不再复制关键响应类型
    - 导航 route/payload 类型化，可见配置控件具备真实保存、错误和刷新反馈
---

# Phase 138 Plan 02：版本化配置、类型化 API 与导航契约

<objective>
把告警、指标、Cron、报表和 Agent tool policy 从直接覆盖配置升级为可预检、发布、审计和回滚的变更流程，并消除前后端契约漂移。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-capability-config-contracts/138-CONTEXT.md
@apps/db-ops-api/src/utils/strict-body.ts
@frontend/src/api/index.ts
@frontend/src/app/ui/navigation.ts
@frontend/src/app/ui/app-events.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 建立配置 draft/validate/publish/rollback 模型</name>
  <files>apps/db-ops-api/sql/migrations/034_versioned_config.sql, apps/db-ops-api/src/config/config-release-service.ts, apps/db-ops-api/tests/config-release.test.ts</files>
  <action>
    config aggregates/versions/releases 记录 type、schemaVersion、content、author、diff、validation result、state、effectiveAt、previous/effective version 和 Operation。首批 adapter 覆盖 alert rules、metric collection、Cron、report config、Agent tool policy；user preferences 不纳入。每类提供 schema validator 和 dry-run/preview；publish 在事务中 CAS effective pointer 并发 outbox，高风险类型要求 Phase 132 policy/Phase 133 Operation。rollback 创建新版本，不删除历史。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/config-release.test.ts -t "draft|validate|publish|rollback|concurrent"</automated></verify>
  <done>无效/竞争发布不改变 effective version；回滚可追到原版本和操作者。</done>
</task>

<task type="auto">
  <name>Task 2: 以 TypeBox/OpenAPI 统一 REST DTO</name>
  <files>apps/db-ops-api/src/http/schemas.ts, apps/db-ops-api/src/http/openapi.ts, apps/db-ops-api/server.ts, frontend/src/api/generated.ts</files>
  <action>
    优先覆盖 auth/ActorContext、resources/observations、Operations、incidents/investigation、reports/notifications、capabilities/config releases。Fastify route 同时绑定 request/response schema，未知字段与错误使用稳定 envelope。生成前端类型和轻量 client，CI 检查生成物无 diff。secret 字段从 schema 层即不可响应。迁移期间逐 endpoint 替换 `any`/手写 interface，并记录未覆盖清单，禁止一次重写全部 server.ts。
  </action>
  <verify><automated>pnpm --filter slide-api typecheck &amp;&amp; pnpm --filter slide-frontend typecheck &amp;&amp; pnpm --filter slide-api exec tsx scripts/check-openapi-generated.ts</automated></verify>
  <done>关键闭环 API 的后端 schema 与前端类型由同一事实源产生，错误响应可判别。</done>
</task>

<task type="auto">
  <name>Task 3: 类型化导航和配置发布 UI</name>
  <files>frontend/src/app/ui/navigation.ts, frontend/src/app/ui/app-events.ts, frontend/src/app/ui/views/cron-jobs-settings.ts, frontend/src/app/ui/views/reports.ts, frontend/src/app/ui/views/metric-registry.ts, frontend/src/app/ui/views/alerts.ts, frontend/e2e/config-release.spec.ts</files>
  <action>
    定义 RouteId -> params 映射和 route helper，替换自由字符串 CustomEvent payload；i18n key 缺失在测试失败而非显示 key。配置页显示 draft diff、validation、publish Operation、effective version、rollback；布尔属性使用 `.property`，复用 app-dialog/form/data-table/toast。保存必须处理 optimistic conflict、API failure 和刷新后 effective state。测试从告警/报表跳转到资源/Operation 的 payload。
  </action>
  <verify><automated>pnpm --filter slide-frontend exec playwright test e2e/config-release.spec.ts</automated></verify>
  <done>LO-01 的导航/认证前请求漂移被纠正，所有可见发布按钮有真实持久结果和失败反馈。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 未验证配置直接生效 | state machine + validator + effective pointer CAS |
| 并发发布覆盖 | expected version / optimistic conflict |
| 恶意策略降权/提权 | ActorContext + approval Operation |
| DTO 漂移重新暴露 secret | response schema allowlist + generated client |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run tests/config-release.test.ts`
- OpenAPI generated diff check、前后端 typecheck。
- `pnpm --filter slide-frontend exec playwright test e2e/config-release.spec.ts`
</verification>

<success_criteria>
1. LO-01、OPT-03 和配置直接覆盖风险关闭。
2. 五类高风险配置可 preview/publish/rollback 并关联 Operation。
3. 输出 `.planning/phases/138-capability-config-contracts/138-02-SUMMARY.md`。
</success_criteria>

