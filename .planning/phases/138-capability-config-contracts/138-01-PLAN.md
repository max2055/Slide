---
phase: 138-capability-config-contracts
plan: 01
type: execute
wave: 1
depends_on: [136-03, 137-03]
files_modified:
  - apps/db-ops-api/src/adapters/capability-matrix.ts
  - apps/db-ops-api/src/database-service.ts
  - apps/db-ops-api/src/collectors/registry.ts
  - apps/db-ops-api/src/collection-capabilities.ts
  - apps/db-ops-api/src/cron/types.ts
  - apps/db-ops-api/server.ts
  - frontend/src/app/ui/views/instances-db.ts
  - frontend/src/app/ui/views/instance-detail.ts
  - docs/slide/USER-GUIDE.md
  - apps/db-ops-api/tests/adapter-capability-matrix.test.ts
  - frontend/e2e/adapter-capabilities.spec.ts
autonomous: true
requirements: [ME-06, TG-05, STATE-84, STATE-95, STATE-110]
must_haves:
  truths:
    - 支持数据库列表在创建 UI、API、adapter、采集器、Agent 和文档中来自同一能力矩阵
    - 只有带环境/版本证据的能力可标记 verified；失败按能力降级而不是伪装整类支持
    - MongoDB、Redis、Elasticsearch 在无完整 adapter/UAT 时不能作为可创建的受支持数据库
---

# Phase 138 Plan 01：Adapter 能力矩阵与 Deferred UAT 清账

<objective>
把“支持某数据库”从散落字符串声明改为可验证能力契约，并实际执行 STATE 中 Phase 84/95/110 的延期 UAT。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-capability-config-contracts/138-CONTEXT.md
@.planning/phases/84-rbac-foundation/84-UAT.md
@.planning/phases/95-dameng-database-support/95-UAT.md
@.planning/phases/110-directadapter-switch/110-UAT.md
@apps/db-ops-api/src/database-service.ts
@frontend/src/app/ui/views/instances-db.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 定义版本化数据库能力矩阵</name>
  <files>apps/db-ops-api/src/adapters/capability-matrix.ts, apps/db-ops-api/src/database-service.ts, apps/db-ops-api/src/collectors/registry.ts, apps/db-ops-api/src/collection-capabilities.ts, apps/db-ops-api/tests/adapter-capability-matrix.test.ts</files>
  <action>
    每种 adapter 声明 driver/version range 和 connect/query/explain/metrics/health/alerts/reports/writeApproval 能力以及 probe。矩阵状态复用 Phase 136 declared/configured/verified/degraded/unsupported；verified 必须附 environment、server/driver version、test run、checkedAt/expiry。服务调度能力前先查矩阵，不用 db_type switch 默认成功。某能力失败只降级该 capability 并给 reason。Mongo/Redis/Elasticsearch 明确 unsupported，除非本 Phase 实际补全 adapter（非目标）。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/adapter-capability-matrix.test.ts -t "matrix|probe|unsupported"</automated></verify>
  <done>所有 db type 均有完整矩阵行，声明、实现和 probe 三者不一致会使测试失败。</done>
</task>

<task type="auto">
  <name>Task 2: 统一 API、创建 UI、Agent/Cron 和文档支持声明</name>
  <files>apps/db-ops-api/src/cron/types.ts, apps/db-ops-api/server.ts, frontend/src/app/ui/views/instances-db.ts, frontend/src/app/ui/views/instance-detail.ts, docs/slide/USER-GUIDE.md, frontend/e2e/adapter-capabilities.spec.ts</files>
  <action>
    增加只读 capability catalog API。实例创建下拉只显示可创建/configured+ 的 adapter，unsupported 在说明区而非可提交选项；后端同样拒绝，不能只靠 UI。详情页按该资源 verified capabilities 渲染 SQL/metrics/alerts/report actions。Cron target 类型、Agent tools 和文档从同一生成/共享 catalog 派生或由一致性测试约束，删除七类型硬编码漂移。
  </action>
  <verify><automated>pnpm --filter slide-frontend exec playwright test e2e/adapter-capabilities.spec.ts &amp;&amp; pnpm --filter slide-api vitest run tests/adapter-capability-matrix.test.ts -t "surface parity"</automated></verify>
  <done>UI/API/文档列表一致，不支持类型无法通过伪造请求创建，能力缺失入口不显示。</done>
</task>

<task type="auto">
  <name>Task 3: 执行四数据库矩阵和 Phase 84/95/110 UAT</name>
  <files>apps/db-ops-api/tests/adapter-capability-matrix.test.ts, frontend/e2e/adapter-capabilities.spec.ts, .planning/phases/138-capability-config-contracts/138-UAT.md</files>
  <action>
    MySQL/PostgreSQL 使用容器，Oracle/Dameng 使用明确记录版本的受控环境；逐项执行连接、只读查询、explain、metrics、health、失败恢复和写审批。重跑 Phase 84 的 RBAC 主体矩阵、Phase 95 的 4 个 Dameng pending、Phase 110 的 6 个 DirectAdapter pending。无法取得商业数据库环境时不得标 verified：记录 blocked evidence 并从生产支持面降级，不把 mock/source regex 当 UAT。
  </action>
  <verify><automated>test -f .planning/phases/138-capability-config-contracts/138-UAT.md &amp;&amp; ! rg -n "pending|testing" .planning/phases/138-capability-config-contracts/138-UAT.md</automated></verify>
  <done>每个延期用例均为 passed/failed/removed-from-support，STATE 不再保留模糊 testing/partial。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 未实现 adapter 被伪装支持 | server-side catalog gate |
| 过期验证长期标绿 | evidence expiry + periodic probe |
| capability probe 泄露凭据 | public evidence DTO + secret redaction |
| 测试环境与生产版本不符 | 记录 server/driver version range |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run tests/adapter-capability-matrix.test.ts`
- `pnpm --filter slide-frontend exec playwright test e2e/adapter-capabilities.spec.ts`
- 检查 84/95/110 UAT 每个 deferred ID 有终态和证据。
</verification>

<success_criteria>
1. ME-06、TG-05 与三个 STATE deferred UAT 清账。
2. 生产支持声明只包含 verified 或明确配置中的能力。
3. 输出 `.planning/phases/138-capability-config-contracts/138-01-SUMMARY.md`。
</success_criteria>

