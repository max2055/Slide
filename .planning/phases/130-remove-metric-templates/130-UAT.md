---
status: complete
phase: 130-remove-metric-templates
source: derived from implementation changes + REVIEW.md
started: 2026-07-10T20:00:00Z
updated: 2026-07-10T20:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill backend, restart. Server boots cleanly without import errors. `/api/health` returns OK.
result: pass

### 2. Template Management Page Gone from Nav
expected: Open frontend. The navigation bar no longer shows "模板管理" tab. Clicking through tabs doesn't include a Templates page.
result: pass

### 3. Metric Templates API Returns 404
expected: `GET /api/metric-templates` returns 404 (route removed).
result: pass

### 4. Instance Templates API Returns 404
expected: `GET /api/instances/1/templates` returns 404 (route removed).
result: pass

### 5. Alert Rules Can Be Listed
expected: `GET /api/alert-rules` still works, returns list of rules. Rules with `template_id` still show the field (backward compat).
result: pass

### 6. Alert Rule Templates POST Validates Required Fields
expected: `POST /api/alert-rule-templates` with empty body returns 400 with error about missing required fields (name, metric_name, operator).
result: pass

### 7. resolveMacrosForRule Uses ES Module Import
expected: Code compiles without `require('./metric-registry')` in alert-evaluator.ts. `resolveMacrosForRule` signature is `(rule: AlertRule)` — single parameter.
result: pass

### 8. No Remaining References to Deleted Files
expected: `grep -r "template-database-service\|metric-templates.ts" --include="*.ts" | grep -v node_modules | grep -v ".planning"` returns no results.
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
