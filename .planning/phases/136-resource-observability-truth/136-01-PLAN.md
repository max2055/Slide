---
phase: 136-resource-observability-truth
plan: 01
type: execute
wave: 1
depends_on: [134-01]
files_modified:
  - apps/db-ops-api/sql/migrations/029_resource_observations.sql
  - apps/db-ops-api/src/resources/types.ts
  - apps/db-ops-api/src/resources/resource-service.ts
  - apps/db-ops-api/src/resources/observation-service.ts
  - apps/db-ops-api/src/resources/capability-service.ts
  - apps/db-ops-api/server.ts
  - apps/db-ops-api/src/resources/resource-service.test.ts
autonomous: true
requirements: [HI-05, HI-08, ME-06, DR-04]
must_haves:
  truths:
    - instance 和 server 通过同一 ResourceRef 查询关系、能力和 Observation，但保留各自子类型表
    - Observation 明确 observed_at、valid_until、source、quality 和 reason，stale/unknown 不等于 healthy
    - 资源关系有类型、来源和有效期，不从 cache_key 或名称猜测
---

# Phase 136 Plan 01：ResourceRef、关系、Observation 与能力状态

<objective>
建立观测领域的最小统一语言，解决实例/服务器逻辑分叉，同时避免创建全部字段可空的巨型资源表。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-resource-observability-truth/136-CONTEXT.md
@apps/db-ops-api/src/instance-database-service.ts
@apps/db-ops-api/src/server-database-service.ts
@apps/db-ops-api/src/collection-capabilities.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 定义 ResourceRef 和显式资源关系</name>
  <files>apps/db-ops-api/sql/migrations/029_resource_observations.sql, apps/db-ops-api/src/resources/types.ts, apps/db-ops-api/src/resources/resource-service.ts, apps/db-ops-api/src/resources/resource-service.test.ts</files>
  <action>
    `ResourceRef {type:'instance'|'server', id}` 使用规范 string/DB 表达；保留 database_instances/servers 作为详情事实源。resource_relations 存 source/target ref、relationType(runs_on/hosts/replicates_to/depends_on)、provenance、validFrom/validUntil，约束去重和自环策略。service 验证两端存在、actor scope 和 relation 类型；提供批量解析避免 N+1。现有 server_id/instance_id 在边界转成 ResourceRef，不立即删除旧 FK。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/resources/resource-service.test.ts -t "ref|relation"</automated></verify>
  <done>同一 API 可解析实例/服务器，关系可追溯来源且过期关系不会作为当前事实。</done>
</task>

<task type="auto">
  <name>Task 2: 实现规范 Observation 读取模型</name>
  <files>apps/db-ops-api/sql/migrations/029_resource_observations.sql, apps/db-ops-api/src/resources/types.ts, apps/db-ops-api/src/resources/observation-service.ts, apps/db-ops-api/src/resources/resource-service.test.ts</files>
  <action>
    Observation 规范化 resource、metricId、dimensions、value/unit、observedAt、validUntil、source、quality(good/degraded/invalid/unknown)、reason。先通过 adapter 读取现有 metrics_history/server_metrics，不强制合并物理表；所有最新值都计算 freshness，缺失返回 unknown 而不是 0。dimensions 使用规范排序/hash，磁盘挂载点不得编码进 metric 名。API 支持批量 latest/range 并限制窗口。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/resources/resource-service.test.ts -t "observation|stale|unknown"</automated></verify>
  <done>fresh/stale/missing/invalid fixture 在 instance/server 得到相同语义。</done>
</task>

<task type="auto">
  <name>Task 3: 持久能力状态和统一查询 API</name>
  <files>apps/db-ops-api/sql/migrations/029_resource_observations.sql, apps/db-ops-api/src/resources/capability-service.ts, apps/db-ops-api/server.ts, apps/db-ops-api/src/resources/resource-service.test.ts</files>
  <action>
    持久 capability key、state(declared/configured/verified/degraded/unsupported)、evidence、checkedAt、validUntil 和 reason。verified 只能由实际探测/UAT 写入；过期自动降为 configured/unknown 视图。增加 resource detail/relations/observations/capabilities API，统一 ActorContext scope 和分页。Phase 138 的公开支持矩阵直接消费此服务。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/resources/resource-service.test.ts -t "capability|api"</automated></verify>
  <done>能力状态不会由静态声明冒充 verified，API 对无 scope 资源返回 404/deny。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 猜测 resource id 越权 | ActorContext resource scope |
| 过期指标冒充当前健康 | validUntil + explicit stale/unknown |
| 伪造关系/能力证据 | provenance 与写权限，verified 受探测器约束 |
| 高基数 dimensions 滥用 | schema/长度/数量限制与 canonical hash |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run src/resources/resource-service.test.ts`
- 对现有 instance/server 数据运行 backfill dry-run，核对数量与孤儿引用。
</verification>

<success_criteria>
1. ResourceRef 成为新领域/API 契约，旧子类型数据不丢失。
2. stale/unknown 在后续告警、健康和报表可复用。
3. 输出 `.planning/phases/136-resource-observability-truth/136-01-SUMMARY.md`。
</success_criteria>

