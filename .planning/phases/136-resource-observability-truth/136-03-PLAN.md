---
phase: 136-resource-observability-truth
plan: 03
type: execute
wave: 3
depends_on: [136-02]
files_modified:
  - apps/db-ops-api/src/alerts/compiled-rule.ts
  - apps/db-ops-api/src/alerts/alert-evaluation-service.ts
  - apps/db-ops-api/src/alert-evaluator.ts
  - apps/db-ops-api/src/server-alert-evaluator.ts
  - apps/db-ops-api/src/consistency-checker.ts
  - apps/db-ops-api/server.ts
  - frontend/src/app/ui/views/health-center.ts
  - frontend/src/app/ui/views/dashboard.ts
  - apps/db-ops-api/tests/unified-alert-health.test.ts
  - frontend/e2e/health-truth.spec.ts
autonomous: true
requirements: [HI-05, HI-06, DR-04]
must_haves:
  truths:
    - instance/server 规则由同一 compiler/evaluator 执行 target、operator、三级阈值、duration、恢复和去重语义
    - 健康输出分 control plane、managed availability、data freshness 和 workflow health
    - 关键 critical/unknown/stale 不会被普通 pass 项稀释成全绿
---

# Phase 136 Plan 03：统一告警语义与健康真相

<objective>
统一两套告警评估器，并重定义健康/readiness 聚合，使 UI 与 API 准确反映测量对象、分母和数据新鲜度。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-resource-observability-truth/136-CONTEXT.md
@apps/db-ops-api/src/alert-evaluator.ts
@apps/db-ops-api/src/server-alert-evaluator.ts
@apps/db-ops-api/src/consistency-checker.ts
@frontend/src/app/ui/views/health-center.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 编译共享告警规则并统一求值</name>
  <files>apps/db-ops-api/src/alerts/compiled-rule.ts, apps/db-ops-api/src/alerts/alert-evaluation-service.ts, apps/db-ops-api/src/alert-evaluator.ts, apps/db-ops-api/src/server-alert-evaluator.ts, apps/db-ops-api/tests/unified-alert-health.test.ts</files>
  <action>
    compiler 验证 ResourceRef target、canonical metric/dimensions、operator、warning/error/critical thresholds、duration、recovery、silence/dedupe。实例与服务器薄适配器只提供 Observation，统一 service 维护窗口和状态转换。SQL 加 target_type/resource filter；missing/stale/invalid 根据规则策略形成 no_data/unknown，不作为 0。用同一 fixture 对两种资源运行并比较事件序列。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/unified-alert-health.test.ts -t "compiled rule|instance and server"</automated></verify>
  <done>同规则/同 Observation 序列在实例和服务器得到相同级别、持续时间、恢复和去重结果。</done>
</task>

<task type="auto">
  <name>Task 2: 重建四维健康/readiness API</name>
  <files>apps/db-ops-api/src/consistency-checker.ts, apps/db-ops-api/server.ts, apps/db-ops-api/tests/unified-alert-health.test.ts</files>
  <action>
    controlPlaneReadiness 只衡量配置/schema/listener/worker；managedResourceAvailability 逐资源连接和状态；dataFreshness 逐资源/关键指标；workflowHealth 衡量 outbox/jobs/notifications/Operations。每维返回 status、numerator/denominator、failed refs、observedAt、reason。总体使用 severity cap：任一必需维度 critical/unknown 不能显示 healthy/100%；不得用检查项数量平均。保留 `/api/health` liveness 的轻量语义，新增/修订 readiness endpoint。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/unified-alert-health.test.ts -t "4 of 5 critical|stale|readiness"</automated></verify>
  <done>4/5 critical fixture 明确显示 managed critical、20% 可用及失败资源，不再“全部就绪”。</done>
</task>

<task type="auto">
  <name>Task 3: 前端呈现测量对象、分母与未知状态</name>
  <files>frontend/src/app/ui/views/health-center.ts, frontend/src/app/ui/views/dashboard.ts, frontend/e2e/health-truth.spec.ts</files>
  <action>
    健康中心按四维展示，不用单一无权重百分比。每维可展开失败 ResourceRef、数据时间和 reason；unknown/stale 采用独立 badge，不渲染绿色。Dashboard 摘要与 health API 共用类型和状态映射。E2E 通过 fixture 对比 UI 文本、API 和持久 Observation，并验证从 critical 恢复后的状态转换。
  </action>
  <verify><automated>pnpm --filter slide-frontend exec playwright test e2e/health-truth.spec.ts</automated></verify>
  <done>操作员能区分 Slide 控制面正常与纳管数据库大面积故障，两者不会互相掩盖。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 错误全绿导致漏报 | severity cap + 逐资源分母 |
| 跨 target 规则误用 | compiler + SQL target filter |
| stale 数据触发/恢复错误 | Observation freshness 显式参与求值 |
| 告警风暴 | 共享 duration/recovery/dedupe 状态机 |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run tests/unified-alert-health.test.ts`
- `pnpm --filter slide-frontend exec playwright test e2e/health-truth.spec.ts`
- 复用 Phase 131 的 5 实例/4 critical 场景验收。
</verification>

<success_criteria>
1. HI-05、HI-06、DR-04 关闭。
2. API、Dashboard、健康中心和报表对 healthy/stale/unknown 定义一致。
3. 输出 `.planning/phases/136-resource-observability-truth/136-03-SUMMARY.md`。
</success_criteria>

