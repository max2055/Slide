---
phase: 136-resource-observability-truth
plan: 02
type: execute
wave: 2
depends_on: [136-01]
files_modified:
  - apps/db-ops-api/sql/migrations/030_metric_identity.sql
  - apps/db-ops-api/src/metric-registry.ts
  - apps/db-ops-api/src/metric-database-service.ts
  - apps/db-ops-api/src/monitor-collector.ts
  - apps/db-ops-api/src/collector.ts
  - apps/db-ops-api/src/server-collector.ts
  - apps/db-ops-api/src/collectors/registry.ts
  - apps/db-ops-api/tests/metric-identity.test.ts
  - apps/db-ops-api/tests/collection-scheduler.test.ts
autonomous: true
requirements: [HI-07, ME-03, ME-04]
must_haves:
  truths:
    - 指标身份为 resource type + metric id + canonical dimensions，显示名不是 key
    - registry 每个 enabled 定义有生产者，生产者输出都有定义或明确 quarantine
    - 调度只采集当前到期指标，成功重置连续失败，disabled provider 不会被返回
---

# Phase 136 Plan 02：指标身份、采集调度与 Provider 状态

<objective>
修复服务器指标命名漂移、全局最短周期过采集和 provider 双重状态源，使配置的频率与实际采集行为一致。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-resource-observability-truth/136-CONTEXT.md
@apps/db-ops-api/src/metric-registry.ts
@apps/db-ops-api/src/monitor-collector.ts
@apps/db-ops-api/src/collectors/registry.ts
@apps/db-ops-api/src/server-collector.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 规范 metric ID 并迁移定义/规则/数据映射</name>
  <files>apps/db-ops-api/sql/migrations/030_metric_identity.sql, apps/db-ops-api/src/metric-registry.ts, apps/db-ops-api/src/metric-database-service.ts, apps/db-ops-api/tests/metric-identity.test.ts</files>
  <action>
    选择并记录 canonical ID（如 cpu_usage/memory_usage/disk_usage）并由 resource type 消歧，禁止 `server_` 前缀与 target_type 双重命名。迁移 metric definitions、alert rules 和存量 server metric 读取映射；磁盘挂载点进入 dimensions。迁移先生成冲突/孤儿报告，再更新；保留限期 alias 只读兼容。registry 暴露 producer/definition reconciliation，启动时 critical mismatch 降低 readiness。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/metric-identity.test.ts</automated></verify>
  <done>审计中的 6517+ 存量服务器样本可通过 canonical definition 查询，定义/生产者双向差集为 0。</done>
</task>

<task type="auto">
  <name>Task 2: 按 resource/provider/metric 调度到期工作</name>
  <files>apps/db-ops-api/src/monitor-collector.ts, apps/db-ops-api/src/collector.ts, apps/db-ops-api/src/server-collector.ts, apps/db-ops-api/tests/collection-scheduler.test.ts</files>
  <action>
    调度状态粒度固定为 `(ResourceRef, providerId, metricId)`，保存 last/next due 与 schedule version。同一 tick 仅批量合并真正到期且同 provider 可批采的指标；collector 接收 due metric ids，不再收集全部定义。配置变更原子重算 next due，避免短周期饿死长周期。暴露 last/next/result 用于诊断，不将 scheduler Map 作为持久事实。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/collection-scheduler.test.ts -t "30s|60s|300s|due"</automated></verify>
  <done>虚拟时钟证明不同周期按各自次数执行，未到期 metric 的 provider 调用次数为 0。</done>
</task>

<task type="auto">
  <name>Task 3: 单一 Provider 状态源与连续失败语义</name>
  <files>apps/db-ops-api/src/collectors/registry.ts, apps/db-ops-api/src/collector.ts, apps/db-ops-api/src/server-collector.ts, apps/db-ops-api/tests/collection-scheduler.test.ts</files>
  <action>
    registry 唯一保存 enabled、consecutiveFailures、lastSuccess/error 和 scope；删除/忽略 provider.enabled 分叉。list/getProvidersByDbType 只返回 enabled。失败按明确的 resource+provider scope 计数，成功调用 reset；达到阈值后状态 degraded/disabled 并产生 Observation reason，人工/自动恢复有冷却策略。覆盖 fail-success-fail、三连败、禁用后不调、恢复后重新调度。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/collection-scheduler.test.ts -t "failure|disable|recover"</automated></verify>
  <done>“连续失败”与实现一致，disabled provider 不再进入采集路径。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 自定义指标高频造成目标过载 | 调度下限、并发限制、due-only 执行 |
| metric alias 污染规则 | canonical id + 有期限只读 alias |
| provider 错误风暴 | 连续失败熔断与退避 |
| 伪造成功重置状态 | 仅完整有效批次更新 success |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run tests/metric-identity.test.ts tests/collection-scheduler.test.ts`
- 迁移前后对 server_metrics 按 metric/dimension 数量核对为守恒。
</verification>

<success_criteria>
1. HI-07、ME-03、ME-04 关闭。
2. 运行诊断能解释每个指标上次/下次采集和 provider 状态。
3. 输出 `.planning/phases/136-resource-observability-truth/136-02-SUMMARY.md`。
</success_criteria>

