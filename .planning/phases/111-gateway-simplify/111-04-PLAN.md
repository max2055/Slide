---
phase: 111-gateway-simplify
plan: 04
type: execute
wave: 2
depends_on:
  - "111-01"
requirements: ["MIG-06"]
files_modified:
  - "frontend/src/openclaw/ui/app-render.ts"
  - "frontend/src/openclaw/ui/app-render-usage-tab.ts"
  - "frontend/src/openclaw/ui/views/usage.ts"
  - "frontend/src/openclaw/ui/views/cron.ts"
  - "frontend/src/openclaw/ui/views/config.ts"
  - "frontend/src/openclaw/ui/views/skills.ts"
  - "frontend/src/openclaw/ui/views/exec-approval.ts"
  - "frontend/src/openclaw/ui/views/agents-panels-tools-skills.ts"
  - "frontend/src/openclaw/ui/views/agents-panels-status-files.ts"
  - "frontend/src/openclaw/ui/app.ts"
  - "frontend/src/openclaw/ui/app-chat.ts"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Usage, cron, config, skills, exec-approval views render placeholder '此功能暂不可用' message"
    - "Unified placeholder template exists and is reused across all 5 placeholder views"
    - "agents-panels-tools-skills.ts renders placeholder for tools and skills panels"
    - "app-render.ts has no imports from deleted controllers' view files"
    - "app-render.ts tab rendering shows placeholder for usage/cron/skills tabs"
    - "app-render-usage-tab.ts removed or renders placeholder"
    - "app.ts handleAbortChat no longer references abortChatRun"
    - "app-chat.ts no longer references abortChatRun"
  artifacts:
    - path: "frontend/src/openclaw/ui/views/usage.ts"
      contains: "暂不可用"
    - path: "frontend/src/openclaw/ui/views/cron.ts"
      contains: "暂不可用"
    - path: "frontend/src/openclaw/ui/views/config.ts"
      contains: "暂不可用"
    - path: "frontend/src/openclaw/ui/views/skills.ts"
      contains: "暂不可用"
    - path: "frontend/src/openclaw/ui/views/exec-approval.ts"
      contains: "暂不可用"
    - path: "frontend/src/openclaw/ui/views/agents-panels-tools-skills.ts"
      contains: "暂不可用"
    - path: "frontend/src/openclaw/ui/views/agents-panels-status-files.ts"
      contains: "暂不可用"
    - path: "frontend/src/openclaw/ui/app-render.ts"
      does_not_contain: "controllers/config"
    - path: "frontend/src/openclaw/ui/app-render.ts"
      does_not_contain: "controllers/cron"
    - path: "frontend/src/openclaw/ui/app-render.ts"
      does_not_contain: "controllers/exec-approvals"
    - path: "frontend/src/openclaw/ui/app-render.ts"
      does_not_contain: "controllers/skills"
    - path: "frontend/src/openclaw/ui/app-render.ts"
      does_not_contain: "controllers/agent-files"
    - path: "frontend/src/openclaw/ui/app-render.ts"
      does_not_contain: "controllers/agent-identity"
    - path: "frontend/src/openclaw/ui/app-render.ts"
      does_not_contain: "controllers/agent-skills"
    - path: "frontend/src/openclaw/ui/app-render.ts"
      does_not_contain: "controllers/sessions"
    - path: "frontend/src/openclaw/ui/app-chat.ts"
      does_not_contain: "abortChatRun"
    - path: "frontend/src/openclaw/ui/app.ts"
      does_not_contain: "handleAbortChat"
  key_links:
    - from: "app-render.ts"
      to: "views/usage.ts"
      pattern: "usage tab → renderUsage placeholder"
    - from: "app-render.ts"
      to: "views/cron.ts"
      pattern: "cron tab → renderCron placeholder"
    - from: "app-render.ts"
      to: "views/skills.ts"
      pattern: "skills tab → renderSkills placeholder"
    - from: "app-render.ts"
      to: "views/config.ts"
      pattern: "config/appearance/system tabs → renderConfig placeholder"
---

<objective>
Replace view files for deleted controllers with placeholder pages using a unified template. Clean app-render.ts of all dead imports and rendering code.

Per D-02: usage, cron, config, skills, exec-approval views become placeholder pages.
Per D-03: Unified placeholder template (D-03).
Per D-05: Agent tools/skills panels become placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-gateway-simplify/111-CONTEXT.md
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/app-render.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/app-render-usage-tab.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/usage.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/cron.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/config.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/skills.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/exec-approval.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/agents-panels-tools-skills.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/agents-panels-status-files.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/app-chat.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/app.ts

<interfaces>
app-render.ts currently imports from (to be cleaned):
- controllers/agent-files.ts — DELETE (Plan 1)
- controllers/agent-identity.ts — DELETE (Plan 1)
- controllers/agent-skills.ts — DELETE (Plan 1)
- controllers/agents.ts — KEEP (loadAgents only)
- controllers/chat.ts — KEEP (loadChatHistory only)
- controllers/config.ts — DELETE entire import
- controllers/cron.ts — DELETE entire import
- controllers/exec-approvals.ts — DELETE entire import
- controllers/sessions.ts — KEEP (loadSessions only)
- controllers/skills.ts — DELETE entire import

app-render.ts lazy-loads these view modules:
- lazyAgents → views/agents.ts — KEEP (agent management still works for overview/files panel)
- lazyCron → views/cron.ts — REPLACE with placeholder
- lazySessions → views/sessions.ts — KEEP (read-only sessions table from Plan 2)
- lazySkills → views/skills.ts — REPLACE with placeholder

Tab rendering in app-render.ts to update:
- "usage" tab — currently rendered via renderUsageTab(state) (line 1130) — REPLACE with placeholder
- "cron" tab — lazyRender(lazyCron) (lines 1131-1229) — REPLACE with placeholder
- "skills" tab — lazyRender(lazySkills) (lines 1545-1590) — REPLACE with placeholder
- "config" / "appearance" / "system" tabs — renderConfigTabForActiveTab() (lines 1642-1644) — REPLACE with placeholder
- "agents" tab — lazyRender(lazyAgents) — KEEP but agents-panels "tools" and "skills" panels show placeholder
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified placeholder template + replace 5 view files</name>
  <files>
    frontend/src/openclaw/ui/views/usage.ts
    frontend/src/openclaw/ui/views/cron.ts
    frontend/src/openclaw/ui/views/config.ts
    frontend/src/openclaw/ui/views/skills.ts
    frontend/src/openclaw/ui/views/exec-approval.ts
    frontend/src/openclaw/ui/views/agents-panels-tools-skills.ts
    frontend/src/openclaw/ui/views/agents-panels-status-files.ts
  </files>
  <action>
    Read each view file before editing.

    Per D-03: Create a reusable placeholder Lit template function. Since the view files are in the same directory, define it either as a shared helper. The best approach is to define a simple render function in a shared utility file. But to keep it simple, define a function `renderPlaceholderPage(featureName: string)` that returns an `html` template:

    ```typescript
    import { html } from "lit";
    import { icons } from "../../../icons.js";  // or appropriate relative path

    export function renderUnavailablePage(featureName: string, message?: string) {
      return html`
        <section class="card" style="text-align: center; padding: 64px 24px;">
          <div style="font-size: 48px; margin-bottom: 16px; opacity: 0.3;">${icons['slash'] || icons['alert-circle'] || ''}</div>
          <h2 style="margin: 0 0 8px 0;">${featureName}</h2>
          <p style="color: var(--muted); margin: 0;">${message || 'DirectAdapter 模式下暂不支持此功能'}</p>
        </section>
      `;
    }
    ```

    Since all the placeholder view files are in `views/`, define the shared function inline in each view file to avoid import complexity. OR create a small `views/unavailable-page.ts` that exports the function, and import it from each view that needs it.

    **Recommended approach**: Create `views/unavailable-page.ts` with the shared function, then each placeholder view file imports and uses it.

    For each of the 5 view files, **replace the entire file content** with a minimal placeholder:

    1. **views/usage.ts**: Replace entire file. Import renderUnavailablePage and export renderUsage() that returns it.
       - Remove: all existing imports (usage-helpers, usage-metrics, usage-query, usage-render-details, usage-render-overview, types)
       - Add: import { html } from "lit"; import { renderUnavailablePage } from "./unavailable-page";
       - Export: `export function renderUsage(props: any) { return renderUnavailablePage('Usage'); }`

    2. **views/cron.ts**: Same pattern. Remove all imports (controllers/cron, format, presenter, types, ui-types). Add import and export renderCron returning placeholder.
       - Export: `export function renderCron(props: any) { return renderUnavailablePage('Cron'); }`

    3. **views/config.ts**: Same pattern. Remove all imports (icons, storage, theme, theme-transition, types, config-form.shared, config-form). Export renderConfig returning placeholder.
       - Note: config.ts exported types (ConfigProps) are used by app-render.ts. Keep the placeholder's renderConfig signature compatible — accept `any` props.
       - Export: `export function renderConfig(props: any) { return renderUnavailablePage('Config'); }`

    4. **views/skills.ts**: Same pattern. Remove all imports (controllers/skills, format, open-external-url, string-coerce, types, skills-grouping, skills-shared). Export renderSkills returning placeholder.
       - Export: `export function renderSkills(props: any) { return renderUnavailablePage('Skills'); }`

    5. **views/exec-approval.ts**: Same pattern.
       - Export: `export function renderExecApprovalPrompt(state: any) { return nothing; }` — this is rendered as a modal over the app, not a full page. Since the controller is deleted, no data will flow, so render nothing.

    6. **views/agents-panels-tools-skills.ts**: Replace renderAgentTools and renderAgentSkills with placeholder content.
       - Keep the file but replace the export functions with placeholders:
       - `export function renderAgentTools(params: any) { return renderUnavailablePage('Tool Access'); }`
       - `export function renderAgentSkills(params: any) { return renderUnavailablePage('Skills'); }`
       - Remove all the complex rendering logic (renderToolBadges, renderEffectiveToolBadge, agent-tools-grid, agent-tools-presets, agent-skill-group, agent-skill-row, etc.)
       - Remove unused imports (tool-policy-shared, skills-grouping, skills-shared, types)

    7. **views/agents-panels-status-files.ts**: Check if there's a cron panel rendering function (`renderAgentCron`). If cron controller is deleted, this panel should also show placeholder. But this file also has `renderAgentFiles` which uses agent-files controller (deleted by Plan 1). Replace both with placeholders.
       - `export function renderAgentFiles(...) { return renderUnavailablePage('Agent Files'); }`
       - `export function renderAgentCron(...) { return renderUnavailablePage('Agent Cron'); }`
       - Remove unused imports (dompurify, marked, format, presenter, channel-config-extras, types, icons)
  </action>
  <verify>
    <automated>
      for f in usage cron config skills exec-approval; do
        grep -q "暂不可用" frontend/src/openclaw/ui/views/$f.ts || echo "FAIL: $f.ts missing placeholder text"
      done
      grep -q "暂不可用" frontend/src/openclaw/ui/views/agents-panels-tools-skills.ts || echo "FAIL: tools-skills missing placeholder"
      test -f frontend/src/openclaw/ui/views/unavailable-page.ts || echo "FAIL: unavailable-page.ts not created"
    </automated>
  </verify>
  <done>
    6 view files replaced with placeholder rendering using unified template. unavailable-page.ts created with reusable renderUnavailablePage function.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean app-render.ts and app-render-usage-tab.ts</name>
  <files>
    frontend/src/openclaw/ui/app-render.ts
    frontend/src/openclaw/ui/app-render-usage-tab.ts
  </files>
  <action>
    Read both files before editing.

    **app-render-usage-tab.ts changes:**
    This file imports from controllers/usage.ts (deleted by Plan 1). Replace the entire file with:
    - Import nothing (or just `import { nothing } from "lit"`)
    - Export `renderUsageTab(state: any) { return nothing; }` — the usage tab will render a placeholder via the main app-render.ts

    **app-render.ts changes:**

    1. **Remove ALL imports from deleted controllers:**
       - `import { loadAgentFileContent, loadAgentFiles, saveAgentFile } from "./controllers/agent-files.ts";` (line 21)
       - `import { loadAgentIdentities, loadAgentIdentity } from "./controllers/agent-identity.ts";` (line 22)
       - `import { loadAgentSkills } from "./controllers/agent-skills.ts";` (line 23)
       - Remove the duplicate `import { loadAgents, type AgentsState } from "./controllers/agents.ts";` at line 33 — KEEP the one at line 24-32 that also imports buildToolsEffectiveRequestKey etc. Wait, since Plan 2 removed tools functions from agents.ts, update the remaining import to only import what's still exported.
       
       Actually, for the import at lines 24-32:
       ```
       import {
         buildToolsEffectiveRequestKey,
         loadAgents,
         loadToolsCatalog,
         loadToolsEffective,
         resetToolsEffectiveState,
         refreshVisibleToolsEffectiveForCurrentSession,
         saveAgentsConfig,
       } from "./controllers/agents.ts";
       ```
       Update to only: `import { loadAgents } from "./controllers/agents.ts";` (saveAgentsConfig is also kept by Plan 2, but check if app-render.ts uses it... no, app-render.ts doesn't call saveAgentsConfig directly. It's only in app-render.ts's import list but check usage... it's not used in app-render.ts's code. Remove it.)

       - Remove import at line 34: `import { loadChatHistory } from "./controllers/chat.ts";` — wait, is loadChatHistory still called in app-render.ts? Check usage... Yes, in the chat tab: `void loadChatHistory(state)`. KEEP this import.
       - Remove lines 35-45: 
         ```
         import { applyConfig, ... loadConfig, ... saveConfig, ... } from "./controllers/config.ts";
         ```
         ALL of config.ts is deleted. Remove entire import block.

       - Remove lines 46-63:
         ```
         import { loadCronJobsPage, ... loadMoreCronRuns, ... addCronJob, ... } from "./controllers/cron.ts";
         ```
         ALL of cron.ts is deleted. Remove entire import block.

       - Remove lines 64-69:
         ```
         import { loadExecApprovals, ... saveExecApprovals, ... } from "./controllers/exec-approvals.ts";
         ```
         ALL of exec-approvals.ts is deleted. Remove entire import block.

       - Update lines 70-77:
         ```
         import { branchSessionFromCheckpoint, deleteSessionsAndRefresh, loadSessions, patchSession, restoreSessionFromCheckpoint, toggleSessionCompactionCheckpoints } from "./controllers/sessions.ts";
         ```
         Update to only: `import { loadSessions } from "./controllers/sessions.ts";`

       - Remove lines 78-89:
         ```
         import { closeClawHubDetail, installFromClawHub, installSkill, loadClawHubDetail, loadSkills, saveSkillApiKey, searchClawHub, setClawHubSearchQuery, updateSkillEdit, updateSkillEnabled } from "./controllers/skills.ts";
         ```
         ALL of skills.ts is deleted. Remove entire import block.

    2. **Remove dead lazy view modules:**
       - Remove `const lazyCron = createLazy(() => import("./views/cron.ts"));` (line 157) — cron view is now a placeholder, no lazy loading needed
       - Remove `const lazySkills = createLazy(() => import("./views/skills.ts"));` (line 159) — skills view is now a placeholder
       - Keep `const lazyAgents` (line 156) — agent overview still works
       - Keep `const lazySessions` (line 158) — read-only sessions table works

    3. **Remove `renderUsageTab` import and usage:**
       - Remove: `import { renderUsageTab } from "./app-render-usage-tab.ts";` (line 10) — the function now returns nothing
       - Actually since we're replacing the file content to return nothing, the import can stay but the function will be a no-op. However, it's cleaner to remove the import and the call.
       - Remove the `renderUsageTab(state)` call at line 1130 (the `${renderUsageTab(state)}` in the template)

    4. **Update tab rendering:**

       Replace the "usage" tab rendering. Currently rendered by `renderUsageTab(state)` (line 1130). After removing that, add an inline placeholder:
       ```html
       ${state.tab === "usage" ? html`<section class="card" style="text-align:center;padding:64px 24px;"><h2>Usage</h2><p style="color:var(--muted)">DirectAdapter 模式下暂不支持此功能</p></section>` : nothing}
       ```

       Replace the "cron" tab rendering (lines 1131-1229):
       Instead of `lazyRender(lazyCron, ...)`, render inline placeholder:
       ```html
       ${state.tab === "cron" ? renderUnavailablePage("Cron") : nothing}
       ```
       Wait, but app-render.ts doesn't import renderUnavailablePage. Since this is a simple one-off, use inline html:
       ```html
       ${state.tab === "cron" ? html`<section class="card" style="text-align:center;padding:64px 24px;"><h2>Cron</h2><p style="color:var(--muted)">DirectAdapter 模式下暂不支持此功能</p></section>` : nothing}
       ```

       Replace the "skills" tab rendering (lines 1545-1590):
       Same inline placeholder pattern:
       ```html
       ${state.tab === "skills" ? html`<section class="card" style="text-align:center;padding:64px 24px;"><h2>Skills</h2><p style="color:var(--muted)">DirectAdapter 模式下暂不支持此功能</p></section>` : nothing}
       ```

    5. **Update "config" tab rendering:**
       The config tabs (config, appearance, system) are rendered by `renderConfigTabForActiveTab()` at line 1642. Since config.ts is now a placeholder, the rendered config will show the placeholder text. This is fine — the appearance and system tabs will also show the "Config" placeholder.

       Actually wait — `renderConfigTabForActiveTab()` is defined locally in app-render.ts (lines 571-681). It calls `renderConfig({...commonConfigProps, ...overrides})`. The renderConfig function in views/config.ts now just shows a placeholder. So the inline placeholder approach would be simpler. But the local function `renderConfigTabForActiveTab()` has a lot of complex state logic for different config subsections. 

       Since all config functionality depends on the deleted controller, simplify: replace the entire `renderConfigTabForActiveTab` function to just return the placeholder. Remove the complex section-selection logic, the `ConfigTabOverrides` type, the section key constants, and all the config state variables.

       Actually, the cleanest approach: the view/config.ts placeholder already handles showing the "Config" message. The renderConfigTabForActiveTab() calls renderConfig() which now returns a placeholder. So the config tab in app-render.ts will naturally show the placeholder text. We can leave the renderConfigTabForActiveTab() function in place — it reduces to calling renderConfig with minimal props.

       But there's still dead code: the config-related state variables, section constants, and ConfigTabOverrides type. These are on the AppViewState type which is defined elsewhere. Let me just make renderConfigTabForActiveTab() return a simple placeholder:
       
       Replace the function (lines 571-681) with:
       ```typescript
       const renderConfigTabForActiveTab = () => {
         return html`<section class="card" style="text-align:center;padding:64px 24px;"><h2>Config</h2><p style="color:var(--muted)">DirectAdapter 模式下暂不支持此功能</p></section>`;
       };
       ```
       
       And remove the dead constants and helper functions that were only used by this function:
       - `COMMUNICATION_SECTION_KEYS` (line 268-270)
       - `APPEARANCE_SECTION_KEYS` (line 271)
       - `AUTOMATION_SECTION_KEYS` (lines 272-275)
       - `INFRASTRUCTURE_SECTION_KEYS` (lines 276-286)
       - `AI_AGENTS_SECTION_KEYS` (lines 287-294)
       - `ConfigSectionSelection` type (lines 295-298)
       - `ConfigTabOverrides` type (lines 300-320)
       - `SCOPED_CONFIG_SECTION_KEYS` set (lines 322-328)
       - `normalizeMainConfigSelection()` function (lines 330-338)
       - `normalizeScopedConfigSelection()` function (lines 340-349)
       - All the config selection state variables: configSelection, communicationsSelection, appearanceSelection, automationSelection, infrastructureSelection, aiAgentsSelection
       - All the config tab rendering calls (communications config, appearance config, automation config, infrastructure config, aiAgents config)

       Also remove the config-related state from `commonConfigProps` — but wait, commonConfigProps is used by renderConfigTab() which is what renderConfigTabForActiveTab calls. Since we're replacing the whole function, we can simplify by removing renderConfigTab() too.
       
       Actually, let me re-think. The approach of just leaving renderConfigTabForActiveTab() as a placeholder that returns inline html is cleanest. All the config-related app state, types, and helpers can remain in the file for now. They're unused dead code, but they don't cause runtime errors. Phase 112 can clean them up. The important thing is that the config view shows a placeholder.

       Wait, but app-render.ts imports from controllers/config.ts which is deleted by Plan 1. So the imports MUST be removed for the code to compile. Let me check if there are any imports from controllers/config.ts within the renderConfigTabForActiveTab function... No, the imports are at the top of the file. We already removed them in step 1.

       But there's another issue: app-render.ts has many references to config-related types that are no longer imported:
       - `ConfigProps` type (from views/config.ts) — used in `ConfigTabOverrides` type
       Since we're replacing the section logic, these references go away.

       Let me also remove the config-related state properties from AppViewState... wait, AppViewState is in another file. Just clean the references within app-render.ts.

    6. **Clean up the agents tab rendering:**
       The agents tab rendering (lines 1230-1544, in the `state.tab === "agents"` block) passes various props to the agents view. Some of these props reference deleted state:
       - `agentFiles: { list, loading, error, active, contents, drafts, saving }` — agent-files controller deleted
       - `agentIdentityLoading`, `agentIdentityError`, `agentIdentityById` — agent-identity controller deleted
       - `agentSkills: { report, loading, error, agentId, filter }` — agent-skills controller deleted
       - `toolsCatalog: { loading, error, result }` — tools catalog functions deleted
       - `toolsEffective: { loading, error, result }` — tools effective functions deleted
       
       Remove these from the rendering props. The agents view (views/agents.ts) will still work for the overview and files panels (but files is also deleted by Plan 1).

       Actually, since Plan 1 deleted agent-files.ts, agent-identity.ts, and agent-skills.ts controllers, the agents view's "files" and "skills" panels will show placeholder text (via the views/agents-panels-status-files.ts and views/agents-panels-tools-skills.ts changes in Task 1). So the agents tab still renders the agent selector and overview, but the files/skills/tools panels show placeholders.

       Remove these from the rendering props:
       - Remove the `agentFiles` block (lines 1257-1265)
       - Remove `agentIdentityLoading`, `agentIdentityError`, `agentIdentityById` (lines 1266-1268)
       - Remove `agentSkills` block (lines 1269-1275)
       - Remove `toolsCatalog` block (lines 1276-1280)
       - Remove `toolsEffective` block (lines 1281-1285)
       - Remove `modelCatalog: state.chatModelCatalog` (line 1288) — keep as this is chat-related, not tools-related

       Also clean the callbacks that reference deleted controllers:
       - `onLoadFiles` (lines 1347) — calls loadAgentFiles which is deleted. Remove this callback.
       - `onSelectFile` (lines 1348-1353) — calls loadAgentFileContent which is deleted. Remove.
       - `onFileDraftChange` (lines 1355-1356) — only used by files panel. Remove.
       - `onFileReset` (lines 1358-1359) — only used by files panel. Remove.
       - `onFileSave` (lines 1362-1368) — calls saveAgentFile which is deleted. Remove.
       - `onToolsProfileChange` (lines 1370-1383) — tools functions deleted. Remove.
       - `onToolsOverridesChange` (lines 1384-1402) — tools functions deleted. Remove.
       - `onSkillsFilterChange` (lines 1414) — agent-skills controller deleted. Remove.
       - `onSkillsRefresh` (lines 1415-1419) — calls loadAgentSkills which is deleted. Remove.
       - `onAgentSkillToggle` (lines 1420-1448) — skills controller deleted. Remove.
       - `onAgentSkillsClear` (lines 1449-1455) — skills controller deleted. Remove.
       - `onAgentSkillsDisableAll` (lines 1456-1462) — skills controller deleted. Remove.

       But wait — the agents view (views/agents.ts) still expects these props in its AgentsProps type. Plan 4 doesn't modify views/agents.ts. Hmm, this is a problem.

       Actually, looking at views/agents.ts, the AgentsProps type (line 76+) has fields for all these. If we don't pass them, TypeScript will error. But since we're making the panels show placeholder content, the props types in views/agents.ts still expect certain fields.

       Actually, the agents view is complex and importing it to understand the exact props is out of scope. Let me take a simpler approach: **pass empty/null values** for the deleted props instead of removing them from the rendering block. This avoids needing to change views/agents.ts's type definitions.

       Actually no — the simpler and correct approach is: the app-render.ts rendering block for agents currently defines a lot of inline props. We keep the block structure but set the deleted controller's props to null/empty defaults:

       Instead of `agentFiles: { list: state.agentFilesList, loading: state.agentFilesLoading, ... }`, pass:
       `agentFiles: { list: null, loading: false, error: null, active: null, contents: {}, drafts: {}, saving: false }`

       Similarly for agentSkills, toolsCatalog, toolsEffective, etc.

       Actually, this is getting too complex. Let me simplify: since views/agents.ts's type definition is complex and changing it would be risky, **leave the agents tab render block as-is** for the props that pass state variables (even though the state won't be populated since the controllers are deleted). The key change is that the panels (views/agents-panels-tools-skills.ts, views/agents-panels-status-files.ts) now render placeholder text regardless of the props they receive. So the agents tab will work — it just won't load data for files/skills/tools/cron panels, and the panels will show "暂不可用".

       So for app-render.ts, the agents tab changes are minimal — just remove references to callback functions that are no longer defined in the components (like onLoadFiles etc). Actually, since views/agents.ts defines these callbacks in its props type and the agents-panel components reference them, removing them would break the type contract. The safest approach:

       Keep the agents tab render block largely as-is. The state will be passed but never populated (controllers deleted). The panels show placeholder. The callbacks can remain no-ops.

       Actually wait — the onLoadFiles callback references `loadAgentFiles(state, agentId)` which is a function from the deleted agent-files.ts controller. If this import is removed from the top of app-render.ts (step 1), the code won't compile.

       OK, the cleanest approach for the agents tab: change the callbacks that reference deleted imports to no-ops:
       - `onLoadFiles: (agentId) => {}`  // was loadAgentFiles
       - `onSelectFile: (name) => {}`   // was loadAgentFileContent  
       - `onFileDraftChange: (name, content) => {}`  // was set drafts
       - `onFileReset: (name) => {}`    // was reset drafts
       - `onFileSave: (name) => {}`     // was saveAgentFile
       - `onToolsProfileChange: (agentId, profile, clearAllow) => {}`
       - `onToolsOverridesChange: (agentId, alsoAllow, deny) => {}`
       - `onSkillsFilterChange: (next) => {}`
       - `onSkillsRefresh: () => {}`
       - `onAgentSkillToggle: (agentId, skillName, enabled) => {}`
       - `onAgentSkillsClear: (agentId) => {}`
       - `onAgentSkillsDisableAll: (agentId) => {}`

       And remove the references to functions that were imported but are now no-ops:
       - `resetAgentFilesState` — only called within the agents render block for resetting on agent switch. Make it a no-op.
       - `loadAgentPanelDataForSelectedAgent` — only called within the agents render block for loading panel data on panel switch. Make it a no-op.
       - `resetAgentSelectionPanelState` — only called within the agents render block. Make it a no-op.

       Wait, but app-render.ts also has the functions `loadAgentPanelDataForSelectedAgent`, `resetAgentSelectionPanelState`, `resetAgentFilesState` defined locally (lines 682-727). These reference deleted imports. Replace them with no-ops:

       - `loadAgentPanelDataForSelectedAgent(agentId) { /* no-op — controllers deleted */ }`
       - `resetAgentFilesState(clearLoading) { /* no-op */ }`
       - `resetAgentSelectionPanelState() { /* no-op — controllers deleted */ }`

    7. **Remove dead config-related imports, constants, and helpers:**
       Remove these imports (that reference deleted views):
       - `import { renderConfig, type ConfigProps } from "./views/config.ts";` (line 120) — config view is now placeholder, but the import is still valid since the file exists. Actually, renderConfig is no longer called directly since we replaced renderConfigTabForActiveTab with inline placeholder. So remove this import.
       - `import { renderExecApprovalPrompt } from "./views/exec-approval.ts";` (line 121) — exec-approval view is now placeholder that returns nothing. Keep the import since it's still used on line 1731: `${renderExecApprovalPrompt(state)}`. Actually since exec-approval.ts still exports renderExecApprovalPrompt (now returns nothing), the import can stay. Remove the call on line 1731 since it's a no-op.
       
       Wait, let me be careful. The import at line 121 is `import { renderExecApprovalPrompt } from "./views/exec-approval.ts";`. If we remove the call at line 1731, the import becomes unused. Remove both.
       
       Similar: `import { renderLoginGate } from "./views/login-gate.ts";` and `import { renderGatewayUrlConfirmation } from "./views/gateway-url-confirmation.ts";` — these are NOT deleted. Keep them.

       Actually, the import of `renderConfig` is used throughout the file in the renderConfigTabForActiveTab() function. Since we're replacing that function, we need to remove the import.

    8. **Remove `resolvedAgentId` references that were used by tools functions:**
       The variable `resolvedAgentId` (line 406-407) is still used by the agents tab for identity data loading. Keep it. But `toolsPanelUsesActiveSession` (line 408-409) is only used by tools panel — this can be removed.

    9. **Clean the sessions tab rendering (lines 1025-1129):**
       The sessions tab currently passes callbacks like onPatch, onToggleSelect, onDeleteSelected, onToggleCheckpointDetails, onBranchFromCheckpoint, onRestoreCheckpoint. Since Plan 2's sessions view no longer accepts these props, these references must be removed.

       The sessions render block currently calls:
       ```
       m.renderSessions({
         loading: state.sessionsLoading,
         result: state.sessionsResult,
         ...
         onPatch: (key, patch) => patchSession(state, key, patch),  // REMOVE
         onToggleSelect: (key) => { ... },  // REMOVE
         onSelectPage: (keys) => { ... },  // REMOVE
         onDeselectPage: (keys) => { ... },  // REMOVE
         onDeselectAll: () => { ... },  // REMOVE
         onDeleteSelected: async () => { ... },  // REMOVE
         onToggleCheckpointDetails: (sessionKey) => toggleSessionCompactionCheckpoints(...),  // REMOVE
         onBranchFromCheckpoint: async (...) => { ... },  // REMOVE
         onRestoreCheckpoint: (...) => restoreSessionFromCheckpoint(...),  // REMOVE
         selectedKeys: state.sessionsSelectedKeys,  // REMOVE
         expandedCheckpointKey: state.sessionsExpandedCheckpointKey,  // REMOVE
         checkpointItemsByKey: state.sessionsCheckpointItemsByKey,  // REMOVE
         checkpointLoadingKey: state.sessionsCheckpointLoadingKey,  // REMOVE
         checkpointBusyKey: state.sessionsCheckpointBusyKey,  // REMOVE
         checkpointErrorByKey: state.sessionsCheckpointErrorByKey,  // REMOVE
         ...
       })
       ```

       Remove all of these. Keep: loading, result, error, activeMinutes, limit, includeGlobal, includeUnknown, basePath, searchQuery, sortColumn, sortDir, page, pageSize, onFiltersChange, onSearchChange, onSortChange, onPageChange, onPageSizeChange, onRefresh, onNavigateToChat.

    10. **Remove dead constants and helpers:**
        After all the above cleanup, check if any of these are now unused:
        - `_pendingUpdate` variable (line 137) — used by createLazy which is still used. Keep.
        - `createLazy` function — keep. Used by lazyAgents, lazySessions.
        - `lazyRender` function — keep. Used by agents, sessions tabs.
        - `lazyAgents` — keep
        - `lazySessions` — keep
        - `lazyCron` — remove (cron is now inline placeholder, not lazy)
        - `lazySkills` — remove (skills is now inline placeholder)
        - `clawhubSearchTimer` — only used by skills tab which is now placeholder. Remove.
        - `UPDATE_BANNER_DISMISS_KEY`, `CRON_THINKING_SUGGESTIONS`, `CRON_TIMEZONE_SUGGESTIONS` — only used by deleted config/cron code. Remove.
        - `isHttpUrl`, `normalizeSuggestionValue`, `uniquePreserveOrder` — only used by config/cron rendering code. Remove.
        - `resolveAssistantAvatarUrl`, AVATAR_DATA_RE, AVATAR_HTTP_RE — used by chat avatar rendering. Keep.
        - `loadPermissionsFromStorage` — used by nav filtering (AUTH-05). Keep.
        - All the section key constants (COMMUNICATION_*, APPEARANCE_*, AUTOMATION_*, INFRASTRUCTURE_*, AI_AGENTS_*) — only used by renderConfigTabForActiveTab. Remove.
        - `SCOPED_CONFIG_SECTION_KEYS` — only used by renderConfigTabForActiveTab. Remove.
        - `normalizeMainConfigSelection`, `normalizeScopedConfigSelection` — only used by renderConfigTabForActiveTab. Remove.
        - All the config-related state variables on `commonConfigProps` — only used by renderConfigTab. Since we simplified renderConfigTabForActiveTab, these are no longer needed. Remove the entire `commonConfigProps` block.
        - `renderConfigTab` — only used by renderConfigTabForActiveTab. Remove.
        - `resolveAssistantAvatarUrl` and its regex constants — used by renderApp. Keep.
        - Note: DON'T remove `loadChatHistory` import from controllers/chat.ts — still used in the chat tab rendering for `onRefresh`

  </action>
  <verify>
    <automated>
      grep -q "controllers/config\|controllers/cron\|controllers/exec-approvals\|controllers/skills\|controllers/agent-files\|controllers/agent-identity\|controllers/agent-skills" frontend/src/openclaw/ui/app-render.ts && echo "FAIL: dead import remains" || echo "OK"
      grep -q "controllers/sessions\|controllers/agents\|controllers/chat" frontend/src/openclaw/ui/app-render.ts || echo "WARN: keep import may be missing"
      grep -c "COMMUNICATION_SECTION_KEYS\|APPEARANCE_SECTION_KEYS\|AUTOMATION_SECTION_KEYS\|INFRASTRUCTURE_SECTION_KEYS\|AI_AGENTS_SECTION_KEYS" frontend/src/openclaw/ui/app-render.ts && echo "FAIL: dead section consts remain" || echo "OK"
      grep -c "renderUsageTab\|lazyCron\|lazySkills\|clawhubSearchTimer" frontend/src/openclaw/ui/app-render.ts && echo "FAIL: dead refs remain" || echo "OK"
      grep -q "onPatch\|onDeleteSelected\|onToggleCheckpointDetails" frontend/src/openclaw/ui/app-render.ts && echo "FAIL: dead session callbacks remain" || echo "OK"
      grep -q "DirectAdapter 模式" frontend/src/openclaw/ui/app-render.ts || echo "FAIL: placeholder missing in app-render"
    </automated>
  </verify>
  <done>
    app-render.ts has no imports from deleted controllers. Usage/cron/skills/config tabs render inline placeholders. Sessions tab passes simplified props. Agents tab uses no-ops for deleted panel callbacks. Dead section constants and helpers removed. app-render-usage-tab.ts returns nothing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean app-chat.ts and app.ts references to abortChatRun</name>
  <files>
    frontend/src/openclaw/ui/app-chat.ts
    frontend/src/openclaw/ui/app.ts
  </files>
  <action>
    Read both files before editing.

    Per D-06: abortChatRun was removed from controllers/chat.ts. Any callers must be cleaned.

    **app-chat.ts:**
    1. Search for references to `abortChatRun` — it was imported from controllers/chat.ts and used in `handleAbortChat` function. 
    2. If `handleAbortChat` calls `abortChatRun`, replace the call with a simple return (no-op since chat.abort RPC throws).
    3. Remove the `import { abortChatRun }` from controllers/chat.ts.

    **app.ts:**
    1. Search for `handleAbortChat` — it's defined in app.ts (line 641-643) as `async handleAbortChat() { await handleAbortChatInternal(this); }`.
    2. The function is called by a stop button in the chat UI. Since abort is no longer functional, this handler should be a no-op.
    3. Remove the import of `handleAbortChatInternal from app-chat.ts` (if it was being imported).
    4. Replace the `handleAbortChat()` method body with a no-op: `async handleAbortChat() { /* chat.abort not available in DirectAdapter mode */ }`
    5. Also check if there's a UI element in app-chat.ts or elsewhere that shows a stop/abort button. If so, it should be hidden or disabled since abort no longer works.

    Let me be more specific:
    - In app-chat.ts, `handleAbortChat` is exported. It likely imports `abortChatRun` from controllers/chat.ts and calls it. Remove the import and make the function return `false`.
    - In app.ts, `handleAbortChat` method calls `await handleAbortChatInternal(this)`. Make this a no-op.
    - Also check if app.ts imports `handleAbortChatInternal` at the top — it does (line 6-7: `import { handleAbortChat as handleAbortChatInternal, ... }`). Keep the import but the call is now a no-op.
  </action>
  <verify>
    <automated>
      grep -q "abortChatRun" frontend/src/openclaw/ui/app-chat.ts && echo "FAIL: abortChatRun ref remains" || echo "OK"
    </automated>
  </verify>
  <done>
    app-chat.ts no longer imports or calls abortChatRun. app.ts handleAbortChat is a no-op.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view replacement | Replacing complex view renderers with simple placeholder text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-04 | Tampering | .ts files | mitigate | Keep export signatures compatible with callers to avoid TypeScript errors |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. All 5 view files (usage, cron, config, skills, exec-approval) render "暂不可用" message
2. unavailable-page.ts created with reusable renderUnavailablePage function
3. agents-panels-tools-skills.ts renders placeholder for tools/skills panels
4. agents-panels-status-files.ts renders placeholder for files/cron panels
5. app-render.ts has no imports from any deleted controller
6. app-render-usage-tab.ts returns nothing
7. app-chat.ts no longer references abortChatRun
8. `npm run build` passes (or `npx tsc --noEmit` in frontend/)
</verification>

<success_criteria>
1. Navigation tabs for usage/cron/skills/config/appearance/system show placeholder pages
2. Agent detail panels (tools, skills, files, cron) show placeholder content
3. Sessions page shows clean read-only data table
4. Chat abort/stop no longer throws (no-op)
5. Entire frontend compiles without TypeScript errors
6. No dead imports from deleted controllers remain in any file
</success_criteria>

<output>
Create `.planning/phases/111-gateway-simplify/111-04-SUMMARY.md` when done
</output>
