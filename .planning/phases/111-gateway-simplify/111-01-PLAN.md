---
phase: 111-gateway-simplify
plan: 01
type: execute
wave: 1
depends_on: []
requirements: ["MIG-06"]
files_modified:
  - "frontend/src/openclaw/ui/controllers/usage.ts"
  - "frontend/src/openclaw/ui/controllers/cron.ts"
  - "frontend/src/openclaw/ui/controllers/config.ts"
  - "frontend/src/openclaw/ui/controllers/skills.ts"
  - "frontend/src/openclaw/ui/controllers/exec-approvals.ts"
  - "frontend/src/openclaw/ui/controllers/models.ts"
  - "frontend/src/openclaw/ui/controllers/agent-files.ts"
  - "frontend/src/openclaw/ui/controllers/agent-identity.ts"
  - "frontend/src/openclaw/ui/controllers/agent-skills.ts"
  - "frontend/src/openclaw/ui/app-settings.ts"
  - "frontend/src/openclaw/ui/app.ts"
  - "frontend/src/openclaw/ui/direct-gateway.ts"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "9 broken controller files no longer exist on disk"
    - "app-settings.ts compiles without imports from deleted controllers"
    - "refreshActiveTab() in app-settings.ts has no case for deleted tab values"
    - "loadOverview() in app-settings.ts no longer calls deleted controller methods"
    - "refreshAgentsTab() in app-settings.ts only calls remaining agents methods"
    - "app.ts compiles without referencing tools-effective functions"
    - "direct-gateway.ts request() only has 4 explicit method branches + generic throw"
  artifacts:
    - path: "frontend/src/openclaw/ui/controllers/usage.ts"
      contains: "DELETED"
    - path: "frontend/src/openclaw/ui/controllers/cron.ts"
      contains: "DELETED"
    - path: "frontend/src/openclaw/ui/controllers/config.ts"
      contains: "DELETED"
    - path: "frontend/src/openclaw/ui/controllers/skills.ts"
      contains: "DELETED"
    - path: "frontend/src/openclaw/ui/controllers/exec-approvals.ts"
      contains: "DELETED"
    - path: "frontend/src/openclaw/ui/controllers/models.ts"
      contains: "DELETED"
    - path: "frontend/src/openclaw/ui/controllers/agent-files.ts"
      contains: "DELETED"
    - path: "frontend/src/openclaw/ui/controllers/agent-identity.ts"
      contains: "DELETED"
    - path: "frontend/src/openclaw/ui/controllers/agent-skills.ts"
      contains: "DELETED"
    - path: "frontend/src/openclaw/ui/app-settings.ts"
      does_not_contain: "controllers/usage"
    - path: "frontend/src/openclaw/ui/app-settings.ts"
      does_not_contain: "controllers/cron"
    - path: "frontend/src/openclaw/ui/app-settings.ts"
      does_not_contain: "controllers/config"
    - path: "frontend/src/openclaw/ui/app-settings.ts"
      does_not_contain: "controllers/skills"
    - path: "frontend/src/openclaw/ui/app-settings.ts"
      does_not_contain: "controllers/exec-approvals"
    - path: "frontend/src/openclaw/ui/app-settings.ts"
      does_not_contain: "controllers/agent-files"
    - path: "frontend/src/openclaw/ui/app-settings.ts"
      does_not_contain: "controllers/agent-identity"
    - path: "frontend/src/openclaw/ui/app-settings.ts"
      does_not_contain: "controllers/agent-skills"
    - path: "frontend/src/openclaw/ui/app.ts"
      does_not_contain: "loadToolsEffective"
    - path: "frontend/src/openclaw/ui/app.ts"
      does_not_contain: "refreshVisibleToolsEffectiveForCurrentSession"
    - path: "frontend/src/openclaw/ui/direct-gateway.ts"
      does_not_contain: "tools.catalog"
    - path: "frontend/src/openclaw/ui/direct-gateway.ts"
      does_not_contain: "tools.effective"
    - path: "frontend/src/openclaw/ui/direct-gateway.ts"
      does_not_contain: "models.list"
    - path: "frontend/src/openclaw/ui/direct-gateway.ts"
      does_not_contain: "config.get"
    - path: "frontend/src/openclaw/ui/direct-gateway.ts"
      does_not_contain: "config.set"
    - path: "frontend/src/openclaw/ui/direct-gateway.ts"
      does_not_contain: "sessions.patch"
    - path: "frontend/src/openclaw/ui/direct-gateway.ts"
      does_not_contain: "sessions.delete"
  key_links:
    - from: "app-settings.ts"
      to: "controllers/usage.ts"
      pattern: "DELETED — no import"
    - from: "app-settings.ts"
      to: "controllers/cron.ts"
      pattern: "DELETED — no import"
    - from: "app-settings.ts"
      to: "controllers/config.ts"
      pattern: "DELETED — no import"
    - from: "app.ts"
      to: "controllers/agents.ts"
      pattern: "loadToolsEffective removed"
    - from: "direct-gateway.ts"
      to: "generic throw"
      pattern: "7 explicit throws removed"
---

<objective>
Delete 9 fully-broken Gateway RPC controllers and clean all files that directly import from them.

Per D-01: usage.ts, cron.ts, config.ts, skills.ts, exec-approvals.ts, models.ts, agent-files.ts, agent-identity.ts, agent-skills.ts — all RPC methods throw in DirectAdapter mode.

Purpose: Remove dead code that causes runtime errors when navigated to. Integration point cleanup so the codebase compiles and functions without these controllers.
Output: 9 controller files deleted. app-settings.ts, app.ts, direct-gateway.ts cleaned of all references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-gateway-simplify/111-CONTEXT.md
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/app-settings.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/app.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/direct-gateway.ts

<interfaces>
Key types exported by deleted controllers that app-settings.ts references:
- UsageState (from controllers/usage.ts) — merged into SettingsAppHost
- CronState (from controllers/cron.ts) — merged into SettingsAppHost
- ConfigState (from controllers/config.ts) — merged into SettingsAppHost
- ExecApprovalsState (from controllers/exec-approvals.ts) — merged into SettingsAppHost
- SkillsState (from controllers/skills.ts) — merged into SettingsAppHost
- AgentFilesState (from controllers/agent-files.ts) — merged into SettingsAppHost
- AgentIdentityState (from controllers/agent-identity.ts) — merged into SettingsAppHost
- AgentSkillsState (from controllers/agent-skills.ts) — merged into SettingsAppHost
- AgentsState (from controllers/agents.ts) — KEPT in app-settings.ts
- SessionsState (from controllers/sessions.ts) — KEPT in app-settings.ts

app.ts currently imports:
- ExecApprovalRequest (from controllers/exec-approval.ts) — DELETE
- ExecApprovalsFile, ExecApprovalsSnapshot (from controllers/exec-approvals.ts) — DELETE
- loadToolsEffectiveInternal, refreshVisibleToolsEffectiveForCurrentSessionInternal (from controllers/agents.ts) — DELETE both
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 9 fully-broken controller files</name>
  <files>
    frontend/src/openclaw/ui/controllers/usage.ts
    frontend/src/openclaw/ui/controllers/cron.ts
    frontend/src/openclaw/ui/controllers/config.ts
    frontend/src/openclaw/ui/controllers/skills.ts
    frontend/src/openclaw/ui/controllers/exec-approvals.ts
    frontend/src/openclaw/ui/controllers/models.ts
    frontend/src/openclaw/ui/controllers/agent-files.ts
    frontend/src/openclaw/ui/controllers/agent-identity.ts
    frontend/src/openclaw/ui/controllers/agent-skills.ts
  </files>
  <action>
    Use `git rm` to delete all 9 files. Do NOT delete the `controllers/config/` subdirectory — it contains form-coerce.ts and form-utils.ts which are utility modules used by config-form.ts and should not be removed.

    The 9 files to delete (per D-01):
    - controllers/usage.ts — 4 RPC methods, all throw in DirectAdapter
    - controllers/cron.ts — 8 RPC methods, all throw
    - controllers/config.ts — 6 RPC methods, all throw
    - controllers/skills.ts — 5 RPC methods, all throw
    - controllers/exec-approvals.ts — 4 RPC methods, all throw
    - controllers/models.ts — models.list throw
    - controllers/agent-files.ts — 3 RPC methods, all throw
    - controllers/agent-identity.ts — agent.identity.get throw
    - controllers/agent-skills.ts — skills.status throw
  </action>
  <verify>
    <automated>for f in usage.ts cron.ts config.ts skills.ts exec-approvals.ts models.ts agent-files.ts agent-identity.ts agent-skills.ts; do if [ -f "frontend/src/openclaw/ui/controllers/$f" ]; then echo "FAIL: $f still exists"; exit 1; fi; done; echo "All 9 files deleted"</automated>
  </verify>
  <done>
    9 controller files removed from git tracking and disk. controllers/config/ subdirectory (form-coerce.ts, form-utils.ts) is NOT deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean app-settings.ts — remove all references to deleted controllers</name>
  <files>frontend/src/openclaw/ui/app-settings.ts</files>
  <action>
    Read @/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/app-settings.ts before editing.

    Remove the following:

    1. **Imports to delete** (lines 4-10, 12, 13-14, 19, 21-22):
       - `import { loadAgentFiles, type AgentFilesState } from "./controllers/agent-files.ts";`
       - `import { loadAgentIdentities, loadAgentIdentity, type AgentIdentityState } from "./controllers/agent-identity.ts";`
       - `import { loadAgentSkills, type AgentSkillsState } from "./controllers/agent-skills.ts";`
       - `import { loadConfig, loadConfigSchema, type ConfigState } from "./controllers/config.ts";`
       - `import { loadCronJobsPage, loadCronRuns, loadCronStatus, type CronState } from "./controllers/cron.ts";`
       - `import { loadExecApprovals, type ExecApprovalsState } from "./controllers/exec-approvals.ts";`
       - `import { loadSkills, type SkillsState } from "./controllers/skills.ts";`
       - `import { loadUsage, type UsageState } from "./controllers/usage.ts";`
       - Keep: imports for loadAgents (line 11), loadSessions (line 20)

    2. **SettingsAppHost type** (lines 63-73): Remove the following type intersections:
       - `AgentFilesState &`
       - `AgentIdentityState &`
       - `AgentSkillsState &`
       - `ConfigState &`
       - `CronState &`
       - `ExecApprovalsState &`
       - `SkillsState &`
       - `UsageState &`
       Keep: `AgentsState &`, `SessionsState &`

    3. **refreshAgentsTab function** (lines 232-256): Remove all branches except the default:
       - Remove `void loadAgentIdentities(app, agentIds)` call
       - Remove `void loadAgentIdentity(app, agentId)` call before the switch
       - Remove `case "files"` branch (calls loadAgentFiles which is deleted)
       - Remove `case "skills"` branch (calls loadAgentSkills which is deleted)
       - Remove `case "cron"` branch (calls loadCron which is deleted)
       - Keep only the default case — the function should loadAgents and set selectedId but not attempt to load panel-specific data
       - Also remove the `if (agentIds.length > 0)` block with loadAgentIdentities
       - Also remove the `const agentId =` resolution — keep it but don't load identity

    4. **refreshActiveTab function** (lines 258-299): Remove the following cases:
       - `case "config":` / `case "appearance":` / `case "system":` block (loads config via deleted controller)
       - `case "usage":` block (calls loadUsage from deleted controller)
       - `case "cron":` block (calls loadCron which is deleted)
       - `case "skills":` block (calls loadSkills from deleted controller)
       - Other cases (overview, sessions, agents, chat, dashboard, instances-db, alerts, reports, llm-usage) stay as-is

    5. **loadOverview function** (lines 484-495): Remove calls to deleted controller functions:
       - Remove `loadCronStatus(app)` — cron controller deleted
       - Remove `loadCronJobsPage(app)` — cron controller deleted
       - Remove `loadSkills(app)` — skills controller deleted
       - Remove `loadUsage(app)` — usage controller deleted
       - Keep: `loadSessions(app)`, `loadOverviewLogs(app)`
       - Remove `loadSessions(app)` from Promise.allSettled — it can be called independently if needed, but sessions controller is kept, so keep it

    6. **loadCron function** (lines 639-647): This function calls loadCronStatus, loadCronJobsPage, loadCronRons — all from the deleted cron controller. Remove the entire `loadCron` export function.

    7. **buildAttentionItems function** (lines 563-636): Remove references to:
       - `host.skillsReport?.skills` — skills controller deleted
       - `host.cronJobs` — cron controller deleted
       - `host.cronStatus` — cron controller deleted
       - These items were already showing error/empty states since the controllers throw, but now the imports are gone. Remove the blocks that reference skills, cron jobs, and cron overdue logic. Keep the lastError and operator.read scope blocks.

    Also check if `hasMissingSkillDependencies` function (lines 528-535) is still referenced after the buildAttentionItems cleanup. If not, remove it too.
  </action>
  <verify>
    <automated>
      grep -c "controllers/usage" frontend/src/openclaw/ui/app-settings.ts && echo "FAIL: usage ref remains" || echo "OK"
      grep -c "loadCron\|SkillsState\|UsageState\|ConfigState\|CronState\|ExecApprovalsState" frontend/src/openclaw/ui/app-settings.ts && echo "FAIL: dead ref" || echo "OK"
      grep -c "loadAgents\|loadSessions\|AgentsState\|SessionsState" frontend/src/openclaw/ui/app-settings.ts || echo "FAIL: kept refs missing"
    </automated>
  </verify>
  <done>
    app-settings.ts compiles without imports from any of the 9 deleted controllers. refreshActiveTab has no cases for config/appearance/system/usage/cron/skills. loadOverview no longer calls deleted methods. refreshAgentsTab only loadsAgents. SettingsAppHost only has AgentsState and SessionsState intersections.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean app.ts + simplify direct-gateway.ts</name>
  <files>
    frontend/src/openclaw/ui/app.ts
    frontend/src/openclaw/ui/direct-gateway.ts
  </files>
  <action>
    Read both files before editing.

    **app.ts changes:**
    1. Remove import: `import { loadToolsEffective as loadToolsEffectiveInternal, refreshVisibleToolsEffectiveForCurrentSession as refreshVisibleToolsEffectiveForCurrentSessionInternal } from "./controllers/agents.ts";` (line 48-50)
    2. Remove the following imports for types from deleted controllers:
       - `import type { ExecApprovalRequest } from "./controllers/exec-approval.ts";` (line 51)
       - `import type { ExecApprovalsFile, ExecApprovalsSnapshot } from "./controllers/exec-approvals.ts";` (line 52)
       - `import type { ClawHubSearchResult, ClawHubSkillDetail, SkillMessage } from "./controllers/skills.ts";` (line 53-56)
    3. In `onSlashAction` (line 434), remove the `case "refresh-tools-effective"` block (lines 446-449) — it calls the deleted refreshVisibleToolsEffectiveForCurrentSessionInternal
    4. In `updated()` lifecycle method (line 526-544), remove the entire block that calls `loadToolsEffectiveInternal` (lines 528-543) — it references the deleted function. The `updated` method's remaining body (the `if (!changed.has("sessionKey")...)` check and the tools state reset) should be removed entirely since there are no tools controllers left to refresh.
       - Actually, keep `updated()` but remove the tools-specific logic. Since after the trim, agents.ts no longer has tools-related state, the tool cascade in updated() serves no purpose. Remove the entire contents of `updated()`. The method can be removed entirely since it was only needed for tools-effective loading.

    **direct-gateway.ts changes:**
    Remove 7 explicit throw branches from `request()` method (lines 187-207):
    - `if (method === 'tools.catalog')` block (lines 187-189)
    - `if (method === 'tools.effective')` block (lines 190-192)
    - `if (method === 'models.list')` block (lines 193-195)
    - `if (method === 'config.get')` block (lines 196-198)
    - `if (method === 'config.set')` block (lines 199-201)
    - `if (method === 'sessions.patch')` block (lines 202-204)
    - `if (method === 'sessions.delete')` block (lines 205-207)
    Keep: 4 working methods (chat.send, chat.history, agents.list, sessions.list) + the generic `throw` at line 209.
  </action>
  <verify>
    <automated>
      grep -q "loadToolsEffective" frontend/src/openclaw/ui/app.ts && echo "FAIL: tools ref remains" || echo "OK"
      grep -q "ExecApprovalRequest" frontend/src/openclaw/ui/app.ts && echo "FAIL: exec ref remains" || echo "OK"
      grep -q "ClawHubSearchResult" frontend/src/openclaw/ui/app.ts && echo "FAIL: skills ref remains" || echo "OK"
      for method in "tools.catalog" "tools.effective" "models.list" "config.get" "config.set" "sessions.patch" "sessions.delete"; do
        grep -q "$method" frontend/src/openclaw/ui/direct-gateway.ts && echo "FAIL: $method remains" || echo "OK"
      done
    </automated>
  </verify>
  <done>
    app.ts no longer imports from deleted controllers or references tools-effective functions. direct-gateway.ts request() has 4 working method branches + 1 generic throw; 7 explicit throw branches removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file deletion | Removing .ts files from the frontend source tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | git rm | mitigate | Deleting specific named files only — no glob or regex patterns |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. All 9 controller files are deleted (git rm + no re-creation)
2. app-settings.ts has no imports from deleted controllers
3. app.ts has no imports from deleted controllers or tools-effective functions
4. direct-gateway.ts request() only has 4 explicit method branches + generic throw
5. `npm run build` passes (or `npx tsc --noEmit` in frontend dir) — check from `frontend/` directory
</verification>

<success_criteria>
1. 9 controller .ts files are gone from git and disk
2. app-settings.ts compiles without dead imports
3. app.ts compiles without dead imports
4. direct-gateway.ts has simplified request() method
5. No TypeScript compilation errors from the changes
</success_criteria>

<output>
Create `.planning/phases/111-gateway-simplify/111-01-SUMMARY.md` when done
</output>
