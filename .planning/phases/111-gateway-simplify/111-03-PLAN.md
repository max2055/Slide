---
phase: 111-gateway-simplify
plan: 03
type: execute
wave: 1
depends_on: []
requirements: ["MIG-06"]
files_modified:
  - "frontend/src/openclaw/ui/chat/slash-commands.ts"
  - "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "8 slash commands removed from slash-commands.ts: /model, /think, /fast, /verbose, /compact, /kill, /redirect, /stop"
    - "Slash-command-executor.ts no longer has handlers for the 8 deleted commands"
    - "/agents, /usage, /steer handlers remain in slash-command-executor.ts"
    - "LOCAL_COMMANDS set in slash-commands.ts no longer contains the 8 deleted command names"
    - "COMMAND_ICON_OVERRIDES no longer has entries for the 8 deleted command names"
    - "UI_ONLY_COMMANDS no longer has redirect entry"
  artifacts:
    - path: "frontend/src/openclaw/ui/chat/slash-commands.ts"
      does_not_contain: '"compact"'
    - path: "frontend/src/openclaw/ui/chat/slash-commands.ts"
      does_not_contain: '"model"'
    - path: "frontend/src/openclaw/ui/chat/slash-commands.ts"
      does_not_contain: '"think"'
    - path: "frontend/src/openclaw/ui/chat/slash-commands.ts"
      does_not_contain: '"fast"'
    - path: "frontend/src/openclaw/ui/chat/slash-commands.ts"
      does_not_contain: '"verbose"'
    - path: "frontend/src/openclaw/ui/chat/slash-commands.ts"
      does_not_contain: '"kill"'
    - path: "frontend/src/openclaw/ui/chat/slash-commands.ts"
      does_not_contain: '"redirect"'
    - path: "frontend/src/openclaw/ui/chat/slash-commands.ts"
      does_not_contain: '"stop"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      does_not_contain: 'case "model"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      does_not_contain: 'case "think"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      does_not_contain: 'case "fast"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      does_not_contain: 'case "verbose"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      does_not_contain: 'case "compact"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      does_not_contain: 'case "kill"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      does_not_contain: 'case "redirect"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      does_not_contain: 'case "stop"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      contains: 'case "agents"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      contains: 'case "steer"'
    - path: "frontend/src/openclaw/ui/chat/slash-command-executor.ts"
      contains: 'case "usage"'
  key_links:
    - from: "slash-commands.ts"
      to: "slash-command-executor.ts"
      pattern: "Both files have same 8 commands removed"
---

<objective>
Remove 8 broken slash commands that depend on deleted Gateway RPC methods.

Per D-07: Delete /model, /think, /fast, /verbose, /compact, /kill, /redirect, /stop.
Per D-08: Keep /agents, /usage (REST APIs available), /steer (basic functionality).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-gateway-simplify/111-CONTEXT.md
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/chat/slash-commands.ts
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/chat/slash-command-executor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 8 slash command registrations from slash-commands.ts</name>
  <files>frontend/src/openclaw/ui/chat/slash-commands.ts</files>
  <action>
    Read @/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/chat/slash-commands.ts before editing.

    Per D-07, remove ALL references to these 8 commands from four data structures:

    1. **COMMAND_ICON_OVERRIDES** (lines 49-75): Remove entries for:
       - `compact: "loader"` (line 60)
       - `stop: "stop"` (line 61)
       - `model: "brain"` (line 65)
       - `think: "brain"` (line 67)
       - `verbose: "terminal"` (line 68)
       - `fast: "zap"` (line 69)
       - `kill: "x"` (line 72)
       - `redirect: "send"` (line 75)

    2. **LOCAL_COMMANDS Set** (lines 77-94): Remove:
       - `"stop"` (line 81)
       - `"compact"` (line 82)
       - `"model"` (line 84)
       - `"think"` (line 85)
       - `"fast"` (line 86)
       - `"verbose"` (line 87)
       - `"kill"` (line 91)
       - `"redirect"` (line 93)

    3. **UI_ONLY_COMMANDS array** (lines 96-113): Remove the entire `redirect` entry:
       - Lines 106-113: Remove from `{ key: "redirect"` through the closing `},` after the description/args/icon/category/executeLocal fields.

    4. **CATEGORY_OVERRIDES** (lines 116-145): Remove entries for:
       - `kill: "agents"` (line 127)
       - `redirect: "agents"` (line 129)
       - `stop: "session"` (line 131)
       - `compact: "session"` (line 134)
       - `model: "model"` (line 137)
       - `think: "model"` (line 139)
       - `verbose: "model"` (line 140)
       - `fast: "model"` (line 141)

    Keep:
       - `/agents` entry — `agents: "monitor"` in icon overrides, `agents` in LOCAL_COMMANDS, `agents: "agents"` in category overrides
       - `/usage` entry — `usage: "bar-chart"` in icon overrides, `usage` in LOCAL_COMMANDS, `usage: "tools"` in category overrides
       - `/steer` entry — `steer: "send"` in icon overrides, `steer` in LOCAL_COMMANDS, `steer: "agents"` in category overrides, `COMMAND_ARGS_OVERRIDES` for steer, `COMMAND_DESCRIPTION_OVERRIDES` for steer
  </action>
  <verify>
    <automated>
      for cmd in compact stop model think fast verbose kill redirect; do
        grep -q "['\"]$cmd['\"]" frontend/src/openclaw/ui/chat/slash-commands.ts && echo "WARN: $cmd ref found (might be in comment)" || echo "OK: $cmd removed from strings"
      done
      grep -q "'agents'" frontend/src/openclaw/ui/chat/slash-commands.ts || echo "FAIL: agents ref missing"
      grep -q "'steer'" frontend/src/openclaw/ui/chat/slash-commands.ts || echo "FAIL: steer ref missing"
    </automated>
  </verify>
  <done>
    COMMAND_ICON_OVERRIDES, LOCAL_COMMANDS, CATEGORY_OVERRIDES, and UI_ONLY_COMMANDS no longer reference any of the 8 deleted commands. /agents, /usage, /steer references remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove 8 slash command handlers from slash-command-executor.ts</name>
  <files>frontend/src/openclaw/ui/chat/slash-command-executor.ts</files>
  <action>
    Read @/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/chat/slash-command-executor.ts before editing.

    Per D-07, remove 8 command cases from the switch statement and their implementation functions.
    Per D-08, keep /agents, /usage, /steer.

    1. **Remove from switch statement** (lines 90-128):
       Remove these case entries:
       - `case "stop":` (line 98) — returns action: "stop"
       - `case "compact":` (lines 103-104) — calls executeCompact
       - `case "model":` (lines 105-106) — calls executeModel
       - `case "think":` (lines 107-108) — calls executeThink
       - `case "fast":` (lines 109-110) — calls executeFast
       - `case "verbose":` (lines 111-112) — calls executeVerbose
       - `case "kill":` (lines 119-120) — calls executeKill
       - `case "redirect":` (lines 123-124) — calls executeRedirect
       Keep:
       - `case "help":` — keep
       - `case "new":` — keep
       - `case "reset":` — keep
       - `case "clear":` — keep
       - `case "focus":` — keep
       - `case "export-session":` — keep
       - `case "usage":` (lines 115-116) — keep
       - `case "agents":` (lines 117-118) — keep
       - `case "steer":` (lines 121-122) — keep

    2. **Remove implementation functions:**
       - `executeCompact()` — lines 151-177
       - `executeModel()` — lines 179-246
       - `executeThink()` — lines 248-290
       - `executeVerbose()` — lines 292-329
       - `executeFast()` — lines 331-367
       - `executeKill()` — lines 423-485 (and its helpers: `resolveKillTargets` lines 487-530, `isWithinCurrentSessionSubtree` lines 532-558, `buildSessionIndex` lines 561-571, `normalizeSessionKey` lines 573-575, `resolveEquivalentSessionKeys` lines 577-591)
       - `executeRedirect()` — lines 810-839 (and its shared helper `resolveSteerTarget` lines 732-764)

    3. **Keep:**
       - `executeHelp()` — keep
       - `executeUsage()` — lines 369-401 — keep (uses sessions.list which is available via REST)
       - `executeAgents()` — lines 403-421 — keep (uses agents.list which is available via REST)
       - `executeSteer()` — lines 771-807 — keep (uses chat.send + sessions.list, both available)
       - `resolveSteerSubagent()` — lines 676-721 — still used by executeSteer
       - `resolveSteerTarget()` — lines 732-764 — still used by executeSteer (and was also used by executeRedirect which is now deleted, but resolveSteerTarget is still needed for executeSteer)
       - `isActiveSteerSession()` — lines 766-768 — still used by executeSteer
       - `loadCurrentSession()` — lines 597-603 — still used by some remaining functions? Check if executeUsage/executeAgents use it. Looking at the switch: executeUsage directly calls client.request and doesn't use loadCurrentSession. executeAgents calls client.request directly. loadCurrentSession was used by executeThink/executeVerbose/executeFast. So loadCurrentSession and its helper `resolveCurrentSession` (lines 605-620) can be removed if no remaining function uses them.
       - Keep: `loadCurrentSession` + `resolveCurrentSession` — check if executeSteer uses them? No, executeSteer doesn't use loadCurrentSession. So these can be removed.
       - Keep: `formatDirectiveOptions()`, `fmtTokens()` — only used by deleted functions. Remove.
       - Keep: `SlashCommandResult`, `SlashCommandContext` types — still needed by remaining commands
       - Keep: `normalizeVerboseLevel()` — only used by deleted executeVerbose. Remove.
       - Keep: `loadThinkingCommandState`, `resolveCurrentThinkingLevel`, `resolveCurrentFastMode` — only used by deleted think/fast functions. Remove.
       - Keep: `loadModelCatalog()` — only used by deleted model/think functions. Remove.

    4. **Clean unused imports:**
       - Remove: `createChatModelOverride`, `resolvePreferredServerChatModelValue` from chat-model-ref
       - Remove: `isSubagentSessionKey`, `parseAgentSessionKey`, `DEFAULT_AGENT_ID`, `DEFAULT_MAIN_KEY` from session-key — check if remaining functions use them. executeSteer uses resolveSteerTarget which calls resolveSteerSubagent which uses isSubagentSessionKey, parseAgentSessionKey. So keep these.
       - Remove: `normalizeOptionalLowercaseString` from string-coerce — only used by normalizeSessionKey which only used by deleted functions
       - Remove: `formatThinkingLevels`, `normalizeThinkLevel`, `resolveThinkingDefaultForModel` from thinking
       - Remove: `ChatModelOverride`, `GatewaySessionRow`, `ModelCatalogEntry`, `SessionsListResult`, `SessionsPatchResult` from types — check what remaining functions use. executeUsage fetches sessions, executeAgents fetches agents. These use client.request with inline types, not the imported ones. So these can be removed. But keep `AgentsListResult` — used by executeAgents.
       - Remove: `generateUUID` from uuid — only used by executeSteer? Let me check: executeSteer calls generateUUID() on line 799. Keep it.
       - Remove: `SLASH_COMMANDS` import — only used by executeHelp which is kept. Keep it.

    Let me be more precise about what to keep:
    - Keep: DirectGatewayClient, normalizeLowercaseStringOrEmpty, generateUUID, AgentsListResult, SLASH_COMMANDS (for executeHelp)
    - Keep: session-key functions used by executeSteer (isSubagentSessionKey, parseAgentSessionKey, DEFAULT_AGENT_ID, DEFAULT_MAIN_KEY)
    - Keep: SlashCommandResult, SlashCommandContext types
    - Keep: resolveSteerSubagent, resolveSteerTarget, isActiveSteerSession (for executeSteer)
    - Remove everything else
  </action>
  <verify>
    <automated>
      for cmd in "case \"model\"" "case \"think\"" "case \"fast\"" "case \"verbose\"" "case \"compact\"" "case \"kill\"" "case \"redirect\"" "case \"stop\""; do
        grep -q "$cmd" frontend/src/openclaw/ui/chat/slash-command-executor.ts && echo "FAIL: $cmd remains" || echo "OK"
      done
      for cmd in "case \"agents\"" "case \"usage\"" "case \"steer\"" "case \"help\"" "case \"new\""; do
        grep -q "$cmd" frontend/src/openclaw/ui/chat/slash-command-executor.ts || echo "FAIL: $cmd missing"
      done
      grep -q "executeModel\|executeThink\|executeFast\|executeVerbose\|executeCompact\|executeKill\|executeRedirect" frontend/src/openclaw/ui/chat/slash-command-executor.ts && echo "FAIL: deleted fn remains" || echo "OK"
      grep -q "executeAgents\|executeUsage\|executeSteer\|executeHelp" frontend/src/openclaw/ui/chat/slash-command-executor.ts || echo "FAIL: kept fn missing"
    </automated>
  </verify>
  <done>
    switch statement has no cases for model/think/fast/verbose/compact/kill/redirect/stop. Implementation functions for those 8 commands are removed. Remaining: help, new, reset, clear, focus, export-session, usage, agents, steer. Unused imports and helpers cleaned.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command removal | Removing slash command registrations may leave unused imports/variables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-03 | Tampering | .ts files | mitigate | Leave /agents, /usage, /steer intact — only delete 8 specified commands |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. slash-commands.ts: COMMAND_ICON_OVERRIDES, LOCAL_COMMANDS, CATEGORY_OVERRIDES have no references to the 8 deleted commands
2. slash-commands.ts: UI_ONLY_COMMANDS has no redirect entry
3. slash-command-executor.ts: switch statement has 9 cases (help, new, reset, clear, focus, export-session, usage, agents, steer)
4. slash-command-executor.ts: no executeCompact/executeModel/executeThink/executeVerbose/executeFast/executeKill/executeRedirect functions
</verification>

<success_criteria>
1. slash-commands.ts no longer registers /model, /think, /fast, /verbose, /compact, /kill, /redirect, /stop
2. slash-command-executor.ts no longer has handlers for those 8 commands
3. /agents, /usage, /steer handlers remain fully functional
</success_criteria>

<output>
Create `.planning/phases/111-gateway-simplify/111-03-SUMMARY.md` when done
</output>
