---
phase: 103-报表重构
plan: 03
type: execute
wave: 2
depends_on: [103-02]
files_modified:
  - frontend/src/openclaw/ui/views/reports.ts
autonomous: true
requirements: [RPT-02, RPT-03]
user_setup: []
must_haves:
  truths:
    - "Users can see report config list with name, instance, type, cron, next-run, status, and actions in reports.ts"
    - "Users can create a new scheduled report config via a modal dialog"
    - "Users can edit an existing config via a pre-filled modal dialog"
    - "Users can delete configs after confirmation"
    - "Users can toggle enable/disable on configs with optimistic UI update"
    - "Slow query label correctly renders from 'slow_query' type name"
  artifacts:
    - path: "frontend/src/openclaw/ui/views/reports.ts"
      provides: "Scheduled config card between stat-cards and generate card, create/edit dialog, delete confirmation, toggle switch"
    - path: "frontend/src/openclaw/ui/views/reports.ts"
      provides: "_reportTypeLabel() uses 'slow_query' key instead of 'slow-query' at line 532"
  key_links:
    - from: "reports.ts firstUpdated()"
      to: "GET /api/reports/configs"
      via: "authFetch in loadConfigs()"
    - from: "reports.ts create/edit dialog submit"
      to: "POST/PUT /api/reports/configs"
      via: "authFetch in create/update methods"
    - from: "reports.ts toggle switch"
      to: "PUT /api/reports/configs/:id"
      via: "optimistic toggle + authFetch"
    - from: "reports.ts delete button"
      to: "DELETE /api/reports/configs/:id"
      via: "confirmation dialog + authFetch"
    - from: "reports.ts next-run display"
      to: "cron package"
      via: "new CronJob(config.cron).nextDates(1) on frontend"
---

<objective>
在前端 reports.ts 页面中添加定时报表配置管理 UI（RPT-02 前端）并修复 _reportTypeLabel 命名（RPT-03 前端）。

Purpose: 让用户能在报表页面可视化地管理定时生成配置（增删改查、启停），并修复类型标签映射。

Output: 修改后的 reports.ts 包含配置卡片、表单弹窗、操作交互。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/max/Coding/39-Slide/.planning/phases/103-报表重构/103-CONTEXT.md
@/Users/max/Coding/39-Slide/.planning/phases/103-报表重构/103-UI-SPEC.md
@/Users/max/Coding/39-Slide/.planning/phases/103-报表重构/103-RESEARCH.md
@/Users/max/Coding/39-Slide/frontend/src/openclaw/ui/views/reports.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scheduled config card UI with config list, empty state, refresh, and fix _reportTypeLabel</name>
  <files>frontend/src/openclaw/ui/views/reports.ts</files>
  <read_first>
    frontend/src/openclaw/ui/views/reports.ts (full file — existing stat-card section at lines 426-431, generate card at 434-466, history card at 469-523, _reportTypeLabel at 528-536)
    UI-SPEC.md "RPT-02: Scheduled Configs Card" section — layout position, interactions, copywriting, component inventory
  </read_first>
  <action>
    **A. Fix _reportTypeLabel at line 532 (per D-09, D-10, UI-SPEC §RPT-03):**
    Change:
    ```typescript
    "slow-query": "慢查询",
    ```
    To:
    ```typescript
    slow_query: "慢查询",
    ```

    **B. Add new state properties** to the @State() section (after line 348):
    ```typescript
    @state() private configs: ReportConfig[] = [];
    @state() private configsLoading = true;
    @state() private editingConfig: ReportConfig | null = null;
    @state() private showConfigDialog = false;
    @state() private showDeleteConfirm = false;
    @state() private deletingConfig: ReportConfig | null = null;
    ```

    **C. Define ReportConfig interface** (after the Report interface, before the class):
    ```typescript
    interface ReportConfig {
      id: number;
      name: string;
      cron: string;
      type: string;
      instance_id: number;
      format: string;
      enabled: boolean;
      created_at: string;
      updated_at: string;
      next_run?: string;
    }
    ```

    **D. Import `cron` package** for next-run computation.
    Add at top of file (after imports):
    ```typescript
    import { CronJob } from "cron";
    ```
    Since this is a frontend Lit component, ensure `cron` is in the frontend's package.json. If not, add it:
    - Check if `cron` is in frontend/package.json. If missing, install: `npm install cron` in the frontend directory.

    **E. Add loadConfigs() method** (after loadInstances()):
    ```typescript
    private async loadConfigs() {
      try {
        const res = await authFetch('/api/reports/configs');
        if (res.ok) {
          const data: ReportConfig[] = await res.json();
          // Compute next run time for each config using cron package
          this.configs = data.map(cfg => {
            try {
              const job = new CronJob(cfg.cron, () => {});
              const nextDate = job.nextDates(1);
              cfg.next_run = nextDate instanceof Date ? nextDate.toISOString() : '—';
            } catch {
              cfg.next_run = '无效表达式';
            }
            return cfg;
          });
        }
        this.configsLoading = false;
      } catch (err: any) {
        console.error('加载定时配置失败:', err);
        this.configsLoading = false;
      }
    }
    ```

    **F. Update firstUpdated() to also load configs:**
    Add `this.loadConfigs();` after `this.loadInstances();`

    **G. Add config card section in render() between stat-cards (ends at line 431) and generate card (starts at line 434):**

    Insert BEFORE the generate card div (before line 434):
    ```html
    <!-- 定时报表配置 -->
    <div class="card">
      <div class="card-header">
        <div>
          <div class="card-title">${icons['calendar']} 定时报表配置</div>
          <div class="card-sub">设置定时报表自动生成任务</div>
        </div>
        <div class="actions">
          <button class="action-btn" @click=${this._refreshConfigs}>${icons['refresh-cw']}</button>
          <button class="action-btn primary" @click=${this._openCreateDialog}>${icons['plus']} 新增配置</button>
        </div>
      </div>

      ${this.configsLoading
        ? html`<div class="loading">加载中...</div>`
        : this.configs.length > 0
          ? html`
              <div class="table-container">
                <table class="table">
                  <thead>
                    <tr>
                      <th>配置名称</th>
                      <th style="width: 100px;">报表类型</th>
                      <th style="width: 120px;">目标实例</th>
                      <th style="width: 110px;">Cron 表达式</th>
                      <th style="width: 100px;">下次执行</th>
                      <th style="width: 80px;">状态</th>
                      <th style="width: 120px;">操作</th>
                    </tr>
                  </thead>
                  <tbody>
                    ${this.configs.map(cfg => html`
                      <tr>
                        <td class="report-title">${cfg.name}</td>
                        <td><span class="type-badge">${this._reportTypeLabel(cfg.type)}</span></td>
                        <td>${cfg.instance_id ? html`<span class="instance-badge">实例 #${cfg.instance_id}</span>` : html`<span style="color:var(--muted)">—</span>`}</td>
                        <td><code style="font-size:var(--text-xs)">${cfg.cron}</code></td>
                        <td><span style="font-size:var(--text-sm);color:var(--muted)">${cfg.next_run ? this._formatTime(cfg.next_run) : '—'}</span></td>
                        <td><span class="status-badge ${cfg.enabled ? 'ok' : ''}">${cfg.enabled ? '启用' : '停用'}</span></td>
                        <td>
                          <div class="actions">
                            <button class="action-btn" @click=${() => this._openEditDialog(cfg)}>${icons['edit']}</button>
                            <button class="action-btn" @click=${() => this._toggleConfig(cfg)}>
                              ${cfg.enabled ? '停用' : '启用'}
                            </button>
                            <button class="action-btn" style="color:var(--destructive)" @click=${() => this._confirmDelete(cfg)}>${icons['trash-2']}</button>
                          </div>
                        </td>
                      </tr>
                    `)}
                  </tbody>
                </table>
              </div>
            `
          : html`
              <div class="empty">
                <div class="empty__content">
                  <div class="empty__icon">${icons['file-text']}</div>
                  <div class="empty__title">暂无定时配置</div>
                  <div class="empty__desc">点击上方"新增配置"创建第一个定时报表任务</div>
                </div>
              </div>
            `
      }
    </div>
    ```

    **H. Use UI-SPEC design tokens and class conventions:**
    - `.card` / `.card-header` / `.card-title` / `.card-sub` — existing classes in reports.ts (lines 45-82)
    - `.action-btn` / `.action-btn.primary` — existing (lines 275-304)
    - `.table` / `.table-container` — existing (lines 163-206)
    - `.status-badge` / `.status-badge.ok` — existing (lines 239-262)
    - `.type-badge` / `.instance-badge` — existing (lines 215-237)
    - `.empty` / `.empty__content` / `.empty__icon` / `.empty__title` / `.empty__desc` — existing (lines 306-341)
    - Icons: `calendar` for card title, `refresh-cw` for refresh button, `plus` for add button, `edit` for edit, `trash-2` for delete, `file-text` for empty state

    **I. Add method stubs** (implementations in Task 2):
    Add these private methods after the class (before closing brace):
    ```typescript
    private _refreshConfigs() {
      this.configsLoading = true;
      this.loadConfigs();
    }

    private _openCreateDialog() { this.editingConfig = null; this.showConfigDialog = true; }
    private _openEditDialog(cfg: ReportConfig) { this.editingConfig = cfg; this.showConfigDialog = true; }
    private _toggleConfig(cfg: ReportConfig) { /* Task 2 */ }
    private _confirmDelete(cfg: ReportConfig) { /* Task 2 */ }
    ```

    These are stubs for now — they will be fully implemented in Task 2.
  </action>
  <acceptance_criteria>
    - `_reportTypeLabel()` line 532 uses `slow_query` key (not `slow-query`)
    - `configs`, `configsLoading`, `editingConfig`, `showConfigDialog`, `showDeleteConfirm`, `deletingConfig` state properties exist
    - `ReportConfig` interface is defined with all fields matching the backend model
    - `loadConfigs()` method exists and is called in `firstUpdated()`
    - Config card renders between stat-cards section and generate card section
    - Config card shows correct columns per UI-SPEC: 配置名称, 报表类型, 目标实例, Cron 表达式, 下次执行, 状态, 操作
    - Empty state shows correct copy: "暂无定时配置" + "点击上方新增配置创建第一个定时报表任务"
    - cron import exists for next-run computation
    - `npm run dev` (or `npx tsc --noEmit` in frontend) compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    Expect no TypeScript compilation errors.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement config dialog (create/edit), delete confirmation, toggle, and CRUD wiring</name>
  <files>frontend/src/openclaw/ui/views/reports.ts</files>
  <read_first>
    frontend/src/openclaw/ui/views/reports.ts (full current file — all classes, the config card added in Task 1, existing _generateReport pattern at lines 551-577 for API call reference)
    UI-SPEC.md "Create/Edit Dialog" section — dialog layout, field labels, submit/cancel buttons
  </read_first>
  <action>
    **A. Add dialog state properties** (if not already in Task 1):
    ```typescript
    @state() private configForm: Partial<ReportConfig> & { instance_id?: number } = {};
    @state() private saving = false;
    ```

    Initialize in a `_resetForm()` method:
    ```typescript
    private _resetForm() {
      this.configForm = {
        name: '',
        cron: '',
        type: 'health',
        instance_id: undefined,
        format: 'html',
        enabled: true,
      };
    }
    ```

    **B. Add create/edit dialog HTML** in render() method.
    Add this AFTER the closing tag of the config card div and BEFORE the opening of the history card div:

    ```html
    <!-- 创建/编辑配置弹窗 -->
    ${this.showConfigDialog ? html`
      <div class="modal-overlay" @click=${() => this.showConfigDialog = false}>
        <div class="modal" @click=${(e: Event) => e.stopPropagation()}>
          <div class="modal-header">
            <div class="modal-title">${this.editingConfig ? '编辑定时报表配置' : '新增定时报表配置'}</div>
            <button class="modal-close" @click=${() => this.showConfigDialog = false}>${icons['x']}</button>
          </div>
          <div class="modal-body">
            <div class="field-row">
              <label class="field-label">配置名称</label>
              <input class="field-input" .value=${this.configForm.name || ''} @input=${(e: any) => this.configForm.name = e.target.value} placeholder="例如：每日健康检查" />
            </div>
            <div class="field-row">
              <label class="field-label">报表类型</label>
              <select class="field-input" .value=${this.configForm.type || 'health'} @change=${(e: any) => this.configForm.type = e.target.value}>
                <option value="health">健康检查</option>
                <option value="performance">性能分析</option>
                <option value="slow_query">慢查询</option>
                <option value="capacity">容量规划</option>
              </select>
            </div>
            <div class="field-row">
              <label class="field-label">目标实例</label>
              <select class="field-input" .value=${this.configForm.instance_id || ''} @change=${(e: any) => this.configForm.instance_id = Number(e.target.value)}>
                <option value="">— 选择实例 —</option>
                ${this.instances.map(i => html`<option value=${i.id}>${i.name}</option>`)}
              </select>
            </div>
            <div class="field-row">
              <label class="field-label">Cron 表达式</label>
              <input class="field-input" .value=${this.configForm.cron || ''} @input=${(e: any) => this.configForm.cron = e.target.value} placeholder="例如：0 8 * * *" />
            </div>
            <div class="field-row">
              <label class="field-label">输出格式</label>
              <select class="field-input" .value=${this.configForm.format || 'html'} @change=${(e: any) => this.configForm.format = e.target.value}>
                <option value="html">HTML</option>
                <option value="pdf">PDF</option>
                <option value="md">Markdown</option>
                <option value="json">JSON</option>
              </select>
            </div>
            <div class="field-row">
              <label class="field-label">启用</label>
              <div class="cfg-toggle" @click=${() => this.configForm.enabled = !this.configForm.enabled}>
                <div class="cfg-toggle__track ${this.configForm.enabled ? 'on' : ''}">
                  <div class="cfg-toggle__thumb"></div>
                </div>
              </div>
            </div>
          </div>
          <div class="modal-footer">
            <button class="action-btn" @click=${() => this.showConfigDialog = false}>取消</button>
            <button class="action-btn primary" @click=${() => this._saveConfig()} ?disabled=${this.saving}>${this.saving ? '保存中...' : this.editingConfig ? '保存' : '创建'}</button>
          </div>
        </div>
      </div>
    ` : ''}
    ```

    Also add delete confirmation dialog:
    ```html
    ${this.showDeleteConfirm && this.deletingConfig ? html`
      <div class="modal-overlay" @click=${() => this.showDeleteConfirm = false}>
        <div class="modal" style="max-width:400px" @click=${(e: Event) => e.stopPropagation()}>
          <div class="modal-header">
            <div class="modal-title">确认删除</div>
            <button class="modal-close" @click=${() => this.showDeleteConfirm = false}>${icons['x']}</button>
          </div>
          <div class="modal-body">
            <p>确定要删除配置"${this.deletingConfig.name}"吗？此操作不可撤销。</p>
          </div>
          <div class="modal-footer">
            <button class="action-btn" @click=${() => this.showDeleteConfirm = false}>取消</button>
            <button class="action-btn" style="background:var(--destructive);color:white;border-color:var(--destructive)" @click=${() => this._deleteConfig()}>删除</button>
          </div>
        </div>
      </div>
    ` : ''}
    ```

    **C. Add dialog CSS** in the `styles` static getter:
    Add these styles (insert at the end of the existing styles block, before the closing `}` of the css tagged template):
    ```css
    /* 弹窗样式 */
    .modal-overlay {
      position: fixed; top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0,0,0,0.5); z-index: 1000;
      display: flex; align-items: center; justify-content: center;
    }
    .modal {
      background: var(--card); border-radius: var(--radius-lg);
      border: 1px solid var(--border); max-width: 600px; width: 90%;
      max-height: 80vh; overflow-y: auto;
    }
    .modal-header {
      display: flex; justify-content: space-between; align-items: center;
      padding: var(--space-lg); border-bottom: 1px solid var(--border);
    }
    .modal-title { font-size: var(--text-lg); font-weight: 600; color: var(--text-strong); }
    .modal-close {
      background: none; border: none; cursor: pointer; color: var(--muted); padding: 4px;
    }
    .modal-body { padding: var(--space-lg); }
    .modal-footer {
      display: flex; justify-content: flex-end; gap: var(--space-md);
      padding: var(--space-lg); border-top: 1px solid var(--border);
    }
    .field-row {
      display: flex; flex-direction: column; gap: var(--space-xs);
      margin-bottom: var(--space-md);
    }
    .field-label {
      font-size: var(--text-sm); font-weight: 600; color: var(--text-strong);
    }
    .field-input {
      padding: 8px 10px; border: 1px solid var(--border);
      border-radius: var(--radius-md); background: var(--input);
      color: var(--text); font-size: var(--text-base);
    }
    .cfg-toggle { cursor: pointer; }
    .cfg-toggle__track {
      width: 36px; height: 20px; border-radius: 10px; background: var(--border);
      position: relative; transition: background 0.2s;
    }
    .cfg-toggle__track.on { background: var(--accent); }
    .cfg-toggle__thumb {
      width: 16px; height: 16px; border-radius: 50%; background: white;
      position: absolute; top: 2px; left: 2px; transition: left 0.2s;
    }
    .cfg-toggle__track.on .cfg-toggle__thumb { left: 18px; }
    ```

    **D. Implement CRUD methods:**
    
    1. `_openCreateDialog()`:
       ```typescript
       private _openCreateDialog() {
         this._resetForm();
         this.editingConfig = null;
         this.showConfigDialog = true;
       }
       ```

    2. `_openEditDialog(cfg)`:
       ```typescript
       private _openEditDialog(cfg: ReportConfig) {
         this.editingConfig = cfg;
         this.configForm = { ...cfg };
         this.showConfigDialog = true;
       }
       ```

    3. `_saveConfig()`:
       ```typescript
       private async _saveConfig() {
         const { name, cron, type, instance_id, format, enabled } = this.configForm;
         if (!name || !cron || !type || !instance_id) {
           alert('请填写所有必填字段');
           return;
         }
         this.saving = true;
         try {
           if (this.editingConfig) {
             await authFetch(`/api/reports/configs/${this.editingConfig.id}`, {
               method: 'PUT',
               headers: this.headers,
               body: JSON.stringify({ name, cron, type, instance_id, format, enabled }),
             });
           } else {
             await authFetch('/api/reports/configs', {
               method: 'POST',
               headers: this.headers,
               body: JSON.stringify({ name, cron, type, instance_id, format, enabled }),
             });
           }
           this.showConfigDialog = false;
           this.loadConfigs();
         } catch (err: any) {
           alert(`保存失败：${err.message}`);
         } finally {
           this.saving = false;
         }
       }
       ```

    4. `_toggleConfig(cfg)`:
       ```typescript
       private async _toggleConfig(cfg: ReportConfig) {
         // Optimistic toggle
         const originalEnabled = cfg.enabled;
         cfg.enabled = !cfg.enabled;
         this.requestUpdate();
         try {
           const res = await authFetch(`/api/reports/configs/${cfg.id}`, {
             method: 'PUT',
             headers: this.headers,
             body: JSON.stringify({ enabled: cfg.enabled }),
           });
           if (!res.ok) throw new Error('更新失败');
           this.loadConfigs(); // Reload to get server state
         } catch (err: any) {
           // Rollback on failure
           cfg.enabled = originalEnabled;
           this.requestUpdate();
           alert(`切换状态失败：${err.message}`);
         }
       }
       ```

    5. `_confirmDelete(cfg)`:
       ```typescript
       private _confirmDelete(cfg: ReportConfig) {
         this.deletingConfig = cfg;
         this.showDeleteConfirm = true;
       }
       ```

    6. `_deleteConfig()`:
       ```typescript
       private async _deleteConfig() {
         if (!this.deletingConfig) return;
         try {
           await authFetch(`/api/reports/configs/${this.deletingConfig.id}`, {
             method: 'DELETE',
             headers: this.headers,
           });
           this.showDeleteConfirm = false;
           this.deletingConfig = null;
           this.loadConfigs();
         } catch (err: any) {
           alert(`删除失败：${err.message}`);
         }
       }
       ```

    **E. Ensure the `configs` state array holds returned data with `instance_name` instead of just instance_id:**
    Update the loadConfigs() method to also fetch instance names if the API doesn't return them. The config card currently shows `实例 #{instance_id}` — if the backend API returns instance names, use them. The simplest approach: look up the instance name from the existing `this.instances` array when rendering.

    **F. Dialog style details:**
    - Follow all UI-SPEC copywriting values exactly for labels, button text, placeholders
    - Width: `max-width: 600px` for the dialog
    - Close on escape key: add `@keydown=${(e: KeyboardEvent) => e.key === 'Escape' && (this.showConfigDialog = false)}` if desired
    - Submit/cancel button styles match existing `.action-btn.primary` / `.action-btn` classes
    - Delete confirmation uses destructive color for the delete button

    **G. Verify icon names are all correct:**
    The icons used must exist in `frontend/src/icons.ts`:
    - `calendar` — config card title
    - `refresh-cw` — refresh button
    - `plus` — add button
    - `edit` — edit action
    - `trash-2` — delete action
    - `file-text` — empty state
    - `x` — close dialog button (verify this icon exists; if not, use an SVG or text "x")
  </action>
  <acceptance_criteria>
    - Create dialog opens with empty form fields when clicking "新增配置"
    - Edit dialog opens with pre-filled values when clicking edit button
    - Dialog validation: required fields (name, cron, type, instance_id) checked before submit
    - POST /api/reports/configs called on create, PUT /api/reports/configs/:id called on edit
    - DELETE /api/reports/configs/:id called with confirmation dialog
    - Toggle switch optimistically updates UI and sends PUT with `{ enabled: !currentValue }`; rolls back on error
    - Dialog closes on cancel/close button
    - Config list reloads after create/edit/delete/toggle
    - Handle dialog close by clicking overlay (click-outside)
    - All form labels and button text match UI-SPEC copywriting
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    Expect no TypeScript compilation errors. Also verify with grep that all 4 CRUD methods exist in reports.ts: `grep -c '_saveConfig\|_toggleConfig\|_deleteConfig\|_confirmDelete\|_openCreateDialog\|_openEditDialog' frontend/src/openclaw/ui/views/reports.ts`.
  </verify>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-to-API | All API calls go through authFetch which includes Bearer JWT token — no direct client-side data exposure |
| Dialog input | User input is validated before sending to API (required fields check, instance selection) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-06 | Spoofing | All config management actions | mitigate | All API calls use authFetch with JWT token; backend preHandler enforces report:view/report:create permissions |
| T-103-07 | Information Disclosure | Toggle switch rollback | accept | Toggle shows optimistic UI; if PUT fails, rolls back to previous state — user sees consistent state |
| T-103-08 | Tampering | npm cron package install | mitigate | cron@4.4.0 is already a dependency of the backend; only verify it is also present in frontend's package.json |

</threat_model>

<verification>

### Overall Phase Checks (Plan C contribution)

1. Config card renders:
   - [ ] Open reports.ts and verify config card appears between stat-cards and generate card
   - [ ] Empty state shows when no configs exist
   - [ ] Config list renders columns matching UI-SPEC
2. Dialog interactions:
   - [ ] Open create dialog, fill fields, submit — config appears in list
   - [ ] Open edit dialog — fields are pre-filled, save updates config
   - [ ] Toggle enable/disable works with optimistic update
   - [ ] Delete opens confirmation, confirm removes config
3. Type naming fix:
   - [ ] grep for "'slow-query'" in reports.ts returns empty
   - [ ] grep for "slow_query" in reports.ts's _reportTypeLabel returns 1

</verification>

<success_criteria>

### Measurable Completion

1. reports.ts shows scheduled config card with all columns per UI-SPEC
2. Create/edit dialog works with validation and CRUD API calls
3. Delete confirmation dialog works with destructive styling
4. Toggle switch has optimistic update with rollback on error
5. Next-run time is computed using CronJob.nextDates()
6. _reportTypeLabel uses 'slow_query' key
7. `cd frontend && npx tsc --noEmit` passes

</success_criteria>

<output>
.planning/phases/103-报表重构/103-03-SUMMARY.md
</output>
