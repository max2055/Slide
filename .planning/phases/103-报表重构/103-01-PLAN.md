---
phase: 103-报表重构
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/db-ops-api/src/templates/reports/layout.ejs
  - apps/db-ops-api/src/templates/reports/health.ejs
  - apps/db-ops-api/src/templates/reports/performance.ejs
  - apps/db-ops-api/src/templates/reports/slow-query.ejs
  - apps/db-ops-api/src/templates/reports/capacity.ejs
  - apps/db-ops-api/package.json
  - apps/db-ops-api/src/report-service.ts
  - apps/db-ops-api/src/report-database-service.ts
  - apps/db-ops-api/sql/migrations/006_fix_report_type_names.sql
autonomous: false
requirements: [RPT-01, RPT-03]
user_setup: []
must_haves:
  truths:
    - "Report HTML is rendered from EJS template files (layout.ejs + per-type), not inline TypeScript strings"
    - "Slow query report type is consistently 'slow_query' in the type system and data"
  artifacts:
    - path: "apps/db-ops-api/src/templates/reports/layout.ejs"
      provides: "Shared HTML shell with DOCTYPE, head, styles, header, body wrapper, and <%- body %> injection point"
    - path: "apps/db-ops-api/src/templates/reports/health.ejs"
      provides: "Health report body content matching current generateHealthReportHTML output"
    - path: "apps/db-ops-api/src/templates/reports/performance.ejs"
      provides: "Performance report body content matching current generatePerformanceReportHTML output"
    - path: "apps/db-ops-api/src/templates/reports/slow-query.ejs"
      provides: "Slow query report body content matching current generateSlowQueryReportHTML output"
    - path: "apps/db-ops-api/src/templates/reports/capacity.ejs"
      provides: "Capacity report body content matching current generateCapacityReportHTML output"
    - path: "apps/db-ops-api/src/report-service.ts"
      provides: "Refactored to use ejs.renderFile() instead of inline HTML string methods"
    - path: "apps/db-ops-api/src/report-database-service.ts"
      provides: "ReportType uses 'slow_query' instead of 'slow-query' at line 7"
    - path: "apps/db-ops-api/sql/migrations/006_fix_report_type_names.sql"
      provides: "UPDATE reports SET type='slow_query' WHERE type='slow-query'"
  key_links:
    - from: "apps/db-ops-api/src/report-service.ts"
      to: "apps/db-ops-api/src/templates/reports/*.ejs"
      via: "ejs.renderFile(path.join(TEMPLATE_DIR, templateName + '.ejs'), context)"
---

<objective>
将 report-service.ts 中 4 个内联 HTML 模板方法提取为 EJS 模板文件（RPT-01），并修复报表类型命名不一致（RPT-03）。

Purpose: 消除 report-service.ts 中 ~200 行内联 HTML 字符串，改为 EJS 文件渲染，使模板可独立编辑；修复 ReportType 中 'slow-query' 与 'slow_query' 的不一致。

Output: 5 个 EJS 模板文件、修改后的 report-service.ts、修复后的 ReportType、迁移 SQL。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/max/Coding/39-Slide/.planning/phases/103-报表重构/103-CONTEXT.md
@/Users/max/Coding/39-Slide/.planning/phases/103-报表重构/103-RESEARCH.md
@/Users/max/Coding/39-Slide/apps/db-ops-api/package.json
@/Users/max/Coding/39-Slide/apps/db-ops-api/src/report-service.ts
@/Users/max/Coding/39-Slide/apps/db-ops-api/src/report-database-service.ts
@/Users/max/Coding/39-Slide/apps/db-ops-api/sql/migrations/001_add_parent_id_to_chat_messages.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EJS template directory and 5 template files</name>
  <files>
    apps/db-ops-api/src/templates/reports/layout.ejs
    apps/db-ops-api/src/templates/reports/health.ejs
    apps/db-ops-api/src/templates/reports/performance.ejs
    apps/db-ops-api/src/templates/reports/slow-query.ejs
    apps/db-ops-api/src/templates/reports/capacity.ejs
  </files>
  <read_first>
    apps/db-ops-api/src/report-service.ts lines 372-585 (the 4 generate*ReportHTML inline templates)
    CONTEXT.md D-01, D-02, D-03 for directory structure and layout design
    UI-SPEC.md "EJS Report Template Color" and "EJS Report Template Typography" sections for hardcoded CSS values
  </read_first>
  <action>
    Create directory `apps/db-ops-api/src/templates/reports/` and write 5 EJS template files:

    **layout.ejs** — Shared HTML shell consumed by all 4 report types via `<%- include('layout', { ... }) %>`:
    - DOCTYPE html + html/head/body tags
    - `<meta charset="UTF-8">`
    - `<title><%= title %> - <%= instanceName %></title>`
    - Inline CSS: body font (-apple-system stack), padding 20px, h1 with accent-color bottom border (per-type via accentColor variable), table styles (width 100%, border-collapse, th with #f5f5f5 background, td with #ddd bottom border)
    - Header section: h1 with title and accent color, instance name, generated at timestamp
    - `<%- body %>` injection point for per-type content
    - Use these hex values per UI-SPEC: body #333333, title accent #409eff, status colors #15803d (ok), #b45309 (warn), #dc2626 (danger)
    - Accept EJS context: `title`, `instanceName`, `generatedAt`, `accentColor`, `icon`, `body`

    **health.ejs** — Health report body:
    - Renders summary grid (3 columns): health score card (class ok/warning/critical based on score thresholds 80/60), CPU usage, memory usage, disk usage, connections, QPS
    - Renders detailed metrics table: CPU usage, memory usage, disk usage, connections, QPS, TPS
    - Matches current `generateHealthReportHTML` HTML structure exactly, but using EJS template expressions
    - Uses the passed context fields from ReportContext (title, instanceName, generatedAt, metrics)
    - Ensure metrics.health_score >= 80 ? 'ok' : >= 60 ? 'warning' : 'critical' matches current logic

    **performance.ejs** — Performance report body:
    - Renders current metrics table (QPS, TPS, connections)
    - Renders Top slow queries table: SQL, avg_time_ms, execution_count, total_time_ms
    - Maps over metrics.top_slow_queries
    - Span 4 columns for no-data placeholder row

    **slow-query.ejs** — Slow query report body:
    - Shows slow query count
    - Renders table: #, SQL (substring 80 chars), avg_time_ms, execution_count, rows_examined
    - Maps over slowQueries array

    **capacity.ejs** — Capacity report body:
    - Renders 2-column summary grid: disk usage percentage, growth trend
    - No table — use hardcoded display for now (matches current template)

    All per-type templates share the same context shape defined by the `ReportContext` interface concept (D-03): common fields (title, instanceName, generatedAt) + per-type data fields. Actual context object is built in report-service.ts.
  </action>
  <acceptance_criteria>
    - Directory `apps/db-ops-api/src/templates/reports/` exists
    - Files `layout.ejs`, `health.ejs`, `performance.ejs`, `slow-query.ejs`, `capacity.ejs` exist
    - layout.ejs contains `<%- body %>` injection point
    - health.ejs uses `metrics.health_score` comparison with thresholds 80 and 60
    - performance.ejs maps over `metrics.top_slow_queries`
    - slow-query.ejs maps over `slowQueries`
    - capacity.ejs shows capacityData.disk_usage and capacityData.growth_trend
  </acceptance_criteria>
  <verify>
    <automated>ls apps/db-ops-api/src/templates/reports/*.ejs | wc -l</automated>
    Check that 5 files exist: count equals 5.
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify ejs package legitimacy and install</name>
  <files>apps/db-ops-api/package.json</files>
  <read_first>
    RESEARCH.md "Package Legitimacy Audit" section — ejs tagged [ASSUMED], slopcheck unavailable
  </read_first>
  <action>
    Before installing ejs from npm, the user must verify the package is legitimate.

    1. Ask the user to visit https://www.npmjs.com/package/ejs and confirm the package is legitimate (15+ years, 30M+ weekly downloads, published by mde)
    2. If user confirms: add `"ejs": "^5.0.2"` to dependencies in package.json (alphabetical order, after "dotenv" or adjacent position) and run `npm install` in apps/db-ops-api/ directory
    3. If user reports suspicious: do NOT install, surface the concern to the planner

    After approval: commit the package.json change with message `chore(103): add ejs@^5.0.2 dependency`.
  </action>
  <acceptance_criteria>
    - package.json contains `"ejs": "^5.0.2"` in dependencies
    - node_modules contains ejs package
    - `npm install` completed without errors
  </acceptance_criteria>
  <verify>
    <automated>grep '"ejs"' apps/db-ops-api/package.json && ls apps/db-ops-api/node_modules/ejs/package.json</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Refactor report-service.ts to use EJS rendering + fix ReportType + create migration SQL</name>
  <files>
    apps/db-ops-api/src/report-service.ts
    apps/db-ops-api/src/report-database-service.ts
    apps/db-ops-api/sql/migrations/006_fix_report_type_names.sql
  </files>
  <read_first>
    apps/db-ops-api/src/report-service.ts (full file — understand all 4 generate methods and private helpers)
    apps/db-ops-api/src/report-database-service.ts (ReportType at line 7)
    apps/db-ops-api/sql/migrations/005_add_refresh_tokens_and_grant_expiry.sql (migration format pattern)
    CONTEXT.md D-01 through D-04, D-09 through D-11
  </read_first>
  <action>
    **A. Fix ReportType in report-database-service.ts (per D-09, D-10):**
    - Change line 7: `'slow-query'` to `'slow_query'`
    - Result: `export type ReportType = 'health' | 'performance' | 'slow_query' | 'capacity';`

    **B. Create migration SQL file at `sql/migrations/006_fix_report_type_names.sql`:**
    Follow the format of migration 005 (header comment, START TRANSACTION, SQL, COMMIT):
    ```sql
    -- ============================================
    -- Migration 006: Fix report type naming inconsistency
    -- Phase 103 — RPT-03: Unify slow-query -> slow_query
    -- ============================================
    -- Purpose: The ReportType type previously allowed 'slow-query' (hyphen)
    -- but the schema ENUM and routing code always used 'slow_query' (underscore).
    -- This migration updates any rows that may have the hyphenated value.
    -- Idempotent: no-op if no matching rows exist.
    -- ============================================
    START TRANSACTION;
    UPDATE reports SET type = 'slow_query' WHERE type = 'slow-query';
    COMMIT;
    ```

    **C. Refactor report-service.ts to use EJS rendering (per D-01, D-02, D-03):**
    
    1. Add imports at the top:
    ```typescript
    import ejs from 'ejs';
    import path from 'path';
    import fs from 'fs';
    ```

    2. Add a TEMPLATE_DIR constant after the imports:
    ```typescript
    const TEMPLATE_DIR = path.resolve(process.cwd(), 'src/templates/reports');
    ```

    3. Replace the 4 private generate*ReportHTML methods with a single private renderTemplate method:
    ```typescript
    private async renderTemplate(
      templateName: string,
      context: Record<string, any>
    ): Promise<string> {
      const templatePath = path.join(TEMPLATE_DIR, `${templateName}.ejs`);
      if (!fs.existsSync(templatePath)) {
        console.warn(`模板文件不存在: ${templatePath}，使用内联回退`);
        return this.getFallbackHTML(templateName, context);
      }
      return ejs.renderFile(templatePath, context);
    }
    ```

    4. Add a getFallbackHTML method that returns the old inline HTML strings as constants (keep the existing strings as private constants/functions for emergency fallback)

    5. Update the 4 public generate methods (generateHealthReport, generatePerformanceReport, generateSlowQueryReport, generateCapacityReport):
       - Change the HTML content generation from calling the old private method to await this.renderTemplate()
       - Example for generateHealthReport:
         ```typescript
         const htmlContent = await this.renderTemplate('health', {
           title: '健康检查报告',
           instanceName,
           generatedAt: new Date().toISOString(),
           accentColor: '#4CAF50',
           icon: '🏥',
           metrics,
         });
         ```
       - For generatePerformanceReport: template 'performance', accentColor '#2196F3', icon '⚡', context includes metrics
       - For generateSlowQueryReport: template 'slow-query', accentColor '#FF9800', icon '🐌', context includes slowQueries
       - For generateCapacityReport: template 'capacity', accentColor '#9C27B0', icon '📈', context includes capacityData
    
    6. Delete the 4 private generate*ReportHTML methods entirely — they are replaced by renderTemplate + fallback HTML

    7. Add a startup validation check. Since report-service.ts is not the server entry point, add a lazy check on first renderTemplate call. Log a warning if TEMPLATE_DIR does not exist.

    8. DO NOT use caching (ejs `cache: true`) — per RESEARCH.md anti-patterns, caching adds complexity for no benefit on infrequent report generation

    **D. Run the migration SQL:**
    After creating the migration file, execute it against the database:
    ```
    mysql -u root -p db_ops_ai < apps/db-ops-api/sql/migrations/006_fix_report_type_names.sql
    ```
    (Use your actual DB password from .env, or manually type it if prompted)

    **E. Cross-reference verification:**
    Run a grep across the entire project for `slow-query` (hyphenated) to confirm no remaining references:
    ```
    grep -rn 'slow-query' --include='*.ts' --include='*.js' --include='*.sql' --exclude-dir=node_modules
    ```
    The only legitimate matches should be in the new slow-query.ejs template filename (which is part of the canonical naming convention since the filename is server-side and never stored in data).
  </action>
  <acceptance_criteria>
    - report-database-service.ts line 7: ReportType uses `'slow_query'` (underscore), not `'slow-query'` (hyphen)
    - migration 006 SQL file exists with UPDATE statement
    - report-service.ts has `import ejs from 'ejs'` and `import path from 'path'` at top
    - report-service.ts has `renderTemplate` private async method using `ejs.renderFile`
    - report-service.ts has `getFallbackHTML` with old inline HTML strings preserved
    - All 4 public generate methods call `this.renderTemplate(templateName, { ... })` instead of old private methods
    - All 4 private generate*ReportHTML methods are removed from report-service.ts (code removed, not just commented)
    - grep for 'slow-query' in .ts/.sql files (excluding node_modules) shows no unexpected matches
    - Migration SQL was executed against the database
    - `npx tsc --noEmit` in apps/db-ops-api passes (compile check)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/db-ops-api && npx tsc --noEmit 2>&1</automated>
  </verify>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-to-API | User provides report type names, instance IDs — validated in routes |
| API-to-Database | Service layer queries report tables via parameterized mysql2 queries |
| EJS renderFile | Data from metrics service flows into EJS template — EJS auto-escapes `<%= %>` by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Tampering | npm ejs package install | mitigate | Task 2: blocking human checkpoint verifies package on npmjs.com before install per RESEARCH.md Package Legitimacy Audit |
| T-103-02 | Information Disclosure | report-database-service.ts fixes type | accept | No PII exposure; type name change has no security impact |
| T-103-SC | Tampering | npm install ejs | mitigate | Blocking human-verify checkpoint (Task 2) verifies package integrity on registry before install |

</threat_model>

<verification>

### Overall Phase Checks (Plan A contribution)

1. Report service uses EJS for all 4 report types:
   - [ ] `grep -c 'ejs.renderFile' apps/db-ops-api/src/report-service.ts` returns >= 1
   - [ ] `grep -c 'generate.*ReportHTML' apps/db-ops-api/src/report-service.ts` returns 0 (no inline methods remaining)
2. Report type naming fixed:
   - [ ] `grep "'slow-query'" apps/db-ops-api/src/report-database-service.ts` returns empty
   - [ ] `grep "'slow_query'" apps/db-ops-api/src/report-database-service.ts` returns 1
3. Compilation: `npx tsc --noEmit` passes in apps/db-ops-api

</verification>

<success_criteria>

### Measurable Completion

1. All 5 EJS template files exist with correct content
2. report-service.ts uses `ejs.renderFile()` and has no inline generate*ReportHTML methods
3. ReportType defines `'slow_query'` (not `'slow-query'`)
4. Migration SQL 006 exists and was executed
5. ejs@^5.0.2 is in package.json and installed
6. `npx tsc --noEmit` passes in apps/db-ops-api

</success_criteria>

<output>
.planning/phases/103-报表重构/103-01-SUMMARY.md
</output>
