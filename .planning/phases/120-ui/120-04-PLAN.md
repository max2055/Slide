---
phase: 120-ui
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - frontend/src/app/ui/views/alerts.ts
  - frontend/src/app/ui/components/alert-list.ts
  - frontend/src/app/ui/components/alert-detail-modal.ts
  - frontend/src/app/ui/components/alert-rule-editor.ts
  - frontend/src/app/ui/components/alert-analysis-viewer.ts
autonomous: true
requirements: [UI-OPT-01]
user_setup: []

must_haves:
  truths:
    - "alerts.ts is split into 4 subcomponents, each <300 lines"
    - "alert-list renders the table/badge view with status indicators per D-18"
    - "alert-detail-modal shows individual alert details in an app-dialog"
    - "alert-rule-editor provides rule editing in an app-dialog"
    - "alert-analysis-viewer displays AI analysis results"
    - "Main alerts.ts is reduced in size and orchestrates subcomponents"
  artifacts:
    - path: "frontend/src/app/ui/components/alert-list.ts"
      provides: "Alert table with filtering and status badges"
      max_lines: 300
    - path: "frontend/src/app/ui/components/alert-detail-modal.ts"
      provides: "Alert detail view in app-dialog"
      max_lines: 300
    - path: "frontend/src/app/ui/components/alert-rule-editor.ts"
      provides: "Alert rule create/edit form in app-dialog"
      max_lines: 300
    - path: "frontend/src/app/ui/components/alert-analysis-viewer.ts"
      provides: "AI analysis results display"
      max_lines: 300
  key_links:
    - from: "alert-list.ts"
      to: "app-badge.ts"
      via: "Uses <app-badge> for severity badges"
    - from: "alert-detail-modal.ts"
      to: "app-dialog.ts"
      via: "Uses <app-dialog> for modal"
    - from: "alert-rule-editor.ts"
      to: "app-dialog.ts + app-form-field.ts"
      via: "Uses shared dialog and form-field"
---

<objective>
Split alerts.ts (2805 lines) into 4 focused subcomponents: alert-list, alert-detail-modal, alert-rule-editor, alert-analysis-viewer.

Purpose: Eliminate the alerts.ts god component (H9). Per D-18, split by functional area so each subcomponent is <300 lines. Subcomponents use the new shared component library (app-dialog, app-form-field, app-badge).
Output: 4 new subcomponent files + refactored alerts.ts orchestrator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-ui/120-CONTEXT.md
@.planning/phases/120-ui/ui-issues-inventory.md

<interfaces>
<read_first order="mandatory">
  frontend/src/app/ui/views/alerts.ts                     # The file being split — all 2805 lines
  frontend/src/app/ui/components/app-dialog.ts            # Shared dialog component
  frontend/src/app/ui/components/app-form-field.ts        # Shared form field
  frontend/src/app/ui/components/app-badge.ts             # Shared badge component
  frontend/src/app/ui/components/app-data-table.ts        # Shared data table
  frontend/src/app/ui/components/app-empty-state.ts       # Shared empty state
  frontend/src/app/styles/tokens.css                      # Design tokens
</read_first>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract alert-list and alert-detail-modal</name>
  <read_first>
    frontend/src/app/ui/views/alerts.ts
    frontend/src/app/ui/components/app-badge.ts
  </read_first>
  <files>
    CREATE: frontend/src/app/ui/components/alert-list.ts
    CREATE: frontend/src/app/ui/components/alert-detail-modal.ts
  </files>
  <action>
    Read alerts.ts (2805 lines) and understand its structure. Identify the sections that render:
    - The alert list table (filter bar + rows with severity badges, status indicators)
    - The alert detail modal (dialog that opens when clicking an alert row)

    **alert-list.ts** — Extract the table rendering + filtering logic:
    - Tag: `<alert-list>`
    - Properties: `alerts: Alert[]`, `loading: boolean`
    - Events: dispatches `alert-select` with alert ID, `alert-create` (new rule), `alert-delete`
    - Render: uses `<app-data-table>` or a standard `<table>` with proper tokens, uses `<app-badge>` with variant based on severity/status
    - Status indicators: severity badge (ok→muted, warn→warn, danger→danger), affected instances count, timestamp
    - Keep all data transformation helpers (status classes, variant mapping) as private methods
    - Limit to 250-300 lines

    **alert-detail-modal.ts** — Extract the detail view:
    - Tag: `<alert-detail-modal>`
    - Properties: `alert: Alert | null`, `open: boolean`
    - Events: dispatches `close` when dialog closes
    - Render: uses `<app-dialog>` with size "md" or "lg", shows alert details (time, severity, instance, message, metrics, status)
    - Limit to 200-250 lines

    **Key actions:**
    - Find the alert interface/type in alerts.ts or the skills data types. If not exported, extract to a shared type or use inline interface
    - Remove the extracted code blocks from alerts.ts, replace with `<alert-list>` and `<alert-detail-modal>` custom element tags
    - Keep the data fetching logic in the parent (alerts.ts) — pass data down via properties
    - Import the new components in alerts.ts or app-render.ts
  </action>
  <verify>
    `ls frontend/src/app/ui/components/alert-list.ts` exists.
    `ls frontend/src/app/ui/components/alert-detail-modal.ts` exists.
    Both files are <=300 lines each:
    `wc -l < frontend/src/app/ui/components/alert-list.ts` <= 300.
    `wc -l < frontend/src/app/ui/components/alert-detail-modal.ts` <= 300.
    Grep: `grep -c "app-badge" frontend/src/app/ui/components/alert-list.ts` >= 1 (uses shared badge).
    Grep: `grep -c "app-dialog" frontend/src/app/ui/components/alert-detail-modal.ts` >= 1 (uses shared dialog).
    `npx tsc --noEmit` passes.
  </verify>
  <acceptance_criteria>
    - alert-list.ts exists (<=300 lines) with filtering + badge rendering
    - alert-detail-modal.ts exists (<=250 lines) with app-dialog wrapper
    - Original alerts.ts reduced by the extracted code
    - Subcomponents use app-badge and app-dialog
    - tsc passes, app renders without errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract alert-rule-editor and alert-analysis-viewer</name>
  <read_first>
    frontend/src/app/ui/views/alerts.ts
    frontend/src/app/ui/components/app-dialog.ts
    frontend/src/app/ui/components/app-form-field.ts
  </read_first>
  <files>
    CREATE: frontend/src/app/ui/components/alert-rule-editor.ts
    CREATE: frontend/src/app/ui/components/alert-analysis-viewer.ts
  </files>
  <action>
    **alert-rule-editor.ts** — Extract the rule create/edit form:
    - Tag: `<alert-rule-editor>`
    - Properties: `rule: AlertRule | null` (null = creating new, object = editing existing), `open: boolean`
    - Events: dispatches `save` with AlertRule data, `close` when cancelled
    - Render: uses `<app-dialog>` with size "lg", form uses `<app-form-field>` for each input
    - Form fields: name, metric (select), condition (select: >/</>=/<=), warning threshold, error threshold, critical threshold, silence_minutes, enabled toggle
    - Keep validation logic (ensure threshold values are numbers, name not empty)
    - Import AlertRule type from the original file or extract it
    - Limit to 250-300 lines

    **alert-analysis-viewer.ts** — Extract the AI analysis panel:
    - Tag: `<alert-analysis-viewer>`
    - Properties: `analysis: AnalysisResult | null`, `open: boolean`, `loading: boolean`
    - Events: dispatches `close`
    - Render: uses `<app-dialog>` with size "xl" (analysis can be long), shows RCA/diagnosis text
    - Use Markdown-style rendering for the analysis result (the existing pattern from ai-analysis-result.ts)
    - Limit to 200-250 lines

    Update alerts.ts: replace the extracted dialog/modals with `@alert-rule-editor>` and `<alert-analysis-viewer>` tags. Wire the events (`save`, `close`) to the parent's data fetching and state management methods.
  </action>
  <verify>
    `ls frontend/src/app/ui/components/alert-rule-editor.ts` exists.
    `ls frontend/src/app/ui/components/alert-analysis-viewer.ts` exists.
    `wc -l < frontend/src/app/ui/components/alert-rule-editor.ts` <= 300.
    `wc -l < frontend/src/app/ui/components/alert-analysis-viewer.ts` <= 250.
    Grep: `grep -c "app-form-field" frontend/src/app/ui/components/alert-rule-editor.ts` >= 1.
    Grep: `grep -c "app-dialog" frontend/src/app/ui/components/alert-analysis-viewer.ts` >= 1.
    `npx tsc --noEmit` passes.
  </verify>
  <acceptance_criteria>
    - alert-rule-editor.ts exists (<=300 lines) with app-dialog + app-form-field
    - alert-analysis-viewer.ts exists (<=250 lines) with app-dialog
    - Original alerts.ts uses extracted subcomponents
    - tsc passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refactor alerts.ts orchestrator + wire subcomponents</name>
  <read_first>
    frontend/src/app/ui/views/alerts.ts
  </read_first>
  <files>
    MODIFY: frontend/src/app/ui/views/alerts.ts
  </files>
  <action>
    After tasks 1 and 2 extracted the rendering logic into 4 subcomponents, refactor alerts.ts to become the orchestrator:

    1. Remove all extracted template HTML (the table rows, modal dialogs, rule editor forms, analysis panel)
    2. Replace with the 4 subcomponent tags:
       ```html
       <alert-list .alerts=${this.filteredAlerts} .loading=${this.loading}
         @alert-select=${this._onSelectAlert}
         @alert-create=${this._onOpenCreate}></alert-list>
       <alert-detail-modal .alert=${this.selectedAlert} .open=${this.detailOpen}
         @close=${this._closeDetail}></alert-detail-modal>
       <alert-rule-editor .rule=${this.editingRule} .open=${this.editorOpen}
         @save=${this._onSaveRule} @close=${this._closeEditor}></alert-rule-editor>
       <alert-analysis-viewer .analysis=${this.currentAnalysis} .open=${this.analysisOpen}
         @close=${this._closeAnalysis}></alert-analysis-viewer>
       ```
    3. Keep data fetching (fetchAlerts, fetchAlertDetail, saveRule, deleteRule) and state management (selectedAlert, filteredAlerts, loading, etc.) in the orchestrator
    4. Keep the `customElements.define("alerts-page", AlertsPage)` registration
    5. Import the 4 new subcomponent files:
       ```ts
       import "../components/alert-list.js";
       import "../components/alert-detail-modal.js";
       import "../components/alert-rule-editor.js";
       import "../components/alert-analysis-viewer.js";
       ```
    6. Replace any hardcoded px values with token references per D-23
    7. Replace any console.error/warn with logger calls per D-24 (if any exist in alerts.ts)
    8. Remove unused imports and dead code

    The target: alerts.ts should be 500-700 lines (orchestrator with data fetching + state + event wiring) with each subcomponent <300 lines. Total code for the alerts feature stays roughly the same but is properly distributed.

  </action>
  <verify>
    `wc -l < frontend/src/app/ui/views/alerts.ts` <= 700 (was 2805).
    Grep: `grep -c "alert-list" frontend/src/app/ui/views/alerts.ts` >= 1.
    Grep: `grep -c "alert-detail-modal" frontend/src/app/ui/views/alerts.ts` >= 1.
    Grep: `grep -c "alert-rule-editor" frontend/src/app/ui/views/alerts.ts` >= 1.
    Grep: `grep -c "alert-analysis-viewer" frontend/src/app/ui/views/alerts.ts` >= 1.
    `npx tsc --noEmit` passes.
  </verify>
  <acceptance_criteria>
    - alerts.ts reduced from 2805 to <=700 lines
    - 4 subcomponents each <300 lines
    - alerts.ts passes data via properties, handles events from subcomponents
    - Subcomponents imported by alerts.ts parent view
    - tsc passes, alerts feature works end-to-end
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Subcomponent events | Event-based data flow from subcomponent to parent orchestrator |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Spoofing | alert-rule-editor save event | mitigate | Parent validates rule data before saving |
</threat_model>

<verification>
1. 4 new component files in frontend/src/app/ui/components/
2. alerts.ts reduced to <=700 lines
3. Each subcomponent <=300 lines
4. Subcomponents use app-dialog, app-form-field, app-badge
5. tsc passes
6. Visual check: alerts page renders correctly with all 4 extracted parts
</verification>

<success_criteria>
- alerts.ts split into alert-list, alert-detail-modal, alert-rule-editor, alert-analysis-viewer
- Each subcomponent <300 lines, alerts.ts <700 lines
- Subcomponents use shared component library
- Data fetching stays in orchestrator
- tsc passes, feature works end-to-end
</success_criteria>

<output>
Create `.planning/phases/120-ui/120-04-SUMMARY.md` when done
</output>
