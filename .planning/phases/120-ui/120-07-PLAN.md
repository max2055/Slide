---
phase: 120-ui
plan: 07
type: execute
wave: 5
depends_on: [06]
files_modified:
  - frontend/src/app/ui/views/sql-console.ts
  - frontend/src/app/ui/views/metric-registry.ts
  - frontend/src/app/ui/views/cron-jobs-settings.ts
  - frontend/src/app/ui/views/approval-dashboard.ts
  - frontend/src/app/ui/views/metric-templates.ts
  - frontend/src/app/ui/views/reports.ts
  - frontend/src/app/ui/views/users-management.ts
  - frontend/src/app/ui/views/dashboard.ts
  - frontend/src/app/ui/views/ai-settings.ts
  - frontend/src/app/ui/views/login-gate.ts
  - frontend/src/app/ui/views/instances-db.ts
  - frontend/src/app/ui/views/schema-management.ts
autonomous: true
requirements: [UI-OPT-01]
user_setup: []

must_haves:
  truths:
    - "All hand-rolled dialog/modal overlays replaced with <app-dialog>"
    - "All hand-rolled form patterns replaced with <app-form-field>"
    - "All hand-rolled card containers replaced with <app-card>"
    - "All custom toast implementations replaced with showToast()"
    - "All .status-badge CSS classes replaced with <app-badge> component"
    - "All '暂无数据' text patterns replaced with <app-empty-state>"
    - "Cron-jobs div-based table replaced with <app-data-table>"
    - "All 12 views use consistent token-based styling"
  key_links:
    - from: "sql-console.ts"
      to: "app-dialog.ts"
      via: ".modal-overlay → <app-dialog>"
    - from: "cron-jobs-settings.ts"
      to: "app-toast-container.ts"
      via: "custom toast error/success → showToast()"
    - from: "cron-jobs-settings.ts"
      to: "app-data-table.ts"
      via: "div-based grid table → <app-data-table>"
    - from: "dashboard.ts"
      to: "app-badge.ts"
      via: ".status-badge CSS class → <app-badge> component"
---

<objective>
Adopt shared components across all remaining views. Replace hand-rolled dialog/modals, cards, forms, tables, toast, badges, and empty states with the new shared component library.

Purpose: Complete the component standardization per D-08. Eliminate the fragmented dialog systems (H1), incompatible form patterns (H2), custom card CSS (H3), div-based table (H5), custom toasts (H6), and scattered badge CSS (M7).
Output: All 12 remaining views use shared components consistently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-ui/120-CONTEXT.md
@.planning/phases/120-ui/ui-issues-inventory.md

<interfaces>
<read_first order="mandatory">
  frontend/src/app/ui/components/app-dialog.ts              # For replacing modal-overlay/dialog-overlay
  frontend/src/app/ui/components/app-form-field.ts          # For replacing form patterns
  frontend/src/app/ui/components/app-card.ts                # For replacing card CSS patterns
  frontend/src/app/ui/components/app-data-table.ts          # For replacing cron-jobs table
  frontend/src/app/ui/components/app-badge.ts               # For replacing .status-badge CSS
  frontend/src/app/ui/components/app-empty-state.ts         # For replacing "暂无数据"
  frontend/src/app/ui/components/app-toast-container.ts     # For replacing custom toasts
</read_first>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace dialog/modal overlays + forms with app-dialog + app-form-field</name>
  <read_first>
    frontend/src/app/ui/views/sql-console.ts
    frontend/src/app/ui/views/metric-registry.ts
    frontend/src/app/ui/views/cron-jobs-settings.ts
    frontend/src/app/ui/views/approval-dashboard.ts
    frontend/src/app/ui/views/metric-templates.ts
    frontend/src/app/ui/views/reports.ts
    frontend/src/app/ui/views/users-management.ts
    frontend/src/app/ui/views/instances-db.ts
    frontend/src/app/ui/components/app-dialog.ts
    frontend/src/app/ui/components/app-form-field.ts
  </read_first>
  <files>
    MODIFY: frontend/src/app/ui/views/sql-console.ts
    MODIFY: frontend/src/app/ui/views/metric-registry.ts
    MODIFY: frontend/src/app/ui/views/cron-jobs-settings.ts
    MODIFY: frontend/src/app/ui/views/approval-dashboard.ts
    MODIFY: frontend/src/app/ui/views/metric-templates.ts
    MODIFY: frontend/src/app/ui/views/reports.ts
    MODIFY: frontend/src/app/ui/views/users-management.ts
    MODIFY: frontend/src/app/ui/views/instances-db.ts
  </files>
  <action>
    Replace hand-rolled dialog/modal overlay patterns with `<app-dialog>` in each view.

    For each view, find `.dialog-overlay` or `.modal-overlay` CSS declarations and their usage in templates. The pattern is:
    ```html
    <div class="dialog-overlay" @click=${...}>
      <div class="dialog">
        ...
      </div>
    </div>
    ```
    Replace with:
    ```html
    <app-dialog .open=${this.showModal} size="md" title="..." @app-dialog-close=${this._closeModal}>
      ...
      <div slot="footer">...</div>
    </app-dialog>
    ```

    Specific views and their dialog patterns:
    - **sql-console.ts** — `.modal-overlay` (line 284 CSS, line 1216 usage): Replace with `<app-dialog>` for tab confirm modal. Also replace any `.card` CSS patterns with `<app-card>`.
    - **metric-registry.ts** — `.dialog-overlay` (line 79 CSS, lines 228+258 usages): 2 dialog instances (delete confirm + create/edit modal). Replace both with `<app-dialog>`.
    - **cron-jobs-settings.ts** — `.dialog-overlay` (line 172 CSS, lines 533+564+614+634 usages): 4 dialog instances (log viewer, form editor, delete confirm, trigger dialog). Replace all with `<app-dialog>`.
    - **approval-dashboard.ts** — `.modal-overlay` (line 62 CSS, line 515 usage): 1 modal. Replace with `<app-dialog>`.
    - **metric-templates.ts** — `.dialog-overlay` (line 101 CSS, lines 538+616+653 usages): 3 dialogs. Replace with `<app-dialog>`.
    - **reports.ts** — `.modal-overlay` (line 353 CSS): 1 modal. Replace with `<app-dialog>`.
    - **users-management.ts** — `.modal-overlay` (line 258 CSS, line 797 usage): 1 modal. Replace with `<app-dialog>`.
    - **instances-db.ts** — "Add instance" dialog (lines 1088-1242): Replace with `<app-dialog>` + `<app-form-field>` for form fields.

    For **forms** within these dialogs, wrap input elements with `<app-form-field>`:
    - Standardize labels: use `<app-form-field>` with `label` prop instead of raw `<label>` elements
    - Ensure validation errors show via `error` prop
    - Use `<app-form-field>` for the "Add instance" form in instances-db.ts (label for each field)

    Remove the `.dialog-overlay` / `.modal-overlay` CSS declarations from each view's `static styles` after all replacements.
  </action>
  <verify>
    Grep: `grep -c ".dialog-overlay\|.modal-overlay" frontend/src/app/ui/views/sql-console.ts` returns 0.
    Grep: `grep -c ".dialog-overlay\|.modal-overlay" frontend/src/app/ui/views/metric-registry.ts` returns 0.
    Grep: `grep -c ".dialog-overlay\|.modal-overlay" frontend/src/app/ui/views/cron-jobs-settings.ts` returns 0.
    Grep: `grep -c ".dialog-overlay\|.modal-overlay" frontend/src/app/ui/views/approval-dashboard.ts` returns 0.
    Grep: `grep -c ".dialog-overlay\|.modal-overlay" frontend/src/app/ui/views/metric-templates.ts` returns 0.
    Grep: `grep -c ".modal-overlay" frontend/src/app/ui/views/reports.ts` returns 0.
    Grep: `grep -c ".modal-overlay" frontend/src/app/ui/views/users-management.ts` returns 0.
    Grep in each view: `grep -c "app-dialog" frontend/src/app/ui/views/sql-console.ts` >= 1.
    Grep in instances-db: `grep -c "app-form-field" frontend/src/app/ui/views/instances-db.ts` >= 1.
    `npx tsc --noEmit` passes.
  </verify>
  <acceptance_criteria>
    - All 8 views: .dialog-overlay / .modal-overlay removed, replaced with <app-dialog>
    - instances-db.ts "add instance" form uses app-form-field
    - Dialog CSS declarations removed from each view's static styles
    - tsc passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace card CSS + toast + table + badge CSS + empty state</name>
  <read_first>
    frontend/src/app/ui/views/cron-jobs-settings.ts
    frontend/src/app/ui/views/dashboard.ts
    frontend/src/app/ui/views/reports.ts
    frontend/src/app/ui/views/users-management.ts
    frontend/src/app/ui/views/ai-settings.ts
    frontend/src/app/ui/views/login-gate.ts
    frontend/src/app/ui/views/instances-db.ts
    frontend/src/app/ui/views/schema-management.ts
    frontend/src/app/ui/components/app-card.ts
    frontend/src/app/ui/components/app-data-table.ts
    frontend/src/app/ui/components/app-empty-state.ts
    frontend/src/app/ui/components/app-toast-container.ts
    frontend/src/app/ui/components/app-badge.ts
  </read_first>
  <files>
    MODIFY: frontend/src/app/ui/views/cron-jobs-settings.ts
    MODIFY: frontend/src/app/ui/views/dashboard.ts
    MODIFY: frontend/src/app/ui/views/reports.ts
    MODIFY: frontend/src/app/ui/views/users-management.ts
    MODIFY: frontend/src/app/ui/views/ai-settings.ts
    MODIFY: frontend/src/app/ui/views/login-gate.ts
    MODIFY: frontend/src/app/ui/views/instances-db.ts
    MODIFY: frontend/src/app/ui/views/schema-management.ts
  </files>
  <action>
    Replace 4 categories of CSS patterns across all remaining views:

    **1. Card CSS → `<app-card>`:**
    In each view, find `.card` CSS class declarations in `static styles` and their `<div class="card">` usage:
    - **dashboard.ts**: `.status-card` CSS class → replace with `<app-card variant="default">`
    - **ai-settings.ts**: `.card` CSS class → replace with `<app-card variant="default">`
    - **approval-dashboard.ts**: `.card` CSS class → replace with `<app-card>`
    - **reports.ts**: `.card` CSS class → replace with `<app-card>`
    - **users-management.ts**: `.card` patterns → replace with `<app-card>`
    - **schema-management.ts**: `.card` patterns → replace with `<app-card>`
    - Remove the `.card` CSS declarations from `static styles` after replacement

    **2. Custom toast → `showToast()`:**
    - **cron-jobs-settings.ts**: Replace the inline toast div (success/error messages with 3s timeout) with calls to `showToast(message, 'success'|'error')`. Remove the custom toast CSS and template.
    - **metric-templates.ts**: Replace `.toast-ok` / `.toast-err` CSS classes and implementation with `showToast()`. Remove custom toast CSS.

    **3. Div-based table → `<app-data-table>`:**
    - **cron-jobs-settings.ts**: Replace the div-based grid table (lines 479-514) with `<app-data-table>`. Map the div columns to `columns` property and rows to `rows` property.

    **4. `.status-badge` CSS class → `<app-badge>`:**
    In views that define `.status-badge` CSS (NOT the component tag):
    - **dashboard.ts** (lines 301-321): Find `<span class="status-badge ok/warn/danger">` patterns. Replace with `<app-badge variant="ok/warn/danger">`. Remove the `.status-badge` CSS from static styles.
    - **users-management.ts** (lines 173-193): Find `<span class="status-badge active/inactive/locked">`. Replace with `<app-badge variant="ok/muted/warn">`. Map active→ok, inactive→muted, locked→warn.
    - **reports.ts** (lines 252-272): Find `<span class="status-badge ok/blue/red">`. Replace with `<app-badge variant="ok/info/danger">`. Map ok→ok, blue→info, red→danger.
    - **instances-db.ts** (line 257): Find `.status-badge` CSS and usage. Replace with `<app-badge>`. Remove CSS.
    - (instance-detail.ts already handled in Plan 05 split)

    **5. "暂无数据" → `<app-empty-state>`:**
    - **dashboard.ts**: Replace "暂无数据" text in empty tables with `<app-empty-state title="暂无数据">`
    - **users-management.ts**: Same
    - **reports.ts**: Same
    - **schema-management.ts**: Replace the existing handled empty state with `<app-empty-state>` for consistency

    Remove all replaced CSS class declarations from each view's `static styles` template literals.
  </action>
  <verify>
    Grep: `grep -c "showToast" frontend/src/app/ui/views/cron-jobs-settings.ts` >= 1 (replaced custom toast).
    Grep: `grep -c "app-data-table" frontend/src/app/ui/views/cron-jobs-settings.ts` >= 1 (replaced div table).
    Grep: `grep -c "app-badge" frontend/src/app/ui/views/dashboard.ts` >= 1 (replaced .status-badge CSS).
    Grep: `grep -c "app-badge" frontend/src/app/ui/views/users-management.ts` >= 1.
    Grep: `grep -c "app-badge" frontend/src/app/ui/views/reports.ts` >= 1.
    Grep: `grep -c "app-card" frontend/src/app/ui/views/dashboard.ts` >= 1 (replaced .status-card CSS).
    Grep: `grep -c "\.card\s*{" frontend/src/app/ui/views/*.ts` returns 0 for modified views (card CSS removed from static styles).
    Grep: `grep -c "暂无数据" frontend/src/app/ui/views/*.ts` returns 0 for modified views (replaced with app-empty-state) — EXCEPT views where "暂无数据" is a fallback string outside visible UI patterns.
    `npx tsc --noEmit` passes.
  </verify>
  <acceptance_criteria>
    - Card CSS replaced with <app-card> in all remaining views
    - Custom toasts (cron-jobs, metric-templates) replaced with showToast()
    - div-based table in cron-jobs replaced with <app-data-table>
    - .status-badge CSS classes replaced with <app-badge> component
    - "暂无数据" text replaced with <app-empty-state>
    - tsc passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component replacement | Replace CSS class patterns with Lit components — same visual, different DOM |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Spoofing | CSS→component replacements | mitigate | Verify each replacement renders correctly — component tag <app-card> must be imported before use |
</threat_model>

<verification>
1. No `.dialog-overlay` / `.modal-overlay` CSS or templates remain in any modified view
2. No `.card` CSS patterns remain in modified view static styles
3. No custom toast implementations remain in cron-jobs or metric-templates
4. No `.status-badge` CSS classes remain in dashboard, users-management, reports, instances-db
5. All views use proper <app-dialog>, <app-card>, <app-badge>, <app-form-field>, <app-empty-state> tags
6. tsc passes
7. npm run dev starts without errors
</verification>

<success_criteria>
- All 12 views adopt shared components consistently
- Dialog, card, form, badge, table, toast, empty state patterns unified
- No hand-rolled CSS patterns for dialogs, forms, cards, badges remain
- tsc passes
</success_criteria>

<output>
Create `.planning/phases/120-ui/120-07-SUMMARY.md` when done
</output>
