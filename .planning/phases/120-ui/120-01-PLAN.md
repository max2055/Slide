---
phase: 120-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/main.ts
  - frontend/src/styles/global.css
  - frontend/src/styles/global.css.d.ts
  - frontend/src/styles/components.css
  - frontend/src/app/styles.css
  - frontend/src/app/styles/base.css
  - frontend/src/app/styles/tokens.css
  - frontend/src/app/styles/layout.css
  - frontend/src/app/styles/components.css
  - frontend/src/app/styles/chat.css
  - frontend/src/app/styles/utilities.css
autonomous: true
requirements: [UI-OPT-01]
user_setup: []

must_haves:
  truths:
    - "Accent color across all UI is blue #409eff, not purple #7c5cff"
    - "Old global.css and components.css are deleted, no longer loaded by main.ts"
    - "CSS files are reorganized to <=7 files with clear layer separation"
    - "Chat CSS is not loaded twice"
    - "z-index follows layer system (10/100/1000/1100)"
    - "Motion tokens are standardized with --duration-* and --ease-* variables"
    - "Disabled elements use consistent opacity 0.45"
  artifacts:
    - path: "frontend/src/app/styles/tokens.css"
      provides: "Single source of design tokens (colors, spacing, typography, shadows, z-index, motion)"
      min_lines: 120
    - path: "frontend/src/app/styles/utilities.css"
      provides: "Animation keyframes, skeleton classes, utility classes, prefers-reduced-motion"
      min_lines: 60
    - path: "frontend/src/app/styles/base.css"
      provides: "Reset, body defaults, scrollbar, focus-visible with blue accent #409eff"
      contains: "#409eff"
    - path: "frontend/src/main.ts"
      provides: "No longer imports old global.css"
      must_not_contain: "styles/global.css"
  key_links:
    - from: "main.ts"
      to: "tokens.css"
      via: "app/styles.css imports chain"
    - from: "layout.css"
      to: "--z-sidebar / --z-dropdown / --z-modal / --z-toast"
      via: "var() usage"
    - from: "components.css"
      to: "chat.css"
      via: "Removed duplicate @import"
---

<objective>
CSS Architecture Reset: Delete dual CSS token system, reorganize into <=7 files by layer, standardize accent color to blue #409eff with z-index layer system and motion tokens.

Purpose: Eliminate the 1013 lines of dead old CSS that conflict with the new system. Establish a single source of truth for design tokens. Fix Chat CSS double loading (C2).
Output: Clean CSS architecture with tokens.css, base.css, layout.css, components.css, chat.css, utilities.css, dreams.css (<=7 files). Blue accent everywhere. z-index layer tokens (10/100/1000/1100). Standardized motion tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-ui/120-CONTEXT.md
@.planning/phases/120-ui/ui-issues-inventory.md

<interfaces>
<read_first order="mandatory">
  frontend/src/main.ts                          # Entry point — remove import './styles/global.css'
  frontend/src/styles/global.css                # DELETE — 350 lines, old purple accent
  frontend/src/styles/components.css            # DELETE — 663 lines, old class system
  frontend/src/styles/global.css.d.ts           # DELETE — type stub for deleted file
  frontend/src/app/styles.css                   # Root stylesheet — rename to styles-base.css or keep, update imports
  frontend/src/app/styles/base.css              # Source of truth for existing tokens — merge accent update
  frontend/src/app/styles/components.css        # Remove the @import "./chat.css" line
  frontend/src/app/styles/layout.css            # Add z-index layer tokens to existing layout rules
  frontend/src/app/styles/chat.css              # 149 bytes, imports chat/ subdir — verify single import path
</read_first>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete old CSS system + fix main.ts import</name>
  <read_first>
    frontend/src/main.ts
    frontend/src/styles/global.css
    frontend/src/styles/components.css
    frontend/src/styles/global.css.d.ts
  </read_first>
  <files>
    DELETE: frontend/src/styles/global.css (350 lines)
    DELETE: frontend/src/styles/components.css (663 lines)
    DELETE: frontend/src/styles/global.css.d.ts
    MODIFY: frontend/src/main.ts
  </files>
  <action>
    Delete three old CSS files: frontend/src/styles/global.css, frontend/src/styles/components.css, frontend/src/styles/global.css.d.ts.

    In main.ts, remove the line `import './styles/global.css';` (line 8). Keep the `import './app/styles.css';` line unchanged.

    Before deleting global.css, grep for any CSS token or class that exists in global.css but NOT in base.css, and is referenced somewhere in the codebase (grep for `--accent-glow`, `--focus-glow`, `--shadow-glow`). If any of these are used outside the old system, copy them to base.css's `:root` block first. Per D-02: "merge useful tokens from global.css that base.css lacks into base.css".

    Verify no remaining imports of `styles/global.css` or `styles/components.css` exist in main.ts.
  </action>
  <verify>
    Grep gate: `grep -c "styles/global.css" frontend/src/main.ts` returns 0.
    Grep gate: `grep -r "styles/global.css" frontend/src/main.ts frontend/src/app/ 2>/dev/null | grep -v node_modules | grep -v '.d.ts' | grep -v '.test.' | wc -l` returns 0.
    File existence: `ls frontend/src/styles/global.css 2>&1` returns "No such file".
    File existence: `ls frontend/src/styles/components.css 2>&1` returns "No such file".
  </verify>
  <acceptance_criteria>
    - main.ts no longer has `import './styles/global.css'`
    - Three files deleted: global.css, components.css, global.css.d.ts
    - --accent-glow, --focus-glow, --shadow-glow tokens (if used anywhere) are merged into base.css before deletion
    - tsc passes with no broken imports
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tokens.css with new accent color + z-index layers + motion tokens + disabled opacity</name>
  <read_first>
    frontend/src/app/styles/base.css          # Extract all :root tokens, modify accent
    frontend/src/app/styles/layout.css         # For current z-index usage context
  </read_first>
  <files>
    CREATE: frontend/src/app/styles/tokens.css
    MODIFY: frontend/src/app/styles/base.css
    MODIFY: frontend/src/app/styles/layout.css
    MODIFY: frontend/src/app/styles.css
  </files>
  <action>
    Create `frontend/src/app/styles/tokens.css` as the single source of all design tokens. Extract the :root block from base.css into tokens.css. Per D-01, change all accent-related tokens from purple #7c5cff to blue #409eff:

    ```
    --accent: #409eff;
    --accent-hover: #66b1ff;
    --accent-muted: #409eff;
    --accent-subtle: rgba(64,158,255,0.08);
    --accent-glow: rgba(64,158,255,0.15);
    --primary: #409eff;
    --ring: #409eff;
    --focus: rgba(64,158,255,0.15);
    --focus-ring: 0 0 0 2px var(--bg), 0 0 0 3px color-mix(in srgb, var(--ring) 70%, transparent);
    --focus-glow: 0 0 0 2px var(--bg), 0 0 0 3px var(--ring), 0 0 12px var(--accent-glow);
    ```

    Per D-22, add z-index layer tokens:
    ```
    --z-sidebar: 10;
    --z-dropdown: 100;
    --z-modal: 1000;
    --z-toast: 1100;
    ```

    Per D-07, add motion duration tokens (keep existing --duration-fast/normal/slow from base.css):
    Ensure --duration-fast: 100ms, --duration-normal: 180ms, --duration-slow: 300ms and --ease-out, --ease-in-out, --ease-spring are present.

    Per D-14, add disabled token:
    ```
    --disabled-opacity: 0.45;
    ```

    Keep all existing tokens from base.css (--space-*, --text-*, --radius-*, --shadow-*, --font-*, colors, etc.) unchanged except for the accent change.

    After creating tokens.css, strip the :root block from base.css to avoid duplication. In base.css, add a `@import "./tokens.css";` at the top.

    In styles.css, add `@import "./styles/tokens.css";` as the FIRST import (before base.css) so tokens are available to all subsequent layers.

    In layout.css, replace all hardcoded z-index values (1, 2, 3, 4, 5, 10, 12, 30, 40, 65, 70, 100, 200, 1000, 1001) with `var(--z-sidebar)`, `var(--z-dropdown)`, `var(--z-modal)`, or `var(--z-toast)` where appropriate. Map:
    - Sidebar/nav z-indexes → --z-sidebar (10)
    - Dropdown/popover → --z-dropdown (100)
    - Modal/overlay → --z-modal (1000)
    - Toast/notification → --z-toast (1100)
    - Keep any z-index < 10 as literal values (these are stacking context internals)
  </action>
  <verify>
    File exists: `ls frontend/src/app/styles/tokens.css`.
    Grep: `grep -- "--accent: #409eff" frontend/src/app/styles/tokens.css` returns match.
    Grep: `grep -- "--z-sidebar: 10" frontend/src/app/styles/tokens.css` returns match.
    Grep: `grep -- "--disabled-opacity: 0.45" frontend/src/app/styles/tokens.css` returns match.
    Grep: `grep -- "--duration-fast: 100ms" frontend/src/app/styles/tokens.css` returns match.
    Grep: `grep -c "tokens.css" frontend/src/app/styles.css` returns >= 1.
    Grep: `grep -c "#7c5cff" frontend/src/app/styles/*.css` returns 0 (no purple remains in CSS files).
  </verify>
  <acceptance_criteria>
    - tokens.css exists with all tokens, accent is #409eff
    - z-index layer tokens defined (10/100/1000/1100)
    - Motion tokens present (duration/ease)
    --disabled-opacity: 0.45 defined
    - layout.css uses var(--z-*) tokens for all z-index > 10
    - base.css imports tokens.css at top
    - styles.css imports tokens.css first
    - No purple #7c5cff remains in any CSS file
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Reorganize CSS to <=7 files + fix Chat double load</name>
  <read_first>
    frontend/src/app/styles.css                # Current import chain
    frontend/src/app/styles/components.css     # Remove @import "./chat.css"
    frontend/src/app/styles/chat.css           # Keeps importing chat/ subdir
  </read_first>
  <files>
    MODIFY: frontend/src/app/styles.css
    MODIFY: frontend/src/app/styles/components.css
    CREATE: frontend/src/app/styles/utilities.css
    MODIFY: frontend/src/app/styles/base.css
    DELETE: frontend/src/app/styles/layout.mobile.css (if merged into layout.css)
  </files>
  <action>
    Per D-03, the final CSS file structure must be <=7 files by layer:

    **Target structure (<=7 files):**
    1. `tokens.css` — All design tokens (:root block with colors, spacing, typography, shadows, z-index, motion)
    2. `base.css` — Reset, body defaults, scrollbar, ::selection, font-face if any
    3. `layout.css` — Shell grid, sidebar navigation, tab bar, responsive breakpoints (merge layout.mobile.css content into this file, then delete layout.mobile.css)
    4. `components.css` — Shared component styles (buttons, forms, tables, badges, dialogs in CSS), login gate,
    - REMOVE `@import "./chat.css";` from **this** file (line 1) — per D-04 / C2 fix
    5. `chat.css` — imports chat/ subdir files (layout.css, text.css, grouped.css, tool-cards.css, sidebar.css) — this file is the single entry point for chat styles
    6. `utilities.css` — Animation keyframes, .skeleton / .skeleton-line classes, .stagger-* classes, prefers-reduced-motion rules, data-reduce-animations, data-density — extract these from base.css
    7. `dreams.css` — Keep as standalone (per D-03 exemption)

    **Update styles.css imports:**
    ```
    @import "./styles/tokens.css";
    @import "./styles/base.css";
    @import "./styles/layout.css";
    @import "./styles/components.css";
    @import "./styles/chat.css";
    @import "./styles/dreams.css";
    ```
    (utilities.css is imported by base.css or directly by components that need skeleton classes)

    **Merge layout.mobile.css into layout.css:**
    Read all content from layout.mobile.css (763 lines, ~100 px values). Insert the mobile breakpoint rules at the bottom of layout.css, wrapping them in `@media (max-width: 768px)` blocks as appropriate. After merge, delete layout.mobile.css.

    **Extract utilities.css:**
    From base.css, extract to utilities.css:
    - All @keyframes blocks (rise, fade-in, scale-in, dashboard-enter, shimmer, pulse-subtle, glow-pulse)
    - All .skeleton / .skeleton-line / .skeleton-stat / .skeleton-block classes
    - .stagger-1 through .stagger-6
    - prefers-reduced-motion block
    - data-reduce-animations block
    - data-density block
    - Leave them in base.css AND also ensure utilities.css has them (or move them and import utilities.css from base.css)

    **Keep config.css and usage.css** as-is (they're page-specific, not general — not counted in the 7 file layer budget). They stay imported via their respective page components.

    Remove layout.mobile.css from styles.css import chain.
  </action>
  <verify>
    Count: `ls frontend/src/app/styles/*.css | grep -v config.css | grep -v usage.css | wc -l` returns <= 7.
    Grep: `grep -c "@import.*chat.css" frontend/src/app/styles/components.css` returns 0 (removed).
    Grep: `grep -c "@import.*chat.css" frontend/src/app/styles.css` returns >= 1 (still there).
    File deleted: `ls frontend/src/app/styles/layout.mobile.css 2>&1` returns "No such file".
    File exists: `ls frontend/src/app/styles/utilities.css`.
    File exists: `ls frontend/src/app/styles/tokens.css`.
    `npm run dev` or `npx tsc --noEmit` passes in frontend directory.
  </verify>
  <acceptance_criteria>
    - Maximum 7 CSS files in styles/ (excluding config.css, usage.css)
    - components.css no longer imports chat.css
    - layout.mobile.css content merged into layout.css, file deleted
    - utilities.css created with animation keyframes + skeleton classes + stagger classes + reduced-motion
    - All imports in styles.css updated to reflect new structure
    - tsc passes, frontend builds without errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CSS file load chain | main.ts -> styles.css -> CSS layers |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Tampering | CSS files | mitigate | Delete old files before adding new ones; verify main.ts no longer references deleted files |
| T-120-02 | Information Disclosure | Deleted CSS types | mitigate | Delete global.css.d.ts alongside global.css to prevent dangling type references |
</threat_model>

<verification>
1. `grep -c "styles/global.css" frontend/src/main.ts` returns 0
2. `ls frontend/src/styles/global.css 2>&1` returns "No such file"
3. `grep -c "#7c5cff" frontend/src/app/styles/*.css` returns 0
4. `grep -c "@import.*chat.css" frontend/src/app/styles/components.css` returns 0
5. `ls frontend/src/app/styles/utilities.css` exists
6. `frontend/build` or tsc passes
7. Frontend dev server compiles without 404 on deleted CSS files
</verification>

<success_criteria>
- All old global.css/components.css deleted, main.ts no longer references them
- tokens.css created with blue accent #409eff, z-index layers, motion tokens, disabled-opacity
- CSS files reorganized to <=7 files per D-03
- Chat CSS no longer double-loaded
- layout.mobile.css merged into layout.css
- tsc passes, dev server starts without broken CSS imports
</success_criteria>

<output>
Create `.planning/phases/120-ui/120-01-SUMMARY.md` when done
</output>
