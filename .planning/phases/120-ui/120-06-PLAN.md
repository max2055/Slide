---
phase: 120-ui
plan: 06
type: execute
wave: 4
depends_on: [04, 05]
files_modified:
  - frontend/src/app/ui/views/chat.ts
  - frontend/src/app/ui/components/chat-message-list.ts
  - frontend/src/app/ui/components/chat-compose-area.ts
  - frontend/src/app/ui/components/chat-tool-result-card.ts
  - frontend/src/app/ui/components/status-badge.ts
  - frontend/src/app/ui/app-render.ts
  - frontend/src/app/ui/views/metric-registry.ts
  - frontend/src/app/ui/views/metric-templates.ts
  - frontend/src/app/ui/views/approval-dashboard.ts
  - frontend/src/app/ui/views/cron-jobs-settings.ts
  - frontend/src/app/ui/views/event-management.ts
autonomous: true
requirements: [UI-OPT-01]
user_setup: []

must_haves:
  truths:
    - "chat.ts is split into 3 subcomponents (chat-message-list, chat-compose-area, chat-tool-result-card)"
    - "Main chat.ts orchestrates subcomponents, handles WebSocket/session management"
    - "status-badge.ts is deleted, replaced by app-badge.ts (created in Plan 03)"
    - "All 5 views using <status-badge> now use <app-badge>"
    - "app-render.ts imports app-badge.ts instead of status-badge.ts"
  artifacts:
    - path: "frontend/src/app/ui/components/chat-message-list.ts"
      provides: "Chat message rendering with grouping, scroll, streaming indicators"
      max_lines: 300
    - path: "frontend/src/app/ui/components/chat-compose-area.ts"
      provides: "Chat input area with send button, typing indicator"
      max_lines: 200
    - path: "frontend/src/app/ui/components/chat-tool-result-card.ts"
      provides: "Tool result card rendering in chat"
      max_lines: 150
  key_links:
    - from: "chat-message-list.ts"
      to: "app-badge.ts"
      via: "Uses <app-badge> for status indicators"
    - from: "chat-tool-result-card.ts"
      to: "app-card.ts"
      via: "Uses <app-card> for tool result rendering"
    - from: "app-render.ts"
      to: "app-badge.ts"
      via: "import changed from status-badge.ts → app-badge.ts"
---

<objective>
Split chat.ts (2069 lines) into 3 subcomponents + rename status-badge to app-badge across all views.

Purpose: Eliminate the chat.ts god component (H9). Per D-20, split message rendering, compose area, and tool results into focused subcomponents. Complete the badge rename per D-10 by replacing all <status-badge> usage with <app-badge> and deleting the old file.
Output: 3 new chat subcomponents + refactored chat.ts orchestrator + badge rename across 5 views.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-ui/120-CONTEXT.md
@.planning/phases/120-ui/ui-issues-inventory.md

<interfaces>
<read_first order="mandatory">
  frontend/src/app/ui/views/chat.ts                             # The file being split
  frontend/src/app/ui/components/app-badge.ts                    # Already created in Plan 03
  frontend/src/app/ui/components/app-card.ts                     # Already created in Plan 03
  frontend/src/app/ui/components/app-toast-container.ts          # For showToast in chat
  frontend/src/app/ui/app-render.ts                              # Import updates
</read_first>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract chat-message-list and chat-compose-area</name>
  <read_first>
    frontend/src/app/ui/views/chat.ts
    frontend/src/app/ui/components/app-badge.ts
  </read_first>
  <files>
    CREATE: frontend/src/app/ui/components/chat-message-list.ts
    CREATE: frontend/src/app/ui/components/chat-compose-area.ts
    MODIFY: frontend/src/app/ui/views/chat.ts
  </files>
  <action>
    Read chat.ts (2069 lines) and understand its structure. It handles:
    - Message list rendering (with streaming, grouping, scroll management)
    - Compose area (textarea input, send button, model selector, session selector)
    - Tool result cards (tool call results displayed in chat)
    - WebSocket management, session management, state management

    **chat-message-list.ts** — Extract the message list rendering:
    - Tag: `<chat-message-list>`
    - Properties:
      - `messages: ChatMessage[]`
      - `streaming: boolean` — whether a message is currently streaming
      - `streamingContent: string` — current partial content
      - `error: string | null` — error message to display
    - Events: dispatches `retry` with message ID, `scroll-to-bottom` when new messages arrive
    - Render: iterates messages, renders each as a message bubble (user vs assistant styling), handles streaming state (show typing indicator + partial content), renders error states inline
    - Use `<app-badge>` for status indicators on messages
    - Keep grouping logic (date separators, sender grouping)
    - Per D-21: limit to 250-300 lines

    **chat-compose-area.ts** — Extract the compose area:
    - Tag: `<chat-compose-area>`
    - Properties:
      - `disabled: boolean` — disable input while streaming
      - `placeholder: string` — input placeholder text
    - Events: dispatches `send` with message text
    - Render: textarea input, send button, character count/submit hint
    - Keep keyboard handling (Enter to send, Shift+Enter for newline)
    - Limit to 150-200 lines

    **Update chat.ts:**
    - Remove extracted HTML templates and CSS
    - Replace with `<chat-message-list>` and `<chat-compose-area>` tags
    - Wire events (.loading, @retry, @send, @scroll-to-bottom)
    - Keep WebSocket management, session management, data fetching
    - Import new components
  </action>
  <verify>
    `ls frontend/src/app/ui/components/chat-message-list.ts` exists.
    `ls frontend/src/app/ui/components/chat-compose-area.ts` exists.
    `wc -l < frontend/src/app/ui/components/chat-message-list.ts` <= 300.
    `wc -l < frontend/src/app/ui/components/chat-compose-area.ts` <= 200.
    Grep: `grep -c "chat-message-list" frontend/src/app/ui/views/chat.ts` >= 1.
    Grep: `grep -c "chat-compose-area" frontend/src/app/ui/views/chat.ts` >= 1.
    `npx tsc --noEmit` passes.
  </verify>
  <acceptance_criteria>
    - chat-message-list.ts exists (<=300 lines) with message rendering, streaming state, app-badge usage
    - chat-compose-area.ts exists (<=200 lines) with textarea+send, keyboard handling
    - chat.ts uses both subcomponents and wires events
    - tsc passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract chat-tool-result-card + refactor chat.ts orchestrator</name>
  <read_first>
    frontend/src/app/ui/views/chat.ts
    frontend/src/app/ui/components/app-card.ts
  </read_first>
  <files>
    CREATE: frontend/src/app/ui/components/chat-tool-result-card.ts
    MODIFY: frontend/src/app/ui/views/chat.ts
    MODIFY: frontend/src/app/ui/app-render.ts
  </files>
  <action>
    **chat-tool-result-card.ts** — Extract tool result rendering:
    - Tag: `<chat-tool-result-card>`
    - Properties:
      - `result: ToolResult` — the tool call result data
      - `collapsible: boolean = true` — whether the card can be collapsed
    - Events: dispatches `collapse-toggle` with result ID
    - Render: uses `<app-card>` with variant "bordered" to wrap the tool result. Shows tool name, status (via `<app-badge>`), duration, and result content (formatted as markdown or structured data)
    - Limit to 150 lines

    **Refactor chat.ts orchestrator:**
    After all 3 subcomponents extracted, refactor chat.ts:
    1. Replace all extracted HTML with the 3 subcomponent tags
    2. Keep WebSocket management, session management, message routing, data fetching
    3. Keep state management (activeSession, messages, streaming, error)
    4. Wire events between subcomponents
    5. Remove dead code, unused imports
    6. Replace any console.* with showToast or silent handling
    7. Target: 500-700 lines
    8. Add `import "./components/chat-message-list.js"` and other imports

    Note: chat.ts exports a `renderChat()` function (from line 50 in app-render.ts). Do not change the export signature — keep `renderChat()` as the entry point.

    **Update app-render.ts:**
    - Change `import "./components/status-badge.ts"` (line 24) to `import "./components/app-badge.ts"`
    - Add imports for the 3 new chat subcomponents
  </action>
  <verify>
    `ls frontend/src/app/ui/components/chat-tool-result-card.ts` exists.
    `wc -l < frontend/src/app/ui/views/chat.ts` <= 700 (was 2069).
    Grep: `grep -c "chat-tool-result-card" frontend/src/app/ui/views/chat.ts` >= 1.
    Grep: `grep -c "app-badge.ts" frontend/src/app/ui/app-render.ts` >= 1 (replaced status-badge.ts).
    Grep: `grep -c "status-badge.ts" frontend/src/app/ui/app-render.ts` returns 0 (no longer imported).
    `npx tsc --noEmit` passes.
  </verify>
  <acceptance_criteria>
    - chat-tool-result-card.ts exists (<=150 lines) with app-card + app-badge
    - chat.ts reduced from 2069 to <=700 lines
    - app-render.ts imports app-badge.ts instead of status-badge.ts
    - export function renderChat() still exported
    - tsc passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace <status-badge> with <app-badge> in all views + delete status-badge.ts</name>
  <read_first>
    frontend/src/app/ui/components/app-badge.ts
    frontend/src/app/ui/views/metric-registry.ts
    frontend/src/app/ui/views/metric-templates.ts
    frontend/src/app/ui/views/approval-dashboard.ts
    frontend/src/app/ui/views/cron-jobs-settings.ts
    frontend/src/app/ui/views/event-management.ts
  </read_first>
  <files>
    MODIFY: frontend/src/app/ui/views/metric-registry.ts
    MODIFY: frontend/src/app/ui/views/metric-templates.ts
    MODIFY: frontend/src/app/ui/views/approval-dashboard.ts
    MODIFY: frontend/src/app/ui/views/cron-jobs-settings.ts
    MODIFY: frontend/src/app/ui/views/event-management.ts
    DELETE: frontend/src/app/ui/components/status-badge.ts
  </files>
  <action>
    Per D-10, replace all `<status-badge>` usage with `<app-badge>` across 5 views:

    1. **metric-registry.ts** (line 206): Change `<status-badge ...>` to `<app-badge ...>`
    2. **metric-templates.ts** (line 469): Change `<status-badge ...>` to `<app-badge ...>`
    3. **approval-dashboard.ts** (line 366): Change `<status-badge ...>` to `<app-badge ...>`
    4. **cron-jobs-settings.ts** (line 416): Change `<status-badge ...>` to `<app-badge ...>`
    5. **event-management.ts** (line 662): Change `<status-badge ...>` to `<app-badge ...>`

    For each view:
    - Change the tag name from `status-badge` to `app-badge`
    - Verify the variant prop is the same (ok, danger, warn, info, muted)
    - Replace any `StatusBadge` type imports with `BadgeVariant` from app-badge.ts if needed

    After all 5 views updated, delete `frontend/src/app/ui/components/status-badge.ts`.

    NOTE: Do NOT touch `.status-badge` CSS classes in other views (dashboard.ts, users-management.ts, instance-detail.ts, reports.ts, instances-db.ts). Those are CSS class names, not component tags. They will be addressed in Plan 07 (component adoption) when those views adopt the shared component.
  </action>
  <verify>
    Grep: `grep -rn "status-badge" frontend/src/app/ui/views/ 2>/dev/null | grep -v "\.status-badge" | grep -v "\.test\."` returns 0 (no <status-badge> tag usage).
    Grep: `grep -rn "<status-badge" frontend/src/app/ 2>/dev/null` returns 0.
    File deleted: `ls frontend/src/app/ui/components/status-badge.ts 2>&1` returns "No such file".
    Grep: `grep -rn "status-badge\.ts" frontend/src/app/ 2>/dev/null` returns 0.
    `npx tsc --noEmit` passes.
  </verify>
  <acceptance_criteria>
    - All 5 views updated: <status-badge> → <app-badge>
    - status-badge.ts deleted
    - No import of status-badge.ts remains in the codebase
    - Only .status-badge CSS classes remain (addressed in Plan 07)
    - tsc passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Chat subcomponent events | Event-based data flow from subcomponents to parent |
| Badge rename | Component tag change, same API |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Spoofing | chat-compose-area send event | accept | Parent validates message text before sending |
| T-120-02 | Tampering | status-badge→app-badge rename | mitigate | Same API, identical behavior — grep verify no <status-badge> tags remain |
</threat_model>

<verification>
1. chat.ts <= 700 lines (from 2069)
2. 3 chat subcomponent files exist, each within size limits
3. chat.ts exports renderChat() unchanged
4. app-render.ts imports app-badge.ts, not status-badge.ts
5. No <status-badge> usage remains (only CSS class .status-badge)
6. status-badge.ts file deleted
7. tsc passes
</verification>

<success_criteria>
- chat.ts split into chat-message-list, chat-compose-area, chat-tool-result-card
- chat.ts reduced from 2069 to <=700 lines
- Subcomponents use app-badge, app-card from shared library
- All 5 view files using <status-badge> updated to <app-badge>
- status-badge.ts deleted
- tsc passes, chat feature works end-to-end
</success_criteria>

<output>
Create `.planning/phases/120-ui/120-06-SUMMARY.md` when done
</output>
