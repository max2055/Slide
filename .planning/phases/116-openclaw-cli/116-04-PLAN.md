---
phase: 116-openclaw-cli
plan: 04
type: execute
wave: 3
depends_on:
  - 116-02   # Env vars + runtime identifiers must be updated first
  - 116-03   # User text must be updated first
files_modified:
  - frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.filters-usage-summary-current-model-provider.cases.ts
autonomous: true
requirements:
  - D-16
  - D-17

must_haves:
  truths:
    - "Test assertions referencing 'OpenClaw' in output strings are updated to reference 'Slide'"
    - "Test harness env vars already updated in Plan 02 — no additional env var work needed here"
  artifacts:
    - path: "frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.filters-usage-summary-current-model-provider.cases.ts"
      provides: "Updated test assertion"
      contains: "Slide"
      not_contains: "OpenClaw"
  key_links:
    - from: ".cases.ts assertion"
      to: "status.ts"
      via: "expect(text).toContain matches the version line string changed in Plan 03"
---

<objective>
Update test assertions to match the new Slide branding strings.

**Purpose:** All runtime code has been updated in Plans 02 and 03 — now update the test assertion strings that check for "OpenClaw" in output. D-16 (test env vars) was already completed in Plan 02 Task 1 when the test harness OPENCLAW_* env vars were renamed to SLIDE_* alongside the source code changes. This plan covers D-17 only: assertions that match user-visible text.
**Output:** ~1 test file updated with corrected assertion strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-openclaw-cli/116-CONTEXT.md
@.planning/phases/116-openclaw-cli/116-RESEARCH.md
@.planning/phases/116-openclaw-cli/116-02-PLAN.md
@.planning/phases/116-openclaw-cli/116-03-PLAN.md

<interfaces>
<!--
Test assertion patterns that check user-visible OpenClaw text.
These must match the new Slide text after Plans 02 and 03 changes.
-->

.cases.ts file:
  Before: expect(text).toContain("OpenClaw");
  After:  expect(text).toContain("Slide");
  (This assertion validates the version line in status.ts which was changed in Plan 03 from
   `🦞 OpenClaw ${VERSION}` to `🦞 Slide ${VERSION}`.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test assertions referencing user-visible OpenClaw text</name>
  <read_first>
    frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.filters-usage-summary-current-model-provider.cases.ts (line 78)
  </read_first>
  <files>
    frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.filters-usage-summary-current-model-provider.cases.ts
  </files>
  <action>
    The only test assertion referencing "OpenClaw" as user-visible text is in:
    `frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.filters-usage-summary-current-model-provider.cases.ts` line 78.

    Change:
    `expect(text).toContain("OpenClaw");`
    To:
    `expect(text).toContain("Slide");`

    This assertion validates the version line from status.ts (`🦞 Slide ${VERSION}`) — already updated in Plan 03 Task 1.

    After the change, run a broad sweep to confirm no other test assertions reference "OpenClaw":

    ```bash
    # Search all test files for "OpenClaw" in assertion-like patterns
    grep -rn 'toContain.*OpenClaw\|toEqual.*OpenClaw\|toBe.*OpenClaw\|toMatch.*OpenClaw' frontend/src/ --include='*.ts' | grep -v node_modules | grep -v '\.d\.ts'
    # Should be 0 matches

    # Search ALL files for remaining user-visible "OpenClaw" patterns in string literals
    grep -rn '"OpenClaw\|OpenClaw ' frontend/src/ --include='*.ts' | grep -v node_modules | grep -v '\.d\.ts' | grep -v 'OpenClawConfig\|@deprecated\|legacy OpenClaw\|extracted from OpenClaw\|^.*//'
    # Should be 0 matches for functional/runtime strings (comments may remain)
    ```
  </action>
  <verify>
    <automated>
grep -q 'toContain.*"Slide"' frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.filters-usage-summary-current-model-provider.cases.ts && ! grep -q 'toContain.*"OpenClaw"' frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.filters-usage-summary-current-model-provider.cases.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - The one .cases.ts assertion uses `toContain("Slide")` instead of `toContain("OpenClaw")`
    - No remaining `toContain("OpenClaw")` patterns in any test file
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test assertion updates only — no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-07 | Spoofing | Test assertion | mitigate | Assertion string must match the updated runtime string from Plans 02/03 |
| T-116-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this plan |
</threat_model>

<verification>
- `grep -rn 'toContain.*"OpenClaw"' frontend/src/ --include='*.ts' | grep -v node_modules | wc -l` = 0
- All test assertions match the current runtime strings from Plans 02 and 03
</verification>

<success_criteria>
1. Test assertions referencing "OpenClaw" now reference "Slide"
2. `npx vitest run --reporter=verbose 2>&1 | tail -30` in `frontend/` shows no new test failures from assertion mismatch
</success_criteria>

<output>
Create `.planning/phases/116-openclaw-cli/116-04-SUMMARY.md` when done
</output>
