---
phase: 116-openclaw-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 116-01   # branding.ts must exist first
files_modified:
  # Env vars (D-05) — non-test source files
  - frontend/src/app/src/media/image-ops.ts
  - frontend/src/app/src/auto-reply/reply/get-reply-run.ts
  - frontend/src/app/src/auto-reply/reply/test-helpers.ts
  - frontend/src/app/src/auto-reply/reply/session-updates.ts
  - frontend/src/app/src/auto-reply/reply/model-selection.ts
  - frontend/src/app/src/auto-reply/reply/get-reply-directives.ts
  - frontend/src/app/src/auto-reply/reply/get-reply.ts
  - frontend/src/app/src/auto-reply/reply/session.ts
  - frontend/src/app/src/auto-reply/reply/get-reply-fast-path.ts
  # UI base path (D-06)
  - frontend/src/app/ui/app-settings.ts
  - frontend/src/app/ui/app.ts
  - frontend/src/app/ui/storage.ts
  # HTTP User-Agent
  - frontend/src/app/src/media/input-files.ts
  # Symbol keys (D-11)
  - frontend/src/app/src/auto-reply/reply/inbound-dedupe.ts
  - frontend/src/app/src/auto-reply/reply/reply-run-registry.ts
  - frontend/src/app/src/auto-reply/reply/queue/state.ts
  - frontend/src/app/src/auto-reply/reply/queue/enqueue.ts
  - frontend/src/app/src/auto-reply/reply/queue/drain.ts
  # Plugin format (D-14)
  - frontend/src/app/src/auto-reply/reply/commands-plugins.ts
  # Data dirs (D-07) — non-test source files
  - frontend/src/app/src/auto-reply/reply/stage-sandbox-media.ts
  # Test harnesses — env vars + data dirs combined
  - frontend/src/app/src/auto-reply/reply.test-harness.ts
  - frontend/src/app/src/auto-reply/reply.directive.directive-behavior.e2e-harness.ts
  - frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.test-harness.ts
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-11
  - D-14

must_haves:
  truths:
    - "No OPENCLAW_* env var references remain in frontend/src/ source files"
    - "No __OPENCLAW_CONTROL_UI_BASE_PATH__ references remain; replaced by __SLIDE_CONTROL_UI_BASE_PATH__"
    - "No Symbol.for('openclaw.*') references remain; replaced by Symbol.for('slide.*')"
    - "No ~/.openclaw/ path references remain; replaced by ~/.slide/"
    - "plugin.format fallback changed from 'openclaw' to 'slide'"
    - "HTTP User-Agent header changed from 'OpenClaw-Gateway/1.0' to 'Slide-Gateway/1.0'"
  artifacts:
    - path: "frontend/src/app/src/media/image-ops.ts"
      contains: "SLIDE_IMAGE_BACKEND"
      not_contains: "OPENCLAW_IMAGE_BACKEND"
    - path: "frontend/src/app/ui/app.ts"
      contains: "__SLIDE_CONTROL_UI_BASE_PATH__"
      not_contains: "__OPENCLAW_CONTROL_UI_BASE_PATH__"
    - path: "frontend/src/app/src/auto-reply/reply/inbound-dedupe.ts"
      contains: 'Symbol.for("slide.inboundDedupeCache")'
      not_contains: 'Symbol.for("openclaw.inboundDedupeCache")'
    - path: "frontend/src/app/src/auto-reply/reply/commands-plugins.ts"
      contains: 'plugin.format ?? "slide"'
      not_contains: 'plugin.format ?? "openclaw"'
    - path: "frontend/src/app/src/media/input-files.ts"
      contains: "Slide-Gateway"
      not_contains: "OpenClaw-Gateway"
  key_links:
    - from: "get-reply-fast-path.ts"
      to: "branding.ts"
      via: "env var OPENCLAW_* replaced by SLIDE_*"
    - from: "reply.test-harness.ts"
      to: "branding.ts"
      via: "env vars + .openclaw path updated"
    - from: "inbound-dedupe.ts"
      to: "branding.ts"
      via: "Symbol.for keys updated"
    - from: "app.ts"
      to: "global scope"
      via: "__SLIDE_CONTROL_UI_BASE_PATH__ declares global window property"
---

<objective>
Replace all runtime identifier OpenClaw references with Slide equivalents.

**Purpose:** Update all functional runtime references — environment variables (D-05), UI base path (D-06), data directory paths (D-07), Symbol.for keys (D-11), plugin format (D-14), and HTTP User-Agent — from `openclaw`/`OpenClaw`/`OPENCLAW` to `slide`/`Slide`/`SLIDE`. All changes are direct string replacements. Test harness env vars and data paths are included here (for the .openclaw path component and OPENCLAW_* environment variables) so that the plan producing those changes is self-contained. Test assertion string updates are deferred to Plan 04.
**Output:** ~20 source files updated with new Slide branding identifiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-openclaw-cli/116-CONTEXT.md
@.planning/phases/116-openclaw-cli/116-RESEARCH.md
@.planning/phases/116-openclaw-cli/116-01-PLAN.md

<interfaces>
<!--
Exact patterns to replace in each file.
The executor should grep for each pattern BEFORE editing to confirm file content.

Branding constants (from Plan 01):
  CLI_NAME = "slide"
  PRODUCT_NAME = "Slide"
  ENV_PREFIX = "SLIDE"
  STATE_DIR = ".slide"
  buildEnvVar(suffix) => "SLIDE_{suffix}"
  buildSymbolKey(key) => "slide.{key}"
  buildUserAgent() => "Slide-Gateway/1.0"
-->

=== Env var replacements (D-05): OPENCLAW_XXX → SLIDE_XXX ===

image-ops.ts:
  Before: process.env.OPENCLAW_IMAGE_BACKEND === "sips"
  After:  process.env.SLIDE_IMAGE_BACKEND === "sips"

get-reply-run.ts:
  Before: process.env.OPENCLAW_TEST_FAST === "1"  (2 occurrences at lines 239, 430)
  After:  process.env.SLIDE_TEST_FAST === "1"

test-helpers.ts:
  Before: process.env.OPENCLAW_TEST_FAST === "1"  (line 24)
  After:  process.env.SLIDE_TEST_FAST === "1"

session-updates.ts:
  Before: process.env.OPENCLAW_TEST_FAST === "1"  (line 112)
  After:  process.env.SLIDE_TEST_FAST === "1"

model-selection.ts:
  Before: process.env.OPENCLAW_DEBUG_INGRESS_TIMING === "1"  (line 64)
  After:  process.env.SLIDE_DEBUG_INGRESS_TIMING === "1"

get-reply-directives.ts:
  Before: process.env.OPENCLAW_TEST_FAST === "1"  (line 428)
  After:  process.env.SLIDE_TEST_FAST === "1"

get-reply.ts:
  Before: process.env.OPENCLAW_TEST_FAST === "1"  (line 151)
  After:  process.env.SLIDE_TEST_FAST === "1"

session.ts:
  Before: process.env.OPENCLAW_DEBUG_INGRESS_TIMING === "1"  (line 277)
  After:  process.env.SLIDE_DEBUG_INGRESS_TIMING === "1"

get-reply-fast-path.ts:
  Before: env.OPENCLAW_ALLOW_SLOW_REPLY_TESTS === "1"  (line 30)
  Before: env.OPENCLAW_STRICT_FAST_REPLY_CONFIG === "0"  (line 30)
  Before: "OPENCLAW_ALLOW_SLOW_REPLY_TESTS=1"  (line 107)
  After:  env.SLIDE_ALLOW_SLOW_REPLY_TESTS === "1"
  After:  env.SLIDE_STRICT_FAST_REPLY_CONFIG === "0"
  After:  "SLIDE_ALLOW_SLOW_REPLY_TESTS=1"

=== Test harness env vars ===

reply.test-harness.ts:
  OPENCLAW_STATE_DIR → SLIDE_STATE_DIR  (lines 128-129, 139-140, 176)
  OPENCLAW_AGENT_DIR → SLIDE_AGENT_DIR  (lines 129, 140, 177)

reply.triggers.trigger-handling.test-harness.ts:
  OPENCLAW_HOME → SLIDE_HOME  (lines 201, 219, 226)
  OPENCLAW_STATE_DIR → SLIDE_STATE_DIR  (lines 202, 220, 227)

reply.directive.directive-behavior.e2e-harness.ts:
  OPENCLAW_AGENT_DIR → SLIDE_AGENT_DIR  (line 135)

=== UI base path (D-06) ===

app-settings.ts line 258:
  Before: window.__OPENCLAW_CONTROL_UI_BASE_PATH__
  After:  window.__SLIDE_CONTROL_UI_BASE_PATH__

app.ts line 72:
  Before: __OPENCLAW_CONTROL_UI_BASE_PATH__?: string;
  After:  __SLIDE_CONTROL_UI_BASE_PATH__?: string;

storage.ts line 92:
  Before: window.__OPENCLAW_CONTROL_UI_BASE_PATH__
  After:  window.__SLIDE_CONTROL_UI_BASE_PATH__

=== HTTP User-Agent ===

input-files.ts line 189:
  Before: init: { headers: { "User-Agent": "OpenClaw-Gateway/1.0" } }
  After:  init: { headers: { "User-Agent": "Slide-Gateway/1.0" } }

=== Symbol keys (D-11): Symbol.for("openclaw.XXX") → Symbol.for("slide.XXX") ===

inbound-dedupe.ts:
  Before: Symbol.for("openclaw.inboundDedupeCache")
  After:  Symbol.for("slide.inboundDedupeCache")
  Before: Symbol.for("openclaw.inboundDedupeInflight")
  After:  Symbol.for("slide.inboundDedupeInflight")

reply-run-registry.ts:
  Before: Symbol.for("openclaw.replyRunRegistry")
  After:  Symbol.for("slide.replyRunRegistry")

get-reply-fast-path.ts:
  Before: Symbol.for("openclaw.reply.complete-config")
  After:  Symbol.for("slide.reply.complete-config")
  Before: Symbol.for("openclaw.reply.full-runtime")
  After:  Symbol.for("slide.reply.full-runtime")

queue/state.ts:
  Before: Symbol.for("openclaw.followupQueues")
  After:  Symbol.for("slide.followupQueues")

queue/enqueue.ts:
  Before: Symbol.for("openclaw.recentQueueMessageIds")
  After:  Symbol.for("slide.recentQueueMessageIds")

queue/drain.ts:
  Before: Symbol.for("openclaw.followupDrainCallbacks")
  After:  Symbol.for("slide.followupDrainCallbacks")

=== Plugin format (D-14) ===

commands-plugins.ts:
  Before: plugin.format ?? "openclaw"  (lines 123-124, 2 occurrences)
  After:  plugin.format ?? "slide"

=== Data directory paths (D-07): .openclaw → .slide ===

stage-sandbox-media.ts line 41 (comment):
  Before: // For remote attachments without sandbox, use ~/.openclaw/media
  After:  // For remote attachments without sandbox, use ~/.slide/media

reply.test-harness.ts:
  Before: path.join(home, ".openclaw", "agents", "main", "sessions")  (line 172)
  After:  path.join(home, ".slide", "agents", "main", "sessions")
  Before: path.join(home, ".openclaw", "agent")  (line 178)
  After:  path.join(home, ".slide", "agent")
  Note: PI_CODING_AGENT_DIR env var name does NOT change (no "OPENCLAW" in name),
        but its VALUE (.openclaw → .slide) must change per D-07.

reply.directive.directive-behavior.e2e-harness.ts:
  Before: PI_CODING_AGENT_DIR: (home) => path.join(home, ".openclaw", "agent")  (line 136)
  After:  PI_CODING_AGENT_DIR: (home) => path.join(home, ".slide", "agent")

reply.triggers.trigger-handling.test-harness.ts:
  Before: process.env.OPENCLAW_STATE_DIR = join(home, ".openclaw");  (line 227)
  After:  process.env.SLIDE_STATE_DIR = join(home, ".slide");

status.ts line 259 (comment — note this file is also touched by Plan 03):
  Before: // Transcripts are stored at the session file path (fallback: ~/.openclaw/sessions/<SessionId>.jsonl)
  After:  // Transcripts are stored at the session file path (fallback: ~/.slide/sessions/<SessionId>.jsonl)
  IMPORTANT: This is a comment-only change. The user-visible text on line 832
             will be changed in Plan 03. Only change line 259 in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all OPENCLAW_* env vars, UI base path, and HTTP User-Agent</name>
  <read_first>
    (See <interfaces> block above for all exact before/after patterns)
    frontend/src/app/src/branding.ts (verify constants before editing)
  </read_first>
  <files>
    frontend/src/app/src/media/image-ops.ts
    frontend/src/app/src/auto-reply/reply/get-reply-run.ts
    frontend/src/app/src/auto-reply/reply/test-helpers.ts
    frontend/src/app/src/auto-reply/reply/session-updates.ts
    frontend/src/app/src/auto-reply/reply/model-selection.ts
    frontend/src/app/src/auto-reply/reply/get-reply-directives.ts
    frontend/src/app/src/auto-reply/reply/get-reply.ts
    frontend/src/app/src/auto-reply/reply/session.ts
    frontend/src/app/src/auto-reply/reply/get-reply-fast-path.ts
    frontend/src/app/ui/app-settings.ts
    frontend/src/app/ui/app.ts
    frontend/src/app/ui/storage.ts
    frontend/src/app/src/media/input-files.ts
     # Test harness env var references
    frontend/src/app/src/auto-reply/reply.test-harness.ts
    frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.test-harness.ts
    frontend/src/app/src/auto-reply/reply.directive.directive-behavior.e2e-harness.ts
  </files>
  <action>
    Read `frontend/src/app/src/branding.ts` first to confirm constants (ENV_PREFIX="SLIDE", PRODUCT_NAME="Slide"), then perform all replacements below.

    **Env var replacements (prefix substitution):**
    All occurrences of `process.env.OPENCLAW_` (or `env.OPENCLAW_` for get-reply-fast-path.ts which destructures `env`) → change to `process.env.SLIDE_` / `env.SLIDE_`:

    1. `image-ops.ts`: `OPENCLAW_IMAGE_BACKEND` → `SLIDE_IMAGE_BACKEND`
    2. `get-reply-run.ts`: `OPENCLAW_TEST_FAST` → `SLIDE_TEST_FAST` (2 occurrences)
    3. `test-helpers.ts`: `OPENCLAW_TEST_FAST` → `SLIDE_TEST_FAST`
    4. `session-updates.ts`: `OPENCLAW_TEST_FAST` → `SLIDE_TEST_FAST`
    5. `model-selection.ts`: `OPENCLAW_DEBUG_INGRESS_TIMING` → `SLIDE_DEBUG_INGRESS_TIMING`
    6. `get-reply-directives.ts`: `OPENCLAW_TEST_FAST` → `SLIDE_TEST_FAST`
    7. `get-reply.ts`: `OPENCLAW_TEST_FAST` → `SLIDE_TEST_FAST`
    8. `session.ts`: `OPENCLAW_DEBUG_INGRESS_TIMING` → `SLIDE_DEBUG_INGRESS_TIMING`
    9. `get-reply-fast-path.ts`: 
       - `env.OPENCLAW_ALLOW_SLOW_REPLY_TESTS` → `env.SLIDE_ALLOW_SLOW_REPLY_TESTS`
       - `env.OPENCLAW_STRICT_FAST_REPLY_CONFIG` → `env.SLIDE_STRICT_FAST_REPLY_CONFIG`
       - String literal `"OPENCLAW_ALLOW_SLOW_REPLY_TESTS=1"` → `"SLIDE_ALLOW_SLOW_REPLY_TESTS=1"`
    10. `reply.test-harness.ts`:
        - Type field `OPENCLAW_STATE_DIR` → `SLIDE_STATE_DIR`
        - Type field `OPENCLAW_AGENT_DIR` → `SLIDE_AGENT_DIR`
        - `process.env.OPENCLAW_STATE_DIR` → `process.env.SLIDE_STATE_DIR` (2 places)
        - `process.env.OPENCLAW_AGENT_DIR` → `process.env.SLIDE_AGENT_DIR` (2 places)
    11. `reply.triggers.trigger-handling.test-harness.ts`:
        - `process.env.OPENCLAW_HOME` → `process.env.SLIDE_HOME` (3 places)
        - `process.env.OPENCLAW_STATE_DIR` → `process.env.SLIDE_STATE_DIR` (3 places)
    12. `reply.directive.directive-behavior.e2e-harness.ts`:
        - `OPENCLAW_AGENT_DIR` → `SLIDE_AGENT_DIR`

    **UI base path (D-06):**
    Replace `__OPENCLAW_CONTROL_UI_BASE_PATH__` → `__SLIDE_CONTROL_UI_BASE_PATH__` in:
    - `app-settings.ts` (1 occurrence)
    - `app.ts` (1 occurrence, type declaration)
    - `storage.ts` (1 occurrence)

    **HTTP User-Agent:**
    `input-files.ts`: Replace `"User-Agent": "OpenClaw-Gateway/1.0"` → `"User-Agent": "Slide-Gateway/1.0"`

    After all changes, run verification:
    ```bash
    grep -rn 'OPENCLAW_\|__OPENCLAW_\|OpenClaw-Gateway' frontend/src/ --include='*.ts' | grep -v node_modules | grep -v '\.d\.ts'
    ```
    The only remaining OPENCLAW_ hits should be in:
    - `update-startup.ts` (already deleted in Plan 01) — should not exist
    - If any remain in files NOT listed in this task, note them — they may need the same substitution pattern
  </action>
  <verify>
    <automated>
grep -rn 'OPENCLAW_' frontend/src/ --include='*.ts' | grep -v node_modules | grep -v '\.d\.ts' | wc -l | xargs test 0 -eq
    </automated>
    (Note: after this task, OPENCLAW_ references in test assertion strings like expect(text).toContain("OpenClaw") remain — those are handled in Plan 04.)
  </verify>
  <acceptance_criteria>
    - Zero `OPENCLAW_` env var patterns remain in source code (non-test-assertion)
    - Zero `__OPENCLAW_CONTROL_UI_BASE_PATH__` remain
    - HTTP User-Agent is "Slide-Gateway/1.0"
    - Files compile without reference errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace Symbol.for keys, .openclaw data dir paths, and plugin format</name>
  <read_first>
    (See <interfaces> block above for exact before/after for each file)
    frontend/src/app/src/branding.ts (verify buildSymbolKey, STATE_DIR)
  </read_first>
  <files>
    frontend/src/app/src/auto-reply/reply/inbound-dedupe.ts
    frontend/src/app/src/auto-reply/reply/reply-run-registry.ts
    frontend/src/app/src/auto-reply/reply/get-reply-fast-path.ts  (Symbol keys only — env vars already done in Task 1)
    frontend/src/app/src/auto-reply/reply/queue/state.ts
    frontend/src/app/src/auto-reply/reply/queue/enqueue.ts
    frontend/src/app/src/auto-reply/reply/queue/drain.ts
    frontend/src/app/src/auto-reply/reply/commands-plugins.ts
    frontend/src/app/src/auto-reply/reply/stage-sandbox-media.ts  (.openclaw comment)
    frontend/src/app/src/auto-reply/status.ts  (.openclaw comment line 259 ONLY)
    frontend/src/app/src/auto-reply/reply.test-harness.ts  (.openclaw paths)
    frontend/src/app/src/auto-reply/reply.directive.directive-behavior.e2e-harness.ts  (.openclaw path)
    frontend/src/app/src/auto-reply/reply.triggers.trigger-handling.test-harness.ts  (.openclaw path)
  </files>
  <action>
    Perform three groups of replacements:

    **A) Symbol.for keys (D-11):**
    Replace all `Symbol.for("openclaw.XXX")` → `Symbol.for("slide.XXX")`:

    1. `inbound-dedupe.ts`: `"openclaw.inboundDedupeCache"` → `"slide.inboundDedupeCache"`, `"openclaw.inboundDedupeInflight"` → `"slide.inboundDedupeInflight"`
    2. `reply-run-registry.ts`: `"openclaw.replyRunRegistry"` → `"slide.replyRunRegistry"`
    3. `get-reply-fast-path.ts`: `"openclaw.reply.complete-config"` → `"slide.reply.complete-config"`, `"openclaw.reply.full-runtime"` → `"slide.reply.full-runtime"`
    4. `queue/state.ts`: `"openclaw.followupQueues"` → `"slide.followupQueues"`
    5. `queue/enqueue.ts`: `"openclaw.recentQueueMessageIds"` → `"slide.recentQueueMessageIds"`
    6. `queue/drain.ts`: `"openclaw.followupDrainCallbacks"` → `"slide.followupDrainCallbacks"`

    **B) Plugin format (D-14):**
    7. `commands-plugins.ts`: Replace `plugin.format ?? "openclaw"` with `plugin.format ?? "slide"` (2 occurrences on lines 123-124).

    **C) Data directory paths (D-07):**
    Replace `".openclaw"` → `".slide"` in path.join() calls and comments.
    Do NOT replace `"openclaw"` as a general substring — only `".openclaw"` (with leading dot):

    8. `stage-sandbox-media.ts` line 41: Replace comment text `~/.openclaw/media` → `~/.slide/media`
    9. `status.ts` line 259: Replace comment text `~/.openclaw/sessions/` → `~/.slide/sessions/`
    10. `reply.test-harness.ts`:
        - Line 172: `path.join(home, ".openclaw", "agents", "main", "sessions")` → `path.join(home, ".slide", "agents", "main", "sessions")`
        - Line 178: `path.join(home, ".openclaw", "agent")` → `path.join(home, ".slide", "agent")` (this is the value for PI_CODING_AGENT_DIR; the env var name stays as `PI_CODING_AGENT_DIR`)
    11. `e2e-harness.ts`: `path.join(home, ".openclaw", "agent")` → `path.join(home, ".slide", "agent")`
    12. `reply.triggers.trigger-handling.test-harness.ts`:
        - Line 227: `join(home, ".openclaw")` → `join(home, ".slide")`
        - Line 260: `join(home, ".openclaw", "agents", "main", "sessions")` → `join(home, ".slide", "agents", "main", "sessions")`

    After all changes, verify:
    ```bash
    # No remaining Symbol keys with openclaw prefix
    grep -rn 'Symbol\.for.*openclaw' frontend/src/ --include='*.ts' | grep -v node_modules | wc -l
    # Should be 0
    ```
    ```bash
    # No remaining plugin.format with "openclaw"
    grep -rn 'format.*"openclaw"\|"openclaw"' frontend/src/app/src/auto-reply/reply/commands-plugins.ts
    # Should have 0 matches
    ```
    ```bash
    # No remaining .openclaw path refs (excluding update-startup.ts which is deleted)
    grep -rn '\.openclaw' frontend/src/ --include='*.ts' | grep -v node_modules | grep -v update-startup | grep -v 'Symbol\.for\|OPENCLAW_'
    # Should be 0 (if any remain, they need the same .openclaw → .slide replacement)
    ```
  </action>
  <verify>
    <automated>
! grep -q 'Symbol\.for.*openclaw' frontend/src/ --include='*.ts' 2>/dev/null && ! grep -q 'format.*"openclaw"' frontend/src/app/src/auto-reply/reply/commands-plugins.ts && ! grep -q '\.openclaw' frontend/src/ --include='*.ts' 2>/dev/null | grep -v node_modules | grep -v update-startup | grep -v 'OPENCLAW_' | grep -q .
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero `Symbol.for("openclaw.*")` occurrences remaining
    - Zero `.openclaw` path references remaining in source files
    - `plugin.format ?? "slide"` appears 2 times in commands-plugins.ts
    - grep confirmations pass with 0 matches for each pattern
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → process.env | Untrusted input from the OS environment; renaming prefix has no security impact |
| Symbol.for global key | Cross-module runtime key; renaming is safe as long as all 8 keys change atomically (handled in Task 2) |
| HTTP User-Agent | Outbound request header; changing from "OpenClaw-Gateway/1.0" to "Slide-Gateway/1.0" is cosmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03 | Spoofing | Symbol.for keys | mitigate | All 8 Symbol.for("openclaw.*") keys replaced atomically in Task 2 to prevent silent cross-module deduplication breakage |
| T-116-04 | Tampering | env var name change | mitigate | Exact string search-and-replace verified by grep; no semantic change since env vars reference the same configuration layer |
| T-116-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this plan |
</threat_model>

<verification>
- `grep -rn 'OPENCLAW_' frontend/src/ --include='*.ts' | grep -v node_modules | wc -l` = 0 (except test assertion strings handled in Plan 04)
- `grep -rn 'Symbol\.for.*openclaw' frontend/src/ --include='*.ts' | grep -v node_modules | wc -l` = 0
- `grep -rn '\.openclaw' frontend/src/ --include='*.ts' | grep -v node_modules | grep -v 'update-startup' | grep -v 'OPENCLAW_' ` = 0
- `grep -rn '__OPENCLAW_' frontend/src/ --include='*.ts' | grep -v node_modules | wc -l` = 0
- `grep -rn 'OpenClaw-Gateway' frontend/src/ --include='*.ts' | grep -v node_modules | wc -l` = 0
</verification>

<success_criteria>
1. All OPENCLAW_ env vars in source code replaced with SLIDE_
2. All __OPENCLAW_CONTROL_UI_BASE_PATH__ replaced with __SLIDE_CONTROL_UI_BASE_PATH__
3. All Symbol.for("openclaw.*") replaced with Symbol.for("slide.*")
4. All ~/.openclaw/ paths replaced with ~/.slide/
5. Plugin format fallback changed to "slide"
6. HTTP User-Agent changed to "Slide-Gateway/1.0"
</success_criteria>

<output>
Create `.planning/phases/116-openclaw-cli/116-02-SUMMARY.md` when done
</output>
