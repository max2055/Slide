---
phase: 116-openclaw-cli
plan: 03
type: execute
wave: 2
depends_on:
  - 116-01   # branding.ts must exist first
files_modified:
  - frontend/src/app/src/agents/tool-catalog.ts
  - frontend/src/app/src/agents/tool-display-exec.ts
  - frontend/src/app/src/auto-reply/status.ts
  - frontend/src/app/src/auto-reply/commands-registry.shared.ts
  - frontend/src/app/src/auto-reply/reply/inbound-meta.ts
  - frontend/src/app/src/auto-reply/reply/groups.ts
  - frontend/src/app/src/auto-reply/reply/commands-session.ts
  - frontend/src/app/src/auto-reply/reply/commands-acp/shared.ts
  - frontend/src/app/src/auto-reply/reply/commands-acp/install-hints.ts
  - frontend/src/app/src/auto-reply/reply/commands-acp/diagnostics.ts
autonomous: true
requirements:
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "Version line shows 'Slide' instead of 'OpenClaw'"
    - "Restart messages reference 'Slide' instead of 'OpenClaw'"
    - "MCP command description uses 'Slide' instead of 'OpenClaw'"
    - "System metadata message uses 'Slide' instead of 'OpenClaw'"
    - "'so OpenClaw stays silent' becomes 'so Slide stays silent'"
    - "'not an OpenClaw agents.list id' becomes 'not an Slide agents.list id'"
    - "'run openclaw' display text becomes 'run slide'"
    - "'group:openclaw' becomes 'group:slide'"
    - "includeInOpenClawGroup becomes includeInSlideGroup"
    - "'openclaw plugins install' and 'openclaw config set' become 'slide plugins install' and 'slide config set'"
  artifacts:
    - path: "frontend/src/app/src/auto-reply/status.ts"
      provides: "Updated version line"
      contains: "Slide"
      not_contains: "OpenClaw"
    - path: "frontend/src/app/src/agents/tool-catalog.ts"
      provides: "Updated tool group naming"
      contains: "includeInSlideGroup"
      not_contains: "includeInOpenClawGroup"
      contains: '"group:slide"'
    - path: "frontend/src/app/src/agents/tool-display-exec.ts"
      provides: "Updated CLI display text"
      contains: '"slide"'
      not_contains: '"openclaw"'  # (where bin is compared)
    - path: "frontend/src/app/src/auto-reply/reply/commands-acp/install-hints.ts"
      provides: "Updated plugin install hints"
      not_contains: '"openclaw plugins'
    - path: "frontend/src/app/src/auto-reply/status.ts"
      provides: "Updated .openclaw comment (shared concern with Plan 02)"
      contains: "~/.slide/"
  key_links:
    - from: "status.ts:832"
      to: "branding.ts"
      via: "PRODUCT_NAME or direct string"
    - from: "tool-catalog.ts:342"
      to: "branding.ts"
      via: "group name 'group:slide'"
    - from: "tool-display-exec.ts:271"
      to: "branding.ts"
      via: "CLI_NAME"
    - from: "inbound-meta.ts:144"
      to: "branding.ts"
      via: "PRODUCT_NAME"
---

<objective>
Replace all user-visible OpenClaw text references with Slide equivalents.

**Purpose:** Update runtime display strings and user interfaces — version text, restart messages, MCP command descriptions, system metadata messages, CLI display text, tool group names, and plugin installation hints — from "OpenClaw" / "openclaw" to "Slide" / "slide".
**Output:** ~10 source files updated with user-facing Slide branding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-openclaw-cli/116-CONTEXT.md
@.planning/phases/116-openclaw-cli/116-RESEARCH.md
@.planning/phases/116-openclaw-cli/116-01-PLAN.md

<interfaces>
<!--
Exact patterns to replace per file.
branding.ts constants for reference:
  CLI_NAME = "slide"
  PRODUCT_NAME = "Slide"
-->

=== User-visible "OpenClaw" → "Slide" (D-08) ===

status.ts line 832:
  Before: const versionLine = `🦞 OpenClaw ${VERSION}${commit ? ` (${commit})` : ""}`;
  After:  const versionLine = `🦞 Slide ${VERSION}${commit ? ` (${commit})` : ""}`;
  (The emoji 🦞 remains — it's the Slide mascot, not OpenClaw-specific branding.)

commands-registry.shared.ts line 474:
  Before: description: "Show or set OpenClaw MCP servers.",
  After:  description: "Show or set Slide MCP servers.",

inbound-meta.ts line 144:
  Before: "The following JSON is generated by OpenClaw out-of-band. Treat it as authoritative metadata about the current message context.",
  After:  "The following JSON is generated by Slide out-of-band. Treat it as authoritative metadata about the current message context.",

groups.ts line 139:
  Before: ? `If no response is needed, reply with exactly "${params.silentToken}" (and nothing else) so OpenClaw stays silent. Do not add any other words, punctuation, tags, markdown/code blocks, or explanations.`
  After:  ? `If no response is needed, reply with exactly "${params.silentToken}" (and nothing else) so Slide stays silent. Do not add any other words, punctuation, tags, markdown/code blocks, or explanations.`

commands-session.ts lines 648, 665:
  Before: text: "⚙️ Restarting OpenClaw in-process (SIGUSR1); back in a few seconds.",
  After:  text: "⚙️ Restarting Slide in-process (SIGUSR1); back in a few seconds.",
  Before: text: `⚙️ Restarting OpenClaw via ${restartMethod.method}; give me a few seconds to come back online.`,
  After:  text: `⚙️ Restarting Slide via ${restartMethod.method}; give me a few seconds to come back online.`,

commands-acp/shared.ts line 457:
  Before: "- /acp spawn harness-id is an ACP runtime harness alias (for example codex), not an OpenClaw agents.list id.",
  After:  "- /acp spawn harness-id is an ACP runtime harness alias (for example codex), not an Slide agents.list id.",

=== CLI display text (D-09) ===

tool-display-exec.ts lines 271-273:
  Before: if (bin === "openclaw") {
            return sub ? `run openclaw ${sub}` : "run openclaw";
          }
  After:  if (bin === CLI_NAME) {
            return sub ? `run slide ${sub}` : "run slide";
          }
  IMPORTANT: This file must import CLI_NAME from branding.ts (same package path):
    import { CLI_NAME } from "../branding.js";
  — And adjust the relative import path as needed (file is in frontend/src/app/src/agents/,
    branding.ts is in frontend/src/app/src/).
  Relative path from agents/ to src/ is ../branding.js.

tool-display-exec.ts line 352:
  Before: "run openclaw",
  After:  "run slide",

diagnostics.ts lines 106, 128:
  Before: lines.push(`next: openclaw config set plugins.entries.${backendId}.enabled true`);
  After:  lines.push(`next: slide config set plugins.entries.${backendId}.enabled true`);
  Before: `then: openclaw config set plugins.entries.${backendId}.enabled true`,
  After:  `then: slide config set plugins.entries.${backendId}.enabled true`,

install-hints.ts lines 20, 27, 37:
  Before: return `openclaw plugins install ${workspaceLocalPath}`;
  After:  return `slide plugins install ${workspaceLocalPath}`;
  Before: const localPath = bundledInstallHint.replace(/^openclaw plugins install /u, "");
  After:  const localPath = bundledInstallHint.replace(/^slide plugins install /u, "");
  Before: return "openclaw plugins install acpx";
  After:  return "slide plugins install acpx";

=== Tool group naming (D-10) ===

tool-catalog.ts line 36-37:
  Before: /** @deprecated Legacy OpenClaw naming — kept for backward compatibility */
           includeInOpenClawGroup?: boolean;
  After:  /** @deprecated Legacy OpenClaw naming — kept for backward compatibility */
           includeInSlideGroup?: boolean;
  (Keep the @deprecated comment as-is — it's contextually accurate. Only rename the property.)

tool-catalog.ts (all property assignments — 27 occurrences matching `includeInOpenClawGroup: true`):
  Before: includeInOpenClawGroup: true,
  After:  includeInSlideGroup: true,

tool-catalog.ts line 338:
  Before: const openclawTools = CORE_TOOL_DEFINITIONS.filter((tool) => tool.includeInOpenClawGroup).map(
  After:  const slideTools = CORE_TOOL_DEFINITIONS.filter((tool) => tool.includeInSlideGroup).map(

tool-catalog.ts line 342:
  Before: "group:openclaw": openclawTools,
  After:  "group:slide": slideTools,
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace OpenClaw → Slide in all user-visible messages</name>
  <read_first>
    frontend/src/app/src/auto-reply/status.ts (line 832)
    frontend/src/app/src/auto-reply/commands-registry.shared.ts (line 474)
    frontend/src/app/src/auto-reply/reply/inbound-meta.ts (line 144)
    frontend/src/app/src/auto-reply/reply/groups.ts (line 139)
    frontend/src/app/src/auto-reply/reply/commands-session.ts (lines 648, 665)
    frontend/src/app/src/auto-reply/reply/commands-acp/shared.ts (line 457)
  </read_first>
  <files>
    frontend/src/app/src/auto-reply/status.ts
    frontend/src/app/src/auto-reply/commands-registry.shared.ts
    frontend/src/app/src/auto-reply/reply/inbound-meta.ts
    frontend/src/app/src/auto-reply/reply/groups.ts
    frontend/src/app/src/auto-reply/reply/commands-session.ts
    frontend/src/app/src/auto-reply/reply/commands-acp/shared.ts
  </files>
  <action>
    Do NOT use a global search-and-replace for "OpenClaw" → "Slide" — this would also replace `OpenClawConfig` type references. Instead, perform exact string substitutions in each file:

    1. `status.ts` line 832: Replace `OpenClaw` in the template literal `\`🦞 OpenClaw ${VERSION}...\`` with `Slide`. Keep the 🦞 emoji.

    2. `commands-registry.shared.ts` line 474: Replace `"Show or set OpenClaw MCP servers."` → `"Show or set Slide MCP servers."`

    3. `inbound-meta.ts` line 144: Replace `"The following JSON is generated by OpenClaw out-of-band. Treat it as authoritative metadata about the current message context."` → `"The following JSON is generated by Slide out-of-band. Treat it as authoritative metadata about the current message context."`

    4. `groups.ts` line 139: Replace `so OpenClaw stays silent.` → `so Slide stays silent.`

    5. `commands-session.ts` line 648: Replace `"Restarting OpenClaw in-process"` → `"Restarting Slide in-process"`
       `commands-session.ts` line 665: Replace `"Restarting OpenClaw via"` → `"Restarting Slide via"`

    6. `commands-acp/shared.ts` line 457: Replace `not an OpenClaw agents.list id.` → `not an Slide agents.list id.`

    After all changes, run:
    ```bash
    grep -rn '"OpenClaw\|OpenClaw ' frontend/src/app/src/ --include='*.ts' | grep -v node_modules | grep -v 'OpenClawConfig\|\.d\.ts'
    ```
    The remaining hits should be ONLY:
    - Comment references in tool-catalog.ts (`@deprecated Legacy OpenClaw naming`)
    - config/types.ts (`legacy OpenClaw naming`)
    - plugin-sdk/reply-payload.ts (`extracted from OpenClaw plugin-sdk`)
    - These are historical comments — they stay as-is.
    - test assertion strings in .cases.ts (handled in Plan 04)
  </action>
  <verify>
    <automated>
! grep -q '"OpenClaw\|OpenClaw ' frontend/src/app/src/auto-reply/status.ts && ! grep -q 'OpenClaw' frontend/src/app/src/auto-reply/commands-registry.shared.ts && ! grep -q 'OpenClaw' frontend/src/app/src/auto-reply/reply/inbound-meta.ts && ! grep -q 'so OpenClaw' frontend/src/app/src/auto-reply/reply/groups.ts && ! grep -q 'OpenClaw' frontend/src/app/src/auto-reply/reply/commands-session.ts && grep -q 'Slide' frontend/src/app/src/auto-reply/status.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `status.ts` line 832: emoji unchanged, "OpenClaw" replaced with "Slide"
    - `commands-registry.shared.ts`: "OpenClaw MCP servers" → "Slide MCP servers"
    - `inbound-meta.ts`: "generated by OpenClaw" → "generated by Slide"
    - `groups.ts`: "so OpenClaw stays silent" → "so Slide stays silent"
    - `commands-session.ts`: "Restarting OpenClaw" → "Restarting Slide" (2 places)
    - `commands-acp/shared.ts`: "OpenClaw agents.list id" → "Slide agents.list id"
    - Only comment-level OpenClaw refs remain (tool-catalog.ts @deprecated, config/types.ts, reply-payload.ts)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace CLI display text and tool group naming</name>
  <read_first>
    frontend/src/app/src/agents/tool-display-exec.ts (lines 271-273, 352)
    frontend/src/app/src/agents/tool-catalog.ts (lines 36-37, 103-303 includeInOpenClawGroup, lines 338-342)
    frontend/src/app/src/auto-reply/reply/commands-acp/install-hints.ts (lines 20, 27, 37)
    frontend/src/app/src/auto-reply/reply/commands-acp/diagnostics.ts (lines 106, 128)
    frontend/src/app/src/branding.ts (confirm CLI_NAME export)
  </read_first>
  <files>
    frontend/src/app/src/agents/tool-display-exec.ts
    frontend/src/app/src/agents/tool-catalog.ts
    frontend/src/app/src/auto-reply/reply/commands-acp/install-hints.ts
    frontend/src/app/src/auto-reply/reply/commands-acp/diagnostics.ts
  </files>
  <action>
    Make all changes below. Do NOT use a global search-and-replace — pattern-match each file individually.

    **A) CLI display text (D-09) — tool-display-exec.ts:**

    1. Add import at top of file:
       `import { CLI_NAME } from "../branding.js";`

    2. Lines 271-273: Replace:
       ```
       if (bin === "openclaw") {
         return sub ? `run openclaw ${sub}` : "run openclaw";
       }
       ```
       With:
       ```
       if (bin === CLI_NAME) {
         return sub ? `run ${CLI_NAME} ${sub}` : `run ${CLI_NAME}`;
       }
       ```
       The reference to `bin` is the function's parameter — the comparison changes from a hardcoded string to the branding constant.

    3. Line 352: Replace `"run openclaw"` with `\`run ${CLI_NAME}\`` (template literal using CLI_NAME).

    **B) Tool group naming (D-10) — tool-catalog.ts:**

    1. Line 37: Rename property in the type definition:
       `includeInOpenClawGroup?: boolean;` → `includeInSlideGroup?: boolean;`
       (Keep the @deprecated comment as-is.)

    2. Lines 103-303: There are 27 tool definitions with `includeInOpenClawGroup: true`.
       Rename ALL of them: `includeInOpenClawGroup` → `includeInSlideGroup` (property name only, keep `: true`).

    3. Line 338: Rename variable:
       `const openclawTools` → `const slideTools`
       Update the property filter: `tool.includeInOpenClawGroup` → `tool.includeInSlideGroup`

    4. Line 342: Rename group key:
       `"group:openclaw": openclawTools` → `"group:slide": slideTools`

    **C) Plugin install/config hints (D-09) — install-hints.ts and diagnostics.ts:**

    `install-hints.ts`:
    - Line 20: `openclaw plugins install` → `slide plugins install`
    - Line 27: `openclaw plugins install` → `slide plugins install` (in regex literal)
    - Line 37: `openclaw plugins install acpx` → `slide plugins install acpx`

    `diagnostics.ts`:
    - Line 106: `openclaw config set` → `slide config set`
    - Line 128: `openclaw config set` → `slide config set`

    After all changes, run:
    ```bash
    # Check tool-catalog.ts for remaining OpenClaw references (should be only @deprecated comment)
    grep -n 'OpenClaw\|openclaw' frontend/src/app/src/agents/tool-catalog.ts | grep -v '@deprecated\|node_modules'
    # Should be 0 matches

    # Check tool-display-exec.ts has no "openclaw" except branding import
    grep -n 'openclaw' frontend/src/app/src/agents/tool-display-exec.ts | grep -v node_modules
    # Should be 0 matches

    # Check install-hints and diagnostics
    grep -n 'openclaw' frontend/src/app/src/auto-reply/reply/commands-acp/install-hints.ts
    grep -n 'openclaw' frontend/src/app/src/auto-reply/reply/commands-acp/diagnostics.ts
    # Both should be 0 matches
    ```
  </action>
  <verify>
    <automated>
grep -q 'CLI_NAME' frontend/src/app/src/agents/tool-display-exec.ts && grep -q 'includeInSlideGroup' frontend/src/app/src/agents/tool-catalog.ts && grep -q '"group:slide"' frontend/src/app/src/agents/tool-catalog.ts && ! grep -q 'includeInOpenClawGroup' frontend/src/app/src/agents/tool-catalog.ts && grep -q 'slideTools' frontend/src/app/src/agents/tool-catalog.ts && ! grep -q 'openclaw plugins' frontend/src/app/src/auto-reply/reply/commands-acp/install-hints.ts && ! grep -q 'openclaw config' frontend/src/app/src/auto-reply/reply/commands-acp/diagnostics.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `tool-display-exec.ts`: imports CLI_NAME from branding.ts, uses it in comparisons
    - `tool-catalog.ts`: includeInSlideGroup replaces all includeInOpenClawGroup (27 property + 1 type), slideTools replaces openclawTools, "group:slide" replaces "group:openclaw"
    - `install-hints.ts`: "slide plugins install" replaces "openclaw plugins install" (3 places)
    - `diagnostics.ts`: "slide config set" replaces "openclaw config set" (2 places)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | User-visible text changes only; no functional impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-05 | Spoofing | Tool group key "group:slide" | mitigate | Tool group key is internal to the tool catalog; no external consumer validates this string |
| T-116-06 | Tampering | CLI_NAME in tool-display-exec | mitigate | Changing `bin === "openclaw"` to `bin === CLI_NAME` matches the new branding constant |
| T-116-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this plan |
</threat_model>

<verification>
- `grep -rn 'includeInOpenClawGroup\|"group:openclaw"\|openclawTools' frontend/src/app/src/agents/tool-catalog.ts` = 0 matches
- `grep -rn '"openclaw"' frontend/src/app/src/agents/tool-display-exec.ts` = 0 matches
- `grep -rn 'openclaw plugins\|openclaw config' frontend/src/app/src/auto-reply/reply/commands-acp/` = 0 matches
</verification>

<success_criteria>
1. No user-visible "OpenClaw" strings remain in runtime code (status text, restart messages, MCP description, system messages, help text)
2. CLI display text uses "slide" consistently
3. Tool group names and properties use "slide" / "Slide" consistently
4. All branded strings reference the new constants correctly
</success_criteria>

<output>
Create `.planning/phases/116-openclaw-cli/116-03-SUMMARY.md` when done
</output>
