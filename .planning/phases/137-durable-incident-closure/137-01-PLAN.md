---
phase: 137-durable-incident-closure
plan: 01
type: execute
wave: 1
depends_on: [133-02, 135-01, 136-03]
files_modified:
  - apps/db-ops-api/sql/migrations/031_outbox_jobs.sql
  - apps/db-ops-api/src/workflows/outbox-service.ts
  - apps/db-ops-api/src/workflows/worker-runtime.ts
  - apps/db-ops-api/src/workflows/job-registry.ts
  - apps/db-ops-api/src/cron/cron-manager.ts
  - apps/db-ops-api/src/cron/cron-executor.ts
  - apps/db-ops-api/src/workflows/worker-runtime.test.ts
autonomous: true
requirements: [DR-01, DR-02, HI-12]
must_haves:
  truths:
    - 业务状态和 outbox event 在同一 MySQL 事务提交，投递为 at-least-once 且 consumer 幂等
    - worker 通过持久 lease/heartbeat/fencing token 认领，崩溃后可接管
    - 容量、清理、报表、告警评估和通知使用类型化 handler，自由文本 LLM 不承担确定性调度
---

# Phase 137 Plan 01：Transactional Outbox 与持久 Worker Runtime

<objective>
建立不依赖单进程内存的工作流底座，确保调度、重试、接管和死信可追踪且不会静默丢失。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-durable-incident-closure/137-CONTEXT.md
@apps/db-ops-api/src/cron/cron-manager.ts
@apps/db-ops-api/src/cron/cron-executor.ts
@apps/db-ops-api/src/notification-service.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 创建 outbox、job、attempt 和 dead-letter schema</name>
  <files>apps/db-ops-api/sql/migrations/031_outbox_jobs.sql, apps/db-ops-api/src/workflows/outbox-service.ts, apps/db-ops-api/src/workflows/worker-runtime.test.ts</files>
  <action>
    新增 outbox_events、workflow_jobs、workflow_attempts/dead_letter 所需字段：event/job id、type/schema version、aggregate ref/version、payload、idempotency key、availableAt、state、attempt、lease owner/expiry/fencing、last error、created/finished。提供事务 helper，调用者在业务写事务内 append outbox。payload 受 schema/大小限制并脱敏。consumer 以 event id/业务 idempotency key 唯一去重；不承诺 exactly-once 外部投递。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/workflows/worker-runtime.test.ts -t "outbox|transaction|dedupe"</automated></verify>
  <done>事务 rollback 时无孤立事件，重复 delivery 只产生一次 job/业务状态变更。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 lease、heartbeat、重试和接管</name>
  <files>apps/db-ops-api/src/workflows/worker-runtime.ts, apps/db-ops-api/src/workflows/outbox-service.ts, apps/db-ops-api/src/workflows/worker-runtime.test.ts</files>
  <action>
    worker 使用数据库时间和 CAS claim，运行时带 fencing token；heartbeat 延长 lease，完成/失败时 token 必须仍有效。按错误分类配置指数退避、最大 attempt 和 dead letter；进程崩溃后 lease 过期可接管。取消和 shutdown 与 Operation 协调。运行指标输出 queued/running/retry/dead-letter/oldest age，不用 in-memory Map 作权威锁。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/workflows/worker-runtime.test.ts -t "lease|heartbeat|takeover|dead letter"</automated></verify>
  <done>kill -9/过期 lease fixture 被第二 worker 接管，旧 fencing owner 不能提交终态。</done>
</task>

<task type="auto">
  <name>Task 3: 将确定性 Cron 映射为类型化 Job handler</name>
  <files>apps/db-ops-api/src/workflows/job-registry.ts, apps/db-ops-api/src/cron/cron-manager.ts, apps/db-ops-api/src/cron/cron-executor.ts, apps/db-ops-api/src/workflows/worker-runtime.test.ts</files>
  <action>
    建立 handler registry 和版本化 input schema。容量、清理、报表、告警评估、通知等任务直接调用服务；LLM 只用于 RCA/摘要并使用 Phase 135 的 AgentRun。CronManager 只计算到期并 enqueue，不能用自由文本描述让模型决定是否执行确定任务。相同 schedule occurrence 具有唯一 key，错过窗口按配置 catch-up/skip 并记录原因。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/workflows/worker-runtime.test.ts -t "typed handler|schedule occurrence"</automated></verify>
  <done>种子报表/清理等任务可定位到具体 handler；重复 tick 不重复 enqueue，同一任务失败可重试/死信。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 重复投递产生重复副作用 | consumer idempotency + Operation key |
| 旧 worker lease 后写入 | fencing token |
| payload 注入 handler | per-type schema validation |
| 永久失败无限重试 | bounded attempts + dead letter + alert |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run src/workflows/worker-runtime.test.ts`
- 两进程、kill -9、DB 短断、重复事件和 poison message 故障注入。
</verification>

<success_criteria>
1. DR-01/DR-02 的内存锁和自由文本调度问题关闭。
2. workflow health 可供 Phase 136 四维健康消费。
3. 输出 `.planning/phases/137-durable-incident-closure/137-01-SUMMARY.md`。
</success_criteria>

