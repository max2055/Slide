---
phase: 137-durable-incident-closure
plan: 03
type: execute
wave: 3
depends_on: [137-02, 132-02]
files_modified:
  - apps/db-ops-api/sql/migrations/033_workflow_closure.sql
  - apps/db-ops-api/src/report-config-database-service.ts
  - apps/db-ops-api/src/report-service.ts
  - apps/db-ops-api/src/server-report-service.ts
  - apps/db-ops-api/src/notification-service.ts
  - apps/db-ops-api/src/incidents/incident-service.ts
  - frontend/src/app/ui/views/reports.ts
  - frontend/src/app/ui/views/event-management.ts
  - frontend/src/app/ui/views/alerts.ts
  - frontend/e2e/durable-closure.spec.ts
  - apps/db-ops-api/tests/durable-closure.test.ts
autonomous: true
requirements: [HI-09, HI-11, ME-07]
must_haves:
  truths:
    - 报表配置创建后可到期生成、持久化、下载并按配置通知，server 范围严格按输入过滤
    - 通知具备 outbox、重试、dead letter、脱敏审计和人工重放
    - 事件只有恢复验证成功后可关闭，只有 reviewed 且 provenance 完整才成为运营记忆候选
---

# Phase 137 Plan 03：报表、通知、事件 UI 与验证闭环

<objective>
完成审计中断裂的用户业务链：配置 -> 到期 -> 执行 -> 持久化 -> 通知 -> 反馈，以及告警 -> 调查 -> 操作 -> 验证 -> 复盘。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-durable-incident-closure/137-CONTEXT.md
@apps/db-ops-api/src/server-report-service.ts
@apps/db-ops-api/src/notification-service.ts
@frontend/src/app/ui/views/reports.ts
@frontend/src/app/ui/views/event-management.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: 打通类型化定时报表链路</name>
  <files>apps/db-ops-api/sql/migrations/033_workflow_closure.sql, apps/db-ops-api/src/report-config-database-service.ts, apps/db-ops-api/src/report-service.ts, apps/db-ops-api/src/server-report-service.ts, apps/db-ops-api/tests/durable-closure.test.ts</files>
  <action>
    report config 校验 target ResourceRefs、schedule/timezone、format、delivery channels 和 enabled/version。到期 occurrence enqueue 类型化 handler，生成报告并以 Operation/outbox 关联。`serverReport.generateReport(serverIds)` 必须在 query/渲染全程只使用输入集合；reports 明确 targetType/refs 并约束 instance/server 归属。重复 occurrence 返回原 report，失败保持可重试状态。覆盖创建、到期、生成、列表、下载和通知事件。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/durable-closure.test.ts -t "scheduled report"</automated></verify>
  <done>单服务器配置不会包含其他服务器，报告目标类型正确，重复 tick 不生成重复报告。</done>
</task>

<task type="auto">
  <name>Task 2: 启动安全、持久的通知投递</name>
  <files>apps/db-ops-api/src/notification-service.ts, apps/db-ops-api/src/workflows/job-registry.ts, apps/db-ops-api/tests/durable-closure.test.ts</files>
  <action>
    notification handler 消费 outbox，调用 Phase 132 outbound-policy 和只写 secret。每次 attempt 记录 channel id/version、template、target type、status、HTTP 分类、脱敏错误；retry/dead letter 遵循 Phase 137 runtime。人工重放要求权限、reason 和新 Operation，但沿用业务 idempotency 防重复投递。测试成功、429/5xx、永久 4xx、SSRF deny、进程崩溃和 dead-letter replay。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run tests/durable-closure.test.ts -t "notification"</automated></verify>
  <done>通知 worker 正常启动；失败可观测、可控重试且任何 API/日志不含 webhook secret。</done>
</task>

<task type="auto">
  <name>Task 3: 暴露报表/事件闭环 UI 与运营记忆门槛</name>
  <files>apps/db-ops-api/src/incidents/incident-service.ts, frontend/src/app/ui/views/reports.ts, frontend/src/app/ui/views/event-management.ts, frontend/src/app/ui/views/alerts.ts, frontend/e2e/durable-closure.spec.ts</files>
  <action>
    Reports 页面渲染已有配置 CRUD、下次运行、最近 Operation、下载和投递状态，失败可按 capability 重试。事件页面显示成员告警、RCA evidence、建议、审批/Operation、验证与状态时间线；close 按钮只有 verification passed 才可用。记忆候选只在 reviewed、验证成功、provenance 完整时由服务端产生，人工接受/拒绝可审计，不把未经验证 LLM 结论自动学习。
  </action>
  <verify><automated>pnpm --filter slide-frontend exec playwright test e2e/durable-closure.spec.ts</automated></verify>
  <done>两条浏览器用户路径刷新后保持持久状态，失败场景有可执行恢复动作。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| 重复报告/通知 | occurrence + business idempotency |
| 报表越界包含其他资源 | ResourceRef scope + query input enforcement |
| 未恢复事件被关闭 | verification gate in service state machine |
| 错误 AI 结论进入长期记忆 | reviewed + verified + human decision |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run tests/durable-closure.test.ts`
- `pnpm --filter slide-frontend exec playwright test e2e/durable-closure.spec.ts`
- 浏览器验证 UI、API、DB、刷新和失败恢复五层一致。
</verification>

<success_criteria>
1. HI-09、HI-11 完整关闭，不只补 UI 或 worker 单点。
2. 告警到事件关闭、报告到通知均形成可恢复业务闭环。
3. 输出 `.planning/phases/137-durable-incident-closure/137-03-SUMMARY.md`。
</success_criteria>

