---
phase: 137-durable-incident-closure
plan: 02
type: execute
wave: 2
depends_on: [137-01, 135-02, 136-03]
files_modified:
  - apps/db-ops-api/sql/migrations/032_incident_investigation.sql
  - apps/db-ops-api/src/incidents/incident-service.ts
  - apps/db-ops-api/src/incidents/investigation-package.ts
  - apps/db-ops-api/src/alert-event-service.ts
  - apps/db-ops-api/src/alert-rca-service.ts
  - apps/db-ops-api/src/ai-analysis-database-service.ts
  - apps/db-ops-api/server.ts
  - docs/integrations/data-loom-contract.md
  - apps/db-ops-api/src/incidents/incident-closure.test.ts
autonomous: true
requirements: [HI-08, ME-07, DR-02]
must_haves:
  truths:
    - 事件状态机、成员告警、分析、Operation、审批和验证拥有显式关系，不解析 cache_key 猜关联
    - 实例和服务器告警均能创建持久 RCA/AnalysisEnvelope
    - InvestigationPackage 是版本化只读投影，Data Loom 不直接读内部表或写 Slide 控制状态
---

# Phase 137 Plan 02：事件状态机、RCA 与 Data Loom 调查契约

<objective>
把告警聚合、调查、建议、执行和验证连接为可追溯事件，并为 Data Loom 提供稳定的只读调查包，而非耦合数据库内部结构。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-durable-incident-closure/137-CONTEXT.md
@apps/db-ops-api/src/alert-event-service.ts
@apps/db-ops-api/src/alert-rca-service.ts
@apps/db-ops-api/sql/migrations/024_data_loom_integration.sql
</context>

<tasks>
<task type="auto">
  <name>Task 1: 建立事件状态机与显式血缘</name>
  <files>apps/db-ops-api/sql/migrations/032_incident_investigation.sql, apps/db-ops-api/src/incidents/incident-service.ts, apps/db-ops-api/src/alert-event-service.ts, apps/db-ops-api/src/incidents/incident-closure.test.ts</files>
  <action>
    事件迁移固定 `open -> investigating -> mitigated -> verifying -> resolved -> closed -> reviewed` 与允许回退；状态事件记录 actor、reason、Operation 和时间。`alert_event_members` 保持事件-告警唯一事实源，不添加重复 source_id。增加显式 incident_analysis/incident_operation/verification relation；迁移 024 的 cross-table IDs 需审计并映射，不使用弱类型 ID 猜实体。每次转换 CAS + outbox，非法跳转拒绝。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/incidents/incident-closure.test.ts -t "state|member|lineage"</automated></verify>
  <done>事件从任一成员告警可追到分析/操作/验证；并发转换只有一条合法路径。</done>
</task>

<task type="auto">
  <name>Task 2: 统一实例/服务器 RCA 与 AnalysisEnvelope</name>
  <files>apps/db-ops-api/src/alert-rca-service.ts, apps/db-ops-api/src/ai-analysis-database-service.ts, apps/db-ops-api/src/incidents/incident-service.ts, apps/db-ops-api/src/incidents/incident-closure.test.ts</files>
  <action>
    RCA 输入为 ResourceRef + incident/member alerts + fresh Observation/evidence；ai_analysis 通过 Phase 135/136 结构兼容 instance/server，旧 instance_id 数据回填显式 subject。缓存/幂等按 incident/version/subject/promptVersion，不再解析 cache_key 获取关系。Agent failed/partial 不推进 investigating -> mitigated；成功 envelope 建立 relation 并发 outbox。测试真实 server alert 无 instance_id 场景。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/incidents/incident-closure.test.ts -t "server RCA|instance RCA|analysis failure"</automated></verify>
  <done>实例和服务器 RCA 均持久化且 UI/API 可追溯，失败不会伪装已分析。</done>
</task>

<task type="auto">
  <name>Task 3: 发布版本化 InvestigationPackage</name>
  <files>apps/db-ops-api/src/incidents/investigation-package.ts, apps/db-ops-api/server.ts, docs/integrations/data-loom-contract.md, apps/db-ops-api/src/incidents/incident-closure.test.ts</files>
  <action>
    定义 packageVersion、incident snapshot/version、resources/relations、member alerts、primary/related analyses、evidence refs、recommendations、Operations、approvals、execution/verification 和 provenance。API 支持 by incident/version、ETag/cursor 和 ActorContext scope；outbox 只发 package available/version，不塞入超大正文。Data Loom 使用独立只读 service credential，不能写事件/审批/Operation；契约文档说明兼容窗口、删除/脱敏和重放。Data Loom 的质量分仅作外部反馈，不能成为 Slide 发布门禁或事实覆盖源。
  </action>
  <verify><automated>pnpm --filter slide-api vitest run src/incidents/incident-closure.test.ts -t "InvestigationPackage|read-only|version"</automated></verify>
  <done>相同 incident version 投影确定且 schema-valid；无 scope/写请求拒绝；包内每个引用可回到 Slide 原记录。</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Data Loom 越权写控制状态 | 只读 credential/API，无写路由 |
| 调查包泄露凭据/个人数据 | DTO allowlist + actor/service scope + redaction |
| 弱类型 cross-table ID 串错实体 | ResourceRef + typed relation/FK |
| 旧投影覆盖新事实 | incident/package version 与 ETag |
</threat_model>

<verification>
- `pnpm --filter slide-api vitest run src/incidents/incident-closure.test.ts`
- 用 instance/server 两套 fixture 验证 package schema 与 provenance。
- 验证 Data Loom credential 对所有 mutating route 返回 403/404。
</verification>

<success_criteria>
1. HI-08 和事件/执行血缘缺口关闭。
2. Slide/Data Loom 边界有版本化契约、权限和回放语义。
3. 输出 `.planning/phases/137-durable-incident-closure/137-02-SUMMARY.md`。
</success_criteria>

