{
  "name": "@slide/agent-core",
  "version": "0.1.0",
  "description": "Production-grade TypeScript agent engine rewritten for Slide from the original Python nanobot design. The current runtime has no dependency on the original Python runtime. LLM-to-tool execution loop with parallel tools, context management, interrupt recovery, and structured tracing.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src/"],
  "dependencies": {
    "openai": "^4.28.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  },
  "keywords": ["agent", "llm", "tool-calling", "ai", "database-ops"],
  "license": "MIT"
}
