# MAX-83 实施计划

> 按 Issue 已授权范围执行，复用 2026-09-19 设计 V2，不增加批准步骤。

目标：三类资源单一主表及可读、可验证的采集配置流程。
架构：共享 Lit 列表组件按 ID 合并只读指标；分页查询并发上限 4、代次隔离；采集筛选需要全量扫描时显式提示。现有业务操作和后端权限契约保持。
技术：Lit、TypeScript、Vitest、Playwright。

1. 新增资源指标摘要和共享列表，覆盖零值、缺失、过期、多维和请求失败；三页接入并保留业务操作。
2. 类型、型号、版本筛选使用完整传入资源集合；独立健康筛选沿用父页面；未知值保留。
3. 详情概览使用摘要；指标与趋势统一入口，保留旧标签兼容。
4. 配置基础/高级参数、中文状态、权限加载、预检试采失效及发布反馈；目录和资源选择使用名称。
5. 运行组件状态测试、受影响模块 gate、浏览器三列表和配置流程；逐项记录证据和确实存在的接口限制。

范围外：重写采集引擎、批量指标 API、无关重构。预算：未设定；实际 token 遥测不可用。停止条件：验收通过，或不可自行恢复的外部依赖阻塞并交付可审阅检查点。

执行结果：已实现共享列表、详情分工、中文配置与名称选择；验证命令、验收映射及接口缺失说明见 `docs/slide/verification/2026-09-20-MAX-83.md`。方案历史内容保留，不把设计阶段的“仅方案”说明作为本次实施限制。
