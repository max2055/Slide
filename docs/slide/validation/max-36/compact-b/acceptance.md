# MAX-36 方案 B 验收记录

## 范围 v2

用户于 2026-09-11 批准附件“运维总览视觉优化方案”的方案 B。基于 main@51c3652（包含已合并的 #37），分支 `feat/max-36-compact-overview`。保留 v1 的真实网络诊断验收缺口，不将视觉优化视为该缺口已完成。执行计划见 `docs/superpowers/plans/2026-09-11-compact-overview.md`。

实施包括紧凑 Tab/筛选工具栏、96px KPI 与语义变体/选中状态、双行风险队列、细状态分布条、上移采集质量、按选择展开关联摘要、完整未纳管空态、数据库引擎与容量趋势组合。页头使用“快照更新”，资源继续显示绝对观测时间与过期/质量状态。仅添加共享组件 CSS part，默认组件外观不变。

## 第 8 节验收

| 项目 | 证据 |
| --- | --- |
| 1 五尺寸及首屏 | Playwright 覆盖 1280×720、1440×900、1920×1080、768×1024、390×844；无页面横向溢出，桌面四 KPI 同行、高度 88–104px、首条完整风险在首屏 |
| 2 1440 密度 | 同一夹具首屏完整容纳五条普通名称风险与采集摘要；状态分布位于其上方。超长名称回归确认操作可见且条目不横向溢出 |
| 3 四视图、双主题、对比度 | 8 张真实截图、8 张双主题夹具截图；自动计算资源链接、风险观测文字、KPI 标签/提示、采集摘要和徽章在实际叠加背景上的对比度 ≥4.5；截图禁用瞬时动画，避免捕获旧 Tab 的淡出帧。非全站对比度审计 |
| 4 交互 | 键盘 Tab/Home/方向键、搜索、引擎选择、KPI 下钻、关联选择、证据入口、显式诊断和详情返回回归通过；保留状态分布下钻的原调用路径并添加 aria-pressed |
| 5 关联选择 | 来源行高亮/aria-pressed 与摘要名称一致；筛选清空选择；刷新或引擎筛选使对象失效时清空 URL；未选择、无关联、关联失败分别展示 |
| 6 空态 | 网络无查看权限、只有查看权限、有管理权限分别验证；按钮正常宽度且仅管理权限显示；无结果、读取失败、截断既有回归保留；零资源不画 KPI 或健康比例 |
| 7 观测与请求 | 真实 overview 200，5 数据库/1 服务器/0 网络设备；保留旧观测与过期标签；真实四视图浏览产生 0 次诊断 POST，无新采集器或虚构趋势 |
| 8 前端门禁 | 66 文件 / 384 单元测试通过；typecheck 通过；build 与 CSP 通过；总览 Playwright 17 项通过；真实只读视觉用例 1 项通过 |

## 命令与证据边界

- `pnpm --filter slide-frontend test`：384 通过；包含总览 15 项。新增单测首次失败为夹具给出告警数却保留空 alertIds，按去重契约修正夹具后通过。
- `pnpm --filter slide-frontend typecheck`：通过。
- `pnpm --filter slide-frontend build`：通过，2 个生产脚本 CSP 检查通过。保留既有大 chunk 提示。
- `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5176 pnpm --filter slide-frontend exec playwright test e2e/dashboard-overview.spec.ts --workers=2`：17 通过。新增广泛文字检查曾发现徽章对比度不足，限定总览调整后通过。
- 环境变量注入实际凭据后执行 `e2e/dashboard-compact-real.spec.ts --output test-results-compact-real --trace off`：1 通过。仅登录与读取，不新增设备/模型任务。
- 最终单元门禁后只补空态 header/footer 局部隐藏和截图禁用动画；受影响浏览器用例及最终 build 再次通过。
- `git diff --check`：通过。

共享组件仅新增 part，已有默认样式原样保留；未发现 app-card/stat-card 专用测试文件，使用全前端回归与总览浏览器集覆盖本次触达能力。后端没有变化，复用 v1 后端证据，不重跑无关后端门禁。

## 实际运行身份与主题

原有 Vite PID 56628 / 5173 / `frontend`，原有 API PID 56657 / 3000 / `apps/db-ops-api` / `tsx server.ts`，两者启动时间 13:47，原日志连接用户终端。未重启或终止它们。候选前端独立 PID 66492 / 5176 / `frontend` / `vite --host 127.0.0.1 --port 5176 --strictPort`，基于本工作区方案 B 候选源码；代理读取原有 3000，后端本轮未改。

真实运行的 `--accent` 为 `#6366f1`，来自当前主题偏好，不是此次写死的主色；保留用户主题，不执行全站主题迁移。实际库存数量、快照时间与无自动诊断证据见 `actual-inventory.json`。截图目录 `real/` 是实际资源，`fixture/` 是自动化测试数据，二者不混充。

本轮临时 Vite 在交付前停止。原始日志、认证数据、浏览器 trace 不作为附件。

## 限制与资源记录

真实网络设备依旧未纳管；其诊断闭环 A11/A12/A20 仍应在用户真实环境部署后补齐。此次真实网络截图仅证明未纳管空态，不证明有设备时诊断成功。已有源码设置 E2E 两项无关断言失配记录仍见 v1 报告，本轮未改这些模块。

硬预算未设定；raw input/cached input/output/费用实测遥测不可用。本轮新增代理 0，累计代理 1、最大深度 1、历史并发峰值 2；未将内部计数冒充模型实际吞吐。
