# MAX-36 运维总览实施与验收记录

## v1.0 执行契约（2026-09-11）

- 需求：Issue 附件 v1.0，已通过 CLI 下载并与仓库需求逐字比较一致。
- 基线：`main@dfef2f0793a845ef258aae1aef52c49b9ebd5cdc`；实施分支 `feat/max-36-operations-overview`。
- 范围：四视图、统计语义、资源定位与返回、只读诊断状态及结果；不含自动修复、新采集器、全站重构。
- 预计涉及：dashboard 与其语义/样式模块、resource-diagnosis、三类详情返回入口、resource-diagnostic-service 与资源分析读取路由、针对性测试与文案。
- 测试：语义/组件 focused checks → 资源与分析接口模块 → 前端 typecheck/test/build → Playwright 可控数据视觉/交互 → 核对运行实例后的真实 API 诊断闭环。
- 开发环境：macOS，本机工作区，pnpm/Vite/Lit/Fastify；浏览器测试使用独立 Vite 端口，不替换已有服务。
- 预算：未设定；实际 input/cached/output token 与费用遥测不可用。启动时子代理 0、深度 0、并发 1；最终累计子代理 1、最大深度 1、并发峰值 2（同一审查代理复用一次）。
- 停止条件：验收完成；或核心真实依赖不可用且已完成可独立实施/验证部分，记录具体阻塞，不以 Mock 当作真实通过。
- 保留用户已有 AGENTS.md 修改和未跟踪文件，不纳入本次提交。

## 能力核查

| 能力 | 选择与证据 |
| --- | --- |
| 资源/总览 | 有界适配：overview 返回允许公开的属性、最新观测和告警 ID，消除额外逐资源前端查询；500 资源/100 告警上限显式提示下界 |
| 指标排行 | 复用 overview 同批最新观测；按页面范围、有效期、质量过滤，不支持/过期保留原因 |
| 容量趋势 | 复用 capacity-trend，局部实例/窗口选择，参数恢复、竞态保护、缺口断线 |
| 服务器/网络历史 | 约定降级：仅最新有效观测及时间，详情提供既有历史入口 |
| 配置备份 | 复用设备 config-backups；总览按需读取选中设备的最近保存记录及脱敏状态，不进行全资源后台请求。读取接口不包含失败尝试记录，已明确说明 |
| 关联证据 | 复用 diagnose 与 evidence；按需请求，不自动运行 Agent |
| 诊断结果 | 有界适配：按资源类型/ID/analysisId 校验主体与权限后读取既有分析记录 |
| 只读边界 | DirectAdapter.invoke 使用 analysisCompletionTools，仅允许绑定 analysisId 的完成工具，不注册运维修改工具 |

## 验收

代码与可控数据验收完成；真实数据库/服务器闭环通过。网络设备当前未纳管，A11/A12/A20 的真实网络路径未验收，因此 **不宣称 R01—R07 全部验收完成**。

| 验收项 | 结果与证据 |
| --- | --- |
| A01 | 通过：四视图统一骨架、范围、专业指标；11 项 Playwright 中五尺寸切换及指标选择断言 |
| A02 | 通过：同批数据库与服务器告警夹具，服务器视图无数据库条目；scope/search/engine 统一过滤 |
| A03 | 通过：后端去重已确认未解决告警；前端按 ID 合并；100 条截断/失败明确提示 |
| A04 | 通过：过期/未来/缺失观测为未知，保留采集风险，单元覆盖 |
| A05 | 通过：无告警离线服务器仍计不可用并进入风险 |
| A06 | 通过：未纳管与搜索无匹配分开；真实网络清单为空；截断遗漏类型不宣称未纳管 |
| A07 | 通过：0/null/不支持/过期分别保留；最新无效记录不回退旧值伪装有效 |
| A08 | 通过：按引擎分组，均值/合计、单位、有效资源数量、观测时间明确；不使用全网单点可达性 |
| A09 | 按约定降级：历史只展示最新观测与详情入口；没有总容量时不预测；备份摘要为按需读取保存记录 |
| A10 | 通过：三类资源名称与操作携带实际类型/ID，服务器与网络直接路径兼容已修复；返回带原范围/分页参数，并恢复保存的滚动位置 |
| A11 | 部分通过：三类型预选及不自动提交单元测试、可控浏览器；真实数据库/服务器证据 GET 200。网络真实设备证据依赖待补 |
| A12 | 部分通过：三类型 queued/cached 状态跟踪、结果权限测试通过；真实实例任务 19087/19090、服务器 19088/19091 完成并有存储结果；真实网络路径未验收 |
| A13 | 通过：重复提交禁用、有限串行查询、离页停止、等待超时保持后端状态，单元及浏览器覆盖 |
| A14 | 通过：结果按主体、账号、sessionVersion 和权限快照校验；路由 ai:view/ai:manage、无资源权限、错误主体回归；实际执行器仅暴露绑定任务的完成工具 |
| A15 | 通过：请求代际保护；全局失败保留快照、分类型库存失败保留原类型；告警失败保留历史值和原时间，但不把旧值当作当前正常证明 |
| A16 | 通过：1280×720、1440×900、1920×1080 四卡同一行、风险首条首屏可见、无页面横向溢出 |
| A17 | 通过：768/390 布局与操作；明细在自身容器滚动 |
| A18 | 通过：深浅主题截图、键盘 Tab/Home/方向键；关键资源操作文字对比度实际计算 ≥4.5，焦点及状态文字可见。未进行全站对比度审计 |
| A19 | 通过：500 资源、100 告警、32 观测上限与部分范围提示；空类型截断回归 |
| A20 | **部分通过/外部依赖**：真实浏览器实例与服务器完成详情→证据→显式诊断→存储结果→返回；网络设备用例明确 skipped，不能视为通过 |

## 验证命令与结果

- `pnpm --filter slide-frontend typecheck`：通过。
- `pnpm --filter slide-frontend test`：66 个文件、382 项通过。之后仅修改下拉选择/资源标签兜底，受影响 focused checks 24 项通过；最终补充证据中文文案与观测截断提示后，直接受影响两模块 20 项通过，typecheck/build 再次通过。
- `pnpm --filter slide-frontend build`：通过，CSP 检查通过；保留既有大 chunk 警告。
- `pnpm --filter slide-api typecheck`：通过。
- `pnpm --filter slide-api exec vitest run src/resources src/ai-agent-bridge.test.ts src/security/server-instance-authorization.test.ts src/adapter/__tests__/direct-adapter.test.ts tests/dashboard.test.ts`：12 个文件、125 项通过。
- `PLAYWRIGHT_BASE_URL=http://127.0.0.1:5176 pnpm --filter slide-frontend exec playwright test e2e/dashboard-overview.spec.ts --workers=2`：11 项通过；补充 1440px 指标选中值回归 1 项通过。这些使用 Mock API，不冒充真实验收。
- 在环境变量注入真实凭据后，`PLAYWRIGHT_BASE_URL=http://127.0.0.1:5176 pnpm --filter slide-frontend exec playwright test e2e/dashboard-overview-real.spec.ts --workers=1`：2 项通过、1 项跳过。真实 API/实际模型/实际存储结果；重验复用同小时缓存，仍读取真实状态。
- 真实 `GET /api/dashboard/capacity-trend?hours=168&instance_id=1`：200，16 个样本，返回真实采样时间字段。
- `git diff --check`：通过。

## 真实运行身份及诊断证据

实际运行的 API PID 93777 → 3006/28896 → `apps/db-ops-api` → `tsx server.ts`；Vite PID 89871 → 5176 → `frontend` → `vite --host 127.0.0.1 --port 5176 --strictPort`。均来自本工作区当前分支，基线 dfef2f0 加本次候选变更，不是其他工作树。日志在 `.multica/max-36-api-final.log` 与 `.multica/max-36-vite.log`；原始日志/浏览器 trace 可能包含认证上下文，不作为外发附件。交付前已停止本次启动的进程，不留下后台任务。

真实账号可见 5 个数据库、1 个服务器、0 个网络设备。初次请求分别得到 202/queued（19087、19088），后续读取 200/completed。完整浏览器链路使用数据库实例 1 / 任务 19090、服务器 3 / 任务 19091，结果均可通过受保护的 `/api/resources/{type}/{id}/analyses/{analysisId}` 读取，见 `real-api.json` 和 `screenshots/real-*.png`。凭据仅保存在测试进程内存/临时浏览器上下文，没有写入本次代码、文档或附件。

## 已知限制与依赖

1. **验收阻塞：没有真实纳管网络设备。** 最小外部输入是一个已授权、可测试的纳管网络设备（包含现有 SNMPv3/SSH 条件）；再运行真实用例补齐 A11/A12/A20。没有创建模拟硬件冒充真实设备。
2. 条件能力：未新增服务器/网络历史接口；支持未知时仅展示有效资源数，不编造分母。配置备份接口只提供保存记录，不能推断失败尝试。
3. 既有长流程 `frontend/e2e/resource-diagnosis.spec.ts` 的诊断/关联/证据部分运行成功，但在后续源码设置部分有两个与本次无关的既有断言失配：1440px 期望缺少已有 `provider: gitlab` 字段；390px `settings-shell select` 命中两个控件。仅将本次按钮名称同步到测试，未修改源码设置或绕过失败。这两项不被计为通过。
4. 结果读取绑定创建者权限快照，权限/session 变化或其他账号会得到不可见结果；需要在新的权限范围内重新发起诊断，以避免历史关联上下文泄露。

## 审查与资源记录

独立审查提出告警失败伪健康、全局截断伪未纳管两项重要问题，均修复、补回归，原审查代理复核确认解决。无新增功能范围版本；容量每小时多采样重复累加、库存异常吞零、直接路径 ID 初始化均直接阻塞原验收，作为 v1.0 内必要修复。

最终累计子代理 1、最大深度 1、并发峰值 2；主线程与子线程 raw input/cached input/output/费用实际遥测均不可用。没有设置硬预算，也没有将估算冒充实测。
